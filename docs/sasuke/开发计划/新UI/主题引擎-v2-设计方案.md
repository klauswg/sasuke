# sasuke 主题引擎 v2 设计方案

> 状态：实现完成；第四轮主题范围验证通过，环境受限的浏览器性能、EXE 与安装包量化按授权放行且不伪造执行结果
> 日期：2026-08-16
> 范围：Theme Contract、Theme SDK、Web Runtime、Tauri 资源投影及共享组件消费层升级  
> 不包含：任何新增正式主题的具体视觉设计

## 1. 一句话结论

Theme Engine v2 在保留 `themeId × colorScheme × visualQuality + personalization` 现有权威模型的前提下，把 v1 只预留或部分实现的字体资源、组件状态 recipe、形状、动效、图标和壁纸补成封闭、可校验、可编译、可回退的正式能力；用户设置仍只选择主题、明暗、质量档、字体来源/字号和头像，不新增图标、壁纸或字体高级选项。

## 2. 背景与根因

v1 已经完成以下基础能力：

- 声明式内置主题包、DTCG token、Style Dictionary 构建和 Web/Rust Catalog。
- `themeId × colorScheme` 外观模型和按主题隔离的视觉质量选择。
- 语义颜色、材质 token、少量稳定 component role 和根 CSS variables。
- UI / 编辑器两套字体与字号继承，以及头像继承。
- Theme Package 不允许注入 JavaScript、HTML、任意 CSS 或远程 URL。

但当前实现与 v1 原设计之间仍有结构性缺口：

1. `fonts` capability 只有字体栈字符串，没有字体文件、字重、字符覆盖、授权和固定有序回退契约；内置 MiSans 的 `@font-face` 仍硬编码在全局 CSS。
2. `component-recipes` 实际只支持背景、前景、边框颜色和材质类型；v1 设计中已定义的 border width、radius、shadow、transition 和更多业务角色没有落地。
3. 当前只有单个全局 radius、shadow、duration 和 easing，无法让不同主题系统化表达控件、表面、弹层、滚动条和交互状态。
4. `icons` 与 `wallpapers` 只存在于 v1 设计文档，没有进入 capability 枚举、Schema、构建产物和运行时消费路径。
5. `assets/` 当前只生成文件 hash 清单，不复制到 Web 产物、不生成安全 URL，也没有按资源种类验证尺寸、字符范围或实际用途。
6. 部分共享组件仍直接使用硬编码圆角、阴影和动效 class；新增高差异主题会迫使业务组件出现主题特判。

根因不是缺少某一套主题 CSS，而是 Theme Contract 的“可表达范围”和 Asset Pipeline 没有完成。v2 必须补齐通用契约，不为某个具体主题增加旁路。

## 3. 目标与非目标

### 3.1 目标

1. 将 Theme Contract 升级到 v2，并由 JSON Schema、Theme SDK、Web Zod 和 Rust `serde` 共同维护同一封闭字段集合。
2. 建立完整主题资源模型：稳定 asset ID、类型、路径、授权、hash、尺寸、字符覆盖、构建 URL 和回退。
3. 让主题包能够声明并实际加载本地 WOFF2/WOFF 字体，支持 UI / 编辑器、字重和不受界面语言影响的多语言固定字体栈。
4. 扩展结构 token 和 component recipe，使共享组件能够消费主题定义的形状、边框、阴影、滚动条、动效和交互状态。
5. 实现按语义槽覆盖的主题图标；缺失或损坏时回退 sasuke 内置 Lucide 图标。
6. 实现按稳定表面槽声明的静态主题壁纸；缺失或损坏时回退语义背景色。
7. 主题切换仍只更新根属性、CSS variables 和轻量资源 locator，不重新请求业务数据、不重建 Markdown、不销毁 CodeMirror。
8. 内置主题资源编译进 Vite `frontendDist`，不为纯视觉资源新增 Tauri IPC、文件系统权限或长生命周期缓存。
9. v1 两个内置主题迁移到 v2 后保持既有视觉和用户偏好语义不变。
10. 通过非分发的 contract fixture 验证字体、图标、壁纸和高差异 recipe，无需新增正式主题。

### 3.2 非目标

- 本方案不定义、命名或实现任何新增正式主题。
- 不实现 `.gbtheme` 用户导入、主题市场、在线下载、主题签名网络和主题热更新。
- 不开放用户自定义图标包、壁纸、壁纸暗度、壁纸模糊、强调色或高级排版参数。
- 不允许主题改变布局结构、信息层级、组件尺寸、触控目标、焦点顺序、业务状态机或路由。
- 不允许主题提供任意 CSS selector、JavaScript、React、HTML、WASM、Canvas shader、视频壁纸或远程资源 URL。
- 不替换 EXE、任务栏、安装包或操作系统级应用图标。
- 不为主题资源建立数据库、后台下载队列或新 IPC 事件流。

## 4. 设计原则与权威事实源

### 4.1 三层 token 继续作为唯一视觉决策链

```text
Primitive
  原始颜色、尺寸、字重、阴影、时长
        ↓
Semantic
  foreground、surface、control-radius、overlay-shadow、motion-fast
        ↓
Component Recipe
  button、input、message、tool-card、dialog、workflow-node
```

- 业务组件不得直接消费 primitive。
- 主题作者只能引用 Contract 公开的 semantic token 和资源 ID。
- component recipe 不接受原始 selector 或任意声明字符串。
- shared shadcn/prompt-kit 组件负责把 component recipe 投影到 DOM。

### 4.2 权威事实源

| 领域 | 权威事实源 | 说明 |
| --- | --- | --- |
| 当前主题和明暗偏好 | 后端 `AppearancePreference` | v2 不新增字段 |
| 字体、字号和头像用户覆盖 | 后端 `PersonalizationPreference` | v2 不新增用户高级字体字段 |
| 可用主题身份与版本 | 生成的 Theme Catalog | 使用稳定 `themeId`，不按名称反查 |
| 主题视觉能力 | Theme Package v2 | token、recipe、font、icon、wallpaper 声明 |
| 物理资源完整性 | 生成的 Asset Manifest v2 | hash、类型、尺寸和输出 URL |
| 当前有效外观 | `EffectiveAppearanceV2` | 运行时投影，不持久化 |
| 图标可访问名称 | sasuke 业务组件 | 主题只替换图形，不替换语义 |

### 4.3 运行时不读取源包

Style Dictionary、Ajv、源 token 和原始资源目录只存在于开发构建期。Web/Tauri 运行时只消费：

- 已验证的 Runtime Theme Package v2。
- 已生成的 CSS。
- 已生成的 Asset Manifest v2。
- 已复制并使用 content hash 命名的同源静态资源。

## 5. Theme Contract v2 总体结构

### 5.1 源包目录

```text
themes/<theme-id>/
├── manifest.json
├── tokens/
│   ├── primitives.tokens.json
│   ├── semantic.tokens.json
│   ├── light.tokens.json
│   └── dark.tokens.json
├── recipes.json
├── presets.json
├── resources.json
├── fonts.json                 # 声明 fonts capability 时必需
├── icons.json                 # 声明 icons capability 时必需
├── wallpapers.json            # 声明 wallpapers capability 时必需
├── visual-quality/
│   └── performance.json       # 声明 visual-quality-profiles 时必需
├── assets/
│   ├── fonts/
│   ├── avatars/
│   ├── icons/
│   ├── textures/
│   ├── wallpapers/
│   └── previews/
├── LICENSES.json
└── dist/
    ├── runtime-theme.json
    ├── builtin-theme.css
    └── asset-manifest.json
```

只有与 capability 对应的声明文件才允许存在。声明 capability 但缺少文件、存在文件但未声明 capability，均构建失败。

### 5.2 Manifest v2

```ts
type ThemeCapabilityV2 =
  | "tokens"
  | "component-recipes"
  | "fonts"
  | "avatars"
  | "textures"
  | "icons"
  | "wallpapers"
  | "visual-quality-profiles";

interface ThemeManifestV2 {
  schemaVersion: 2;
  contractVersion: 2;
  id: `builtin.${string}` | `user.${string}`;
  version: string;
  source: "builtin" | "user";
  name: LocalizedText;
  author: string;
  schemes: ["light", "dark"] | ["dark", "light"];
  capabilities: ThemeCapabilityV2[];
  visualQualityProfiles?: ThemeVisualQualityManifest;
}
```

v2 仍要求所有正式主题同时提供 light 和 dark，不允许 `system` 跨主题包回退。

## 6. 统一资源契约

### 6.1 Asset ID 与物理文件分离

主题 token、字体栈、图标槽、壁纸槽和头像只引用稳定 asset ID，不直接保存文件路径。

```ts
type ThemeAssetKind =
  | "font"
  | "avatar"
  | "icon"
  | "texture"
  | "wallpaper"
  | "preview";

interface ThemeAssetSourceV2 {
  id: string;
  kind: ThemeAssetKind;
  path: `assets/${string}`;
  licenseId: string;
  required?: boolean;
}
```

构建器从真实文件推导并生成：

```ts
interface ThemeAssetRecordV2 {
  id: string;
  kind: ThemeAssetKind;
  mediaType: string;
  bytes: number;
  sha256: string;
  outputUrl: string;
  width?: number;
  height?: number;
  fontMetadata?: ResolvedFontMetadata;
  required: boolean;
}
```

主题作者不得填写 `bytes`、`sha256`、`outputUrl`、图片尺寸或从字体文件可读取的真实元数据，避免声明与文件事实分裂。

### 6.2 支持格式

| 资源 | v2 格式 | 说明 |
| --- | --- | --- |
| 字体 | WOFF2、WOFF | 禁止 TTF/OTF 和远程 URL |
| 图标 | PNG、WebP | 单色图标使用 alpha mask；v2 不开放 SVG |
| 壁纸 | PNG、WebP | 仅静态图片 |
| 纹理 | PNG、WebP | 小尺寸、可平铺 |
| 头像 | PNG、WebP | 沿用主题头像能力 |
| 预览 | PNG、WebP | 设置页主题预览 |

v2 不开放 SVG，避免为未来不受信任主题引入 SVG 脚本、外链、滤镜和解析差异。单色可着色图标通过 PNG/WebP alpha mask + `currentColor` 实现。

### 6.3 路径与授权校验

- asset ID 在单个主题包内唯一，使用小写 kebab-case。
- 路径必须规范化后位于主题根目录下的 `assets/`。
- 拒绝绝对路径、`..`、符号链接、大小写冲突路径和重复物理文件映射。
- 每个 asset 必须关联 `LICENSES.json` 中存在的 `licenseId`。
- 同一物理文件可以被多个语义槽引用，但 Asset Manifest 只保留一份记录。
- 构建失败不得保留部分生成目录；使用临时目录完成后原子替换 `dist` 和 Web generated assets。

### 6.4 初始资源预算

沿用 v1 的 package 级上限作为 v2 初始硬边界：最多 128 个资源、源文件总计不超过 16 MiB。同时增加按类型校验：

- 单个 icon：最大 128×128、最大 256 KiB。
- 单个 texture：最大 1024×1024、最大 2 MiB。
- 单个 wallpaper / preview：最大 4096×4096、最大 16 MP、最大 4 MiB。
- 单个字体：最大 8 MiB；字体总量仍受 16 MiB package 上限约束。

这些数值作为 SDK 常量和构建测试固定。只有实际合规主题资源无法满足且完成包体、解码和内存测量后，才允许统一调整，不能由单个主题覆盖。

## 7. 字体资源与多语言排版

### 7.1 Font Face

```ts
type ScriptCode = string; // 校验为 ISO 15924 四字母代码

interface ThemeFontFaceV2 {
  id: string;
  family: string;
  runtimeFamily: string;       // 仅存在于生成产物，由 SDK 按主题派生
  assetId: string;
  weight: 400 | 500 | 600 | 700;
  style: "normal" | "italic";
  display: "swap";
  coverage: {
    scripts: ScriptCode[];
    locales?: string[];       // BCP 47
    unicodeRanges?: string[];
  };
  metrics?: {
    sizeAdjust?: string;
    ascentOverride?: string;
    descentOverride?: string;
    lineGapOverride?: string;
  };
}
```

SDK 校验：

- `assetId` 必须指向 `kind=font` 的 WOFF2/WOFF。
- 作者 `family + weight + style + coverage` 在单主题内不得冲突；生成 CSS 使用 `runtimeFamily` 作为浏览器匹配 identity，跨主题不得竞争。
- `runtimeFamily` 是编译器所有的派生字段，主题作者不得填写，用户偏好不得持久化；作者 family 与产品 `displayName` 保持独立。
- `unicodeRanges` 必须符合 CSS `U+...` 封闭语法，不接受任意描述符。
- `locales` 规范化为 BCP 47；`scripts` 校验 ISO 15924 格式。
- 字体声明的 weight 必须与读取到的字体元数据一致；不允许把同一个 Regular 文件伪装成多个字重。
- `font-display` 固定为 `swap`，主题作者不可修改。

### 7.2 Font Stack

```ts
interface ThemeFontStackV2 {
  id: string;
  displayName: LocalizedText;
  defaultFaces: string[];
  systemFallbacks: string[];
}

interface ThemeTypographyPresetV2 {
  uiStackId: string;
  uiSize: number;
  uiLineHeight: number;
  editorStackId: string;
  editorSize: number;
  editorLineHeight: number;
  weights: {
    read: 400 | 500;
    emphasize: 400 | 500 | 600;
    announce: 500 | 600 | 700;
  };
}
```

解析顺序：

```text
stack defaultFaces
  -> stack systemFallbacks
  -> builtin.sasuke 对应 UI / editor 安全字体栈
```

主题包固定声明完整有序栈，界面语言不得选择、删除或重排 family。字体 face 的 `coverage` 继续描述资源覆盖并可生成 `unicode-range`，实际中英文混排由浏览器逐字形 fallback；语言只用于 `displayName` 本地化和文档语义。

### 7.3 用户设置保持不变

`PersonalizationPreference` 继续只保存：

```ts
type FontPreference =
  | { source: "theme" }
  | { source: "local"; family: string };

type FontSizePreference =
  | { source: "theme" }
  | { source: "custom"; px: number };
```

设置页“默认字体”卡片显示 `ResolvedTypography.displayName`，例如：

```text
默认字体 · <当前主题解析出的字体名称>
```

不向用户开放 font face、语言字体、fallback、字重、行高、字距或资源包选择。用户显式本机字体覆盖继续优先于主题字体。

### 7.4 字体加载边界

- SDK 将 font face 编译为 `@font-face`，URL 指向同源 hash 资源。
- CSS 可以包含全部 face 声明，但 WebView 只请求当前 stack、weight 和字符范围实际命中的文件。
- 字体加载失败不阻塞应用壳；先使用安全 fallback，再由浏览器 swap。
- 语言切换不解析或写入根字体 CSS variables，只更新 i18n、文档语言语义与 stack `displayName` 展示。
- 非当前主题字体不得被 CSS 实际引用，因此不请求、不解码。

## 8. 结构、材质、动效与滚动条 token

v2 将 v1 `material.radius/shadow/motion*` 拆分为职责明确的组，材质只描述光学效果。

```ts
interface ThemeShapeTokensV2 {
  radiusControl: string;
  radiusSurface: string;
  radiusOverlay: string;
  radiusAvatar: string;
  radiusPill: string;
  borderHairline: string;
  borderDefault: string;
  borderStrong: string;
}

interface ThemeElevationTokensV2 {
  none: string;
  surface: string;
  overlay: string;
  floating: string;
  pressed: string;
  pressOffset: 0 | 1 | 2;
}

interface ThemeMotionTokensV2 {
  mode: "smooth" | "stepped" | "none";
  durationFast: string;
  durationNormal: string;
  durationSlow: string;
  easingStandard: string;
  easingEnter: string;
  easingPress: string;
}

interface ThemeScrollbarTokensV2 {
  width: string;
  thumbRadius: string;
  thumbInset: string;
  minLength: string;
  buttons: "none" | "visible";
}
```

约束：

- dimension 使用受控单位和范围解析器，不把任意 CSS 字符串原样透传。
- shadow 限制层数、blur、spread 和总字符串长度。
- easing 只接受 `linear`、标准 cubic-bezier 或 `steps()`；拒绝任意函数。
- `mode=none` 只关闭装饰运动，不关闭业务 loading、进度和状态反馈。
- `prefers-reduced-motion` 始终具有更高优先级。
- scrollbar token 只控制视觉几何，不改变滚动行为和最小可操作性。

## 9. Component Recipe v2

### 9.1 Surface Recipe

```ts
type ThemeVisualState =
  | "default"
  | "hover"
  | "active"
  | "selected"
  | "focus"
  | "disabled";

interface ThemeStateRecipeV2 {
  background?: SemanticBackgroundRef;
  foreground?: SemanticForegroundRef;
  border?: SemanticBorderRef;
  elevation?: "none" | "surface" | "overlay" | "floating" | "pressed";
  opacity?: number;
  press?: boolean;
}

interface ThemeSurfaceRecipeV2 {
  background: SemanticBackgroundRef;
  foreground: SemanticForegroundRef;
  border: SemanticBorderRef;
  borderWidth: "none" | "hairline" | "default" | "strong";
  borderStyle: "solid" | "double" | "dashed";
  radius: "none" | "control" | "surface" | "overlay" | "avatar" | "pill";
  elevation: "none" | "surface" | "overlay" | "floating";
  material: "flat" | "subtle" | "elevated";
  motion: "none" | "color" | "surface" | "press";
  states?: Partial<Record<Exclude<ThemeVisualState, "default">, ThemeStateRecipeV2>>;
}
```

校验不变量：

- state 修改背景时必须同时给出合法前景，或显式继承已验证的前景配对。
- focus state 不能关闭 engine-owned focus ring，也不能把 ring 宽度降到最低要求以下。
- disabled 可以降低对比，但仍须满足 disabled 控件和文字最低要求。
- `press=true` 只允许使用 package 级 `pressOffset`，不能写任意 transform。
- `motion=color` 只过渡颜色属性，`motion=surface` 额外过渡 elevation，只有 `motion=press` 可以过渡 transform；包含 `press=true` 的 active state 必须声明 `motion=press`。浮层、弹窗、面板等定位或承载型 surface 不得因主题 recipe 获得 transform transition。
- 组件库的定位宿主可以声明 `data-theme-material-layer="isolated"`；Theme SDK 必须把会建立 fixed containing block 的 `backdrop-filter` 投影到无交互 `::before` 视觉层，定位节点本身不得承载 filter。该标记表达定位所有权，不得包含主题 ID、浏览器类型或业务菜单名称。
- theme state 不能改变 `display`、`visibility`、尺寸、padding、position 或 DOM 顺序。
- recipe 的颜色、材质、状态、elevation、motion 与几何声明统一编译到 CSS `components` cascade layer，作为当前主题对 role 的默认值；组件或变体显式声明的背景、前景、边框色、focus ring、阴影、动效、无边框、单边框、圆形、pill、虚线或 joined-control 规则必须能够覆盖默认值。主题仍通过 semantic token 和 recipe 声明视觉，不允许用高优先级 selector 越过组件显式契约。
- 不拥有完整 perimeter 的结构 role 必须显式使用 `borderWidth:none + radius:none`，布局分隔只能由对应 shell/panel/sheet owner 绘制，不能用四边 recipe border 模拟单边 separator。

### 9.2 稳定角色

v2 保留 v1 已存在角色并新增业务语义角色：

```text
shell
titlebar
sidebar
navigation-item
panel
card
composer
message-user
message-assistant
message-disclosure
runtime-control
activity
tool-card
permission-card
dialog
sheet
popover
input
button-primary
button-secondary
button-ghost
editor
diff
workspace-tab
workflow-node
workflow-edge
scrollbar
```

角色由共享组件标注。页面不得为了主题在局部添加另一个 selector，也不得读取 `themeId`。

### 9.3 共享组件消费

- shadcn Button/Input/Card/Dialog/Sheet/Popover/Select/Dropdown/Tooltip 等统一消费 role CSS variables。
- prompt-kit 消息、Markdown、Composer、tool card 和 activity 消费会话角色。
- 用户消息隐藏上下文使用 `message-disclosure`，AI 输出判定/控制产物使用 `runtime-control`；业务组件只保留 disclosure、joined control、focus 与 invalid/destructive 变体拓扑。
- Workflow 节点、边和 controls 消费 workflow 角色。
- CodeMirror 只更新 theme extension 和 CSS variables，不销毁 `EditorView`。
- `rounded-full`、固定 shadow 和固定动效优先从共享组件移除；确为业务语义的圆形状态点和头像保留专用 role。

## 10. 图标能力

### 10.1 语义槽而不是组件名

```ts
type ThemeIconSlotV2 =
  | "navigation.conversation"
  | "navigation.search"
  | "navigation.agent"
  | "navigation.context"
  | "navigation.run-mode"
  | "navigation.settings"
  | "entity.task"
  | "entity.workflow"
  | "entity.agent"
  | "entity.file"
  | "entity.folder"
  | "conversation.thought"
  | "conversation.attachment"
  | "tool.read"
  | "tool.write"
  | "tool.command"
  | "permission.request"
  | "status.running"
  | "status.success"
  | "status.warning"
  | "status.error"
  | "action.send"
  | "action.continue"
  | "action.stop";
```

首批槽位只覆盖跨页面重复、具有稳定业务语义的图标。页面专属、低复用或第三方品牌图标继续由产品代码管理。

### 10.2 Icon Descriptor

```ts
interface ThemeIconDescriptorV2 {
  assetId: string;
  renderMode: "mask" | "image";
  nativeSize: 16 | 20 | 24 | 32;
  imageRendering: "auto" | "pixelated";
}

interface ThemeIconMapV2 {
  defaults: Partial<Record<ThemeIconSlotV2, ThemeIconDescriptorV2>>;
  schemes?: {
    light?: Partial<Record<ThemeIconSlotV2, ThemeIconDescriptorV2>>;
    dark?: Partial<Record<ThemeIconSlotV2, ThemeIconDescriptorV2>>;
  };
}
```

- `mask` 使用资源 alpha 通道并继承 `currentColor`，适用于单色主题图标。
- `image` 保留原图颜色；必须分别验证 light/dark 对比度。
- `imageRendering=pixelated` 只作用于主题自有图标，不作用于用户图片、附件、壁纸或业务资产。
- 主题不提供 `aria-label`、tooltip 或危险动作文案；这些仍由业务组件负责。

### 10.3 ThemeIcon 共享组件

```tsx
<ThemeIcon
  slot="navigation.search"
  fallback={Search}
  aria-hidden="true"
/>
```

`ThemeIcon` 只负责图形解析：

1. 当前 scheme override。
2. theme defaults。
3. sasuke 代码内 Lucide fallback。

资源加载失败时同一次渲染降级 fallback，不清空按钮、不改变 accessible name，也不让整个主题失效。

### 10.4 图标运行时边界

- `ThemeAssetsContext` 只保存已解析的 icon descriptor map 和稳定 URL，不保存二进制。
- Context 仅在主题或 scheme 改变时更新，不跟随 composer、会话或工作区高频状态。
- 图标主题切换允许图标组件重渲染，但不得让消息正文、Markdown 或编辑器因 Context 订阅而重渲染。
- 可通过拆分 `ThemeIconContext` 与 `ThemeRuntimeCommandsContext` 保持最小订阅边界。

## 11. 壁纸能力

### 11.1 稳定表面槽

```ts
type ThemeWallpaperSlotV2 =
  | "app"
  | "conversation"
  | "workspace"
  | "settings";
```

每个可用 surface 在 DOM 上标记稳定 `data-theme-wallpaper-slot`。主题不能提供选择器。

### 11.2 Wallpaper Descriptor

```ts
interface ThemeWallpaperDescriptorV2 {
  assetId: string;
  fit: "cover" | "contain" | "tile";
  position:
    | "center"
    | "top"
    | "bottom"
    | "left"
    | "right"
    | "top-left"
    | "top-right"
    | "bottom-left"
    | "bottom-right";
  repeat: "no-repeat" | "repeat" | "repeat-x" | "repeat-y";
  opacity: number;
  overlayColor: SemanticBackgroundRef;
  overlayOpacity: number;
}

interface ThemeWallpaperMapV2 {
  light: Partial<Record<ThemeWallpaperSlotV2, ThemeWallpaperDescriptorV2>>;
  dark: Partial<Record<ThemeWallpaperSlotV2, ThemeWallpaperDescriptorV2>>;
}
```

v2 不允许自定义 blend mode、任意 filter、视频、GIF、Canvas 或 pointer-driven parallax。若未来确有需求，必须以新 Contract 版本和独立性能预算引入。

### 11.3 层级与回退

- 壁纸使用 surface 自身的 `::before` 背景层，业务内容位于同一 surface 的正常 stacking context。
- overlay 使用主题语义色，保证正文与控件对比度；不能依赖壁纸图像本身恰好较暗或较亮。
- 最具体的 surface 只绘制自己的壁纸；不得让 app、conversation、workspace 多层壁纸同时透叠。
- 当前槽缺失时直接使用该 surface 的语义背景色。
- 切换主题前只预加载当前可见槽的 wallpaper；失败时继续使用语义背景色并完成主题切换。
- Tauri 原生窗口背景仍同步 `windowSurface` 实色，不使用壁纸，避免 WebView 创建/resize 期间暴露透明或错误底色。

### 11.4 视觉质量档

`performance` 可以关闭 wallpaper 和 texture，但不能换成另一张表达不同内容的图片：

```ts
interface WallpaperPerformanceOverrideV2 {
  enabled: false;
}
```

关闭后回退该 surface 的语义背景色。设置页仍只有既有“完整效果 / 性能优先”，不增加壁纸开关。

## 12. Runtime Theme Package v2

```ts
interface RuntimeThemePackageV2 {
  schemaVersion: 2;
  contractVersion: 2;
  id: string;
  version: string;
  source: ThemeSource;
  name: LocalizedText;
  author: string;
  capabilities: ThemeCapabilityV2[];
  schemes: {
    light: ThemeSchemeV2;
    dark: ThemeSchemeV2;
  };
  recipes: ThemeRecipeMapV2;
  assets: ThemeAssetManifestSummaryV2;
  fonts?: ThemeFontRuntimeV2;
  icons?: ThemeIconMapV2;
  wallpapers?: ThemeWallpaperMapV2;
  visualQualityProfiles?: ThemeVisualQualityProfilesV2;
}
```

`ThemeSchemeV2` 包含：

- `windowSurface` 与 preview palette。
- semantic colors。
- material optical tokens。
- shape、elevation、motion、scrollbar。
- typography preset 与 avatar preset。

Runtime Package 只包含 locator 和已解析值，不内嵌字体或图片字节。

## 13. Theme SDK v2 编译链路

### 13.1 构建顺序

```text
发现 themes/*/manifest.json
  -> 校验 Manifest v2 与 capability/file 一致性
  -> 解析 DTCG token 与 alias
  -> 校验 recipes / fonts / icons / wallpapers
  -> 扫描物理资源并校验路径、签名、MIME、尺寸、字体元数据与授权
  -> 生成 content hash 资源名
  -> 生成 Runtime Theme Package v2
  -> 生成 Asset Manifest v2
  -> 生成 @font-face、token、recipe、wallpaper CSS
  -> 原子写入 theme dist
  -> 原子重建 Web generated catalog/CSS/assets
  -> 生成 Rust builtin catalog
```

任一步失败都不覆盖上一次完整生成产物。

### 13.2 Web 输出目录

```text
web/public/theme-assets/<theme-id>/
  <sha256-prefix>-<safe-name>.<ext>

web/src/themes/generated/
  catalog.json
  builtin-themes.css
  asset-catalog.json
```

- `web/public/theme-assets` 是构建生成目录，每次 `themes:build` 原子清理并重建。
- Vite 将其作为同源静态文件复制进 `frontendDist`。
- CSS 和 runtime JSON 只引用 `/theme-assets/...`，不引用源码目录或绝对文件路径。
- URL 文件名包含 hash，主题升级不会命中旧资源内容。

### 13.3 Tauri 资源边界

内置主题字体、图标和壁纸属于 Web 前端资源，应进入 `frontendDist`，不进入 Tauri `bundle.resources`：

- 不需要 `resolveResource`。
- 不需要 fs plugin scope。
- 不需要新增 IPC command。
- 不向 WebView 暴露任意本地文件路径。
- 原生 Core 只继续消费生成的 Rust Catalog 和 `windowSurface`。

未来实现用户 `.gbtheme` 导入时，再单独设计安装目录、原子激活、asset protocol 和 scoped permission；本方案不提前建立两套资源路径。

### 13.4 依赖选择

- 继续使用 Style Dictionary、Ajv、Zod 和 Rust `serde`。
- Node 构建期使用成熟的图片尺寸/MIME 解析库校验 PNG/WebP，不自行实现图片解码器。
- 字体元数据读取优先选择纯构建期、无网络、支持 WOFF/WOFF2 的成熟库。
- 新依赖不得进入 Web 运行时 bundle；Theme SDK 依赖只作为 devDependency。

## 14. Web Runtime 解析与应用

### 14.1 EffectiveAppearanceV2

```ts
interface EffectiveAppearanceV2 {
  themeId: string;
  themeVersion: string;
  colorScheme: "light" | "dark";
  visualQuality?: "full" | "performance";
  material: ResolvedMaterialTokensV2;
  shape: ResolvedShapeTokensV2;
  elevation: ResolvedElevationTokensV2;
  motion: ResolvedMotionTokensV2;
  scrollbar: ResolvedScrollbarTokensV2;
  semantic: ResolvedSemanticTokensV2;
  recipes: ResolvedRecipeMapV2;
  typography: {
    ui: ResolvedTypographyV2;
    editor: ResolvedTypographyV2;
  };
  avatars: ResolvedAvatarMap;
  icons: ResolvedThemeIconMapV2;
  wallpapers: ResolvedThemeWallpaperMapV2;
}
```

字体 family 的外观投影不接收 app locale：

```text
Theme Package + scheme + quality + personalization
  -> EffectiveAppearanceV2
```

### 14.2 原子应用顺序

1. 解析目标 package、scheme 和 quality。
2. 解析并校验当前可见 wallpaper URL；失败标记为无 wallpaper。
3. 在同一提交中更新根 `data-theme`、`data-color-scheme`、`data-visual-quality`、`data-material-model` 和 CSS variables。
4. 更新低频 `ThemeIconContext` 值。
5. 同步 Tauri 原生窗口 `windowSurface`。
6. 设置页从当前 ThemeFontStack 独立读取本地化 `displayName`，不改变 `EffectiveAppearanceV2` 的字体 family。

主题切换不等待字体完成下载；字体由 `font-display: swap` 渐进生效。壁纸只做可见槽的有界预加载，不阻塞其他 UI。

### 14.3 React 更新边界

- semantic、shape、motion、scrollbar、wallpaper 主要通过根 CSS variables 和稳定 DOM role 生效。
- 只有 ThemeIcon 需要读取 descriptor map；Context 不包含 Theme Package 完整对象。
- 当前不可见页面不订阅 wallpaper 状态。
- 主题切换不得触发会话、文件、Git、Agent 或 Runtime 数据刷新。
- 已完成 Markdown、历史消息、CodeMirror 文档和右侧 workspace resource identity 保持不变。

## 15. 设置页行为

### 15.1 不新增用户入口

v2 不增加以下设置：

- 图标包选择。
- 壁纸选择、暗度、模糊和开关。
- 中文/英文/其他语言字体分别选择。
- 字重、行高、fallback、字体加载策略。
- 形状、圆角、阴影或动效风格。

这些均由当前主题包和 visual quality 决定。

### 15.2 现有字体卡动态化

当前“默认字体 · MiSans”改为读取 `ResolvedTypography.displayName`：

```text
默认字体 · <当前主题与语言解析出的字体名称>
```

- 预览使用当前有效字体栈。
- 用户选择本机字体时继续显示本机字体选中态。
- 恢复按钮只把来源改回 `theme`，不写具体主题字体名称。
- 主题或应用语言改变时动态更新名称，不写入用户偏好。

### 15.3 主题预览

主题抽屉继续只展示名称、选中态和 light/dark 视觉预览。预览图由主题包资源提供，可以包含该主题的字体、图标和壁纸效果，但设置页不拆分展示这些内部能力。

## 16. v1 到 v2 的迁移策略

项目仍处于开发阶段，采用破坏式 Contract 升级，不维护 v1/v2 双运行时。

### 16.1 需要迁移

- 新建 `theme-manifest-v2.schema.json` 和 `theme-package-v2.schema.json`。
- `theme-sdk/src/contract.mjs`、Web Zod 和 Rust `serde` 一次性切换到 v2。
- `builtin.sasuke` 与 `builtin.tech-neutral` manifest 改为 `schemaVersion=2 / contractVersion=2`。
- v1 `material.radius/shadow/motion*` 映射到 v2 shape/elevation/motion。
- v1 简化 recipes 映射为 v2 显式字段，视觉值保持一致。
- MiSans 从全局硬编码字体迁入 sasuke 主题资源契约；技术中性可以引用同一官方资源定义或拥有受构建器去重的独立 asset 记录。
- 现有滚动条固定几何迁入两个主题的 scrollbar token。
- Catalog、生成 CSS、Asset Manifest 和测试期望全部升级到 v2。

### 16.2 不需要迁移

- `AppearancePreference` schema 不变。
- `PersonalizationPreference` schema 不变。
- `themeId` 不变，用户已选主题继续有效。
- `colorScheme`、`visualQualityByTheme`、字体来源、字号来源和头像来源不变。
- 不新增数据库字段或 localStorage key。

### 16.3 删除旧路径

升级完成后删除：

- v1 Schema 和 v1 runtime parser。
- `styles.css` 中硬编码的主题字体 `@font-face`。
- v1 material 中已迁移到 shape/elevation/motion 的重复字段。
- 共享组件中被 role token 替代的固定圆角、阴影和动效 class。
- 只为两个旧主题存在的字体名称硬编码和测试断言。

v1 用户主题导入尚未开放，因此不提供 v1 package 兼容层。若磁盘上出现未知 v1 包，Catalog 将其标为 unsupported，并返回结构化错误，不尝试猜测转换。

## 17. 结构化错误

沿用 `code + params`，新增或细化：

```text
theme.contract-version-unsupported
theme.capability-file-mismatch
theme.asset-id-duplicate
theme.asset-kind-mismatch
theme.asset-media-type-invalid
theme.asset-dimensions-exceeded
theme.asset-license-missing
theme.font-metadata-invalid
theme.font-coverage-invalid
theme.font-stack-unresolved
theme.icon-slot-unknown
theme.icon-asset-invalid
theme.wallpaper-slot-unknown
theme.wallpaper-asset-invalid
theme.recipe-state-invalid
theme.recipe-contrast-invalid
theme.generated-output-failed
```

构建期错误包含 package、source file、JSON path 和稳定 code；后端运行时仍只向前端返回稳定 code/params，用户文案由 i18n 映射。

## 18. 安全评审

### 18.1 信任边界

- v2 当前只构建仓库内官方主题，但所有输入仍按未来不受信任包的标准校验。
- 主题资源不执行；PNG/WebP 作为图片或 mask，WOFF/WOFF2 仅作为字体。
- 不接受远程 URL、SVG、GIF、视频、CSS、HTML、JavaScript 或 WASM。
- 生成 URL 必须为当前应用同源 `/theme-assets/...`。
- Web Runtime 不接触原始文件系统路径。

### 18.2 Tauri 边界

- 内置主题资源进入 Vite `frontendDist`，不扩大 Tauri capability。
- Core 不接收主题资源字节，不解析图标或壁纸，不增加长任务。
- 原生窗口只接收已校验的 `windowSurface` 颜色。
- 当前 `csp: null` 不因本方案新增远程 origin；后续单独启用 CSP 时必须允许同源 `font-src` 和 `img-src`。CSP 改造不与本次主题升级耦合，避免扩大变更面。

## 19. 性能影响评审

### 19.1 数据规模与算法

- 内置主题预计个位数，Catalog 仍为线性小集合。
- Theme SDK 构建扫描单包最多 128 个资源，复杂度为 O(theme + asset bytes)。
- Runtime resolver 只解析当前主题，font/icon/wallpaper map 使用稳定 ID Map，查找 O(1)。
- 不增加会话、文件、Markdown 或编辑器数据读取。

### 19.2 包体与加载

- 字体、图标和壁纸会增加安装包体积，必须受 16 MiB/主题预算约束并在发布构建记录差异。
- `@font-face` 使用 `unicode-range` 和实际字体栈懒加载；未使用 face 不请求。
- 主题图标由可见 `ThemeIcon` 按需加载；同一 hash URL 复用浏览器缓存。
- 壁纸只预加载当前可见槽；非当前主题、不可见页面壁纸不请求和不解码。
- Asset Catalog 只包含 metadata 和 URL，不把二进制放入 React state。

### 19.3 渲染与合成

- 静态壁纸只占一个 surface 背景层，不在消息、列表行或工具步骤重复创建图层。
- wallpaper overlay 与内容共享稳定 stacking context，不使用持续动画。
- icon mask 使用固定尺寸，不在运行时缩放超大资源。
- stepped/smooth motion 都只消费预定义 keyframe 和有限 CSS 属性；禁止 `transition: all`。
- visual quality 可以关闭 wallpaper/texture 和重材质效果，但不能改变业务语义。

### 19.4 验收基线

- 主题切换不新增数据请求。
- 已完成历史消息和 Markdown render 次数保持不变。
- CodeMirror `EditorView` identity 保持不变。
- 非当前主题字体、图标和壁纸没有网络/asset request。
- 当前壁纸解码后不产生超出文件尺寸预算的图片内存。
- 生产构建记录主题资源总字节、Vite 输出增量和应用安装包增量。
- 壁纸主题 fixture 在正常窗口、最大化窗口和快速切换主题时不得出现持续长帧或无界 GPU memory 增长。

## 20. 过度设计评审

### 20.1 本方案为何不过度设计

- 字体文件、图标和壁纸都是 v1 已声明但未完成的真实能力，不是为假设主题新增通用页面 DSL。
- shape/elevation/motion/scrollbar 是当前硬编码样式无法被主题包表达的最小结构集合。
- 图标按有限语义槽覆盖，不建立任意组件替换系统。
- 壁纸只支持四个稳定 surface、静态图片和有限布局参数，不建设背景场景引擎。
- 运行时复用 CSS variables、Vite static assets 和低频 Context，不新增数据库、队列、缓存服务或 IPC。
- 多语言字体只定义 face、coverage 和 stack；不实现下载式语言包，等出现真实包体问题再扩展。

### 20.2 明确不引入的抽象

- 任意 CSS AST 或 selector DSL。
- 主题级 React component registry。
- 图标脚本、SVG sanitizer 或远程 icon service。
- 动态壁纸播放器、shader、粒子系统和帧调度器。
- 用户级壁纸/图标偏好和覆盖优先级。
- v1/v2 双解析器和长期兼容桥。

## 21. 分阶段实施计划

### 阶段 0：冻结 v2 Contract

实施状态：已完成。

1. 定义 Manifest/Runtime/Asset Schema v2。
2. 定义 capability/file 一致性规则。
3. 定义 shape/elevation/motion/scrollbar 与 recipe 状态白名单。
4. 定义 Font Face/Stack、Icon Slot 和 Wallpaper Slot 枚举。
5. 同步 Web Zod、Theme SDK contract 和 Rust `serde`。

验收：非法字段、未知 slot、错误 asset kind、缺失 light/dark、capability/file 不一致均稳定失败。

### 阶段 1：Asset Pipeline v2

实施状态：已完成。

1. 增加 `resources.json` 与 `LICENSES.json` 交叉校验。
2. 校验 MIME、文件签名、尺寸、字体元数据、数量和体积。
3. 使用临时目录生成 hash 资源并原子替换输出。
4. 生成 Asset Manifest v2 和 Web asset catalog。
5. 将资源复制到 `web/public/theme-assets`。

验收：重复 ID、路径越界、符号链接、错误扩展名、尺寸超限、缺失授权和部分输出均被拒绝。

### 阶段 2：字体与多语言回退

实施状态：已完成。

1. 增加 fonts capability 文件和编译器。
2. 生成 `@font-face`。
3. 实现固定有序 stack resolver 和安全 fallback。
4. 把 UI / editor 字重、行高和显示名称投影到根变量。
5. 设置页默认字体卡读取当前有效显示名称。
6. 将 MiSans 从全局 CSS 迁入主题资源。

验收：中英文混排、缺字回退、缺失 font asset、本机字体覆盖、语言切换时 family 不变和字体加载失败均正确收敛。

### 阶段 3：结构 token 与 Recipe v2

实施状态：已完成；本开发节点未新增或执行 contract test。

1. 拆分 material、shape、elevation、motion 和 scrollbar。
2. 扩展 component roles 与 state recipe。
3. 先改共享 shadcn，再改 prompt-kit、workflow 和 editor surface。
4. 删除被 token 替代的固定圆角、阴影和动效。
5. 为新增 role 建立 DOM contract tests。

验收：两个现有主题无视觉回归；fixture 可以只改主题包得到明显不同的形状、状态和动效。

### 阶段 4：主题图标

实施状态：已完成运行时、slot registry、构建契约与首批应用壳消费；两个既有内置主题未新增具体图标设计，使用 Lucide fallback。

1. 实现 icon capability、slot registry 和 schema。
2. 构建 mask/image 两种 descriptor。
3. 实现 ThemeIcon 与 Lucide fallback。
4. 迁移首批跨页面共享图标。
5. 验证 light/dark、currentColor、品牌图标保留和错误资源回退。

验收：缺失任一主题 icon 不影响操作、可访问名称或主题启用；用户图片不受 `image-rendering` 影响。

### 阶段 5：主题壁纸

实施状态：已完成运行时、surface 标记、预加载、失败回退与 quality disable；两个既有内置主题未新增具体壁纸设计，使用语义底色 fallback。

1. 实现 wallpaper capability、slot registry 和 schema。
2. 为 Shell、Conversation、Workspace、Settings 标记稳定 surface。
3. 生成 wallpaper CSS variables 和安全 overlay。
4. 实现可见槽预加载、失败回退和 quality disable。
5. 验证原生窗口 resize 安全底色。

验收：壁纸失败只回退当前 surface；无叠层、无正文对比度下降、无额外 IPC。

### 阶段 6：v1 破坏式迁移与清理

实施状态：已完成。

1. 迁移 sasuke 与技术中性源包。
2. 更新生成 Catalog/CSS/Asset Manifest。
3. 更新主题数量和 Contract 版本测试。
4. 删除 v1 Schema、parser、硬编码字体和旧样式消费路径。
5. 更新设计文档和实施状态。

验收：仓库不存在 v1 runtime consumer，用户偏好无需迁移且原主题选择保持有效。

### 阶段 7：完整验证

实施状态：第四轮主题范围验证通过。Theme SDK 44/44、Web 194 文件 1279/1279、主题核心三文件行覆盖率 98.69%、Rust 主题定向 4/4、生产构建与 4/4 资源 bytes/SHA-256 均通过；生成 CSS 的 16 个 `family + weight + style` 匹配键全部唯一，跨主题冲突为 0。当前会话未提供内置浏览器后端，运行中的 sasuke 客户端又承载当前工作流，浏览器与 EXE 交互按环境阻塞放行且未伪造通过。Rust workspace 仍为 791/793，两个失败是前三轮已独立复现且不在主题改动范围内的 Git source-control 既有回归，保留为全仓门禁已知问题。

开发回修状态：已修复测试发现的真实接口边界缺陷。system scheme 在缺少 matchMedia 时使用 light 安全值；wallpaper 查询、Image 预加载和图标变更事件分发均按运行环境 capability 投影。v1 版本、`material.shadow`、全局 TTF 与旧成功日志等断言不恢复兼容路径，必须由后续测试节点迁移到 Contract v2；缺失 fixture 与 60% 核心覆盖率也由测试节点补齐。

第三轮开发回修状态：第二轮确认的 4 项主题真实缺陷均已从现有契约根部修复。字体声明名现在与 `fontkit` 读取的 canonical family 校验，同时保留合法命名空间和 `Variable/VF` 标记；资源发布先复制完整 staged snapshot，在 catalog 激活后清理所有未引用文件，适配 Windows watcher 持有目录句柄的环境；wallpaper 的成功与失败回调共同校验 generation、具体 `Image` request identity 与 surface 生命周期；应用入口已删除 Fontsource Inter 全局导入。

第四轮开发回修状态：跨主题字体冲突已从 identity 模型修复。作者 `family` 继续承载元数据校验和用户偏好名称；Theme SDK 为生成 package 派生 `runtimeFamily`，安全回退主题保持既有 identity，其余主题使用稳定主题作用域。`@font-face`、生成 CSS 字体栈、Web resolver、个性化自定义栈与 Rust Catalog 统一消费 runtime identity；主题切换只重新投影当前自定义栈，不改写持久化偏好。

1. Theme SDK 单测与 fixture build。
2. Web contract、resolver、组件和设置页测试。
3. Rust Catalog/serde 测试。
4. 前端类型检查、生产构建和 Rust compile/test。
5. 内置浏览器验证 light/dark、窄窗、长会话和设置页。
6. EXE 验证原生窗口底色、主题切换和资源加载。
7. 记录包体、资源请求、长帧和内存结果。

### 21.1 开发实现结果（2026-08-16）

- JSON Schema、Theme SDK Contract、Web Zod 与 Rust serde 已统一切换到 Contract v2，并拒绝 v1 package。
- `resources.json`、`LICENSES.json`、字体/图片元数据校验、content-hash 静态资源、Asset Manifest 与 Web/Rust Catalog 已打通；Windows 发布顺序先复制完整 staged snapshot、再激活 catalog、最后清理未引用 hash，避免 watcher 占用时留下半套产物或陈旧资源。
- 两个内置主题已迁移完整结构 token、state recipe 和字体资源。Inter 与 MiSans 以 WOFF2 随主题包分发；MiSans 为约 2.49 MiB 的简体常用字子集并声明 `zh-CN/Hans` 覆盖元数据，子集未覆盖的 CJK 字形继续由后续系统字体回退。
- Web Runtime 已实现固定有序字体栈解析、独立本地化显示名称、结构变量、ThemeIcon/Lucide fallback、四类 wallpaper surface 的可见预加载与失败回退。共享 shadcn、prompt-kit、应用壳、工作流、编辑器和 Diff 已消费稳定 role。
- 最终构建核验：2 个主题共 4 条资源记录全部进入 `web/dist/theme-assets`，文件字节数与 SHA-256 全部匹配，`web/dist` 中不存在旧 `.ttf`。
- 开发节点验证命令：`npm run themes:build`、`npm run web:build`、`cargo check --workspace` 均通过。Vite 仅保留仓库既有的大 chunk 与动静态混合导入告警；Rust 仅保留既有的 unused/private-interface 告警。
- 过度设计复核：未新增数据库、IPC、队列、缓存服务、用户偏好字段、任意 CSS/SVG/远程资源或 v1 兼容解析器；新增抽象仅对应 v1 已声明但未闭环的真实能力。
- 性能复核：单主题最多 128 个资源且总量 16 MiB，当前每主题资源约 2.54 MiB；运行时 Map 查找 O(1)，只加载当前 stack/可见 surface，图标 Context 低频更新，不触发业务数据刷新、Markdown 重建或 CodeMirror 销毁。

### 21.2 第三轮开发回修结果（2026-08-16）

- Theme SDK 在既有 `fontkit` 元数据投影上增加 family 一致性校验，不引入字体特例映射；声明名必须以 canonical family 为核心，只允许包命名空间与通用 variable 标记。
- `web/public/theme-assets` 发布改为完整快照同步：所有新 content-hash 文件就绪后激活 Web/Rust catalog，再按 staged 文件/目录集合删除陈旧产物；运行中的 Windows watcher 无需被终止。
- wallpaper surface 使用 `WeakMap<HTMLElement, HTMLImageElement>` 记录当前请求，`onload/onerror` 只有在 generation、request identity 和连接状态均属于当前投影时才能写入或清理 CSS 变量。
- `web/src/main.tsx` 与 npm 依赖清单删除 `@fontsource-variable/inter`，主题字体只经由内置主题的 Asset Manifest 注册。生产产物不再包含额外 `inter-*.woff2`，消除第二轮记录的 7 个文件、218,512 bytes 与对应旁路请求。
- 开发节点验证：`npm run themes:build`、`npm run web:build`、`cargo check --workspace` 均通过；4 条主题资源记录在 `web/public` 与 `web/dist` 的字节数、SHA-256 全部匹配，陈旧 fixture、额外 Fontsource Inter 和 `.ttf` 数量均为 0。Vite 与 Rust 仅保留此前已有的 chunk/import 和 unused/private-interface 告警。本开发节点按角色约束未执行测试，交由后续测试节点复验完整矩阵。
- 过度设计复核：没有新增 aggregate、持久字段、队列、缓存、依赖或兼容层；family 校验、snapshot 清理和 request identity 均复用既有主题资产与 generation 生命周期。
- 性能复核：构建期仅对上限 128 个资源做有界 O(n) snapshot 同步与清理；运行时每个可见 wallpaper surface 仅增加一次 WeakMap O(1) 读写，条目随 DOM 回收。移除全局字体旁路直接减少 218,512 bytes 生产资源与 7 个潜在请求，不扩大 React 订阅或渲染范围。

### 21.3 第四轮开发回修结果（2026-08-16）

- Contract v2 的生成态字体 face 新增编译器所有的 `runtimeFamily`；JSON Schema、Theme SDK、Web Zod、Web Runtime 与 Rust serde 已同步闭合。作者 `fonts.json` 出现该字段会以 `theme.package-invalid` 拒绝。
- sasuke 安全回退主题保留既有运行时 family；Tech Neutral 使用带主题作用域的 runtime family。生成 CSS 的 16 个 `family + weight + style` 匹配键均只对应一个 URL，冲突数为 0；每个 URL 均属于声明该 runtime identity 的当前主题。
- 自定义字体偏好继续保存作者 family。Web 应用投影在设置预览、应用启动与主题切换时映射到当前主题 runtime family；主题/语言变化会重新应用低频个性化投影，不新增持久字段或旁路状态。
- 开发节点验证：`npm run themes:build`、`npm run web:build`、`cargo check --workspace` 均通过；4 条 Asset Manifest 记录在 `web/public` 与 `web/dist` 的 bytes/SHA-256 全部匹配，无陈旧资源、额外 Fontsource Inter 或 TTF。本开发节点按角色约束未执行测试，交由后续测试节点复验新增接口和浏览器冷加载。
- 过度设计复核：`runtimeFamily` 只解决作者 identity 与浏览器全局匹配 identity 无法共用一个字段的具体不变量，属于生成态派生值；未新增数据库、aggregate、缓存、队列、请求管理器、依赖或兼容层。
- 性能复核：构建期对最多 128 个 face/asset 做有界 O(n) 派生；个性化投影以 Map 做当前主题 face 的 O(n) 建表和最多 16 项 O(1) 查找，只在启动、主题/语言或偏好变化时执行。运行时不再存在跨主题字体候选，预期消除非当前主题的 48,256-byte Inter 与 2,615,016-byte MiSans 请求。

### 21.4 round-002 开发复核结果（2026-08-16）

- 复核确认 Theme Contract v2、Asset Pipeline、字体运行时 identity、图标/壁纸回退和共享组件消费均已完整实现；本轮未发现需要新增业务抽象或补丁式兼容的代码缺口。
- 开发节点重新执行 `npm run themes:build`、`npx tsc -p web/tsconfig.build.json`、`npx vite build --config web/vite.config.ts` 与 `cargo check --workspace`，全部通过。Vite 完成 3,721 个模块转换；仅保留既有 large chunk/动静态 import 告警，Rust 仅保留 8 条非主题既有 warning。本节点按角色约束未编写或执行测试。
- 当前 `web/dist` 为 454 个文件、20,268,139 bytes；其中 `theme-assets` 4 个文件共 5,326,544 bytes，与 `web/public/theme-assets` 的文件数和字节数一致。相对 HEAD 旧入口的单个 20,000,736-byte MiSans TTF，主题静态资源净减少 14,674,192 bytes，且生产产物不再包含 TTF。
- 当前环境无法安全取得完整变更前 Vite/安装包基线，也未提供可用的浏览器性能采集后端；长帧、GPU/图片解码内存和安装包总增量按用户明确授权作为环境阻塞放行，记录为未执行而非执行通过。功能、契约、生产构建和静态资源完整性结论不受影响。
- 过度设计复核：没有新增状态、依赖、缓存、队列、IPC、持久字段或兼容层；继续复用现有 canonical theme identity、generation/request identity 和 shadcn/prompt-kit copy-in 消费层。
- 性能复核：本轮没有增加运行时代码；资源仍受每主题 128 项/16 MiB 硬边界限制，运行时仅低频投影当前主题并按需加载可见资源，不引入全量业务数据刷新、N+1、无界缓存、事件线程阻塞或更大的 React 订阅范围。

### 21.5 用户反馈主题级联回修（2026-08-16）

- 根因不是单个顶栏样式，而是 v2 生成 CSS 以高优先级把 recipe 声明直接写到所有 role，除几何外还会覆盖 shadcn/Tailwind 已声明的 focus ring、`shadow-none`、局部阴影、背景/边框色、transform 和 transition。
- Theme SDK 将 recipe 的颜色、材质、状态、elevation、motion 与 `border-width / border-style / border-radius` 全部编译到 CSS `components` layer；主题继续明确声明 role 默认视觉，组件 utility 只在确有变体、交互或拓扑约束时覆盖。Contract 新增 `radius:none`，Theme SDK、JSON Schema、Web Zod 与 Rust serde 同步闭合。
- 两个内置主题的 shell、titlebar、sidebar、panel、sheet、editor、diff 恢复为无完整 perimeter；activity、tool-card、button-primary、button-secondary 不再凭 role 新增完整边框。已有 Card、outline button、Sheet 单边 separator、tool 审计下分隔、工作流圆形/虚线和 split button 几何继续由组件显式规则表达。
- ACP usage/processing 行移除错误的 composer surface 身份，只继承真正的外层 composer。共享顶栏不显示下边框或四边圆角，左右栏与工作区仅保留既有布局分隔。
- 完成全属性浏览器复核：sasuke 与技术中性的 titlebar/sidebar 均为四边 `0px`、四角 `0px`；outline button 保持透明背景、可见边框和 `shadow-xs`；默认 input 保持显式背景/边框/阴影，聚焦时 3px ring 与基础阴影同时存在；split send 保持 `rounded-none + shadow-none + transition-all`，侧栏保持自身 opacity transition。验证后已恢复 sasuke 与原明暗模式偏好。
- 构建验证：`npm run themes:build`、`npx tsc -p web/tsconfig.build.json`、`npx vite build --config web/vite.config.ts`、`cargo check --workspace` 均通过；仅保留既有 Vite large chunk/混合导入告警和 Rust unused/private-interface 告警。本开发节点按角色约束未编写或执行测试。
- 过度设计复核：只增加现有 radius 枚举的 `none` 值并将既有 recipe 统一纳入原生 CSS Cascade Layers；没有新增 role、依赖、状态、缓存、兼容层或组件专用覆盖表。
- 性能复核：生成选择器数量与匹配复杂度保持 O(role)，级联层只影响 CSS 优先级；不增加 DOM、React render、事件、I/O、内存缓存或运行时脚本。

### 21.6 sasuke 顶栏表面一致性回修（2026-08-16）

- sasuke 浅色主题将 `titlebar` token 从 `#ffffff` 校正为 sidebar surface 的 `#fafafa`，使共享顶栏与其下方左侧导航在无分隔线时形成连续背景；深色 sasuke 和技术中性主题无需调整。
- 继续复用现有 titlebar/sidebar role 与 recipe，不修改共享组件、不读取 `themeId`，也不新增 token、状态、依赖或兼容层。
- 性能复核：仅改变一个构建期颜色常量及其生成产物，不增加 CSS 规则、DOM、React render、I/O、缓存或运行时计算。

### 21.7 会话辅助表面主题化（2026-08-16）

- Theme Contract v2 新增 `message-disclosure` 与 `runtime-control` 两个稳定 role，并同步 Theme SDK、生成 Schema、Web Zod、Rust serde、两个内置主题 recipe 和共享会话组件；不新增语义 token，继续复用现有 `activity`、`message-user`、`tool-card` 与 `border` 色彩事实。
- sasuke 将隐藏上下文与 AI 输出判定设计为低对比、无硬边框的层次表面；技术中性主题使用透明/白色表面、hairline 边框和主题自身 control 圆角。组件删除正常态 `primary` 染色与硬编码完整 perimeter，只保留展开内容分隔、joined artifact 操作、focus ring 和 invalid/destructive 语义变体。
- disclosure 展开状态继续位于单条消息的最小组件边界，runtime-control 继续消费 canonical timeline 投影；主题切换只改变 CSS recipe，不新增业务状态、持久字段或 timeline 派生路径。
- 同轮审计修通既有 role 的消费：权限申请和 Elicitation 使用 `permission-card`，独立思考/Activity 批次/上下文压缩使用 `activity`，文件变更摘要移除对 shadcn `Card` 表面的二次覆盖，会话树弹层和 Select trigger 分别回归 `popover`、`input`。附件尺寸、Markdown 结构、状态色、错误态、focus ring、joined separator 与响应式布局继续由共享组件负责，不进入主题包。
- 过度设计复核：只新增两个已经承担独立视觉职责的封闭 role，没有增加 token、依赖、缓存、队列、并发机制或兼容层；避免把两个主题的样式分支复制进业务组件。
- 性能复核：新增常量级 recipe 选择器与 DOM role 属性，不增加 React state、订阅、事件、I/O 或 Markdown 解析；生成与匹配复杂度仍为 O(role)，不会扩大历史消息重渲染范围。
- 视觉回修：根据双主题实机审视，将 `activity` recipe 在 sasuke 与技术中性中统一收敛为透明、无边框、无圆角、无 elevation 的纯文本摘要行；消息级复制操作缩为 20px 操作行与图标按钮，避免次要动作拉大消息节奏。
- 介入卡回修：两个主题的 `permission-card` 均改为透明外层、hairline 边界与零 elevation；权限参数使用局部左分隔，权限选项默认透明，Elicitation 只在选中/hover/错误等局部状态保留反馈，不再用整卡背景色表达等待状态。
- 回修评审：继续复用既有 `activity`、`permission-card`、prompt-kit `MessageActions` 与 shadcn `Button/Card`，不新增 role、token、依赖或状态。修改仅影响构建期 recipe 与常量级 class，不增加 DOM、React render、I/O、缓存或运行时计算，性能风险可忽略。
- 交互色校正：权限卡 Allow 系列选项的静态文字使用 `accent-foreground`，hover/focus 与 Elicitation 选项统一使用 `accent/60` 背景和强调边界；Reject 维持中性。Activity 摘要静态使用 `muted-foreground`，hover/focus/open 只提升为普通 `foreground`，不使用主题强调色，各状态均保持透明背景。

### 21.8 浮层 motion 语义修正（2026-08-16）

- 修正 Theme SDK 将 `surface` 与 `press` 都编译为 transform transition 的契约缺陷：`surface` 只允许颜色、边框与 elevation 过渡，transform 仅属于 `press`。Dropdown、Select、Popover、Dialog、Sheet 等定位型 surface 继续消费主题背景、材质、圆角和阴影，不再与 Radix 自有定位/开合 transform 竞争。
- recipe invariant 要求任何 `active.press=true` 必须同时声明 `motion=press`；两个内置主题的 ghost button 同步纠正声明。业务组件不读取主题 ID，也不增加 WebView2、复合菜单或具体 role 特判。
- 过度设计复核：复用现有四档 motion 枚举，只修正编译映射和已有校验，不增加 role、token、DOM wrapper、依赖、状态或兼容层。
- 性能复核：减少非按压 surface 的 transform transition，避免无意义的合成层竞争；CSS 选择器数量、运行时状态、渲染订阅和 I/O 均不增加。
- WebView2 复核反馈显示仅移除 transform transition 后二级菜单仍会令一级菜单滚动条瞬现。Radix `SubContent` 的 fixed Popper wrapper 位于一级 Content DOM 内，父级 `backdrop-filter` 会建立新的 fixed containing block；共享 Dropdown Content 因此声明 isolated material layer，Theme SDK 将 filter 投影到无交互伪元素，保留背景、材质、圆角和阴影且不改变菜单焦点树。
- 隔离层复核：只增加一个静态 data attribute 与一个伪元素，不增加 React state、事件、Portal、wrapper DOM 或依赖；伪元素没有后代，不会成为二级 Popper 的定位祖先。合成层数量与原材质实现相同，菜单打开复杂度、布局测量次数和渲染订阅均不增加。

### 21.9 Variable Font 与浮层定位边界回修（2026-08-16）

- 字体根因：v2 `FontFace` 只允许单值 `weight`，构建器因此把同一个 variable WOFF2 重复注册为 400/500/600/700 静态 face，导致组件请求的 330/380/450/520 被浏览器吸附到更粗档位。契约破坏式替换为 `weightMin + weightMax`，SDK 校验区间有序且包含于字体元数据范围，并生成 CSS range descriptor；两个内置主题各只保留一个 Inter variable face 与一个 MiSans variable face。
- 字体投影：Inter 暴露资产原生 `100–900`，MiSans 按现有产品层级暴露 `250–520`；固定有序栈、主题级 runtime family 隔离和自定义字体偏好保持不变。Theme SDK、生成 JSON Schema、Web Zod、包级 dist、Web/Rust Catalog 与 README 同步维护。
- Portal 根因：模态浮层默认 Portal 依赖 `body` 隐式宿主，无法通过接口保证祖先不建立 fixed containing block 或裁剪。应用壳新增与 `#root` 同级的稳定 overlay host，明确保持 `overflow: visible`、`contain/transform/filter/backdrop-filter: none`；Dialog、Sheet、AlertDialog wrapper 默认使用该 host，同时保留显式 `container` 覆盖能力与既有焦点语义。
- 菜单根因：Dropdown/Context Menu 的 Radix Content/SubContent 同时承担定位、裁剪、材质和 transform 动画，WebView2 中定位矩阵与开合矩阵会竞争。共享组件增加单个内部视觉层：Radix 节点只保留定位、焦点、dismiss 与事件 props，视觉层承载主题 role、材质、圆角、overflow 和全部 slide/zoom/fade 动画；业务菜单不新增特判。
- 回归：Theme SDK 固定合法区间生成、反向区间和越界区间拒绝；Web 契约固定两个 variable range、跨主题 match key 唯一、专用 host 的 DOM/CSS 边界，以及四类菜单 positioner 不包含动画/裁剪而视觉层完整承载这些声明。
- 过度设计复核：字体复用既有 metadata，Portal 复用 Radix `container`，菜单复用现有 shadcn copy-in，只新增一个静态 host 和每个已打开菜单的一层视觉 DOM；没有新增依赖、持久字段、React state、Context、缓存、队列或平台分支。
- 性能复核：每主题 font-face 从 8 条降为 2 条，减少 CSS 匹配键和字体 face 注册；host 查询是低频常量级 DOM ID 查找，菜单只增加一个静态 wrapper，不新增测量、监听或重渲染。定位节点不再参与动画 transform，可降低无意义的合成与重定位竞争。

## 22. 测试矩阵

### 22.0 round-001 第二轮测试结论（2026-08-16）

- 结论：不通过。第一轮的旧 v1 断言、接口环境缺口和覆盖率不足已经清除，但 4 项由需求矩阵直接导出的真实契约缺陷仍需由开发节点修复后复验。
- Theme SDK：TAP 计数 41/44 通过；其中一个失败子测试同时使父级组失败，实际有 2 个业务断言未满足：声明 `family` 未与字体文件元数据比对，生成目录重建后仍保留旧文件。其余 v2/v1、capability 对称、token/recipe、路径/链接、重复资源、签名/MIME、尺寸/数量/体积、授权、coverage/stack、icon/wallpaper 与 last-known-good 原子性矩阵均已固化并通过。
- Web Runtime：194 个测试文件中 192 个通过，1278 个用例中 1276 个通过。失败为旧主题 `Image.onerror` 没有 generation/request 身份校验、可清空新主题 wallpaper，以及 `web/src/main.tsx` 仍全局导入 `@fontsource-variable/inter/wght.css`、绕过主题资产图。
- 覆盖率：主题核心 `theme.ts`、`theme-contract.ts`、`ThemeAssetsContext.tsx` Lines 98.18%（216/220）、Statements 95.80%、Branches 84.61%、Functions 93.10%，达到至少 60% 的目标；失败断言仍由 `reportOnFailure` 如实保留。
- Rust/Tauri：主题 Catalog 与 appearance migration 定向测试 4/4 通过；workspace 全量 793 个用例中 790 个通过、3 个失败，其中 ACP timing 用例单独精确复跑通过，2 个非主题 Git source-control 用例单独复跑仍失败。
- 浏览器：两个内置主题的 light/dark/system 切换、设置页、640px 窄窗、恢复正常宽度、`/chat` deep link 和 app/workspace/conversation wallpaper slot 均通过，无横向溢出，控制台无 error/warn。页面资源清单实际观察到主题包字体和额外 `/assets/inter-*.woff2` 请求，确认全局字体旁路不是静态误报。
- 构建与资源：`npm run web:build` 通过并保留既有 Vite 告警；4/4 个 `web/public` 与 `web/dist` 主题资源的字节数、SHA-256 均匹配，主题资产总计 5,326,544 bytes，`.ttf` 数量为 0。全局 Inter import 另生成 7 个 `inter-*.woff2`，共 218,512 bytes。
- EXE：PID 25256 的运行中客户端继续占用 `target/static-dev/default/debug/sasuke-desktop.exe`；为避免中断当前工作流，没有终止该进程或用旧二进制冒充当前源码验收。本项按环境问题放行，但不标记为已执行通过。
- 过度设计复核：测试只扩充现有 Node/Vitest fixture 与接口用例，没有新增生产抽象、持久字段、依赖、缓存、队列或并发机制；当前缺陷均应在既有 asset pipeline 与 generation identity 内修复，不需要复制 canonical 状态。
- 性能复核：主题解析仍为 O(1) Map 查找，浏览器切换未触发控制台异常或布局溢出；无全量扫描、无界缓存或新订阅边界。全局 Inter 旁路造成 218,512 bytes 的额外构建资源和非主题资产请求，是本轮明确的包体/加载性能回归，不能放行。

### 22.0.1 round-001 第三轮测试结论（2026-08-16）

- 结论：不通过。第二轮的 4 项主题缺陷均已通过定向回归，但浏览器资源时间线与新增接口测试确认跨主题 font-face 匹配身份冲突，当前主题会请求另一主题的字体资源。
- Theme SDK：44/44 通过；字体 canonical family 校验、完整快照发布、陈旧资源清理和 last-known-good 均通过。
- Web Runtime：新增回归前为 194 文件、1278/1278；新增“一个 CSS font-face 匹配身份不得指向多个主题 URL”接口测试后为 1278/1279。主题核心 `theme.ts`、`theme-contract.ts`、`ThemeAssetsContext.tsx` 行覆盖率 98.67%（223/226）。
- 浏览器：`/settings` 与 `/chat` 的主题、light/dark/system、640px、1463px、1920px、wallpaper slot、无横向溢出和 0 console error/warn 均通过；但最终 `builtin.sasuke` 冷重载实际请求 `builtin.tech-neutral` 的 Inter/MiSans，违反“非当前主题字体不得请求”。全局 `/assets/inter-*.woff2` 旁路已经清除。
- 构建与资源：生产构建通过；4/4 主题资源在 public/dist 的 bytes 与 SHA-256 一致，总计 5,326,544 bytes，无陈旧文件、额外 Fontsource Inter 或 TTF。
- Rust：主题定向 4/4 通过；workspace 为 791/793，两个既有非主题 Git source-control 用例精确复跑仍失败。
- EXE：运行中的 sasuke 客户端承载当前工作流，为避免中断未启动第二个原生客户端；按用户允许的环境问题放行，但不标记为已执行通过。
- 过度设计复核：新增测试只解析现有生成 CSS 的封闭 `@font-face` 契约，没有新增生产依赖、状态或兼容层。修复应在生成器中统一 font-face runtime identity 或共享 URL，不需要复制主题 canonical state。
- 性能复核：额外字体请求直接违反加载边界；当前构建仍为有界 O(n)，运行时无新增缓存、队列或重型订阅。应以浏览器冷重载只出现当前主题字体 URL 作为修复验收。

### 22.0.2 round-001 第四轮测试结论（2026-08-16）

- 结论：主题范围通过。上一轮新增的跨主题 `font-face` 匹配身份回归已经转绿；Theme SDK 44/44、Web 194 文件 1279/1279、主题核心三文件行覆盖率 98.69%（226/229）、Rust 主题定向 4/4、生产构建及静态资源完整性均通过。
- 生成 CSS 共 16 个 `family + weight + style` 匹配键，16 个键全部唯一且冲突数为 0；sasuke 保留安全回退 runtime identity，Tech Neutral 使用主题作用域 runtime identity。作者 `family` 仍用于元数据与持久化偏好，应用时才映射为当前主题 `runtimeFamily`。
- 4 条 Asset Manifest 记录在 `web/public` 与 `web/dist` 的 bytes、SHA-256 全部一致，总计 5,326,544 bytes；无缺失、陈旧资源、额外 Fontsource Inter 或 TTF。
- 本轮会话未提供内置浏览器后端，无法执行 `/settings`、`/chat` 的真实浏览器冷加载网络复验；PID 25256 的 sasuke 客户端承载当前工作流，也未启动第二个 EXE。两项均按用户允许的环境问题放行，但不记录为已执行通过；网络隔离仍由新增 CSS 接口回归和生成资源契约固定。
- Rust workspace 为 791/793，失败仍是 `commit_review_aggregates_duplicate_paths_into_one_endpoint_diff` 与 `unborn_repository_has_an_empty_history_page_instead_of_an_error` 两个既有非主题 Git source-control 用例；主题改动未触及该文件，作为全仓门禁已知问题保留。
- 过度设计复核：测试未新增生产抽象、依赖、状态、缓存、队列或兼容层；`runtimeFamily` 只作为生成态派生 identity 解决浏览器全局匹配冲突，不复制 canonical 主题状态。
- 性能复核：资源扫描仍受每主题 128 个资源和 16 MiB 上限约束；运行时映射为低频有界 O(n) 建表与 O(1) 查找。静态契约确认不再存在会触发非当前主题字体的竞争 identity，未发现新增全量加载、N+1、无界缓存或过大 React 订阅。

### 22.0.3 round-002 测试结论（2026-08-16）

- 结论：主题范围通过。Theme SDK 44/44、Web 全量 194/194 文件 1279/1279、Web 主题定向 36/36、Rust 主题 Catalog/偏好迁移 4/4、TypeScript 与 Vite 生产构建均通过；主题核心 `theme.ts`、`theme-contract.ts`、`ThemeAssetsContext.tsx` 行覆盖率为 98.25%（225/229）。
- Web 全量首次在 SDK/Rust 并行争用资源时有一个非主题 `app-scheduled-task-detail.test.ts` 超过 60 秒；该用例脱离并行负载后精确复跑 1/1 通过，随后在无并行争用条件下全量复跑为 194/194 文件、1279/1279 用例通过，判定为测试资源争用抖动而非产品失败。
- 4 条 Asset Manifest 记录在 `web/public` 与 `web/dist` 的 bytes、SHA-256 全部一致，总计 5,326,544 bytes；生产产物无 TTF，生成 CSS 的 16 个 `family + weight + style` 匹配键冲突数为 0。
- 浏览器验证通过两个主题切换、sasuke/system、技术中性/dark、`/settings`、`/chat`、640px 窄窗与恢复宽度；app/conversation/workspace/settings wallpaper slot 完整，无可见横向溢出，控制台 0 warn/error。sasuke 冷加载只观察到当前主题两条字体 URL；预览后端 reload 后恢复默认偏好，无法执行技术中性持久冷重载，按授权记录为环境限制而非执行通过，跨主题字体隔离由 CSS identity 接口回归固定。
- Rust workspace 为 791/793；`commit_review_aggregates_duplicate_paths_into_one_endpoint_diff` 与 `unborn_repository_has_an_empty_history_page_instead_of_an_error` 两个既有 Git source-control 用例精确复跑仍失败。它们不在主题变更文件与主题验收范围内，但全仓门禁仍如实记录为非全绿。
- EXE 与安装包增量、GPU/图片解码内存和持续长帧量化未执行：当前运行中的 sasuke 客户端承载本工作流，且没有安全的变更前安装包/性能基线；按用户允许的环境问题放行，不标记为已执行通过。
- 过度设计复核：本轮未新增测试框架、生产抽象、状态、依赖、缓存、队列或兼容层；复用现有 Node/Vitest/Rust/browser 验证面，没有为已被接口契约覆盖的场景复制 canonical 数据。
- 性能复核：构建期资源扫描保持每主题 128 项/16 MiB 的有界 O(n)，运行时保持当前主题低频投影与 Map O(1) 查找；冷加载资源清单未出现非当前主题字体，未发现全量业务数据刷新、N+1、无界缓存、主线程阻塞或扩大的 React 订阅范围。环境受限的长帧、GPU 内存和安装包量化保留为未执行。

### 22.1 Theme SDK

- v2 manifest/package 正常构建。
- v1 package 被明确拒绝。
- capability/file 严格一致。
- token alias 循环、缺失引用和非法值失败。
- recipe 任意字段、未知 role/state 和非法前景背景组合失败。
- asset 路径逃逸、符号链接、大小写冲突和重复 ID 失败。
- MIME/签名不一致、尺寸/体积/数量超限失败。
- font metadata、weight、coverage、固定 stack 和引用失败。
- icon slot、renderMode、nativeSize 和 asset kind 失败。
- wallpaper slot、fit、position、opacity 和 asset kind 失败。
- 失败构建不破坏上一次完整生成产物。

### 22.2 Web Runtime

- `themeId + scheme + quality + personalization` 解析稳定。
- UI / editor 字体域不串值。
- 主题字体显示名称随主题和语言变化。
- 本机字体与自定义字号继续覆盖主题值。
- 图标按 scheme override -> default -> Lucide 回退。
- 图标错误不删除按钮和 accessible name。
- wallpaper 按 visible slot 加载并在失败时回退语义背景。
- performance 关闭 wallpaper 后不改变其他主题语义。
- 主题切换不重建 timeline、Markdown 和 CodeMirror。
- Icon Context 更新不让非图标重型子树重渲染。

### 22.3 Rust/Tauri

- Rust Catalog 只接受 Contract v2。
- 内置主题 ID 与用户 Appearance Preference 保持一致。
- 未知/损坏主题仍回退 `builtin.sasuke`。
- 原生窗口背景只使用 `windowSurface`。
- 主题资源全部来自 `frontendDist`，无需新增 fs/capability。
- release bundle 包含所有 Asset Manifest 记录文件且 hash 对应。

### 22.4 视觉与交互

- 两个内置主题 light/dark/system 无视觉回归。
- default、hover、active、selected、focus、disabled、loading 状态前景背景配对正确。
- focus ring 始终可见，键盘导航和危险操作语义不变。
- 正常宽度、窄窗口、重新拉宽、最大化和长文本无溢出或重叠。
- 会话、Markdown、工具卡、权限卡、工作流、右侧文件、Diff 和设置页 role 覆盖完整。
- 图标尺寸不改变按钮和触控目标。
- 壁纸 surface overlay 保持正文 4.5:1、UI 边界 3:1。
- 用户图片、附件缩略图和业务预览保持原始渲染方式。

## 23. 文档同步

实施时必须同步维护：

- `docs/sasuke/产品设计文档/interaction/app/settings.md`
- `docs/sasuke/产品设计文档/interaction/app/shell.md`
- `docs/sasuke/产品设计文档/interaction/app/conversational-home.md`
- 涉及 editor/diff role 时维护 `workspace-files.md` 和 `source-control.md`
- `theme-sdk/README.md`
- 本方案的阶段状态、验证结果和已知限制

只有通用规则本身发生变化时才修改 `docs/sasuke/rules/`，不得把已有交互、性能和数据完整性规则复制为第二真源。

## 24. 完成定义

Theme Engine v2 只有同时满足以下条件才算完成：

1. Theme Contract、JSON Schema、Web Zod、Rust `serde` 和 SDK 构建器全部切换到 v2。
2. v1 parser、Schema、硬编码字体和重复消费路径已删除，不存在双版本运行时。
3. 两个现有内置主题迁移完成，视觉与用户偏好语义无回归。
4. 字体文件、字重、覆盖范围、固定有序 stack 和 fallback 通过主题包声明并实际生效，界面语言不参与 family 解析。
5. 设置页默认字体名称来自当前有效主题投影，不新增高级字体设置。
6. shape、elevation、motion、scrollbar 和 state recipe 被共享组件实际消费。
7. icons capability、首批语义槽、ThemeIcon 和 Lucide fallback 完整可用。
8. wallpapers capability、四个 surface slot、overlay、预加载和失败回退完整可用。
9. 图标和壁纸不新增用户设置，不改变业务可访问名称、布局或交互语义。
10. 所有主题资产通过 hash、类型、尺寸、授权和路径校验并进入 Vite `frontendDist`。
11. 主题切换不新增数据请求、不重建历史 Markdown、不销毁 CodeMirror、不扩大 Tauri 权限。
12. SDK、Web、Rust、生产构建、浏览器、EXE、视觉、性能和包体验收全部通过。
13. 仅使用非分发 fixture 即可证明 v2 能表达字体、图标、壁纸和高差异组件视觉，而不引入任何新增正式主题设计。
