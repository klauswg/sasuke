# 桌面排版偏好实施记录

## 2026-08-14 — UI / 编辑器字体域拆分 ✅

- [x] “设置 → 个性化 → 字体”拆分为“界面 UI”和“编辑器”两个 Collapsible 分区，分别配置字体与基准字号；展开状态仅通过 `sessionStorage` 在应用会话内记忆。
- [x] UI 字号通过派生 token 覆盖侧栏、设置、聊天正文、Thought 与普通 Markdown；聊天代码只使用等宽字形，字号仍由 UI token 派生。
- [x] 编辑器字体与字号只覆盖 CodeMirror 文件、运行产物、Markdown 编辑器、Git Diff 与本轮 Diff。
- [x] 桌面偏好写入改为前端串行 latest-wins、后端单次 load/save；恢复默认不会与输入框失焦重复提交，也不再清空 task、workflow、round 等无关状态。
- [x] 全局常规字重语义校准为 `400 / 500 / 600`，并用 `text-ui-*` token 替代普通 UI 中绕过字号偏好的固定像素字号。
- [x] 性能影响限于低频根 CSS 变量更新与单次配置写入；未增加全局 Context、编辑器重建、额外依赖、缓存或无界队列。
- [x] 通过生产构建、Vitest 接口测试及 Rust 字号边界/偏好持久化测试固化回归边界。
