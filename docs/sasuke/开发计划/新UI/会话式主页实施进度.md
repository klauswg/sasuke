# 会话式主页 — 实施进度

> 最后更新：2026-08-20

## 2026-08-20 共享标题栏品牌标识可见性校准

- 需求：用户反馈紧凑标题栏中的 sasuke Logo 视觉尺寸偏小。
- 根因：品牌标识虽然复用共享 `AppTitleBar`，但静态 layout token 将其限制为 24×36px；边框和内边距进一步降低了彩色标识的有效可视面积，和 16px 标题相比权重不足。
- 方案：将唯一的 `brandMarkClassName` token 提升至 28×36px。保留 canonical `/logo.svg`、标题、28px 导航按钮、36px 顶栏和平台拖拽/窗口控制策略，不增加页面级尺寸、状态、依赖或兼容路径。
- 过度设计评审：现有共享品牌组件和资源链已经正确，只需完善静态布局规格；不新增抽象、持久化字段或响应式分支。
- 性能评审：仅变更静态 Tailwind class；没有状态更新、数据加载、额外 DOM、I/O 或热路径开销。
- 验收固化：`web/tests/app-title-bar.test.ts` 断言共享品牌规格为 `size-7`，防止后续紧凑化回退。

---

## 2026-07-23 Direct 配置边界与创建失败修复

- 产品边界：Direct 的 Agent、模型和权限全部在快速会话 composer 内完成，运行模式管理页删除 Direct tab 和配置区，只保留工作流与 AUTO；Direct 校验错误不再提供跳转运行模式管理的“去配置 / 修复”按钮。
- 根因修复：Direct 内部 worker 使用 `raw-agent` prompt envelope，本应完全绕过 sasuke 角色层，但 workflow profile resolver 仍对所有 worker 强制解析 profile，导致创建时报 `direct-agent is not associated with role`。
- 数据规则：profile resolver 按 prompt envelope 分流；`runtime-managed` worker 继续强制有效 profile，`raw-agent` worker 不解析 profile 且禁止绑定非空 profile，确保 Direct 的空 system prompt 不被角色内容污染。
- 验收固化：新增 resolver 正反例单测与 Direct task 创建接口回归测试；运行模式管理 toolbar 测试明确断言不包含 Direct。

---

## 构建状态
- ✅ `npm run web:build` — TypeScript 类型检查 + Vite 生产构建通过（1964.72 KB JS + 177.45 KB CSS）
- ✅ `npm run web:test` — 59 个测试文件、391 个用例通过
- ✅ 本地 UI deep link 验证 — `/` 自动进入 `/chat`，标题栏无工作台入口；`/tasks` 旧工作台仍可访问且控制台无错误

---

## 2026-07-22 工作台入口观察期隐藏

- 需求：暂时隐藏产品内工作台入口，默认直接进入会话主页；工作台页面保留一段观察期，后续根据反馈决定是否删除。
- 根因：共享标题栏仍暴露 Workbench / Conversation 双向切换，同时启动状态会读取历史 `sasuke-ui-mode`，使“会话主页为主入口”的信息架构无法稳定生效。
- 方案：移除共享标题栏模式 toggle 及其前端回调链；根路径 `/` 统一解析为会话主页并替换到 `/chat`；启动与浏览历史按当前路由决定 UI mode，不再由历史模式偏好覆盖。保留 `/tasks` 等显式工作台 deep link 和页面实现，不新增隐藏开关、兼容层或 fallback。
- 验收固化：`web/tests/routes.test.ts` 覆盖根路径默认会话主页、`/chat` 稳定解析和 `/tasks` 工作台 deep link 保留；`web/tests/app-title-bar.test.ts` 覆盖共享顶栏不再渲染工作台/会话模式切换控件。

---

## 2026-07-10 工作流编辑器输入画布闪烁修复

- 问题：运行模式管理页编辑工作流模板时，右侧 Inspector 输入框每输入一个字符，左侧 ReactFlow 画布会闪烁，尤其是目标、prompt、schema 等长文本字段。
- 根因：`WorkflowEditor` 用完整 `workflow` 对象同时驱动画布投影和右侧表单；配置字段逐键更新时也会触发 `workflowToFlow()` 重算全部 nodes/edges。
- 方案：新增作者态工作流画布签名，只包含 entry、节点 id/type/provider 与边 from/to/on 等影响画布结构或呈现的字段；ReactFlow 投影依赖该签名、选中态、校验高亮和终点显隐。右侧配置字段仍更新完整草稿，保证保存正确，但不再逐键刷新画布。
- 验收固化：`web/tests/workflow-editor-graph-signature.test.ts` 覆盖 Inspector-only 配置不改变画布签名，provider/edge 等画布字段改变时签名必须变化。

---

## 2026-07-10 旧 UI 工作空间选择页与新 UI 切换边界修复

- 问题：旧 UI 打开“选择工作空间”覆盖页后切换到新 UI，会话模式仍被旧 UI 选择页占住；只有打开一个旧 UI workspace 后才恢复正常。同时旧 UI 最近工作空间列表只能打开，不能移除历史项。
- 根因：`workspacePickerOpen` 是 App 级状态，`renderConversationContent()` 也优先渲染它，导致旧 UI 的覆盖层跨越了新旧 UI 状态边界；最近 workspace 只有 `choose_workspace` / `select_recent_workspace` 写入和选择接口，缺少“移除最近记录但不切换 workspace”的命令。
- 方案：将 workspace picker 明确定义为工作台模式覆盖层，启动自动打开仅限 `uiMode=workbench && needsWorkspace=true`；切到会话模式时关闭该覆盖层，并且在旧 UI 尚未选择 workspace 时不把占位 cwd 同步到新 UI。新增 `remove_recent_workspace` 命令和前端 API，最近列表行使用 shadcn Button + Tooltip 与 Trash2 图标提供移除操作；当前 workspace 与唯一有效 recent 项禁用移除，命令层返回结构化错误兜底。
- 验收固化：后端新增最近 workspace 删除持久化单测；前端新增 workspace picker scope 单测覆盖工作台 picker 边界、当前/最后一个 recent 禁删规则，并补齐 API facade / desktopApi / browserApi 对 `remove_recent_workspace` 的透传与拒绝测试。

---

## 2026-07-09 运行模式管理新增工作流模板草稿修复

- 问题：新 UI 的运行模式管理页点击“新增模板”后只显示居中空态文案，没有进入工作流编辑器；旧 UI 创建任务抽屉同流程正常。
- 根因：新 UI 的新增模板分支把 `wfEditWorkflow` 置为 `null`，而页面用 `wfEditWorkflow` 作为是否渲染 `WorkflowEditor` 的条件，导致新增模板被当成无草稿空态。
- 方案：抽出共享 `createBlankWorkflowDraft()`，旧 UI 创建任务抽屉和新 UI 运行模式管理页都用同一套空 `WorkflowDsl` 草稿；新 UI 新增模板时设置 `wfEditTemplateId = null` 且 `wfEditWorkflow = blankWorkflow`，选择器显示“新增模板（未保存）”并立即展示编辑器。
- 验收固化：`web/tests/run-mode-management-page.test.ts` 覆盖运行模式管理页新增模板入口必须创建可编辑 workflow 草稿，防止回退为 `null` 空态。

---

## 2026-07-04 AUTO 全局 Goal 输入空格修复

- 问题：会话主页新建会话使用 AUTO 模式时，全局 Goal 输入框输入空格后会立即消失，导致无法输入包含空格的目标文本。
- 根因：`ConversationComposer` 的 AUTO 会话级配置在每次 `onChange` 时立刻 `trim()` 后保存到父级 `runMode`，随后 `useEffect` 又把父级配置反灌回输入框，导致正在编辑中的尾随空格被删除。
- 方案：区分编辑态与有效性判定。编辑态通过 `optionalRunModeText` 保留原始字符串；创建会话 payload 边界通过 `normalizeConversationAutoConfigForSubmit` 保留非空文本原样，仅把纯空白值折叠为未设置。
- 横向检查：前端显式拦截空格的输入相关位置主要是可键盘激活的按钮/列表项；普通 `Input` / `Textarea` 未发现同类每按键 trim 反灌问题。运行模式管理页的全局 Goal 是表单保存模式，不存在会话主页这种逐键同步链路。
- 验收固化：`web/tests/conversation-run-mode-config.test.ts` 覆盖编辑态保留空格、提交态不裁剪非空文本、以及受控输入反灌不吞尾随空格。

---

## 2026-06-30 首页 composer 草稿跨页面保留

- 问题：在会话主页输入未发送正文（及附件）后，离开会话主页再返回时，正文与附件不应丢失；离开路径包括跳转运行模式管理（去配置 / 修改 AUTO / 修改工作流）、打开其他会话窗口、进入设置等普通跨页面导航。
- 根因：`ConversationComposer` 的 `content` 与 `attachments` 原为组件本地 `useState`，组件卸载会销毁草稿；附件图片的 `previewUrl` 也不能由短生命周期 hook 在普通卸载时释放。
- 方案：沿用「创建任务草稿跨页面保留」同一心智，`web/src/lib/conversation-composer-draft.ts` 由 App 层 owner 统一持有正文与附件，经 `ConversationComposerDraftProvider` 下发；`ConversationComposer` 受控消费草稿，`useAttachmentPicker` 支持外部附件状态注入。
- 生命周期：普通跨页面导航不清空草稿，也不释放图片 preview URL；仅显式移除/清空附件、创建会话成功、切换目标工作空间或应用卸载时释放附件资源并清空对应草稿。
- 验收固化：`web/tests/conversation-composer-draft.test.ts` 覆盖正文/附件跨页面保留、普通保留不释放 preview URL、owner 清理路径释放 preview URL 与 reset 语义。

## Phase 1 — 基础形态与文档 ✅
| 文件 | 操作 | 说明 |
|---|---|---|
| `web/src/pages/ConversationHomePage.tsx` | 新增 | 会话主页（含 composer props） |
| `web/src/pages/ConversationRunPage.tsx` | 新增 | 会话运行时页面 |
| `web/src/pages/RunModeManagementPage.tsx` | 新增 | 运行模式管理 |
| `web/src/components/conversation/ConversationShell.tsx` | 新增 | 会话式壳 |
| `web/src/components/conversation/ConversationSidebar.tsx` | 新增 | 侧边栏（workspace、置顶、task 行） |
| `web/src/components/conversation/ConversationComposer.tsx` | 新增 | 新建会话输入框 |
| `web/src/components/conversation/ConversationRunHeader.tsx` | 新增 | 运行时顶部栏 |
| `web/src/components/AppTitleBar.tsx` | 新增 | 新旧 UI 共用自定义桌面顶栏 |
| `web/src/components/conversation/ConversationSessionSwitcher.tsx` | 新增 | Session 切换器 |
| `src-tauri/src/commands_conversation.rs` | 新增 | 13 个 Tauri 命令 |
| `src-tauri/src/view_models_conversation.rs` | 新增 | Conversation VM 类型 + 构建函数 |
| `docs/sasuke/产品设计文档/interaction/app/conversational-home.md` | 新增 | 产品设计文档 |
| `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md` | 新增 | 产品设计文档 |
| `docs/sasuke/产品设计文档/interaction/app/run-mode-management.md` | 新增 | 产品设计文档 |
| `web/src/types.ts` | 追加 | 17 个新类型 |
| `web/src/i18n.ts` | 追加 | conversation.* / runMode.* 中英文 |
| `web/src/api/client.ts` | 追加 | 13 个 API 方法签名 |
| `web/src/api/desktop.ts` | 追加 | Tauri 实现 |
| `web/src/api/browser.ts` | 追加 | 浏览器 mock |
| `web/src/api.ts` | 追加 | re-export |
| `src/config/mod.rs` | 追加 | StateConfig 扩展 conversation 字段 |
| `src/storage/mod.rs` | 追加 | state.json 路径（已存在） |
| `src/app/mod.rs` | 追加 | load/save_state() 含 conversation 字段 |
| `src-tauri/src/main.rs` | 追加 | 模块声明 + 命令注册 |
| `web/src/routes.ts` | 微量修改 | 扩展 /chat/* 路径 |
| `web/src/components/Shell.tsx` | 微量修改 | WorkbenchShell/ConversationShell 拆分 |
| `web/src/App.tsx` | 微量修改 | uiMode 状态 + conversation 页面渲染 |
| `src-tauri/src/commands.rs` | 微量修改 | 2 个 fn 改 pub + write_json import |

---

## Phase 2 — 会话侧边栏 ✅
- [x] `ConversationSidebar.tsx` — workspace 分组、置顶区、状态圆点、相对时间
- [x] `ConversationShell.tsx` — 独立组件，接收 sidebar VM
- [x] App.tsx 集成 — sidebar 状态、search dialog 占位

## Phase 3 — 运行模式 + 创建流程 ✅ (前端骨架)
- [x] `ConversationComposer.tsx` — 文本输入、运行模式切换、Agent/Model 选择
- [x] `RunModeManagementPage.tsx` — AUTO模式 + 工作流模式
- [x] `ConversationHomePage.tsx` — 集成 Composer

## Phase 4 — 会话运行时 ✅ (前端骨架)
- [x] `ConversationRunPage.tsx` — 运行时主页面，复用 ACPChatDialog
- [x] `ConversationRunHeader.tsx` — 标题、操作按钮
- [x] `ConversationSessionSwitcher.tsx` — round/node/attempt 层级树
- [x] 活跃 session chooser（多 session 折叠行）
- [x] 重跑二次确认弹窗

## Phase 5 — 运行中行为打磨 ✅
- [x] at-bottom 状态暴露 — `onAtBottomChange` prop 传递到 ConversationRunPage
- [x] 自动切换/折叠规则 — session 完成时位于底部自动切换到下一活跃 session
- [x] 互斥 composer 状态 — `AcpExternalComposerState` (`invalid-workflow` / `runtime-error`) 覆盖 composer 区域
- [x] 产物/附件信息区 — `ConversationAssetsBar` 集成到 ConversationRunPage

## Phase 6 — 搜索、置顶、重跑 ✅
- [x] 跨 workspace 搜索 — search 结果从 task_path 提取 project_id 并匹配 workspace 名
- [x] pin/unpin/reorder — 新增 `reorder_pinned_conversations` 后端命令 + 前端 API
- [x] title inline edit — ConversationRunHeader 点击标题进入编辑，Enter/blur 提交，Escape 取消
- [x] 自动标题截取长度配置化 — 新建会话自动标题的首行截取长度从硬编码 `12` 收敛为 `configs/app-config.toml` 的 `conversationAutoTitleMaxChars`，默认保持 12，且仅影响之后新建会话
- [x] task-level attachments — ConversationComposer 附件按钮启用，hidden file input 收集文件名

## Phase 7 — 附件上传（完整链路）✅
- [x] ConversationComposer — 真实附件状态（picker + drag/drop + paste + chips + 图片缩略图预览 + 大图 Dialog）
- [x] App.tsx — 会话首页提交链路：validateConversationCreate → createConversationRun → 跳转运行态
- [x] pick_attachment_files — Tauri 系统文件选择器 IPC 命令
- [x] validateConversationCreate — 真实校验（附件类型/大小/数量、agent/模板）
- [x] createConversationRun — 真实 task 创建（AUTO workflow 生成、附件持久化到 `authoring/inputs`、run 启动）
- [x] rerunConversationTask — 真实重跑（kill 运行中 run → 启动新 run）
- [x] 文档更新 — conversational-home.md / conversational-runtime.md / 实施进度

## Phase 8 — 共享 ACP 附件上传（新旧 UI 运行态） ✅
- [x] ACPChatDialog — picker + drag/drop + paste + 附件 chips + 图片缩略图预览 + 大图 Dialog
- [x] submitPrompt — 发送时附带附件文件名信息；发送后自动清空附件
- [x] RoundDetailPage（旧 UI）— 复用 ACPChatDialog 附件能力，无需单独改动

- [x] 会话删除能力 — 侧边栏 task 行新增删除按钮；删除前弹出不可撤销确认；确认后删除 `~/.sasuke` 对应 task 目录，并在系统支持时优先移入回收站；运行中的会话禁止删除

## Phase 9 — 验证与收敛
- [x] cargo check 通过
- [x] tsc 类型检查通过
- [x] Vite build 通过
- [x] agent-browser 验证 — /chat 页面正常，中文 i18n 正确，toggle 双向切换正常
- [x] 侧边栏 hover 操作区收敛 — task 行补齐 `w-full/min-w-0/overflow-hidden` 约束；约束 Radix ScrollArea 内部 content wrapper 为 `w-full/min-w-0`，避免长标题把时间撑出侧边栏视口；hover 操作用绝对定位贴右显示，未 hover 时不占位
- [x] 侧边栏顶部压缩 — 降低品牌区、导航按钮、分隔线与分组间距，把更多纵向空间留给置顶和工作空间会话列表
- [x] 多工作空间支持 — 置顶区可折叠并与工作空间列表共用滚动区域，顶部功能入口保持固定；工作空间标题 sticky 吸附 + hover 显示 +/- 操作；底部添加工作空间入口；添加去重 + 历史加载；删除仅移除侧边栏；持久化 workspace 列表和 `lastConversationWorkspace`；模式切换自动同步工作空间上下文；启动自动展开上次工作空间，新会话自动定位并展开对应工作空间
- [x] 快速对话 workspace 切换保留 composer 草稿（2026-08-19）— composer 选择器与左侧工作空间“新会话”入口统一视为导航上下文切换，不再清理 App 层未提交草稿；正文、图片和其他附件保持不变，草稿仅在创建成功或用户明确清空/放弃时 reset。补充草稿状态单元测试并保留创建成功清理回归。
- [x] 多工作空间命令修复（2026-06-15）— `create_conversation_run` / `rerun_conversation_task` / `get_conversation_run` / `validate_conversation_create` / `switch_conversation_session` / `update_task_metadata` / `show_conversation_attachment` 全部改为按 `projectId` 解析 workspace-scoped `App`（复用 `workspace_entry_for_project` + `app_for_workspace`），不再使用旧 UI 全局 `DesktopContext.repo_root`；`switch_conversation_session` 和 `show_conversation_attachment` 新增 `projectId` 参数并透传全链路（Frontend API → 类型 → 组件 props）；`ConversationComposer` 使用本地 `selectedProjectId` 消除切换 workspace 后立即提交时的状态竞争；侧栏聚合改为命令层基于 `DesktopContext` 统一构建 `ConversationWorkspaceSource`，VM 只做 source → sidebar 映射，避免在 VM 内直接 `App::new` 造成配置来源不一致；新 UI → 旧 UI 切换复用 `applyWorkspace` 刷新旧 UI 页面栈
- [x] 会话工作空间身份与旧状态迁移收敛（2026-07-24）— `conversationWorkspaces` 成为侧栏和搜索的唯一列表来源，删除会话模式对 `DesktopContext.repo_root` 的隐式注入；新增共享 workspace resolver，统一首轮、Direct 追问、历史、权限、停止、附件和置顶链路的 Windows 大小写解析。`stateSchemaVersion=1` 一次性按规范路径去重，并迁移 `lastConversationWorkspace`、`conversationRunModes`、`conversationPins`；规范 run-mode key 优先保留，迁移完成后原子写回，后续启动直接跳过。
- [x] 工作空间安全移除交互（2026-07-24，2026-07-25 紧凑化）— 复用 shadcn/ui `AlertDialog`，确认标题展示工作空间名称，说明磁盘文件和历史会话保留；提供右上关闭、取消、浅红危险操作和 pending 防重复。弹窗收敛为约 480px 最大宽度、24px 内边距与小号正文/按钮。后端按解析后身份删除列表项及其置顶/运行模式/最后活跃引用，不删除 task/run/session；当前打开被移除工作空间时安全返回会话主页。补齐键盘 `focus-within` 操作显示、空工作空间文案和前后端回归测试。
- [x] 侧边栏 sticky 标题 / overlay 滚动条层级修复 — 提升 ScrollArea scrollbar 层级，滚动经过工作空间 sticky 标题时滚动条连续显示，不再被标题背景截断
- [x] 侧边栏工作空间展开意图隔离（2026-08-15）— 展开状态只在初次加载时按最后活跃 workspace 初始化，后续侧栏 VM 刷新仅按稳定 `projectId` 对齐集合并保留用户手动收起；composer 草稿 workspace 与侧栏展开解耦，新建/重跑成功改用带 request identity 的显式 reveal 事件，覆盖同 workspace 重复展开且不受追问完成刷新影响；补充组件接口回归测试固定草稿切换、刷新和显式 reveal 三条契约
- [x] 会话信息栏单行布局 — sessionId 上移至第一行与模型名、权限模式同行；新增查看产物按钮（有产物/附件时显示），点击弹窗列出产物和附件，支持点选查看具体内容
- [x] 工作流编辑/查看按钮 — 新增查看工作流按钮（Eye 图标）；编辑工作流打开 Sheet 内嵌 WorkflowEditor；查看工作流改为复用旧 UI 运行态工作图数据链路，显示实际路径、节点状态图标、产物/附件计数等运行信息
- [x] 会话态产物按钮修复 — `ConversationRunVm` 改为返回当前选中 session 的 artifacts / attachments；`ACPChatDialog` 打开产物详情时透传 AI-DYNAMIC 的 `outerNodeId/outerAttemptId`，保证按钮展示与详情打开都基于真实 session 数据
- [x] 产物弹窗弱化遮罩 — 仅给 ACP 产物列表/详情弹窗增加轻量 blur overlay，并进一步降低主体面板底色/边框/阴影权重，不影响其他 Dialog
- [x] 快速 session 切换 — 新增 `switch_conversation_session` 轻量 Tauri 命令，切换时不重建整个 session tree，只加载目标 session 的 ACP 数据和产物，消除 1-2 秒延迟

## Phase 10 — 输入附件展示与重跑闭环 ✅
- [x] ConversationRunVm 新增 `inputAttachments` 字段 — 读取 task `authoring/inputs` 目录
- [x] 前端类型 ConversationRunVm 同步新增 `inputAttachments`
- [x] ConversationAssetsBar — 三区展示：输入附件（Upload 图标 + 蓝色标记）/ 产物 / 附件
- [x] ACPChatDialog — 新增 showConversationAttachment API，支持点击查看输入附件（文本读取、图片 base64 预览）
- [x] 重跑自动复用 task 级 `authoring/inputs`
- [x] 校验错误码 i18n — conversation.validation.* 中英文映射
- [x] show_conversation_attachment Tauri 命令 — 支持图片 base64、文本文件读取
- [x] cargo check & tsc --noEmit 通过

## Phase 11 — 附件交互修正 ✅
- [x] 图片大图预览收敛为固定适配尺寸原图查看，不支持缩放或拖拽
- [x] 预览 Dialog 去掉右上角关闭叉号，仅保留点击空白遮罩关闭
- [x] 新会话输入框 — 补强 textarea 本体拖拽上传；桌面端基础配置与 channel overlay 都关闭 Tauri WebView 默认文件拖拽接管，并用页面级 dragover 预处理消除禁止光标闪烁
- [x] ACP 输入框 — 补强 PromptInputTextarea 本体拖拽上传；桌面端基础配置与 channel overlay 都关闭 Tauri WebView 默认文件拖拽接管，并用页面级 dragover 预处理消除禁止光标闪烁
- [x] 文档同步更新为“沉浸式遮罩预览 + 固定原图查看”说明
- [x] 会话侧边栏选中态间距优化 — 会话列表、置顶列表和展开 run 列表增加轻微纵向 gap，避免连续选中背景粘连
- [x] 置顶区标题统一 — 置顶区工作空间名与工作空间区标题统一为同一套小号全大写字重/字距样式；置顶区改为对称上下分隔线包裹，消除上下横线视觉粗细差；同时压缩区块与标题下方留白，使置顶会话行距更接近工作空间列表
- [x] 新旧 UI 共享自定义顶栏 — 新增 `AppTitleBar` 统一承载侧边栏折叠、品牌 icon+标题、Workbench/Conversation toggle 与窗口控制；桌面端启动后关闭整窗原生 decorations，顶栏颜色跟随主题切换，侧边栏内移除重复的品牌与形态切换入口
- [x] Windows 顶栏拖拽与外轮廓补强 — 共享顶栏改为整条非交互区域默认可拖，按钮与 Workbench/Conversation toggle 显式退出拖拽区，并用 Tauri `startDragging` + WebView app-region 双通道提升 Win10/Win11 拖拽命中稳定性；根壳层新增不影响点击的主题化轮廓 overlay，顶部线使用独立 token 加强，弥补 Win10 无原生 decorations 时顶部轮廓缺失
- [x] 侧边栏折叠动画 — 新旧 UI 侧边栏保留 DOM，通过宽度与透明度过渡实现顺滑收起/展开，不再瞬时卸载
- [x] 新会话底部工作空间切换区视觉收敛 — 将 workspace trigger 调整为与附件入口同层级的低对比胶囊样式，弱化独立深色块感，使其与 composer 底栏背景更协调
- [x] 新会话时段欢迎标题 — 用系统本地时区驱动“早上好 / 上午好 / 中午好 / 下午好 / 晚上好 / 夜深了”欢迎语；独立标题组件只在下一个时段边界、窗口聚焦或页面恢复可见时校准，不做高频轮询，不刷新页面或影响 composer 草稿与配置状态；标题统一使用 `title` 语义 token，浅色以深黑建立视觉锚点，深色由各主题提供对应高对比标题色
- [x] 新会话英文欢迎语收短 — 使用 `Good morning / Good afternoon / Good evening / It's late` 搭配统一的 `What shall we work on?`，避免英文逐字翻译造成标题过长
- [x] 顶栏/侧边栏拼接感弱化 — 顶栏与侧边栏统一为同一 surface 底色，移除强横向分割线；右侧主区补齐弱 top/left 边界，并在左上交汇处加入圆角衔接；主区圆角后方继续露出 sidebar surface，而不再把侧边栏自身裁成圆角，降低“上下拼接”的切缝感
- [x] 侧边栏分组标题左收对齐 — 置顶标题、置顶区工作空间标题和工作空间区标题统一左收，与会话标题列对齐；保留 chevron 表达层级，不再把分组标题额外右推
- [x] 会话顶部双层标题收窄 — `ConversationRunHeader` 与 `ACPSessionHeader` 同步缩小纵向 padding、标题字号与按钮高度，弱化两层头部的视觉突兀感，把更多高度留给消息内容
- [x] composer 下方资源条收窄 — `ConversationAssetsBar` 同步缩小上下 padding、chip 高度、文字尺寸与横向间距，降低单个文件时的空白感
- [x] composer 与资源条垂直间距收紧 — 压缩 `ACPChatDialog` 底部容器、usage 面板和模型/权限状态栏的下方留白，减少输入区与资源条之间的断层感
- [x] composer / 资源条双分隔线移除 — 去掉 `ConversationAssetsBar` 独立顶部 border，直接复用输入区底边，让文件列表更像输入区的自然延续
- [x] 资源条上贴 composer 底边 — 将 `ConversationAssetsBar` 顶部内边距压到 0，进一步缩短输入区底边到首行文件项的距离
- [x] 顶部产物入口去重 — 移除 `ACPSessionHeader` 的“查看产物”按钮，统一通过 composer 下方文件项进入产物弹窗，消除顶部与底部双入口
- [x] 底部文件直达详情修复 — 点击 `ConversationAssetsBar` 文件项时立即打开对应详情弹窗；关闭弹窗时清理选中产物状态，避免残留上次内容
- [x] 会话主题色收敛 — 顶部两层 header 改为更可区分的相邻暗色 surface；用户消息从纯主色块改为主色混入 card 的柔和底色，降低深色主题下的反差
- [x] 顶部 header 合并为单块双行 — 去掉 run / session 两行之间的分裂感，统一为一个连续 surface；第二行作为弱化元信息层，整体高度进一步压缩
- [x] 顶部 header 等比压缩 — run 行和 session 行统一缩小上下留白，避免只把第二行单独压扁后破坏块级节奏
- [x] 顶部 header 细调收口 — 进一步优先压缩第一行高度，并缩短 run / session 两行之间的垂直缝，让单块双行更连贯
- [x] Session switcher 状态标记收敛 — 去掉 attempt 前的 `lucide Circle` 伪图标，改为纯色小圆点表达状态，避免出现发黑小方块的视觉噪声
- [x] Session switcher 运行态增强 — 选中行继续保留状态标记；运行中改为“主色圆点 + 外圈脉冲 halo”，比单个蓝点更容易识别为活跃会话
- [x] 顶部当前 session trigger 补齐状态标记 — 顶部 `round/node/attempt` trigger 前也显示与树内一致的状态圆点，避免上下两处语义不一致
- [x] 侧边栏层级语言统一 — 快捷入口与功能入口统一为同一档按钮高度/字号，separator 弱化为辅助分组线，置顶标题与 workspace 标题统一为分组标题语言
- [x] 侧边栏 section gap 去重 — 收掉根容器全局 gap 对相邻分组的重复加成，并把“置顶”标题字号恢复到更接近正文分组标题的级别
- [x] 侧边栏整体字号上调 — 所有可点击入口与分组标题统一上调一档，在保持层级关系的前提下提高可读性
- [x] 功能入口组内间距微增 — Agent 管理 / 上下文管理 / 运行模式管理之间略微拉开，改善连续图标行的拥挤感
- [x] 侧边栏滚动边界与固定区再收敛（2026-08-15）— 顶部快捷/功能入口保持固定，置顶区与工作空间列表合并到同一 shadcn `ScrollArea`；展开时“置顶”标题在自身容器范围内 sticky，并由首个 workspace sticky 标题自然接替。“置顶”与“定时任务”统一消费 `text-sm` UI 字号和 `sidebar-foreground` 前景色，顶部按钮高度、组内间距与分隔线留白各收紧一档，并以布局契约测试固定滚动区层级和样式 token
- [x] ACP 模型选择器可视化修正 — 底部模型栏改为与权限一致的胶囊式下拉触发器；下拉内容改为顶部 popper 弹层并受窗口高度约束，避免选项溢出窗口

## Phase 12 — AUTO / ACP / AI-DYNAMIC 收敛 ✅
- [x] 模型下拉选中态收敛 — WorkflowEditor、会话 composer、ACP 会话栏和运行模式页只在触发器展示模型名称，长描述留在下拉项中换行展示，避免越界
- [x] WorkflowEditor 必填标识 — 普通 worker、AI-DYNAMIC、edge 的必填项增加红色 `*`；AI-DYNAMIC 动态策略要求至少一个可用 agent
- [x] AI-DYNAMIC 模型输出规则 — 动态 Agent 策略补齐 bootstrap 模型选择；可选动态 Agent 模型下拉支持清空；决策指南为空时每个可选 Agent 必须锁模型且 proposal 不输出 `model`，决策指南非空时 proposal 必须输出 `model`，但 runtime 优先使用已配置模型
- [x] 会话 composer 状态修正 — 运行中锁定输入；`waiting-for-user-input` 暂停态显示继续按钮；runtime 继续时工作流无效显示修改按钮；正常结束后的 ACP 追问不要求 workflow 合法；用户 stop 后的 `process-interrupted` 改为恢复输入框，发送时仍走 runtime continue，只是把默认“继续”替换成用户输入
- [x] ACP session 配置切换 — 模型和权限选中后乐观更新当前会话快照，并同步 ACP `configOptions.currentValue`
- [x] ACP 权限“不指定”语义统一 — 权限模式新增独立 `permissionModeOverride`，不再从 Agent `currentModeId` 反推 sasuke 选择；Direct、AUTO、Workflow 可空权限文案统一为“不指定”，会话内选择具体权限后移除“不指定”，same-session prompt、runtime continue 与 AI-DYNAMIC inner continue 只继承显式 override。
- [x] ACP runtime continue 会话配置继承 — stopped/runtime-abnormal 会话中切换模型或权限模式后再发送继续，后端从当前 attempt 的 ACP session 快照读取 sasuke 显式 model / permission override，并作为本次 continue override 传入 provider，避免恢复时回退到 workflow 初始配置；补充 provider invocation 回归测试固化 `deepseek -> gpt-5.4` 与 `ask -> bypassPermissions` 场景
- [x] AUTO 模式重构 — AUTO 等价运行时生成 `AI-DYNAMIC -> end`；运行模式页管理模板级配置和模板保存/切换，composer 管理会话级权限与全局 Goal，WORKFLOW 模式提供模板下拉和跳转入口
- [x] 运行模式管理全宽化 — 页面改用与 Agent 管理、上下文管理、设置页一致的 `PageHeader` + 全宽滚动主体；工作流编辑器去掉外层额外边框容器，避免居中窄页和嵌套面板感。
- [x] 运行模式管理缺陷收敛 — Select 默认使用 popper 定位修复抽屉内模型列表漂移；AUTO 保存/另存增加重复名与配置校验反馈；Agent/工作流选择保留不可选项并展示原因；composer 下方持久展示 AUTO 阻断错误；ACP follow-up 发送读取当前 session model override；快速会话创建移入后台 blocking task 并设置 busy 状态
- [x] 工作流模板另存收敛 — 运行模式页保存/删除后同步本地和父级模板 store；另存为新模板跳过来源 `workflow.id` 重复校验，由后端生成新的 `workflow-{uuid}` 并最多重试 3 次；旧 UI 创建任务抽屉同步同一规则；AUTO 允许调用工作流中的异常项在同组下方展示并用 tooltip 承载原因
- [x] 用户级模板持久化 — 工作流模板迁到 `~/.sasuke/context/workflows.json`，AUTO 模板迁到 `~/.sasuke/context/auto-templates.json`；两类模板均跨 workspace 共享，旧项目级 workflow 文件和旧 localStorage AUTO 模板在首次读取时迁移
- [x] AUTO 模板下拉删除 — AUTO 模板通过后端 JSON store 持久化；运行模式页模板下拉补齐删除按钮，删除当前模板时保留正在编辑的配置字段
- [x] 快速对话非法 WORKFLOW 阻断 — WORKFLOW 模式发送前复用运行模式管理页工作流模板校验；非法模板不创建 task/run，错误持久展示在 composer 下方现有修复横幅
- [x] 运行模式管理保存入口前置 — 页面级保存按钮移动到 WORKFLOW / AUTO 分段入口右侧，两个 tab 共用同一提交入口，保存反馈跟随顶部按钮展示；保留模板保存/另存按钮在各自模板操作区，避免默认运行模式保存与模板库动作混淆
- [x] 验证 — `npm run web:build`、`cargo check` 通过

## Phase 13 — ACP live session 接入修正 ✅
- [x] 新 UI 创建/重跑会话改为后台启动 run，命令返回后立即进入运行态页面，不再等 ACP provider 完整结束或请求权限才返回。
- [x] `create_conversation_run` / `rerun_conversation_task` 使用带 `acp_live_update` 的 App 实例，确保新 UI 发起的 ACP 自动工作流与旧工作台一样推送 `sasuke://acp-session-updated`。
- [x] `App.tsx` 接入 ACP live event 到会话式运行态 VM：收到当前 run 的新 attempt 事件后刷新 session tree、选中对应 session，并同步侧边栏；同一 attempt 后续流式消息继续由 `ACPChatDialog` 订阅增量合并。
- [x] AI-DYNAMIC 内部会话查看工作流入口 — AUTO / WORKFLOW 中选中 AI-DYNAMIC 内部 session 时，顶部补齐 Eye 查看按钮；按钮复用会话运行态 `workflowGraph` 与旧 UI `GraphView` 实际路径展示，编辑入口仍仅限普通 WORKFLOW 模式
- [x] AI-DYNAMIC 内部工作流终态刷新修复 — `ConversationRunVm.workflowGraph` 对内部 session 优先按 `outerNodeId/outerAttemptId` 返回 dynamic runtime graph，避免 run 完成后退回外层 AI-DYNAMIC 容器图；查看图节点点击同步支持内部 session leaf 跳转
- [x] 会话主页处理模式与运行模式管理页的分段入口顺序统一为 WORKFLOW / AUTO。
- [x] AI-DYNAMIC 外层终态事件选中保持 — 当前选中内部 session 时，外层 AI-DYNAMIC 容器 terminal event 不再覆盖 selected session key，终态 run refresh 继续请求内部 runtime graph
- [x] 验证 — `npm run web:build`、`cargo check` 通过

## Phase 14 — 图片/内存附件 materialize ✅
- [x] `materialize_conversation_attachments` — 桌面端新增内存附件落盘命令，将粘贴、拖拽、浏览器 File 对象保存到 sasuke 临时输入附件区并返回本地路径
- [x] `useAttachmentPicker.resolveAttachmentPaths` — 发送前统一把无 path 的附件 materialize，再复用现有 `attachmentPaths -> authoring/inputs -> provider task-inputs` 链路
- [x] `ConversationComposer` / `ACPChatDialog` — 附件保存成功后才清空输入；保存失败时保留 prompt 与附件 chip，并展示前端 i18n 错误文案
- [x] 后端附件扩展名校验改为复用 `provider::supported_attachment_extensions()`，避免前端、创建校验与 provider 解析范围不一致
- [x] 测试补充 — 后端 materialize 文件落盘/重名去冲突/不支持类型单测；前端 API facade 穿透测试
- [x] AI-DYNAMIC 输入附件消费闭环 — 内部 worker/merge/acceptance 与普通 worker 复用 task `authoring/inputs`，统一注入 provider `task-inputs` content block；补充动态节点回归测试，防止图片已落盘但未传给 agent
- [x] 会话消息图片预览 — 用户消息下方图片附件显示缩略图，点击使用独立全屏原图预览；非图片附件仍走详情弹窗，`data:image/...base64` 仅作为内部 src，不再以文本形式展示
- [x] 会话资源条契约收敛 — `ConversationRunVm` 与 `switch_conversation_session` 复用同一套 session 资源列表逻辑；session tree leaf 填充真实 `artifactCount / attachmentCount`；前端资源条按文件项自身 `roundId/nodeId/attemptId` 归属过滤，不再依赖 ACP session cwd 校验；补充中文节点 `测试/attempt-002` 的接口层与前端纯函数回归测试，保证 attempt 目录已有 `artifacts` / `attachments` 时底部文件 chip 可展示
- [x] Dynamic continue session 首屏归属修正 — `AcpSessionVm` 输出 leaf 身份字段，`cwd` 保持 attempt 存储目录，`providerCwd` 承载 provider 工作目录；会话主页优先按 leaf 身份匹配 selected session，避免 continue-ref 的 workspace cwd 把 base session 判丢并触发长时间初始 loading

## Phase 15 — 侧边栏 run 展开与选中态修正 ✅
- [x] 侧边栏 run 选中态改为 `projectId + taskId + runId` 组合身份，修复多个会话同时展开且都有 `run-003` 时全部高亮的问题
- [x] 会话 run 历史展开状态上提到 `ConversationSidebar` 统一管理；展开一个会话时自动收起其他会话，保持侧边栏单一焦点
- [x] 单 run task 也展示 run 子列表；只有 `run-001` 时仍提供具体 run 行，保证右键停止菜单有稳定锚点
- [x] 置顶区与普通工作区的 run 子列表展开槽按区域隔离；同一 task 在两处展示时，点击一处不会联动展开另一处
- [x] 置顶区与普通工作区的 task/run 选中高亮按区域隔离；同一 task 在两处展示时，只高亮最后交互区域的实例
- [x] 补充前端纯函数单元测试，固化同名 run 不跨会话共享选中态

## Phase 16 — 运行时跨 workspace scoping ✅
- [x] 后端 `commands.rs` 14 个 ACP/runtime 命令补齐可选 `project_id`，未传时保持旧 UI 行为，传入时使用 workspace-scoped `App` 解析路径
- [x] 后端 `AcpSessionUpdatedEventVm` 新增 `projectId`，所有 emit 函数和 emitter 工厂透传
- [x] 前端 `AcpSessionUpdatedEventVm` / API / desktop / browser 全线补齐 `projectId` 可选参数
- [x] `ACPChatDialog` 所有 ACP session / send / stop / permission / model / raw / artifact / attachment / manual check 调用传入 `projectId`
- [x] `App.tsx` 增加 `rememberConversationWorkspace` helper，统一 workspace 选择入口；新建对话不再重置为 null
- [x] `ConversationSidebar` 接收 `activeWorkspaceId`，主页态优先显式选中 workspace 再回退到 persisted `lastActiveWorkspaceId`
- [x] 新 UI `continueRun` / `saveTaskWorkflow` / `onSaveWorkflow` 传入 conversation `projectId`
- [x] ACP live event 订阅在 new UI 运行态按 `projectId` 过滤
- [x] `create_conversation_run` / `rerun_conversation_task` 在成功产生新 run 后持久化当前 `projectId`，避免失败创建或仅草稿选择提升 workspace
- [x] 侧栏 VM 与前端本地 state 都按最后活跃 workspace 置顶；异步 `getConversationSidebar` 返回不会覆盖最后活跃 workspace
- [x] Conversation → Workbench 切换先完成最后活跃 workspace 的 `selectRecentWorkspace`，再进入旧 UI；查看历史 run 和 composer 草稿选择不改变该目标

## Phase 17 — macOS 标题栏平台分流 ✅
- [x] 后端 `AppBootstrapVm` 新增 `platform`，由桌面端 runtime 统一返回 `macos` / `windows` / `linux` / `unknown`，前端不再自行分散判断平台
- [x] macOS 启用 Tauri overlay title bar、隐藏系统标题文本并保留原生左上角 traffic lights；Windows/Linux 继续使用共享顶栏右侧自定义最小化、最大化/还原、关闭按钮
- [x] `AppTitleBar` 同时服务工作台模式和会话模式，macOS 为 traffic lights 预留左侧安全间距，非 macOS 保持现有自定义窗口控制
- [x] 保留现有前端打开 Tauri 原生目录选择器的 workspace picker 链路，不吸收 PR 51 的后端 picker 迁移

## Phase 18 — 会话缓存生命周期身份修正 ✅
- [x] `ConversationRunVm` 暴露 `taskUuid`，优先来自 `TaskState.uuid`，历史任务缺失时回退到 `taskId`
- [x] `ConversationRunPage` 将 `taskUuid` 传入 `ACPChatDialog` 作为 cache namespace，并纳入组件 key
- [x] `ACPChatDialog` 的 loaded events / optimistic events 内存缓存不再只依赖 `taskId/runId/roundId/nodeId/attemptId`，避免删除最高编号 task 后重新创建同编号会话时恢复旧消息
- [x] 补充前端缓存隔离单测和后端 VM 字段单测，固化同编号不同 task 生命周期不串会话

## Phase 19 — 默认工作流采访偏好与 elicitation 收敛 ✅
- [x] 采访开关仅在内置 `default` 工作流显示，偏好按 workspace 持久化；自定义模板不再被按 `interview` 节点名裁剪，运行模式编辑器始终展示真实完整模板
- [x] elicitation 回答提交后关闭交互卡片，不再注入独立用户消息气泡；Agent 原生 `AskUserQuestion` 工具卡片及 completed 输出继续保留
- [x] 修复 completed run 的 follow-up elicitation：response signal 不再被 snapshot 状态提前删除，由 runtime 读取、持久化并完成 JSON-RPC 回包后统一清理
- [x] 增加前端 run mode / timeline 回归测试与 Rust durable signal / 默认模板边界测试
- [x] 新增 pull request 实质检查：Rust 格式化与 workspace tests、前端 tests 与 production build
- [x] PR checks 跨平台修正 — runtime layout 测试改用不可解析的虚拟相对 repo root，避免 Windows `canonicalize` 的磁盘实际大小写与 Linux 字面路径产生不同 project id

## Phase 20 — Direct 持续 Agent 会话与 follow-up 生命周期 ✅

- [x] completed run 上的 ACP follow-up 生命周期改为后端 per-attempt live activity 派生，页面切换后仍恢复处理中、计时、token 和停止。
- [x] Direct 侧边栏活动圆环恢复（2026-08-05）— ACP 高频实时事件新增轻量 `activity` 投影，直接读取 per-attempt prompt control registry，不重建完整 lifecycle/timeline；前端优先同步该字段并在终态 `null` 时清除，修复并发性能分层后 completed run 追问期间圆环消失的问题。Rust 事件序列化与 Web 状态投影回归覆盖 running 和 terminal clear。
- [x] 停止反馈去遮罩（2026-08-05）— 配合 `stop_active_session` / `pause_run` 快速 accepted 控制面，破坏式删除 ACP 会话画布与 run 页面两套全局“停止中”遮罩及其专用状态/文案；停止进度统一保留在 composer 的状态行、按钮 pending 和输入锁定中，后台 lifecycle/session/sidebar 校准继续执行，避免快速停止时页面闪屏。Web 回归固定会话画布不再挂载停止遮罩且 composer 反馈仍保留。
- [x] ACP 切页水位与历史流式归属收敛（2026-08-05）— attempt + branch 全局事件路由增加事件数/估算字节双重有界的 latest-wins 交接缓冲，陈旧分页快照首次重挂载即合并后台 text/tool；缓冲缺口用按 `newestSeq` 语义增量的 `afterSeq` 追平。快照与 replay 固定静态，只有交接后的当前轮 live text/thought 使用打字机，工具/用户/终态边界立即结算，避免“检查”卡住和 completed follow-up 重播历史正文。
- [x] ACP 水位交接持续收敛与 revision 分页加固（2026-08-06）— 删除固定四次追平窗口，改为页面先展示、后台可取消且 2 秒封顶的指数退避，payload 缺口覆盖前保持静态；普通 replay 等 durable snapshot 覆盖稳定 generation 后再回收。branch replay 严格执行 64 项 LRU，项目身份严格匹配，external-store 订阅按 key 稳定。`afterSeq` 按 revision 顺序分页、同 revision 原子返回、语义顺序展示，防止累计旧块跨页抬高游标造成漏块；Web/Rust 接口回归覆盖延迟持久化、多页非单调 revision 和容量边界。
- [x] 新增 Direct typed config、per-Agent workspace 记忆、RawAgent prompt envelope 和内部单 Worker workflow。
- [x] 快速会话完成 `Direct / 工作流 / AUTO`、Agent icon picker、composer 内模型/权限控件。
- [x] Direct runtime 隐藏 workflow/session tree/run outcome，header 与侧边栏/搜索统一展示 Agent identity。
- [x] Direct 使用 `lastActivityAt` 更新时间；Workflow/AUTO 原状态点规则不变。
- [x] Direct 侧边栏身份对齐收敛 — Agent icon 缩小并与 Workflow/AUTO 状态点共用等宽身份槽位，所有会话标题起点一致；Direct 不再展示 run 子列表。
- [x] Direct ACP header 移除“系统提示”按钮；RawAgent system prompt 为空，不保留禁用态入口，原始帧入口继续保留。
- [x] Direct Agent 空状态增加紧凑“+”按钮，复用现有 Conversation 导航直接进入 Agent 管理页，同时保留跨页面 composer 草稿。
- [x] 用户消息隐藏上下文气泡宽度修正（2026-07-25）— 移除折叠态 inline-size containment，并将隐藏区根节点、Trigger、Content 改为无百分比宽度的嵌套 grid stretch。固定 `rem` 与 `65cqi` 上限都会让气泡随容器增长而在宽屏留下越来越多尾部空白，最终改为“最大消息列内排版 + `Range.getClientRects()` 行宽测量”：折叠态取标签与可见正文实际行宽最大值，展开态再纳入隐藏正文；ResizeObserver、展开状态和字体加载驱动重测。新增纯函数固化三类宽度的 max 规则，并保留 `82cqi` 仅作为测量/消息列上限。
- [x] 增加 Rust/Web 接口级回归并同步产品设计文档与 MVP 计划。

## Phase 21 — 侧边栏相对时间边界修正 ✅

- [x] 相对时间格式化从会话侧边栏组件下沉到共享 `datetime` 模块，任务行与 run 行统一消费同一接口。
- [x] 时间单位改为连续区间：7–29 天显示周、30–364 天显示月、365 天起显示年，消除 28–29 天的 `0mo` 与 360–364 天的 `0y`。
- [x] 增加前端纯函数单元测试，覆盖分钟、小时、天、周、月、年全部切换边界，以及内部 Unix 秒时间戳、未来时间和非法输入。

## Phase 22 — 侧边栏最近活动排序统一 ✅

- [x] task 行统一以 `lastActivityAt` 作为最近活动接口；后端从会话元数据与 run `updatedAt` 中取最大值，Direct、Workflow、AUTO 不再分叉排序。
- [x] run 历史与 `latestRun` 改为按 `updatedAt` 倒序，展示时间、状态来源和列表位置保持一致。
- [x] 排序前统一解析 Unix 秒、RFC 3339 与历史本地日期时间，修复不同时间格式直接字符串比较导致的错序。
- [x] 增加 Rust 接口级回归，覆盖跨运行模式混合时间格式排序，以及旧 run 后续更新后提升为最近 run。

## Phase 23 — 主页 composer 自动增高统一 ✅

- [x] 会话主页正文输入从固定高度原生 textarea 收敛到会话追问已使用的 prompt-kit `PromptInputTextarea` 自动尺寸能力，移除两套高度生命周期。
- [x] 主页版心由 `max-w-4xl` 收窄为 `max-w-3xl`；正文初始最小高度设为 56px，随内容增长至 320px 上限，超过上限后仅正文区内部滚动。
- [x] 主页继续按可用主区横向几何居中；增加 64–80px 响应式底部光学留白，使欢迎标题与 composer 整体自然上移约 32–40px，修正下方配置块造成的视觉重心偏低。
- [x] 自动尺寸计算显式区分未达上限的隐藏滚动条和达到上限后的内部滚动，并补充前端接口级回归测试固化宽度、最小高度、增长和封顶行为。

## Phase 24 — 应用壳边界与 composer 表面统一 ✅

- [x] 左侧 resize handle 改为透明命中区，可见分隔统一交给中间主区域的圆角边界绘制，消除顶部直线与圆角的冲突。
- [x] prompt-kit 共享 textarea 在明暗主题下均透明继承 composer 外层背景，发起会话与会话追问不再出现独立灰色色块。
- [x] 按本次交付约定不执行自动化测试、构建或前端交互验证，由需求方直接验收视觉效果。

## Phase 25 — 运行模式管理页信息收敛 ✅

- [x] 删除标题下随 tab 变化、但不帮助用户决策的说明文案。
- [x] 删除仅返回会话首页的页内“返回”按钮，并移除其回调链路；运行模式管理与 Agent 管理、上下文管理、设置统一使用侧栏在同级页面间导航。
- [x] 增加页面壳回归测试，固定标题区不再渲染说明或返回操作。

## Phase 26 — 会话切换原子呈现与首屏预取（实现完成，待实机验收）

- [x] 将侧栏选中的目标会话与当前完整呈现的会话分开管理；目标快照准备期间保留旧聊天、composer 和右侧工作区，完整身份匹配后一次切换，消除“旧聊天 + 新右栏”的混合帧。
- [x] 导航请求增加递增 request id 与 `projectId/taskId/runId` 完整身份校验，快速连续选择只允许最后一次结果提交；目标实时订阅延后到首轮提交完成后启动。
- [x] 复用独立 change set 接口建立 96 项有界 LRU，导航时并行预取 selected branch 时间线尾部最近 12 个文件变更；卡片首帧同步读缓存，不再显示“加载变更文件中”的中间布局。
- [x] 增加导航呈现、陈旧请求淘汰、文件变更预取与同步首帧组件测试，并执行生产构建；内置浏览器本地 URL 被安全策略阻断，真实页面慢速切换保留为需求方实机验收项，不记录为已自动验证。

## Phase 27 — 运行模式模板交互收敛 ✅

- [x] AUTO 模板选择器补齐与工作流模板一致的“新增模板”入口；选择器分为新增、不使用和已保存列表，新增创建独立空白草稿并显示未保存状态；两类模板已保存列表均使用固定最大高度和内部滚动。
- [x] AUTO 新增草稿选中态修复 — 草稿状态高亮“新增模板”，不再因模板 ID 为空而误高亮“不使用模板”；补充状态判定回归测试。
- [x] 工作流模板另存后按保存结果的模板名解析权威模板身份，同步当前运行模式 `workflowTemplateId` 和编辑选择；模板库刷新不再以旧运行模式状态覆盖选择，避免另存后回退默认工作流。
- [x] 增加运行模式模板选择回归测试，覆盖无序模板返回时仍选中新保存的模板，以及 AUTO 新增入口渲染。

## Phase 28 — Composer 工具栏比例收敛 ✅

- [x] 桌面宽度下将工作空间列收窄为最小 12rem、0.75 份弹性宽度；模型、权限和发送所在配置列扩展为最小 28rem、1.25 份弹性宽度，保持模型与权限等宽。
- [x] 扩展响应式布局接口测试，固定上述桌面列比例，避免后续修改重新挤压模型与权限选择器。

## Phase 29 — 内置工作流模板本地化 ✅

- [x] 内置工作流模板按稳定 ID `default` 在展示层本地化，不再直接展示历史持久化名称；英文界面显示 `Default workflow`，中文界面显示“默认工作流”。
- [x] 运行模式管理、会话 composer、任务创建和工作流编辑器统一消费共享展示函数；自定义模板名保持原样，工作流引用 ID 不变。
- [x] 增加模板显示名称单元测试，固定内置模板翻译与自定义模板透传行为。

## Phase 30 — 会话侧栏紧凑宽度 ✅

- [x] 左侧会话导航最小可拖拽宽度由 200px 收窄为 176px；保留完整折叠入口、触控目标和标题截断规则。
- [x] 增加布局常量回归测试，防止后续调整重新提高紧凑侧栏下限。

## Phase 31 — 右侧工作区首次拖拽上限同步 ✅

- [x] 右侧分隔条按下时同步提交“用户正在拖拽”状态，使 `react-resizable-panels` 在手势首帧即读取 1440px 配置上限，不再被旧的已保存宽度截断。
- [x] 增加壳层回归断言，固定分隔条拖拽开始时的同步上限解除。

## Phase 32 — 会话运行态视觉收敛 ✅

- [x] Thought 普通态移除嵌套卡片阴影，展开正文使用 `18rem` 有界、可聚焦的主题滚动区；长内容不再无限拉长会话，内部滚动不提升到会话根状态。
- [x] prompt-kit Tool 收敛为低边界透明紧凑行，首行统一为操作名、等宽关键参数、状态与常显展开箭头；详情继续按需读取并使用细竖线表达层级。
- [x] Todo/Plan 展开区改为完成勾选、运行环、待处理小号中性空心点组成的任务行，并保留文字状态；删除与 `0/N` 摘要重复的序号圆环。composer session 耗时跟随状态栏正文排版，仅使用 tabular figures 稳定数字宽度并复用既有间距。
- [x] 未引入新组件依赖、缓存、队列或高频 timer；Activity 详情懒加载、canonical lifecycle、秒级计时和现有 prompt-kit/shadcn 数据边界保持不变。
- [x] 增加长 Thought 键盘滚动、Tool 按需详情与关键参数、Todo 状态映射、composer 计时排版回归；执行目标前端测试、类型检查、生产构建和内置浏览器明暗主题验证。
- [x] 桌面字体重量与 UI / 编辑器排版设置（2026-08-14）— 将 Tailwind 全局 `medium / semibold / bold` 语义校准为 `400 / 500 / 600`；“设置 → 个性化 → 字体”按“界面 UI / 编辑器”两个 Collapsible 分区，分别提供字体、字号、预览与本机字体选择，展开状态仅用 `sessionStorage` 在应用未关闭时记忆。UI 字号（12–18px，默认 14px）通过派生 token 覆盖侧栏、聊天正文、Thought 和普通 Markdown；编辑器字体/字号（10–18px，默认 12px）只覆盖所有 CodeMirror 文件、运行产物与 Git/本轮 Diff，聊天代码字号回归 UI 派生层。偏好提交改为前端串行 latest-wins、后端一次 load/save，不再清空 task/workflow/round；恢复默认阻止 blur 双提交。生产构建、Vitest 接口测试与 Rust 持久化/范围测试固化该边界。性能影响限于低频根 CSS 变量更新和单次配置写入，不引入全局 Context、编辑器重建、额外依赖或无界队列。

## Phase 33 — 主工作区层级阴影统一 ✅

- [x] 会话壳与传统工作台壳的右侧主区域统一消费主题 `material shadow / edge shadow`，保留现有左上圆角与弱边界，在浅色、深色及性能视觉质量下由同一主题配置控制层级。
- [x] 仅为两个公共主 surface 增加静态合成效果，不新增状态、依赖、缓存或页面级分支；单层有界阴影不会扩大 React 渲染范围，也不引入高频重绘动画。
- [x] 增加应用壳接口回归测试，固定两个桌面壳共享相同阴影 token 与层级，避免后续只修复单一页面或退回硬编码阴影。
- [x] 定向 Vitest 与前端生产构建通过；内置浏览器验证运行模式管理页在浅色、深色、720px 窄窗口以及恢复正常宽度时均保持主题阴影、圆角和无横向溢出，并确认设置页的传统工作台壳使用相同实现、测试后的明暗模式偏好恢复为“跟随系统”。

## Phase 34 — 主工作区阴影可见性修正 ✅

- [x] 根因确认：通用 material shadow 的浅色强度仅为 6%，且主要向下投影；整块主工作区填满右侧时，下方阴影被窗口边界裁切、左侧扩散又过弱，虽然计算样式存在阴影但用户无法感知层级。
- [x] 新增共享 `--workspace-main-surface-shadow`，本阶段曾采用单层、零偏移的有限半径投影验证外层 surface host；该阶段性参数随后由 Phase 35 的 composer 主投影与无 spread 环境层替换，不影响 dialog、composer 等现有 elevated surface。
- [x] 会话壳与传统工作台壳统一切换到该语义阴影，删除对通用 material shadow 的错误复用；不新增状态、依赖、动画或页面级分支。
- [x] 删除曾在圆角处形成灰色扇形的多层负向投影；回归契约明确禁止负偏移，并固定单层阴影和两个壳层消费。视觉验收以正常宽度下边缘可辨认、圆角无局部灰块、浅色与深色均成立为准，不再仅以 computed style 非空作为通过标准。
- [x] 阴影绘制层修正：`react-resizable-panels` 会在中心面板内容包装层设置 `overflow: auto`，内层 `<main>` 的外阴影会被完整裁掉；会话壳将投影与圆角提升到该包装层自身，由外侧 `data-panel` 的 `overflow: visible` 放行，`<main>` 只保留背景、边界和内容裁切。工作台壳没有中间裁切层，继续由直属 `<main>` 绘制。
- [x] 回归契约固定会话壳阴影必须属于中心 `ResizablePanel` 包装层、不得重新下沉到 `<main>`；浏览器验收同时检查包装层 computed shadow、父级 overflow 边界与侧栏区域内的实际可见投影，并完成浅色、深色、720px 窄窗口及恢复正常宽度检查，未出现圆角灰块或横向溢出；测试后的外观偏好恢复为“跟随系统”。

## Phase 35 — 主工作区自然投影与顶部绘制修正 ✅

- [x] 根因确认：中心面板外层已允许投影，但再外一层 `ResizablePanelGroup` 仍由组件内联设置 `overflow: hidden`；其上边界与标题栏底边重合，导致顶部投影被整条裁掉，而向左扩散仍处于容器内部，最终只留下侧栏中的垂直灰带。
- [x] 仅在应用工作区面板组纵向覆盖为 `overflow-y: visible`，让顶部投影越过面板组边界；横向使用 `overflow-x: clip`，避免右缘投影在窄窗口下扩大页面滚动宽度。左、中、右面板内部继续各自负责内容裁切，主区 surface host 继续统一承载圆角和投影，不改变面板尺寸、拖拽、响应式收起或滚动边界。
- [x] `--workspace-main-surface-shadow` 以主题现有 `--gb-material-shadow` 与 `--gb-material-edge-shadow` 作为与 composer 一致的主投影，再增加一层无 spread、低透明度的 16px 环境阴影，补足贴近标题栏的大面板顶部接触层级，并扩大渐隐范围让左侧自然衰减；删除会形成左侧灰带的 `18px + 2px spread` 零偏移强扩散。
- [x] 回归契约固定面板组顶部放行、主区外层绘制及 composer material token 复用；验证覆盖正常宽度、720px 窄窗口、恢复宽度和明暗主题，顶部与左侧均自然渐隐，无圆角灰块或横向溢出。

## Phase 36 — 右侧工作区投影连续性与硬边清理 ✅

- [x] 展开后的右侧工作区由自身非裁切 surface host 消费与中心区相同的 `--workspace-main-surface-shadow`，顶部环境层同步增强但继续保持 16px blur、零 spread；中心区和右侧区顶部形成同源、连续的主题化投影。
- [x] 删除中心主区内层 `<main>` 重复的实体 `border-left`，左侧层级仅由外层自然投影表达；系统缩放下不再出现阴影旁额外的粗硬竖边。
- [x] 中心与右侧工作区之间继续由既有 resize handle 表达可调整边界，不新增覆盖层、状态、监听器或尺寸计算；面板 resize、响应式收起和滚动所有权保持不变。
- [x] 契约测试固定左右 surface host 共用阴影 token，并禁止中心内层恢复 `border-left`；浏览器验收覆盖右侧工作区展开/收起、明暗主题、正常与窄窗口，确认顶部连续、左侧柔和且无横向溢出。

## Phase 37 — 主工作区环境阴影强度微调 ✅

- [x] 对比参考应用后确认左侧偏深的根因不是单个环境层强度，而是环境阴影与 composer material shadow 在长边重复扩散；删除工作区对 `--gb-material-shadow` 的直接叠加，composer 自身材质不受影响。
- [x] 共享 token 改为方向分工：四周使用 16px blur、零 spread、主题窗口边缘色 45% 的轻环境层；顶部使用 `0 -8px 16px -8px`、主题色 85% 的 top-only 层，保持顶部辨识度而不继续加深左侧。
- [x] 中心区与右侧工作区继续统一消费同一 token，不新增状态、组件或响应式分支；契约测试固定方向、强度和禁止 material shadow 重叠。

## Phase 38 — 会话正文滚动条弱化 ✅

- [x] 会话正文继续复用 prompt-kit `ChatContainer` 与现有主题滚动条，不新增自绘滚动组件；仅在 `ConversationViewport` 增加语义变体，最终将轨道固定为透明，静止 thumb、hover thumb 校准为主题前景色的 11% / 22%。
- [x] 变体只影响会话主滚动 viewport，设置页、资源树、右侧工作区和全局滚动条 token 保持不变；滚动行为、交互宽度、键盘能力和 stick-to-bottom 生命周期不变。
- [x] 增加样式契约测试，固定会话 viewport 挂载语义类与三档低对比 token；纯 CSS 静态覆盖不引入 React 重渲染、事件监听、动画或额外绘制循环。

## Phase 39 — 会话滚动 viewport 全高度恢复 ✅

- [x] 根因确认：composer 作为会话根 flex column 的独立收缩项占用了主滚动区高度，导致原生滚动条轨道在输入区上方提前结束；问题来自布局所有权不完整，不是滚动条皮肤或 WebView 行为。
- [x] 会话滚动 viewport 保持占满 session header 下方全部高度；composer surface 从持续增长的 prompt-kit `ChatContainer.Content` 中移到同一 viewport 的稳定 overlay sibling，timeline 通过局部 footer 高度 CSS 变量保留等量底部 padding，消息、干预卡与贴底终点不会被遮挡。
- [x] “回到最新”闪烁诊断补齐视觉事实：diagnostics 模式下按 `probeId` 有界采集按钮与 viewport/footer/composer/timeline 的 geometry、computed style、hit-test、animation、mutation、resize 和 React commit；正常产品路径不创建额外 observer 或 RAF。
- [x] “回到最新”流式闪烁根因已由 visual probe 现场日志确认：按钮 DOM identity 与显隐稳定，但自动 newer-edge 追页错误复用了用户点击 pending，令 Button 的 `disabled` 属性和 `opacity` 在流式 canonical handoff 中反复切换。按钮 pending 已收窄为显式点击命令状态；自动追页继续复用 canonical-head single-flight，但不再改变按钮交互态，用户点击插入飞行中自动请求时仍等待 trailing handoff 完成。
- [x] “返回最新消息”入口继续锚定 composer footer 上方，但不再参与 timeline 的 sticky 重排。`ConversationViewport` 只用一个 `ResizeObserver` 观察 footer block size 并直接写局部 CSS 变量，不提交 React state、不监听 scroll、不复制 spacer，也不创建第二套滚动容器。
- [x] 增加结构契约测试，固定 footer 必须位于 `ConversationViewport` 内部且使用 sticky bottom，避免后续再次退回挤压滚动 viewport 的根级 flex sibling。

## Phase 40 — 工作区展开时会话滚动条绘制路径阶段性尝试（已由 Phase 42 替换）

- [x] 阶段性方案曾在支持 `::-webkit-scrollbar` 的环境中把会话标准属性恢复为 `auto`，希望强制使用 11% / 22% WebKit thumb；后续像素对比证明该方案没有稳定绘制所有权，且与右侧 Tab 的专用 WebKit 路径共同造成应用内混用。
- [x] 该阶段保留的轨道透明和会话低对比 token 继续有效；强制 WebKit 绘制的 `auto` 覆盖由 Phase 42 删除。

## Phase 41 — 主页窄宽度发送操作收敛 ✅

- [x] 根因确认：主页 composer 的 trailing actions 已使用容器查询网格，但发送 split-button 的列定位误挂在内部主按钮上；WORKFLOW / AUTO 等无模型与权限项的模式只剩一个网格子项时，会自动落入首列并被 `w-full + flex-1` 拉成半栏宽。
- [x] 工具栏按既有 `isDirect` 语义投影为 simple / configured 两种布局：WORKFLOW / AUTO 从小容器断点起让附件、工作空间和紧凑发送组直接同排，只有极窄宽度才换行；Direct 继续为模型、权限和发送保留配置型换行策略。
- [x] Direct 宽桌面配置组不再切换为依赖子项固有宽度的 flex，统一使用 `模型 1fr / 权限 1fr / 发送 auto` 显式三列；发送末列稳定贴住工具栏右边缘。提交操作组继续统一普通发送与定时创建的 32px 高度和内容宽度。
- [x] 不新增业务状态、组件、监听器、尺寸测量或依赖，继续复用 prompt-kit 输入区、shadcn Button / DropdownMenu 和 Tailwind 容器查询。契约测试固定 simple 同排、configured 多行边界以及宽 Direct 的末列锚点。

## Phase 42 — 原生滚动条跨平台渲染路径统一 ✅

- [x] 对右侧工作区展开/收起截图进行像素取样：静止 thumb 颜色仅为 `#E4E4E4` 与 `#E1E1E1` 的 3 级差异，主要视觉差异来自约 7px 与 4px 的可见宽度，确认根因是标准属性与 WebKit 伪元素两套渲染路径混用，不是主题透明度或工作区状态 token。
- [x] 应用内浏览器原生滚动容器统一采用互斥能力分支：支持 `scrollbar-width: thin` 时只使用标准属性；不支持时才由 `@supports not (scrollbar-width: thin)` 启用完整 `::-webkit-scrollbar` fallback，覆盖 Windows WebView2、macOS WKWebView 与 Linux WebKitGTK 的新旧运行环境。
- [x] 删除会话区和右侧 Tab 条的 `scrollbar-width: auto` / `scrollbar-color: auto` 强制切换及 4px 专用 WebKit 例外；右栏展开、收起或 resize 只改变既有布局宽度，不再改变滚动条绘制所有权。
- [x] 不新增组件、依赖、状态、事件监听、ResizeObserver 或运行时样式写入；CSS 能力查询只在样式解析阶段生效，滚动热路径、React 渲染范围和内存占用不变。契约测试固定 fallback 必须互斥、禁止 `auto` 覆盖，并固定右侧 Tab 复用统一主题类。

## Phase 43 — Direct 单行标题栏外缘间距对齐 ✅

- [x] 根因确认：Direct 单行与 Workflow/AUTO 双行页头分别维护垂直间距，但双行第一行初始 `py-2` 明显偏松；收紧后再按两种内容结构的实际视觉密度分别校准，避免为了数值相同牺牲单行或双行的观感。
- [x] 双行第一行保持 `py-0.5`，第二行顶部保持 0、底部使用 `pb-1`；Direct 单行使用 `py-1.5`。删除任意像素值，正常与窄窗口统一消费 Tailwind spacing scale；文字行高、操作区和消息区布局不变。
- [x] 不新增状态、组件、依赖、尺寸测量或响应式分支，仅调整现有 Tailwind 静态类；组件渲染范围、事件频率、内存和滚动性能不变。服务端渲染契约测试分别固定双行首行与 Direct 单行的外缘间距。

## Phase 44 — Composer 配置截断值提示 ✅

- [x] 根因确认：模型与权限选择器已经统一截断选中值，但共享触发器缺少完整值展示契约；修复下沉到 ACP composer 配置触发层，同时覆盖单项模型、模型与思考强度组合值及权限，不按具体模型名或页面宽度打补丁。
- [x] 复用项目现有 shadcn/Radix Tooltip；仅当选中值实际溢出时在 hover 与键盘 focus 展示全文，短文本不显示冗余提示，点击打开下拉前关闭 Tooltip，并禁止回退到浏览器原生 `title`。
- [x] 溢出判断只在当前短文本控件的 hover/focus 事件发生时读取一次 `scrollWidth/clientWidth`，局部瞬时状态不提升到 composer 或页面，不新增依赖、ResizeObserver、轮询、缓存或宽泛订阅。
- [x] 增加接口与 DOM 回归测试，覆盖溢出阈值、长权限值悬浮/聚焦展示、短值不展示、共享模型触发器结构及原有菜单样式保持。

## Phase 45 — 快速对话工作空间选择器内容宽度 ✅

- [x] 根因确认：workspace 身份、选中状态与响应式列分配均正确，但选择器本身的 `flex-1` 把短名称也强制拉满；修复收敛到展示层宽度所有权，不改动 canonical `projectId` 或 workspace 接口。
- [x] 工作空间控件改为内容宽度，同时以现有父列可用宽度为上限；长名称只截断文本，图标和下拉箭头保持可见。
- [x] 复用 shadcn/Radix Tooltip 与共享溢出判定 hook，仅在文本真实截断时响应 hover/focus 展示完整名称；测量只发生在当前控件的交互事件中，不新增依赖、Observer、轮询、缓存或宽泛状态订阅。
- [x] 增加布局接口与 DOM 回归测试，固定内容宽度、现有宽度上限、长名称 Tooltip 和短名称无冗余提示。
- [x] 21 个相关 Vitest 用例与前端生产构建通过；内置浏览器在 1280px、720px 及恢复宽度后验证短名称均维持约 171px 内容宽度、不随 631px / 277px 可用列宽拉伸，页面无横向溢出。

## Phase 46 — 大字号/缩放下快速对话工具栏单行保持 ✅

- [x] 根因确认：工作空间控件已按内容宽度收缩，但 simple toolbar 仍在 `sm` composer 容器档位才切入双列；界面字号或系统缩放放大 `rem` 阈值后，会在操作实际容得下时提前换行。
- [x] simple toolbar 复用现有 Tailwind 命名容器查询，将单行双列切换点收紧到 `xs`；工作空间仍可缩并截断，发送仍保持固有宽度与行末对齐，仅真实极窄容器换行。
- [x] 不新增状态、尺寸测量、ResizeObserver、依赖或响应式分支；纯 CSS 容器档位调整不扩大 React 渲染范围或主线程工作。
- [x] 更新布局接口测试，固定 simple toolbar 必须在 `xs` 而非 `sm` 档位保持单行。

## Phase 47 — 快速会话右侧工作区继承与会话隔离 ✅

- [x] 根因确认：原模型把 Tab 与活动 Tab 放在会话 scope，却把 `requestedOpen / openRevision` 放在全局 shell state，导致任一会话开关右栏会影响所有会话；同时 `promoteDraft` 只删除草稿条目，造成新建目标会话继承展开外观却丢失内容。两者都是同一聚合生命周期被拆开的设计缺陷。
- [x] 将 Tab、活动 Tab、展开状态和打开 revision 统一收回草稿/完整会话 scope；宽度保留为跨会话布局偏好。普通会话切换只读取目标完整 identity 的状态，关闭 A 会话右栏不会写入 B 会话。
- [x] 将草稿提升改为同一 store 内、仅面向本次新建目标会话的一次原子作用域转换：迁移完整工作区状态和 locator，统一重绑定资源 `scopeKey`、草稿附件/定时配置的作用域 key 以及文件浏览器内部当前文件；完成后删除草稿条目，其他会话不读取或复制该状态。
- [x] 不新增第二套 identity、缓存、Context、effect 或兼容旁路，复用现有 24 项有界 LRU 与 workspace store；常态开关为单 scope O(1)，草稿转换为当前 Tab 数量的 O(n) 且只在创建成功时执行一次，不增加加载、I/O 或渲染范围。
- [x] 8 个右侧工作区相关测试文件共 36 项通过，前端类型检查与生产构建通过；内置浏览器实测固定快速会话的 Tab/目录内容向目标会话继承、第二会话初始关闭、返回第一会话恢复、关闭第一会话不影响已打开的第二会话，并覆盖 720px 窄窗口与恢复宽度，控制台无错误。

## Phase 48 — 功能页往返侧栏宽度收敛 ✅

- [x] 根因确认：左栏 `sidebarWidth` 与 canonical 三栏目标始终正确，但从中心最小宽度 640px 的功能页返回会话页时，`react-resizable-panels` 首次 `setLayout` 仍按旧约束把 `332 / 544 / 484px` 夹成 `236 / 640 / 484px`；原实现只识别宽度为 0 的缺失 Panel，未识别部分偏离。
- [x] 保留现有宽度事实源与一次性 canonical layout transaction；首次 applied layout 任一 Panel 与目标相差超过 1px 时，只在下一动画帧按最新约束有界重放一次，不把瞬时夹取结果写回用户偏好。
- [x] 不新增 React state、ResizeObserver、持久字段、缓存或无界重试；页面布局档位变化最多增加一次下一帧布局复核，不进入连续 resize 热路径。
- [x] 增加纯函数与 DOM 回归测试；4 个工作区相关测试文件共 38 项通过，前端生产构建通过。应用内浏览器在 1362px 下将左右栏调整为约 332 / 484px 后验证“快速对话 → 定时任务 → 快速对话”恢复为约 332 / 544 / 484px；缩至 720px 自动收起左栏并恢复 1362px 后仍回到同一宽度，控制台无 warning/error。

## Phase 49 — 快速对话工作树与顶部信息栏 ✅

- [x] 普通快速对话将工作空间选择器移入 composer 顶部信息栏，并增加按工作空间持久化的“主工作区 / 新工作树”选择；定时任务模式继续使用原底栏工作空间入口且固定主工作区。非 Git 工作空间仍展示工作树选项，选择时复用 AI-DYNAMIC 的 Git capability preflight、结构化错误和 `GitRequirementDialog`，失败不写入偏好也不创建会话。
- [x] 工作树 run 复用现有 `PreparingWorkspace` 生命周期与 Git helper，从源仓库当前 `HEAD` 在 sasuke 用户数据目录的受管 `worktrees/` 下创建独立目录和分支；`RunState.worktree` 作为运行位置事实源，Agent cwd 使用该目录。创建期间停止会持久化 Paused 并阻止迟到的 Agent 启动，重跑从新的 `HEAD` 创建新的 run worktree。
- [x] 会话详情状态栏在当前 run 存在 worktree 时显示本地化的工作树标识，并通过项目 Tooltip 暴露完整路径；详情页 composer 保持原有独立外壳，不消费快速页信息栏几何。
- [x] 快速页阶段性收敛为独立的内收低栏：左右各使用 24px spacing token，两端使用 24px 圆角；背景通过 CSS `light-dark()` 消费既有主题语义面，浅色取 `surface-high`、深色取 `conversation-background`。该阶段保留的 8px surface 重叠在无壁纸时成立，但会在半透明 composer 下透出底层色带，已由 Phase 50 的非重叠绘制结构替换。
- [x] 不新增组件依赖、尺寸监听、缓存或高频状态；工作位置偏好仍是按 project ID 的有界小对象，视觉调整为静态 Tailwind utilities，不扩大 composer 输入时的 React 渲染范围。3 个相关前端测试文件共 41 项通过，前端生产构建通过；内置浏览器在 1280px 浅色与 720px 深色下验证信息栏始终各内收 24px、覆盖 8px、无横向溢出，并在结束后恢复默认视口与系统主题。

## Phase 50 — 快速对话顶部栏壁纸透色修正 ✅

- [x] 根因确认：顶部栏是不透明 surface，输入框是半透明 composer；负 margin 与 z-index 只改变绘制顺序，无法阻止底层栏色穿过半透明输入框，壁纸场景因此出现整条横向白带。这是 surface 拓扑错误，不是圆角、边框或颜色强度问题。
- [x] 顶部栏降为 40px 并与输入框在正常文档流中零间距相邻；删除输入框的 `-mt-2`、叠放 z-index 和覆盖关系。输入框继续保持完整 16px 上圆角，并在左右 24px 内收栏外侧自然展开，半透明区域下方只剩会话背景。
- [x] 不新增遮罩、组件、状态、监听器或主题分支；纯静态 CSS 拓扑调整不增加 React 渲染、布局测量、绘制循环或内存占用。契约测试固定 40px 高度、垂直居中、无 padding 补高和无 surface 重叠。该阶段的直线肩部随后由 Phase 51 的有界曲线连接肩替换。
- [x] 3 个相关前端测试文件共 41 项通过，前端生产构建通过；内置浏览器在 1280px 浅色与 720px 深色下实测顶部栏高度 40px、左右内收 24px、间距 0、交叠 0，页面无横向溢出，验证后恢复默认视口与系统主题并关闭测试页。

## Phase 51 — 快速对话顶部栏收窄与双侧曲线连接 ✅

- [x] 顶部栏左右内收由 24px 增至 32px；栏底左右各增加一个 16px 同色反向四分之一圆连接肩，透明圆心位于外侧上角，使轮廓先向信息栏内部弯入，再外展到输入框 16px 上圆角切点；不再使用向外鼓出的实心圆角。
- [x] 两个连接肩完全位于输入框上方，不改变顶部栏与输入框间距 0、交叠 0 的 surface 拓扑；半透明 composer 下方仍只有会话背景，不恢复会透出壁纸色带的覆盖结构。
- [x] 连接肩复用顶部栏单一主题 CSS 变量，不新增颜色、DOM、状态、监听器、遮罩或绘制循环；使用组件拓扑范围内的静态伪元素和径向渐变构造反向圆角，性能影响为两个固定 16px 装饰层。

## Phase 52 — 快速对话顶部栏比例收紧 ✅

- [x] 保留 Phase 51 已确认的内凹连接方向，将顶部栏左右内收从 32px 增至 40px、高度从 40px 降至 36px；连接肩同步由 16px 调整为 24px，仍精确落到输入框 16px 上圆角切点，避免栏体变窄后连接断开。
- [x] 仅调整既有布局 token 与静态径向渐变尺寸，不新增 DOM、状态、监听器或依赖；渲染范围与输入热路径不变，无额外 I/O、缓存、队列或并发成本。

## Phase 53 — 快速对话顶部栏整体宽度收紧 ✅

- [x] 将“收窄”落实到整个外轮廓：主体左右内收 64px，32px 反向圆角后在输入框左右各 32px 处结束；不再仅收窄顶部主体而让曲线底部延伸到输入框上圆角切点。
- [x] 顶部栏高度进一步降至 32px，内部透明操作控件同步降至 28px；保留现有字号、图标、截断和可访问名称，不改变工作空间与工作位置交互。
- [x] 仅修改既有静态几何 token，不新增抽象、状态、依赖、监听器、I/O 或缓存；两个固定伪元素不扩大 React 渲染范围，对输入热路径无性能影响。

## Phase 54 — 快速对话顶部栏圆角绘制域修正 ✅

- [x] 根因确认：32px 连接肩与 32px 栏高相等，伪元素从栏顶开始绘制并穿过 32px 顶部圆角的透明区，导致主体左右边缘露出垂直色缝；该问题来自上下曲线绘制域相交，而非颜色或抗锯齿。
- [x] 保持栏高 32px、曲线底部左右各内收 32px；主体改为左右各内收 48px，顶部圆角与底部连接肩各使用 16px 半径，使两个绘制域在栏高中线衔接且互不穿透。
- [x] 延续单一主题 surface、零间距和零交叠结构；不新增遮罩、DOM、状态、监听器或依赖，固定两个 16px 径向渐变不会扩大渲染范围或输入热路径成本。

## Phase 55 — 工作空间切换焦点归还修正 ✅

- [x] 根因确认：Radix Select 关闭时默认将焦点归还触发器；pointer 选择也触发程序化回焦，因而在切换完成后出现类似持续选中的 focus ring。问题属于选择器关闭焦点契约，不是工作空间选中状态或主题样式错误。
- [x] 使用短生命周期 ref 记录最后一次选择输入方式；pointer 关闭时阻止自动回焦并 blur 触发器，键盘关闭时保留 Radix 默认焦点归还与 `focus-visible`。不删除共享焦点样式，不影响单工作空间静态入口。
- [x] 不新增 React state、effect、全局监听器或依赖；ref 更新不触发渲染，不扩大 composer 输入热路径或内存生命周期。

## Phase 56 — 顶部信息栏工作空间入口主题材质统一 ✅

- [x] 根因确认：工作空间选择器虽然已有 `info` 透明变体，但 Select trigger 又无条件追加定时任务 `toolbar` 变体的深色静态背景，导致深色主题出现独立常驻色块；问题属于共享控件变体样式泄漏，不是顶部信息栏材质或几何设计缺陷。
- [x] 将 trigger 表面样式按既有 `info / toolbar` 变体隔离；`info` 在浅色与深色主题下都保持透明，仅保留 hover / focus 反馈，`toolbar` 继续保留底栏胶囊背景。增加 DOM 样式契约，固定深色透明类且禁止信息栏入口重新消费胶囊背景类。
- [x] 仅调整既有静态 Tailwind class 投影，不新增组件、状态、依赖、监听器、I/O 或缓存，不扩大 React 渲染范围和输入热路径；现有变体已足够表达差异，无新增抽象，方案无过度设计与明显性能风险。
- [x] 定向单测 7 项与前端生产构建通过；应用内浏览器在浅色、深色主题下实测入口背景均为透明并继承信息栏材质，深色静态态不再出现独立色块，控制台无 warning / error，验证后恢复“跟随系统”并关闭测试页与开发服务。

## Phase 57 — 会话侧栏跨工作空间终态点全局收敛 ✅

- [x] 根因确认：run 终态订阅位于当前 `ConversationRunPage` effect 内，按当前 `projectId/taskId/runId` 过滤；后台或其他工作空间 run 完成后，durable run 已 terminal，但侧栏继续持有创建时的 running 摘要。
- [x] App shell 改为唯一全局订阅 run-state event，按完整 locator 只投影目标 run 与其 task/置顶副本的 `status/outcome`；当前详情通过稳定 ref 接收同一事件并只刷新当前 `ConversationRunVm`，后台 run 不加载详情。
- [x] 删除 run-state 刷新链中的全量 `getConversationSidebar()`；非目标 workspace、工作空间列表、偏好、时间与会话正文 identity 保持不变，terminal 状态拒绝非终态回退。复用既有事件、VM 和状态机，不新增依赖、缓存或轮询。
- [x] Vitest 增加跨工作空间同名 task/run、置顶副本、非目标 identity、terminal 单调性、全局唯一订阅及详情刷新不调用侧栏接口的回归契约；前端生产构建通过。桌面客户端用 `sasuke-debug` 后台工作流与 `sasuke` 前台详情切换验证：运行中蓝点和停止后暂停黄点只做局部收敛，侧栏展开、滚动、选中与排序未重载，另一工作空间详情未被后台运行事件刷新；验证后恢复原页面和工作空间状态并关闭测试客户端。

## Phase 58 — 会话工作树标识末端对齐 ✅

- [x] 将会话 composer 信息栏的工作树标识固定为最后一项，并通过既有 flex 剩余空间始终贴在最右侧；上下文窗口等元信息保持原有顺序与交互。
- [x] 工作树 Git fork 图标与文字间距收紧为 4px，继续保留路径 Tooltip、键盘焦点、截断和窄宽度收缩能力。
- [x] 只调整既有 DOM 顺序与静态 Tailwind utilities，不新增组件、状态、依赖、测量或监听器；渲染范围、memo 比较和流式热路径不变，无过度设计或明显性能风险。

## Phase 59 — 快速对话顶部信息栏连续 Bézier 侧边 ✅

- [x] 根因确认：顶部圆角与底部反向圆角无论怎样单独缩放，仍会形成曲率分段；底部缩至 8px 后还暴露出明显竖直脖子。目标轮廓需要上下端均为水平切线、中段连续加速的一条曲线，因此不能继续用两个圆角拼接。
- [x] 保持顶部主体左右各内收 48px 与 32px 栏高，删除圆角伪元素；两侧改为固定 48×32px 的镜像三次 Bézier SVG 背景，统一路径为 `M0 32 C18 32 30 0 48 0`，从输入框外端平缓起步并在顶部主体外端自然收平。
- [x] SVG 只承担无障碍树外的静态 surface 背景，控件、DOM 交互层级与零重叠拓扑保持不变；不新增状态、依赖、I/O、缓存、测量或监听器，不扩大 React 渲染范围和输入热路径，方案无过度设计或明显性能风险。
- [x] 布局与组件契约固定左右镜像、48×32px 绘制域、统一路径及旧伪元素删除；前端生产构建与浅色、深色、窄宽度 deep link 视觉验收通过后交付。

## Phase 60 — 快速对话顶部操作栏整体收窄 ✅

- [x] 根因确认：上一轮把“整个顶部操作栏宽度”误判成“两侧曲线宽度”，导致正确的 48×32px 连续 Bézier 被横向压缩。问题属于既有好设计的宽度消费对象实现错误，不需要回退圆角拼接或修改组件结构。
- [x] 恢复左右各 48px 的 SVG 实际宽度、原 48×32 viewBox、路径和镜像；根据视觉复核将包含两侧曲线的顶部操作栏总宽定为输入框的 90% 并整体居中，中间实体使用 `90% - 96px`，因此只调整整体而不改变已确认的曲度。
- [x] 仅调整既有静态 Tailwind 几何契约并同步布局、DOM 契约和两类设计文档；不新增状态、依赖、I/O、缓存、测量或监听器，不扩大 React 渲染范围和输入热路径，方案无过度设计或明显性能风险。
- [ ] 契约测试、前端生产构建及内置浏览器 1280px / 720px / 恢复 1280px 实测待完成；验收需确认总形状宽度与输入框宽度之比为 0.90、左右曲线各为 48px、surface 间距为 0 且无横向溢出。

## Phase 61 — 快速对话顶部操作栏圆角梯形 ✅

- [x] 视觉目标调整为圆角梯形：最终采用 70% 整体宽度、28px 栏高、48×28px 左右绘制域和顶部圆滑过渡，删除侧边底部的水平切线与回弯，不再沿用完整连续 Bézier 的“∫”轮廓。
- [x] 左右 SVG 改为镜像的“底部端点 → 约 50° 直线斜边 → 顶部切线连续圆角 → 水平顶边”路径，将顶部内角定为约 130°；斜边直接落在输入框上沿，仅顶部做圆角，因此不存在底部曲度、竖直脖子、拼缝或额外叠层。
- [x] 复用现有静态 SVG、surface token、DOM 和 Tailwind 布局，不新增组件、状态、依赖、测量或监听器；渲染范围、输入热路径与内存占用不变，方案无过度设计或明显性能风险。
- [x] 工作空间与工作位置两个触发器改由信息栏既有 flex 容器整体水平居中，保留内容宽度、顺序、间距、截断、Tooltip 与下拉交互；不增加绝对定位、包装状态或测量逻辑。
- [ ] 契约测试、前端生产构建及内置浏览器正常/窄宽度视觉验收待完成；验收需确认总宽比为 0.70、栏高与左右绘制域高度均为 28px、左右绘制域各宽 48px、顶部内角约 130°、下半段为直线且页面无横向溢出。

## Phase 62 — 会话主页 Composer 密度与视觉层级调整 ✅

- [x] 欢迎标题从 `text-2xl` 提升到 `text-3xl`；视觉复核后撤销底部停靠分区，欢迎标题、输入框、处理模式与 Direct / Workflow / AUTO 对应操作栏恢复为同一个正常文档流内容组，通过既有 64–80px 响应式底部留白在可用主区内光学居中。
- [x] 圆角梯形总宽由输入框 70% 调整为 80%；工作空间与工作位置触发器在实体内贴左排列，组间距归零但保留各按钮内部点击热区、截断、Tooltip 和下拉交互。
- [x] 顶部信息栏改由完整 80% 圆角梯形承担布局根节点，中间矩形表面左右各内收 48px、两侧 SVG 位于根节点内部；触发器从外轮廓左侧 32px 的安全平顶位置开始，比原矩形左边缘提前 16px。触发器水平内边距和内部图文间距均为 6px，不使用负 margin、transform 或绝对定位交互控件。
- [x] 底部 composer 主区块间距最终从 16px 收至 6px；处理模式、Workflow、AUTO 外层上下内边距收至 4px，处理模式 Tabs 与 Workflow / AUTO 选择器降至 28px；Direct Agent 行从 56px 收至 40px、药丸从 40px 收至 32px，AUTO 目标输入最小高度从 56px 收至 36px。focus ring、横向滚动和文本完整性保持不变。
- [x] Workflow 模式操作栏的前导图标由 `Workflow` 统一为运行模式入口和管理页使用的 `Route`；仅复用同一成熟 Lucide 图标，不新增依赖或图标状态映射。
- [x] 只调整静态 CSS 布局、字号 token 与现有图标；不新增状态、I/O、缓存、测量或监听器，不扩大 React 订阅和渲染边界，方案无过度设计或明显性能风险。
- [ ] 定向契约测试、前端生产构建以及 1280px / 720px 内置浏览器验收待完成；需确认整组内容光学居中、欢迎语字号、80% 顶栏、进一步左移的紧凑触发器、Route 图标及无横向溢出。

## Phase 63 — Direct 会话侧栏 activity 全局收敛 ✅

- [x] 根因确认：正常完成样本中 ACP prompt 已返回 `end_turn` 且 durable run/node/session 均已 terminal；pre-accept 停止样本中 `session/new` 尚未返回，session snapshot 已 `cancelled` 而 run 按可恢复语义保持 `paused / process-interrupted`。两类路径都不能依赖 run 状态清理呼吸；`sasuke://acp-session-updated` 只在当前 run effect 内订阅时，后台、跨工作空间或短会话监听竞态会遗漏显式 `activity: null`，侧栏继续持有旧 `running` 投影。
- [x] App shell 改为唯一全局 ACP session update 订阅，按完整 `projectId + taskId` 只更新目标工作空间 task 与置顶副本；当前详情通过稳定 ref 消费同一事件，删除页面级重复监听，不请求完整 sidebar 或后台详情。
- [x] 后端 activity 由 event attempt 查询改为 task 根下聚合全部 prompt control；单个 attempt 结束后仍有 sibling prompt 时保持 active，全部结束后才发 `null`。已 `stopped` 但因 session 复用暂留 registry 的 control 不计为活动；显式 `null` 优先于暂停/停止中的 lifecycle。重复同 phase 更新保持 sidebar、非目标 workspace 与未命中 task identity。
- [x] 复用既有 ACP event、task activity VM 与 prompt control registry，不新增依赖、缓存、轮询、队列或状态机；事件更新只扫描目标 workspace 摘要和置顶列表，不加载 session tree、timeline、raw frame 或 Markdown，方案无过度设计及明显性能风险。
- [ ] 定向 Vitest、Rust 单测、前端生产构建及桌面客户端短 Direct 会话终态验收待完成。

## Phase 64 — ACP 空事件提示去卡片化 ✅

- [x] 根因确认：“暂无 ACP 事件”是无交互的状态提示，旧实现却叠加了虚线边框、弱背景、圆角和大内边距，错误将其投影为独立卡片；问题属于现有好的空态语义实现不完善，不涉及 session 数据、状态机或加载契约。
- [x] 复用现有 i18n 文案与 Tailwind 语义色，将空态收敛为居中弱化的语义文字，删除背景、边框、圆角和卡片容器视觉；不新增组件、依赖、状态、订阅或测量。
- [ ] 视觉层级契约测试、前端生产构建与内置浏览器 deep link 验收待完成；验收需确认浅色/深色、正常/窄宽度下都只有文字，无卡片表面且无横向溢出。

## Phase 65 — 会话标题 Task 单一事实源 ✅

- [x] 根因确认：侧边栏消费 `ConversationTaskRowVm.title`，会话页头和 run LRU 缓存消费 `ConversationRunVm.title`，创建/重命名又分别异步刷新两份投影；短标题更新或失败被静默吞掉后，两份状态可长期分叉。
- [x] 后端删除 `ConversationRunVm.title/autoTitle`；创建命令返回 `{ task, run }`，重命名命令返回持久化后的权威 Task 行。Task 行构建统一复用同一投影函数，并保留完整 `projectId + taskId`、置顶状态与顺序。
- [x] 前端页头和 Direct ACP 组合页头统一按 `projectId + taskId` 从 sidebar Task 状态派生标题；创建与重命名只应用权威 Task 快照，同时更新 workspace 列表和置顶副本，不再写 run 或依赖全量 sidebar 刷新。失败保持旧标题并显示错误。
- [x] run LRU 写入时显式清除旧版本遗留的 `title/autoTitle`；ACP session title 保持在 session 领域，不进入 Task 标题链路。
- [x] 新增前端状态契约和缓存回归测试，覆盖跨 workspace 同 taskId 隔离、创建插入、置顶同步、无权威返回不改标题及旧缓存标题清除；Rust 测试覆盖重命名返回权威 Task 行以及 Run DTO 不序列化标题。
- [x] 复用现有 Task/sidebar 状态、Tauri command 返回值和有界 run LRU，不新增事件、轮询、缓存、状态机或依赖；单次更新只扫描目标 workspace task 列表与置顶列表，复杂度与现有侧栏局部更新一致，无过度设计或明显性能风险。
- [x] 定向 Vitest 6 个文件 49 个用例、TypeScript 类型检查、`cargo check -p sasuke-desktop`、Rust 格式检查、diff whitespace 检查与前端生产构建通过；当前 `maling` 开发窗口只读复核确认侧栏 Task 标题完整显示 `hi`。
- [ ] 新后端桌面实例的真实 `hi` 创建/重命名闭环未完成：独立 Tauri 测试构建因磁盘空间不足停止，临时 `target/title-sync-test` 已删除；Rust 测试二进制另被工作区既有的旧字段初始化/缺失测试导入错误阻塞。全量前端测试 1471/1472 通过，唯一失败为既有文件变更卡片 `mb-2` / `mb-3` 间距契约，与标题链路无关。

## Phase 66 — Direct 短标题常态截断修复 ✅

- [x] 浏览器实测纠正初判：Task 标题数据始终为 `hi`，外层改成 `flex-none` 后仍会裁剪。常态 trigger 的 `-ml-1` 让 shrink-to-content 外层比按钮真实宽度少 4px，trigger 的 `max-width: 100%` 随即把同样 4px 从 `h1` 扣掉；`hi` 常态文字区实测仅 7.8px、内容需要约 12px，因此只显示 `h`。编辑 input 没有负外边距，所以能显示完整值。
- [x] 保留标题外层 `flex-none + max-width: 40%` 处理真实兄弟项空间竞争，删除 trigger 的负左外边距并保留对称点击内边距；短标题按完整 intrinsic width 展示，长标题仍只在超过页头上限时截断，不改变标题状态、编辑交互、Agent 身份或 sessionId 布局顺序。
- [x] 更新 EditableConversationTitle 与 ACPSessionHeader DOM/CSS 契约测试，分别固定 trigger 不得用负外边距缩减 intrinsic slot，以及 Direct 标题不得参与 flex shrink；不新增组件、状态、测量、监听器或依赖，不增加渲染与布局热路径成本，无过度设计或性能风险。
- [x] 定向 Vitest 3 个文件 15 个用例、TypeScript 类型检查与前端生产构建通过；内置浏览器确认 `hi` / `ab` / 中文短标题完整、超长标题按 40% 上限省略，用户已完成桌面实测验收。

## Phase 67 — 会话主表面与 Composer 细边界统一

- [x] 根因确认：会话主工作区只绘制上边界，圆角左边遗漏；快速对话 composer 保留 prompt-kit 边界，但会话详情共享布局常量用 `border-0` 主动取消；图片缩略图也只有背景与圆角，三处表面边界契约不一致。
- [x] 复用现有 `sidebar-border`、主题 `border` token、prompt-kit composer 和共享 `ComposerContextArea`，为主工作区圆角左边与上边、两类 composer 外轮廓及图片缩略图统一增加 1px 边界；用户视觉复核后保留原粗细，仅将 composer/缩略图从 `border/60` 提升为完整 `border`、主工作区从低对比混合色提升为 `sidebar-border`，全部随主题和深浅模式切换，不新增组件、状态或依赖。
- [x] 新增 DOM/源码契约覆盖主工作区双边界、快速对话与会话详情 composer 边界以及图片预览边界。
- [ ] 按用户要求，本次不执行定向 Vitest、前端生产构建与浏览器视觉验收。

## Phase 68 — 草稿图片查看 Tab 原生复制与另存为

- [x] 根因确认：草稿图片已正确进入独立右侧工作区 Tab，并复用统一附件 identity 与图片画布；缺口是画布没有桌面图片的原生上下文动作，属于现有好设计实现不完整，不需要新增预览 Dialog、附件副本或第二套 Tab 状态。
- [x] 复用项目现有 shadcn/ui Context Menu、Tauri 系统保存对话框、`image` 解码库、`atomic-write-file` 和成熟 `arboard` 剪贴板库，为图片右键增加“复制图片”“图片另存为”。后端仅返回稳定结构化错误码，前端维护中英文文案和当前画布内的处理中/完成/失败反馈。
- [x] 路径来源只传本地路径，粘贴等内存来源仅在用户触发动作后按需序列化，并受 25 MB 单文件上限约束；图片解码、剪贴板写入与原子落盘进入 blocking pool。无全量扫描、N+1、缓存、队列、持久字段或大范围 React 订阅，方案无过度设计，图片常态查看与缩放热路径不增加 I/O。
- [x] 前端接口测试覆盖路径与内存来源、保存取消和右键动作投影；Rust 单测覆盖 RGBA 剪贴板投影、原始编码字节保存及稳定错误码。TypeScript 类型检查、Rust `cargo check`、定向 Vitest 与 Rust 单测通过。
- [x] 修复图片右键菜单未打开：`react-zoom-pan-pinch` 一方面默认开启右键平移，另一方面通过 `.content img { pointer-events: none }` 让事件命中缩放内容层而非图片；因此把 Radix Trigger 直接 `asChild` 到 `<img>` 永远收不到右键事件。改为与图片同尺寸的 `inline-flex` Trigger 包装层，并显式关闭右键平移，左键拖拽继续由缩放画布处理；不覆盖依赖 CSS、不扩大到空白画布，也不新增状态或依赖。
- [x] 新增真实缩放库与真实 Radix Context Menu 的 DOM 回归测试，固定 Trigger 包装层、右键事件不被取消、菜单动作可见和左键拖拽继续生效；内置浏览器实测图片 hit-test 命中 Trigger，右键后菜单状态为 `open` 且两项动作可见，左键拖拽后 transform 正常变化。修复只改变单个图片节点的事件命中，不增加 I/O、监听器、状态订阅或渲染范围，无过度设计和性能风险。
- [x] 修复粘贴/截图等内存图片的复制与另存为失败：前端发送 camelCase `dataBase64`，Rust enum 字段此前仍按 snake_case 反序列化，导致 Tauri 在进入 command 前拒绝请求；桌面 DTO 显式绑定 `dataBase64`，并以 JSON 反序列化单测固定跨层契约，不增加兼容分支或第二套来源模型。
- [x] composer 图片缩略图复用查看 Tab 的 shadcn/ui Context Menu 与同一 `copyAttachmentImage` / `saveAttachmentImageAs` 接口；菜单仅作用于图片缩略图，非图片附件和删除按钮保持原行为。操作状态局部归属单个 attachment chip，处理中防重复并提供成功/失败可访问反馈，不新增全局状态、依赖、缓存或持久字段。
- [x] 回归测试覆盖真实 Radix 缩略图右键菜单、复制动作附件参数与非图片隔离；Rust 图片动作单测与定向 Vitest 通过。路径来源仍为 O(1) 单文件后台读取，内存来源仍为用户触发后单图按需序列化，无常态 I/O、全量扫描、N+1、无界缓存或额外渲染订阅；方案未过度设计且无新增性能风险。
- [x] 修复 Windows 图片复制性能根因：`arboard::set_image` 在接收已解码 RGBA 后同步重新编码 PNG，再额外生成 DIBV5。2559×1488、5.7 MiB PNG 的开发构建基线为读取 3 ms、解码 638 ms、剪贴板写入 5.32 s、总计 5.96 s。Windows 改为直接复用原始 PNG 字节并同时生成标准 `CF_DIBV5`，保留跨应用粘贴兼容；同图正式函数实测降至约 801 ms，并从系统剪贴板读回 2559×1488，约提升 7.4 倍。新增依赖只是把既有 `arboard` 已使用的 `clipboard-win 5.4.1` 提升为 Windows 直接依赖，没有引入新版本、缓存、队列或持久状态；单次内存上限仍受 25 MiB 编码源与解码边界约束。
- [x] 缩略图与查看 Tab 的 `copied/saved` 成功反馈统一为 1.8 秒 transient state，超时后恢复原文件信息；定时器在状态变化和组件卸载时清理。定向 Vitest 4 个文件 8 项通过，覆盖两处反馈自动恢复；每次成功仅新增一次有界定时更新，不扩大订阅或渲染范围。
- [ ] 桌面 EXE 系统剪贴板写入与系统另存为对话框真实交互待完成后收口。

## Phase 69 — Session Tree 有界下拉与进程内展开记忆

- [x] 根因确认：Session Tree 采用手写 `absolute + document mousedown` 浮层，未获得视口碰撞与可用高度；round/node 展开状态保存在随下拉关闭而卸载的子组件内，因此每次重开必然恢复默认展开。问题属于现有 session tree 数据设计正确、交互载体与 transient 状态归属实现不完整。
- [x] 复用现有 shadcn/Radix `Popover`、`ScrollArea` 与 run 级 12 项 `BoundedLruCache`：下拉按 Radix 可用视口高度和 32rem 设计上限取较小值并在树内容区滚动；展开布尔映射按完整 `projectId + taskId + runId` 及 round/node/outer-node 树路径隔离，关闭下拉、切页和 run 快照刷新后恢复，应用退出后自然清空。
- [x] 删除页面级 outside-click 监听和手写绝对定位，不新增依赖、业务状态机、持久字段、I/O、轮询或第二套 session tree；单次展开只复制当前 run 的小型布尔映射并局部重渲染下拉，缓存仍受既有 12 个 run 上限约束，无全量扫描、N+1、无界缓存或主线程热路径风险，方案无过度设计。
- [x] 定向 Vitest 6 个文件 24 项、TypeScript 类型检查、前端生产构建与 diff whitespace 检查通过。内置浏览器 deep link 实测：短树按内容保持约 100px；49 条长树在 1280×720 下以 512px viewport 滚动，滚轮后 `scrollTop` 从 0 到约 454px且 Radix scrollbar 可见；720×520 窄窗口自动降至约 437px并保持在视口内，恢复宽窗口后回到 512px。关闭重开下拉、切到首页再返回同一 run 均保留手动收起，控制台无 warning/error；临时长列表 fixture 与验证页均已清理。

## Phase 70 — 工作树会话目录贯穿修复

- [x] 根据真实 `acp.raw.jsonl` 定位根因：工作树 run 首次 `session/new` 使用正确 worktree，但停止后的普通追问由桌面 command 重新硬编码主项目目录，导致同一 session 的 `session/resume` 回落主工作区并覆盖 `worker-ref.continue_ref.cwd`；问题属于 prompt 与执行目录在接口层被拆散，不是 worktree 创建或节点调度失败。
- [x] 将普通节点与 AI-DYNAMIC leaf 的会话 prompt 准备接口统一返回 `PromptBundle + adapter workspace + session workspace`；桌面 follow-up 直接消费同一次 `WorkerInvocation` 的目录解析，普通 run 以 `RunState.worktree` 为权威，dynamic leaf 以 workspace catalog 为权威，adapter 仍使用项目主目录。删除 command 层两处 `repo_root` session cwd 硬编码，旧 continue-ref cwd 不再覆盖 canonical workspace。
- [x] 不新增持久字段、依赖、缓存、扫描、锁或第二套 workspace 状态；只把既有调用链中的三个同生命周期值收拢为临时 DTO，prompt 准备仍是固定次数的小型状态读取，provider 执行、并发边界和渲染范围不变，无过度设计或明显性能风险。
- [x] 增加普通工作树停止后追问、普通主工作区与 AI-DYNAMIC prompt 目录回归测试；3 项 Rust 定向测试、`cargo check -p sasuke-desktop`、目标 Rust 文件格式化及 diff whitespace 检查通过。工作树用例显式注入指向主目录的旧 `continue_ref.cwd`，准备结果仍以 `RunState.worktree` 收敛；dynamic 用例按 node→workspace catalog 验证独立 leaf worktree。
- [x] AUTO 从外层会话 worktree 启动时，dynamic `workspace-main` 继续继承该目录；fanout child、同链后续节点、merge 与 acceptance 只沿既有 workspace catalog 的 `workspaceId` 转换，不把外层路径强制覆盖到内部 leaf，支持“外层会话 worktree → 内部分派 worktree → 向上归并”的嵌套模型。
- [x] 根因确认：composer 工作树角标仍直接消费 run 级启动位置，导致选中 AI-DYNAMIC child session 后 Tooltip 继续显示外层路径。改为在 `AcpSessionVm` 构建时按完整 locator 派生当前物理 worktree：普通 session 使用 `RunState.worktree`，dynamic session 使用 node→workspace catalog；主工作区和已释放 child 不投影角标，前端只消费当前选中 session 的路径。
- [x] 不新增 canonical 字段、持久状态、轮询、缓存、Context 或订阅；VM 构建只读取单个 run 与最多 20 个节点的既有 dynamic catalog，查找为有界 O(n)，流式消息热路径、内存与渲染范围不变。复用现有 `AcpUsagePanel`、Tooltip 和 worktree 图标，无新增组件依赖，方案无过度设计或明显性能风险。
- [x] Rust 单测覆盖 child worktree、继承外层 worktree 的 dynamic main、项目主工作区、released child 以及 dynamic catalog 缺失时失败关闭；前端集成测试固定 AUTO 选中 child 时必须把 child path 而非 run path 传给 composer。定向 Vitest 2 个文件 27 项、4 项 Rust 回归、TypeScript、`cargo check -p sasuke-desktop`、前端生产构建与 diff whitespace 检查通过。内置浏览器 deep link 实测 session child 路径可显示角标并在 Tooltip 展示完整路径，移除当前 session worktree 后角标为 0，控制台无错误；临时 fixture、页面和开发服务已清理。

## Phase 72 — Summary-first 会话树工作树投影

- [x] 根因确认：会话详情页已采用 summary-first，`ConversationRunVm.selectedSession` 有意保持为空，但工作树角标仍读取该正文字段，导致已有 worktree run 在详情页看起来像未常驻。
- [x] 会话树 leaf 新增轻量派生 `worktreePath`：普通 leaf 从 `RunState.worktree` 获取；AI-DYNAMIC leaf 按当前 node 的 `workspaceId` 查询 dynamic workspace catalog，保留 child worktree、自身 main workspace 继承和 released/解析失败关闭规则。前端 composer 信息栏直接消费选中 leaf，不恢复完整 session 查询、不新增持久字段、轮询、缓存或订阅。
- [x] 补充 Rust VM 与前端回归契约，固定 `selectedSession=null` 时仍显示 run/dynamic main 的工作树路径，并保留 dynamic child 使用自身路径；路径投影只读取当前 run 与 dynamic graph 的有界节点集合，复杂度为既有 VM 构建 O(n)，不扩大流式消息渲染范围。

## Phase 71 — ACP Timeline 增量投影与停止控制面解耦

- [x] 根因确认：停止 accepted、provider terminal 与完整 session/timeline 查询耦合，旧 timeline 每次重进从头合并 patch；ACP facet 又缺少独立 revision，导致全量扫描延迟和 terminal/accepted 乱序共同形成长期 stopping。按 append log + materialized projection + checkpoint + tail replay 行业模式扩展现有 TimelineStore，不给 full scan 增加缓存或超时补丁。
- [x] 新增 `acp.timeline.index.json` V3，按 root/Agent branch 维护 generation、offset/revision/fingerprint、item/semantic locator、Todo、pending interaction、processing retry identity、commands、usage/timing、Agent transcript、accepted prompt 与 runtime output candidate。timeline 仍是唯一正文事实；V1/V2、缺失、损坏或 compaction offset 变化时 blocking rebuild 一次并推进 generation。
- [x] runtime 写时增量维护 projection，默认 256 patch checkpoint，terminal/正常关闭强制 checkpoint，崩溃只回放最多 256 条尾记录。compaction 在 timeline 锁内读取最新 checkpoint 后分配 generation；多 writer 不覆盖 projection。健康 JSONL append 只检查最后 1 字节，修复每次 append 重读完整文件造成的 O(N²)。
- [x] session 查询、重进、分页、Agent 树、prompt queue accepted 恢复与 runtime artifact 标注全部走 index/current-page/locator；旧全量 parser、semantic pagination、diagnostics 和 Agent rebuild 仅在测试构建作为 oracle。首次 legacy migration 进入 blocking pool，正常查询不执行 usage repair、raw fallback 或 stale lifecycle 写回。
- [x] stop accepted 不读取 timeline 或构建 session VM；retry、permission、elicitation 使用 durable branch/item/revision identity 与 status/revision CAS。V3 index 从 timeline 物化当前 processing retry ID，封闭 timeline 已 flush、snapshot 尚未替换的停止/崩溃窗口。provider cancel 从 runtime 首次观察取消开始共用 10 秒 deadline，终态只发布常量大小 lifecycle patch。
- [x] attempt metadata 持久化 ACP revision、turn/prompt identity、operation ID 与 live activity；accepted 与 terminal 使用不同 revision。前端分别按 runtime/ACP/promptQueue revision 合并，同 revision 同 turn terminal 压制迟到 running/stopping。
- [x] TimelineStore 14 项、prompt queue 20 项、permission 9 项、elicitation 13 项、Agent branch 20 项、lifecycle/前端 composer 回归通过；Rust/桌面 check、TypeScript 与前端生产构建通过。新增回归固定 index pending retry identity 必须压过落后的 session metadata hint；10,000 revision 合成用例第二次查询 tail=0。
- [x] Release 真实 fixture：task-276 4.39 MB/1699 行，一次迁移 1607 ms、第二次查询 191 ms；task-284 4.04 MB/1071 行，一次迁移 2085 ms、第二次查询 227 ms。两者均返回当前页 30 条且 tail=0，达到正常重进后端查询小于 1 秒目标。
- [x] 桌面 Direct task-030 真实 `Start-Sleep` 停止约 156 ms accepted，provider 在 10 秒上限前提前结束；AUTO task-032 run-002 两个并行 leaf 的停止分别约 244 ms/145 ms accepted，二者最终均为 ACP `cancelled`、leaf runtime `paused`，父 graph/outer attempt/run 收敛为 `paused / process-interrupted`，先停止 leaf 未被迟到事件恢复。task-026 的旧 graph 已通过 `0.2 -> 0.3` 一次性迁移补齐 leaf runtime facet。permission/elicitation/retry 与 terminal event 丢失恢复属于协议态/故障注入场景，由接口级回归覆盖；未伪称桌面手工通过。测试进程在最终回归后关闭。

## Phase 72 — 运行模式工作流角色校验加载竞态修复 ✅

- [x] 根因确认：运行模式页以空数组初始化 profile catalog，`WorkflowEditor` 在目录接口返回前启动实时校验，把“尚未加载”错误解释为“已加载且角色不存在”，因此进入工作流模式后短暂显示全节点角色失效，目录到达后又自动消失。
- [x] 为工作流实时校验增加显式 profile catalog 生命周期契约；加载期间继续执行 DSL 结构、必填项和拓扑校验，仅延后依赖权威 profile catalog 的引用存在性校验，目录到达后自动执行完整校验。保存和另存入口在目录就绪前禁用，显式保存路径仍始终执行完整校验。
- [x] 回归测试固定“加载中不误报角色删除但保留结构错误、加载完成后恢复角色引用错误”，并固定运行模式页就绪状态透传与两类保存入口禁用契约。
- [x] 复用既有校验器和模板操作行，不新增全局 store、缓存或依赖；加载期少执行一次 profile ID 集合查询并避免一次错误告警重绘，无新增全量扫描、I/O、订阅范围或明显性能风险，方案无过度设计。
- [x] 定向 Vitest 2 个文件 31 项、前端 TypeScript 与生产构建通过；内置浏览器 deep link `/chat/run-modes` 后切换工作流模式，在立即、400ms、1.4s 三个时间点均未出现“角色不存在或已删除”，工作流编辑器持续可见且控制台无 warning/error。全量 Vitest 1506 项中 1504 项通过；仅 `composer-context-alignment` 的 CRLF 敏感源码断言与 `conversation-prompt-queue` 的既有样式契约失败，均不涉及本次改动文件。

## Phase 73 — Profile catalog 三态契约与定时任务成功反馈生命周期修复 ✅

- [x] 根因修复：删除 `WorkflowEditor` 可选 `profiles` / 默认 `profileCatalogReady=true` 旁路，所有入口统一传入 `loading | ready | error` 的 `profileCatalog`；普通 Worker 与 AI-DYNAMIC 的角色引用校验都只在 `ready` 时执行。
- [x] RunMode、TaskList、Workflow、会话右侧工作区入口统一使用有请求代次保护的 catalog hook；加载期间继续做结构校验并禁用保存，失败在编辑区显示结构化错误和重试按钮，避免静默失败导致永久禁用。
- [x] 定时任务创建成功提示从 `ConversationComposer` 提升到 App 根层，路由从 `scheduled-task-create` 返回普通主页后仍可见，点击链接进入定时任务管理，5 秒后清理；不新增全局 store、轮询或持久字段。
- [x] 新增 catalog error/retry/迟到响应接口回归、动态节点延迟校验和跨路由提示源码契约；四个原有工作流校验文件与定时任务测试同步更新。
- [x] 方案复审：目录请求最多保存当前入口的一份有界 Profile 列表，重试只由用户触发；每次响应仅更新所在入口和编辑器订阅范围，无全量扫描、N+1、无界缓存或热路径 I/O，复杂度与现有页面数据量匹配。
- [x] 收口 PR 既有 CI 阻断：Timeline replay 集成测试按写时 compaction 后的 3 个 canonical 行断言；prompt queue 测试与已落地的 composer 完整边界设计一致；源码契约测试统一换行后再断言，消除 Windows CRLF 假失败。以上均只修正过期验收，不改变对应生产行为。
- [x] 收口 CI 的稳定身份过期断言：`runtime_layout` 测试改为校验 `{可读 slug}--{8 位 BLAKE3}` 的项目 ID 形状，并用 `SasukePaths::project_id` 组装运行时目录断言，避免把已废弃的纯 slug 当作 canonical identity。
- [x] 验收：前端 TypeScript、生产构建和全量 Vitest（234 个文件、1510 项）通过，Rust fmt 与 `acp_replay_regression`（2 项）通过；Windows 本机全量 Rust all-targets 受链接器 PDB `LNK1318 LIMIT` 资源上限阻断，不是测试断言失败。内置浏览器因当前权限拒绝本地 preview 地址，关键交互改由真实 React/jsdom 组件测试固化。

## Phase 74 — 并行会话选择条密度与表面统一 ✅

- [x] 根因确认：并行会话 identity、状态点和选择事务均正确，视觉割裂来自选择条单独使用 `bg-muted/5` 半透明材质并保留偏大的 `py-2`；属于现有会话设计实现不完整，不涉及 session 数据或生命周期缺陷。
- [x] 复用运行标题栏与 ACP session header 已有的 `content-header` 主题 token，将活动会话选择条改为实色连续表面，纵向留白收紧为 `py-1`、条目间距收紧为 `gap-1.5`，保留轻量底边界、自然换行、点击热区和运行状态反馈。
- [x] 增加视觉层级接口契约，固定实色 token、紧凑间距并禁止恢复半透明背景；只调整既有 Tailwind utilities，不新增组件、状态、依赖、缓存、I/O、测量或监听器，React 订阅与渲染范围不变，无过度设计或明显性能风险。
- [ ] 定向 Vitest、TypeScript、前端生产构建以及内置浏览器正常/窄宽度、浅色/深色主题验收待完成。

## Phase 75 — AI-DYNAMIC internal leaf 终态刷新与粘性跟随

- [x] 根因确认：dynamic leaf 完成后的首次 ACP session update 发生在 graph 仍持有最后 leaf 过渡运行投影时；graph 随后 durable `completed` 却没有第二次 leaf update，因此 composer 会永久停在“处理中”直到重启。控制 JSON 已正确后置标注到 timeline，但前端用 lifecycle `displayStatus` 而非 dynamic canonical `runtime.status` 判断完整 session 刷新，导致同 identity 的内存流式正文不重新查询。
- [x] 复用既有 dynamic ACP session update、ConversationRun 完整快照合并和 prompt-kit session 查询：graph success terminal 落盘后定向刷新最后 leaf；AI-DYNAMIC lifecycle 以 `runtime.status` 触发 terminal 查询，从磁盘获取 `runtimeControlOutputDisplay`。不取消 metrics 事件过滤，不把 pre-commit `NodeCompleted` 当导航事实，也不增加轮询、缓存或第二套 timeline。
- [x] 自动跟随只对完整 locator 同属一个 `roundId + outerNodeId + outerAttemptId` 的 internal siblings 放宽。当前 active leaf 保持粘性，terminal 后才跟随新 active sibling；不同 outer attempt、普通 Workflow、Direct、manual mode 和 non-runtime 目标保持原行为。纯滚动脱底保留 transient 恢复 identity，回到同一会话底部恢复；手动选择会清除恢复资格。
- [x] 接口回归覆盖 graph durable completed 后才发布 leaf update、completed graph 释放 processing 投影、dynamic terminal 状态触发 session 查询、同 outer sibling 跟随、并行不抢占、跨 outer/manual 隔离、请求中 revalidation、滚动恢复与手动锁定。
- [x] 验收收口：TypeScript、前端生产构建、桌面端 `cargo check`、AI-DYNAMIC 集成回归（25 项）、dynamic graph/桌面 lifecycle Rust 回归，以及相关前端回归（66 项，其中跟随定向 38 项）均通过；`git diff --check` 通过。内置浏览器 deep link 验证完成态与运行中会话正文、状态区和 composer 均正常渲染，控制台无应用错误；浏览器 fixture 不模拟真实 AI-DYNAMIC 并行时序与控制 JSON 后置标注，这两项由上述确定性 Rust/React 接口回归固化，不伪记为浏览器实时复现。
- [x] 方案自评：没有新增 aggregate、持久字段、依赖、轮询、缓存或队列；只在 graph terminal 增加一次 O(1) 定向 lifecycle 通知，前端继续消费既有合并与查询链路。并行事件仍由现有 120ms coalescing 和单 in-flight refresh 背压，普通高频 live event 不增加完整查询；复杂度与实际缺口匹配，无过度设计或明显性能风险。

## Phase 76 — AI-DYNAMIC leaf lifecycle 单水位收敛

- [x] 根因确认：`runtimeExecutionRevision` 只随 leaf-local execution phase 推进，但 Conversation VM 会在 workspace transition 与 graph terminal 期间把 outer phase 投影到 leaf；outer revision 与 leaf revision 属于不同领域，旧实现却让同一个 UI runtime facet 在两个不可比较的水位间切换，造成 terminal 快照无法严格淘汰旧 processing。Direct 没有 outer/inner 双层，普通 Workflow 只有单一 run execution 聚合，因此不受影响；这是 AI-DYNAMIC 正确分层设计下 lifecycle revision 契约不完整，不是三种模式都需要重写。
- [x] 破坏式将 dynamic graph schema 升到 `0.4`，把 leaf `runtimeExecutionRevision / runtimeExecutionUpdatedAt` 替换为 `runtimeLifecycleRevision / runtimeLifecycleUpdatedAt`。leaf begin/phase/pause/complete、workspace graph 接管/释放和最终 graph terminal 全部推进同一个 leaf-owned 水位；outer `run.execution.revision` 与 ACP `acpRevision` 保持独立，不新增 composite、第二套 revision 或跨域比较。
- [x] workspace transition 不再用 `currentNodeIds` 猜 owner；scheduler 把 causal leaf identity 传入现有 transition 边界，entry 暂存并写入该 leaf 的 `PreparingWorkspace` phase，exit 恢复原 phase，两次都在 durable graph 写入后定向失效 session。无关并行/历史 leaf 保持自己的 phase/revision，fanout 新 leaf 在真正 begin execution 时初始化水位。
- [x] `0.3 -> 0.4` 迁移在统一 graph load 边界同步改写已存在的 leaf `node.json` 投影，先 leaf 后 graph，失败后由旧 graph 幂等重试；第二次读取不改盘。ViewModel 只把显式 `leaf.phase=PreparingWorkspace` 或最后 graph 收尾 leaf 视为 graph owner，terminal 使用严格更大的 leaf revision 释放 composer。
- [x] 接口回归已固定 leaf-local/graph-owned revision 推进、迁移一次性与投影同步、workspace entry/exit durable 事件顺序、fanout causal 隔离、并行 sibling 不继承 preparing、graph completed 后最终 leaf 严格新 revision，以及前端迟到旧 revision 不覆盖 terminal。
- [x] 方案与性能复审：复用现有 `DynamicNodeState`、workspace transition、graph lock、原子 JSON 和 Conversation lifecycle merge；不新增 aggregate、持久字段、依赖、缓存、轮询、队列或前端特判。transition 只遍历显式 causal owner 集合，规模受既有 fanout 上限约束；session 通知从全部 active sibling 收紧为 owner，未增加热路径扫描、渲染范围或锁外等待。Direct/普通 Workflow 生产路径不变。

## Phase 77 — AI-DYNAMIC graph pause 投影释放与控制产物实时收敛

- [x] 根因确认：控制 JSON 的 `runtimeControlOutputDisplay` 已在 timeline durable 后正确写入，但 dynamic session update 只携带 lifecycle，已挂载的 root session 正文没有重新查询；同时 graph 在最后 completed leaf 之后因 paused sibling 收敛为 `paused` 时，没有推进并发布临时借用 leaf 的 lifecycle revision，前端缓存中的同 revision active 投影可在重进时覆盖终态。属于既有 leaf-owned lifecycle 与轻量失效通知设计正确、`Running -> Paused` 释放边界和正文消费链路实现不完整。
- [x] core 层统一 graph-owned leaf 投影判定；graph 从 `running` 进入 `paused` 前收集受影响 owner，已被 pause 转换推进 revision 的 leaf 不重复推进，其余 owner 使用既有 `runtimeLifecycleRevision` 前进。graph 先持久化，再只向本次 lifecycle 实际变化的 leaf 发布失效通知；`currentNodeIds` 不用于通知 sibling。
- [x] 当前选中的 AI-DYNAMIC root session 收到 `runtime.active=false + runtime.phase=terminal` lifecycle-only 通知后，复用既有 session query/apply 链路执行一次当前页大小的正文刷新；同一挂载周期重复通知合并，locator 切换后的迟到响应由既有 generation/active guard 丢弃。Direct、普通 Workflow 和 Agent branch 生产行为不变。
- [x] 接口回归覆盖 durable paused graph 后才发布 completed leaf 的严格新 revision、paused sibling 不被误推进、桌面 lifecycle 释放为 terminal/inactive、控制 JSON 无需重进即可渲染、重复终态通知不并发查询、Direct/Workflow 隔离及 locator 切换迟到响应丢弃。
- [ ] Rust 定向测试、前端 Vitest、类型检查、生产构建、桌面交互与 diff whitespace 验收待完成。
- [x] 方案与性能复审：不新增 aggregate、持久字段、revision 域、缓存、轮询、队列或依赖；后端只在 graph 终态边界遍历既有有界 dynamic 节点，前端每个选中 dynamic attempt 至多增加一次受页大小限制的查询，不扩大 live event、React 订阅或 Markdown 重渲染范围，无 N+1、无界内存、事件线程阻塞或明显性能风险，方案不过度设计。
