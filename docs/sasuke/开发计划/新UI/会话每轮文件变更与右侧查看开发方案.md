# 会话每轮文件变更与右侧查看开发方案

## 1. 文档状态

- 状态：已实现，待产品实机验收。
- 编制日期：2026-08-04。
- 实施日期：2026-08-04。
- 节点附件卡实施日期：2026-08-28。
- 体验修正：2026-08-05 完成变更卡异步加载后的首帧折叠时序修正；初始关闭态不再执行 Radix 退出动画，用户首次操作后才启用展开/收起动效。
- 适用范围：Conversation 会话详情、ACP 根会话与 Agent 分支会话、通用右侧工作区。
- 关联方案：
  - [右侧工作区与 Agent 分支会话重构计划](./右侧工作区与Agent分支会话重构计划.md)
  - [右侧工作区文件系统与实时编辑开发方案](./右侧工作区文件系统与实时编辑开发方案.md)
  - [会话优化](./会话优化.md)
- 本期目标：
  1. 用户附件继续绑定用户消息、artifact 继续使用现有消息内特殊样式；点击后统一在右侧工作区查看。
  2. 移除会话 composer 上方独立的“产物与附件”展开栏。
  3. 每个可见 prompt turn 结束后，通过同一个 change set 互斥展示两类结果：本轮新写入 attempt `attachments/` 的节点附件使用交付物卡，其他 ACP tool diff 使用普通文件变更卡；用户上传附件与 canonical artifact 不进入这两类结果。
  4. 修改文件进入只读 diff；新增文件进入该轮不可变原文；删除文件不提供内容入口。
- 后续目标：Git 仓库、commit 列表和任意两个版本端点的文件变更查看。本期只预留数据和接口，不实现 Git 管理 UI。

## 2. 可行性与工作量结论

这两个改动可以实现，但不能只按两个前端组件改动估算。

- “把文件放进右侧工作区查看”较直接：现有 `RightWorkspaceResource`、文件 Tab、CodeMirror、图片预览和安全 preview grant 都可以复用。
- “准确展示每一轮 prompt turn 的工具文件变化”仍是运行时能力：需要把 ACP diff 按稳定 turnId 持久化、折叠同路径的多次修改，并保存不可变前后版本。节点附件只对 attempt `attachments/` 做 turn 前后有界路径集合差分，不扫描或比较 live workspace，因此不会把用户或其他并发 session 的普通磁盘变化算入当前 turn。
- 完整质量版本预计 **7～10 个开发日**，主要成本在 ACP diff 持久化、同路径 mutation 链折叠、不可变版本存储、右栏 Diff Viewer 和接口级测试。

用户附件和 artifact 的消息内展示不与文件变更卡合并：前者表达 prompt 输入，后者表达 Agent 结构化输出；两者只复用右侧文件查看能力。

## 3. 问题判断

### 3.1 不是右侧工作区设计缺陷

当前通用右侧工作区已经具备：

- 按资源 kind 注册 renderer / close resolver。
- 多 Tab、会话 scope、LRU 和响应式 Dock / Sheet。
- `file` 资源、CodeMirror 6、文本/Markdown/图片/不支持格式的读取与预览。
- 会话 Markdown 本地文件链接打开、Tab 复用和行列定位。

因此不新建“附件侧栏”或“产物查看器”，继续扩展通用文件资源与 Diff 资源即可。

### 3.2 根因是资源领域和 turn 生命周期缺失

现有实现存在以下结构性问题：

1. `AssetItemVm` 只包含名称和 attempt locator，不包含真实文件版本，无法保证历史内容稳定。
2. `ConversationRunVm.artifacts / attachments` 是当前选中 session 的 attempt 级聚合，适合旧展开栏，不适合生成 prompt-turn 文件变化。
3. `AcpSessionAssetsPanel + ACPArtifactsDialog` 把文件当成 composer 上方的 session 附属信息，而不是可复用的工作区资源。
4. 用户消息附件和 artifact 已拥有正确的消息归属，但其点击查看尚未统一进入右侧工作区。
5. 当前文件 Tab 指向 live workspace path；旧 turn 的 diff 若继续读取 live path，会被后续修改覆盖。

正确方向是继续使用唯一的 **turn-scoped file change set**：普通 changes 由 ACP tool diff 驱动并保存 immutable file version；节点新增附件由 `attachments/` 路径集合差分驱动，并在同一次 finalize 中与普通 changes 互斥分类。用户上传附件与 artifact 保持现有消息生命周期，不进入该 change set；各类文件只在右侧查看器层复用通用能力。

## 4. 目标与非目标

### 4.1 目标

1. 一个可见用户 prompt 到对应 Agent 处理终止，形成一个稳定 `PromptTurn`。
2. 每个 turn 对普通文件只记录 ACP tool call 返回的 diff；另在 turn 开始/结束各读取一次受控 `attachments/` 相对路径集合，只判定结束时新存在的节点附件。两类证据在同一次 finalize 中分类，不读取 live workspace 作为普通文件最终状态。
3. tool diff 中的普通文件使用统一不可变版本协议；节点新增附件保存稳定 manifest membership 并以 live editable 文件打开；用户上传附件与 artifact 使用各自现有消息 locator 打开右侧工作区。
4. 卡片和右栏只读取持久化版本，不根据当前目录重新猜测历史。
5. 根会话与 Agent branch 各自只展示归属于本 branch 的 change set。
6. 完成、失败、取消的 turn 都要结算已发生的文件变化。
7. hidden repair 属于触发它的可见 turn，不单独生成用户可见卡片。
8. 未来 commit/tree/blob 比较接入时不替换前端卡片和 Diff Viewer。

### 4.2 非目标

- 本期不实现 Git 初始化、提交、暂存、撤销、Review、commit 列表或多 commit 选择器。
- 本期不在卡片中提供截图示例里的“撤销”“审核”按钮；没有后端语义的按钮不进入 UI。
- 不把 Round 详情页现有 artifact / attachment 排障入口一并删除；本期只替换 Conversation / ACP 会话详情主路径。
- 不把右侧工作区改造成 IDE，也不增加 terminal / command bar 心智。
- 不宣称共享 main workspace 中的变化一定由当前 Agent 因果产生；该场景只能表达“本 turn 观察窗口内的净变化”。

## 5. 领域模型

### 5.1 生命周期归属

```text
AcpAttempt
└── PromptTurn (turnId / promptId)
    ├── user message
    ├── user attachments（消息自有，不进入 change set）
    ├── agent messages / artifact（消息自有，不进入 change set）
    ├── tools / hidden repair
    └── TurnFileChangeSet
        ├── FileChange[]
            ├── beforeVersion?
            └── afterVersion?
        └── TurnAttachment[]
```

- `AcpAttempt` 是长会话容器。
- `PromptTurn` 是本需求的最小归属单位。
- `TurnFileChangeSet` 与 `turnId`、branch 和 attempt locator 一一绑定。
- 文件版本属于历史快照生命周期，不属于 React Tab 生命周期，也不依赖文件当前是否仍存在。

### 5.2 后端持久化模型

建议在 Rust 领域层新增以下结构，序列化时使用 camelCase：

```rust
struct TurnFileChangeSet {
    id: String,
    turn_id: String,
    prompt_event_id: String,
    branch_id: String,
    locator: AcpAttemptLocator,
    status: TurnFileChangeSetStatus, // capturing | finalized | partial
    started_at: String,
    finished_at: Option<String>,
    summary: TurnFileChangeSummary,
    changes: Vec<TurnFileChange>,
    attachments: Vec<TurnAttachment>,
    limitation_codes: Vec<String>,
}

struct TurnAttachment {
    id: String,
    relative_path: String,
    name: String,
    byte_length: u64,
}

struct TurnFileChange {
    id: String,
    change_kind: FileChangeKind, // added | modified | deleted | renamed
    logical_path: String,
    previous_logical_path: Option<String>,
    mime_type: Option<String>,
    text: bool,
    added_lines: Option<u64>,
    deleted_lines: Option<u64>,
    before_version: Option<FileVersionRef>,
    after_version: Option<FileVersionRef>,
    limitation_code: Option<String>,
}

struct FileVersionRef {
    id: String,
    storage_kind: FileVersionStorageKind, // captured_blob；以后可增加 git_blob
    content_hash: String,
    byte_length: u64,
    encoding: Option<String>,
    line_ending: Option<String>,
}
```

约束：

- `added` 只有 `afterVersion`。
- `deleted` 只有 `beforeVersion`。
- `modified` 同时拥有 before / after。
- `renamed` 先预留枚举与 `previousLogicalPath`，本期采集器可保守投影为 delete + add，不做相似度猜测。
- 行数统计只用于文本；二进制使用 `null`，不能伪造 `0`。
- change set 不保存 `sourceKind`；`changes[]` 与 `attachments[]` 已由字段表达不同领域，且必须天然互斥。用户上传附件和 artifact 不通过伪造 mutation 接入。

### 5.3 通用文件资源 locator

将现有只覆盖 workspace / external 的 `WorkspaceFileLocatorVm` 提升为带 tag 的 `FileResourceLocatorVm`：

```ts
type FileResourceLocatorVm =
  | { kind: 'workspace'; projectId: string; canonicalPath: string; relativePath: string | null }
  | { kind: 'external'; projectId: string; canonicalPath: string; grantId: string }
  | { kind: 'conversation-asset'; projectId: string; locator: AcpAttemptLocatorVm; assetKind: 'user-attachment' | 'artifact'; name: string }
  | { kind: 'conversation-version'; projectId: string; changeSetId: string; versionId: string };
```

- `conversation-asset` 由现有用户消息附件或 artifact 消息提供，只负责将既有消息资源打开到右栏，不进入 turn change set。
- `conversation-version` 对应 ACP tool diff 持久化的不可变前后版本；两类 locator 都不向前端暴露 runtime 绝对路径。
- 后端通过 locator 解析到受控的 attempt blob / asset root，并继续执行 canonicalize、symlink 和 preview grant 校验。
- 历史版本一律只读；现有 live workspace 文件继续保持原自动保存语义。

### 5.4 右侧工作区资源

新增一种资源，不把 Diff 塞进普通文件 Tab 的局部状态：

```ts
type FileDiffWorkspaceResource = {
  kind: 'file-diff';
  key: string;
  scopeKey: string;
  title: string;
  changeSetId: string;
  changeId: string;
  beforeVersionId: string;
  afterVersionId: string;
};
```

- 新增文件：打开现有 `file` 资源，locator 指向 `conversation-version` 的 after version。
- 修改文件：打开 `file-diff` 资源。
- 删除文件：行保持非交互，不创建右栏资源。
- live 文件 key 继续按 canonical path 复用；历史文件 key 按 version ID；diff key 按 before + after version ID。这样同一路径在不同 turn 的内容不会串 Tab。

## 6. 开源组件调研与选型

调研日期：2026-08-04。

| 组件 | 调研版本 | 协议 | 能力 | 结论 |
| --- | ---: | --- | --- | --- |
| [`@codemirror/merge`](https://codemirror.net/docs/ref/#merge) | 6.12.2 | MIT | CodeMirror 官方双栏 `MergeView`、单栏 `unifiedMergeView`、changed chunk、未修改区折叠、行 gutter、chunk 导航 | **采用**。与现有 CodeMirror 6 extension、语言识别和主题直接复用 |
| [`@uiw/react-codemirror`](https://github.com/uiwjs/react-codemirror) | 项目已使用 4.25.11 | MIT | React CodeMirror 6 包装器 | **继续采用**，用于普通文件；Diff 底层直接接官方 merge API |
| [`react-codemirror-merge`](https://github.com/uiwjs/react-codemirror/tree/master/merge) | 4.25.11 | MIT | 同维护者的双栏 React 包装 | 评估后不首选。官方 API 同时支持 unified / side-by-side，更适合右栏响应式，并可复用现有稳定 extension 工厂 |
| [`similar`](https://github.com/mitsuhiko/similar) | 3.1.1 | Apache-2.0 | 在 Rust 中比较两份文本，输出新增/删除行和 diff hunks，支持 Myers、Patience、Histogram | **采用**，只负责根据已持久化的 tool-reported before / after 计算 `+N/-N` 和后端比较结果；不负责持久化，也不读取工作区 |
| [Git `diff`](https://git-scm.com/docs/git-diff) | 本机 Git 能力 | GPL-2.0 外部程序 | commit/tree/blob/worktree、`--numstat -z`、rename、binary、任意两个端点 | **后续 Git provider 采用**。本期 tool diff 采集不调用 Git；未来调用必须走 `process::background_command()` |
| [shadcn/ui Card / Collapsible / ScrollArea / Button / Tooltip](https://ui.shadcn.com/docs/components) | 项目 copy-in 版本 | MIT | 可访问卡片、展开、滚动与操作行 | **采用现有 copy-in**，不自研基础卡片与按钮 |
| [`react-diff-view`](https://github.com/otakustay/react-diff-view) | 3.3.3 | MIT | 消费 Git unified patch 的 React diff 视图 | 不采用。会形成第二套代码渲染、主题和语言系统，并把前端绑到 patch 文本 |
| [`diff` / jsdiff](https://github.com/kpdecker/jsdiff) | 9.0.0 | BSD-3-Clause | 浏览器 JavaScript 文本 diff | 不采用。CodeMirror merge 和 Rust `similar` 已覆盖需求 |
| [`react-compare-slider`](https://github.com/nerdyman/react-compare-slider) | 4.0.0 | MIT | 图片/任意 React 内容前后滑块对比 | 本期可选。若验收要求修改图片必须交互对比再引入；普通新增图片继续复用现有安全图片预览 |

### 6.1 选型结论

1. 文本 diff 使用官方 `@codemirror/merge`，不引入独立 diff UI。
2. tool diff 的 old / new 正文使用 blake3 CAS + 原子文件持久化；Rust `similar` 只比较这两份正文并计算 `+N/-N`。未来 commit/tree/blob 的枚举和内容读取使用 Git CLI。
3. 文件变更卡只组合现有 shadcn/ui copy-in 与 Lucide icon。
4. 用户附件和 artifact 消息点击继续复用当前文件 renderer，不进入文件变更卡，也不新建专用查看器。

## 7. Turn 文件采集方案

### 7.1 稳定 turnId

- 用户点击发送前即生成稳定 `turnId`，继续沿用当前 command 传入的 `promptId`。
- runtime 自动首轮也必须生成稳定 `turnId`，不能依赖 UI 是否传入。
- `user_prompt_event.raw.promptId`、通知去重、usage journal 和文件 change set 使用同一个业务 turn identity；现有 timeline event ID 可保留为事件 ID，不再兼任业务 turn ID。
- hidden output repair 继承原可见 `turnId`，不得生成第二张文件卡。

### 7.2 唯一采集来源

```text
生成 turnId
  → record user prompt event
  → session/prompt
  → 收到 ACP toolCall / toolCallUpdate 的结构化 diff
  → 按 toolCallId + eventSeq + contentIndex 持久化 mutation
  → hidden repair 继续写入同一 turnId
  → turn 终止时折叠 mutation chain
  → 原子写入 change set
  → timeline 追加 fileChangeSet 指针事件
```

change set 在当前可见 turn 的 prompt 调用及其 hidden repair 全部终止后关闭。它不读取 `git status`、不扫描目录、不监听文件系统，也不根据工具名称猜测写入；没有 ACP diff 的 execute / shell / MCP 操作不进入卡片。

### 7.3 ACP diff 归一化

ACP 已经完成 provider 差异转换，本层只消费标准结构化 diff，不增加 Claude / Codex / Gemini 等 provider adapter。每个合法 diff 至少需要：

- `path`；
- 完整 `oldText` / `newText` 中至少一个存在；
- 所属 `toolCallId`、timeline `eventSeq` 和 content item index。

sasuke 只做协议校验、路径规范化和安全边界判断，不把 raw tool name 作为采集条件。diff 缺字段或历史内容被截断时不伪造统计，记录结构化 limitation code。

### 7.4 Mutation 折叠

同一 turn 内按规范化 logical path 分组，再按 `eventSeq + contentIndex` 排序：

1. 第一条 mutation 的 `oldText` 作为本轮 tool-reported before。
2. 最后一条 mutation 的 `newText` 作为本轮 tool-reported after。
3. 相邻 mutation 校验 `previous.afterHash == next.beforeHash`；不一致时保留证据并将该文件标记为 non-linear，不静默拼接错误链。
4. before / after hash 相同表示本轮工具最终报告已恢复原内容，不进入卡片。
5. `None → blob` 为 added，`blob → blob` 为 modified，`blob → None` 为 deleted，`None → None` 不展示。
6. added / deleted / modified 的行数统计根据合成后的 tool-reported before / after 重新计算，不能累加各 tool call 的 `+/-`。

示例：

```text
Write:  不存在 → B
Edit:   B → C
卡片:   added，打开内容 C

Write:  A → B
Edit:   B → C
卡片:   modified，展示 A → C
```

这里的 `C` 是最后一次 tool call 当时返回并持久化的内容，不是用户稍后点击卡片时磁盘上的最新内容。后续 tool call、用户编辑、其他 session 或 Git 操作改变 live 文件，都不能改变已经 finalized 的卡片。

### 7.5 明确排除项

- 用户上传附件继续绑定用户消息；点击消息中的图片或文件 chip 打开右侧工作区，不写入 change set。
- canonical artifact 继续由 Agent 消息的现有特殊样式承载；点击该消息打开右侧工作区，不写入 change set。
- turn 开始时以稳定 turnId 幂等持久化一次 attempt `attachments/` 普通文件相对路径 baseline；自动重试或 hidden repair 不覆盖。turn 结束时只扫描一次同一受控目录，`after - before` 是本轮新增节点附件集合；不扫描 `artifacts/`、workspace 或其他目录，不以 mtime/watcher 推断。
- 同一次 finalize 先折叠 tool mutations，再用新增 attachment canonical path 集合对 `Added` 候选做互斥分流：命中者只进入 `attachments[]`，不得进入 `changes[]`；已有 attachment 的 `Modified` 仍作为普通 change。禁止先生成两个 UI 列表再去重。
- baseline 缺失、扫描失败或超过 `captureMaxEntries` 时失败关闭附件识别；创建后删除的路径不展示，既有文件仅修改不提升为附件。附件集合只挂到 root change set；Agent branch 中命中的同一 `Added` 也要排除，避免跨卡重复。

### 7.6 并发与 branch 归属

- mutation 由当前 ACP prompt 的 tool call 直接报告，并绑定稳定 turnId，不受共享 workspace 中其他 session 的磁盘写入影响。
- 根会话与 Agent branch 根据持久化 timeline / branch ownership 归属 change set，不根据路径或自然语言推断。
- UI 可直接使用“本轮文件变化”，无需 shared-workspace attribution 降级标记。

### 7.7 持久化与崩溃恢复

建议目录：

```text
attempt/
├── acp.timeline.jsonl
├── acp.turn-file-mutations.jsonl
├── turn-file-change-sets/
│   └── <turn-id>.json
└── acp.file-blobs/<hash-prefix>/<blake3>
```

- 每个 ACP diff 的 old / new 全文先原子写入 blake3 CAS，再把 mutation journal 持久化并 `sync_data`；幂等键由 `turnId + toolCallId + eventSeq + contentIndex + beforeHash + afterHash` 构成。
- finalized change set 使用临时文件 + rename 原子写入；重复 finalize 同一 turnId 必须幂等。
- `fileChangeSet` timeline item 只保存 summary 和 `changeSetId`，详情点击/展开时懒加载，避免会话分页携带大清单和正文。
- runtime 重建发现 mutation journal 已存在但 change set 未 finalized 时，只重放该 turn 已持久化的 mutation；不读取当前 workspace 猜测缺失内容。若 turn 已确定终止则恢复 finalize，否则等待原 prompt 生命周期继续处理。
- CAS 以 blake3 去重，写入使用原子文件；同一 attempt 的多个 turn 不重复保存相同内容。

## 8. 后端接口方案

### 8.1 查询接口

```text
get_turn_file_change_set(locator, branchId, changeSetId)
read_file_resource(locator: FileResourceLocatorVm, preferSource?)
get_file_comparison(changeSetId, changeId)
issue_file_version_preview(versionId)
```

`get_file_comparison` 返回：

```ts
interface FileComparisonVm {
  changeSetId: string;
  changeId: string;
  path: string;
  renderKind: 'text' | 'image' | 'unsupported';
  language: string | null;
  stats: { addedLines: number | null; deletedLines: number | null };
  before: WorkspaceFileSnapshotVm | null;
  after: WorkspaceFileSnapshotVm | null;
  limitationCode: string | null;
}
```

- 卡片列表不预加载 file comparison。
- text comparison 在打开 Tab 后才加载 before / after 正文。
- image comparison 返回两个短期 preview grant，不返回任意本地 URL。
- deleted 行没有点击行为，因此正常 UI 不调用 comparison；接口仍允许内部诊断读取 before version。

### 8.2 timeline 投影

新增 `AcpUiEvent.kind = "fileChangeSet"`：

- stable ID：`turn-file-change-set:{turnId}`。
- seq 位于该 turn 所有普通消息、tool、repair 和 runtime output 落盘之后。
- 一个 change set 在 branch 分页中计为一个稳定语义块。
- live update 只在 finalized 后发布一次；不在流式过程中反复改变卡片位置。
- branch projection 使用持久化 branch ownership，不根据路径或文本推断归属。

## 9. 前端实现方案

### 9.1 文件变更卡

新增组合组件 `TurnFileChangesCard`，使用现有 shadcn/ui：

- 外层：`Card`。
- 文件列表展开：`Collapsible`。
- 长列表：卡内有限高度 `ScrollArea`，不占用整个消息流。
- 行操作：`Button variant="ghost"` 与 `Tooltip`。
- 图标：Lucide `FileDiff / FilePlus2 / FileMinus2`。

布局：

```text
┌ 本轮变化 23 个文件               +361  -108 ┐
├ src/.../mod.rs                    +10    -3 ┤
├ src/.../models.rs                  +2    -0 ┤
├ docs/plan.md                       +8    -0 ┤
└ 再显示 20 个文件                         ┘
```

交互规则：

- 默认展示条数来自统一配置 `turnFileCardPreviewLimit`，不在组件内散落硬编码。
- 修改行可点击，打开或激活 `file-diff`。
- 新增行可点击，打开该 turn 的 after version 原文；文本/代码/Markdown 使用现有 CodeMirror/Markdown renderer，图片使用现有图片 renderer。
- 删除行展示路径和删除统计，但使用非交互行，不设置伪按钮、hover 或焦点。
- 普通变更卡不显示“附件 / 产物”等来源标签；其中所有条目都表示 ACP tool call 明确报告且未被分流为本轮新增节点附件的文件变化。
- 卡片作为 ACP 结构化行，不显示头像，但与 tool / thought 保持相同横向位置。
- 无文件变化的 turn 不渲染空卡片。
- partial change set 在标题区显示一个可聚焦告警 icon，Tooltip 通过 i18n 解释未捕获原因；不把内部实现说明常驻在消息流。

### 9.1.1 节点新增附件卡

- `TurnFileChangesCard` 只加载一次 change set，并直接消费后端互斥的 `attachments[]` 与 `changes[]`；前端不读取路径、不分类、不去重。附件卡排在普通变更卡之前。
- 附件卡同样使用 shadcn `Card + Collapsible + ScrollArea + Button` 与 Lucide 图标。默认展示条数来自 `appConfig.turnFiles.attachmentCardPreviewLimit`，默认 1；更多项通过“展开其余 N 个附件”在卡内展开。
- 点击附件立即打开 `turn-attachment:<changeSetId>:<attachmentId>` 右栏 Tab；面板随后调用受控 resolver，以完整 attempt locator、branch、changeSetId、attachmentId 校验 manifest membership、attachments root 与 symlink 边界，不接受前端任意绝对路径。
- resolver 签发绑定单项目、单 canonical path、读写权限和 TTL 的 external grant，并启动现有 watcher。面板复用 `FileContent` 与 `FileContentStore`；Markdown 默认渲染，可切换源码并编辑，自动保存、revision/CAS、原子写入、冲突处理、切换冲刷和关闭释放全部沿用现有契约。

### 9.2 Diff Viewer

新增 `FileDiffWorkspacePanel`：

- 本期默认使用 `@codemirror/merge` 的单栏 `unifiedMergeView`，视觉和交互对齐评审截图：删除行在上、插入行在下、上下文行保持单列连续阅读。
- 不根据右栏宽度自动切换 side-by-side，避免用户阅读位置跳变；双栏 `MergeView` 只作为后续可选视图模式预留，不进入本期默认入口。
- 配置 `EditorState.readOnly`、`EditorView.editable.of(false)` 与 `mergeControls: false`；本期无 accept / reject / apply / revert controls。
- 复用 `WorkspaceFileEditor` 的语言识别、字体、主题 token、搜索和行号 extension 工厂，不复制一套 CodeMirror 主题。
- 开启 `highlightChanges`、diff gutter 和 `collapseUnchanged`；长段未修改内容折叠为类似“55 行未修改”的整行浅色圆角入口，点击或键盘操作后展开。
- 顶部标题固定为“本轮修改 Diff”，表示当前 Prompt Turn 的首个 oldText 与最后 newText 之间的比较；上一处/下一处变化按钮只在官方 changed chunk 至少为两处时显示。导航只改变 CodeMirror selection / scroll，不重建编辑器。
- 大文本超过配置上限时不挂载完整 merge view，返回 limitation code 并提供“分别查看前后版本”的只读入口。

视觉结构：

```text
┌ 本轮修改 Diff                       +26  -10 ┐
├ M  docs/.../方案.md                +26  -10 ┤
├ 55 行未修改                                  ┤
│ 58  上下文行                                  │
│ 59- 删除内容                                  │
│ 59+ 新增内容                                  │
│ 60  上下文行                                  │
├ 131 行未修改                                 ┤
│ ...                                           │
└───────────────────────────────────────────────┘
```

- 顶部比较工具栏和当前文件栏使用 sticky 布局；滚动正文时仍能看到版本语义、文件名和 `+N/-N`。
- “本轮修改 Diff”比较第一条 tool diff 持久化的 old version 与最后一条 tool diff 持久化的 new version；不能使用“当前文件”等会让用户误以为读取 live workspace 的文案。
- 文件状态 `A / M / D`、增删统计和折叠入口使用语义 token；浅色/深色主题均避免大面积高饱和红绿背景，只强调行 gutter、行号和实际变化文本。
- 折叠入口、版本选择和变化导航必须可聚焦，并提供中文/英文 aria-label；不能只依赖红绿区分状态。
- 本期一个 `file-diff` Tab 只挂载当前文件的一份 CodeMirror 实例。变更卡中的文件行负责切换/打开对应文件，不在一个长页面中为每个文件重复挂载编辑器。

图片与二进制：

- 新增图片直接使用现有图片预览。
- 修改图片第一版可使用前后并排预览；若视觉验收要求拖动比较，再引入 `react-compare-slider`。
- 不支持二进制 diff 时仍可打开 `file-diff` 展示名称、MIME、before / after 大小与 hash；前端根据 `turn-files.binary-diff-unsupported` 显示文案。

### 9.3 用户消息附件

- 保留用户气泡中的图片缩略图和文件 chip，因为它们表达本轮输入，不属于旧的 session 聚合展开栏。
- 点击缩略图/chip 不再打开 `MessageImagePreviewDialog` 或 `ACPArtifactsDialog`，统一在右侧工作区打开对应 conversation version。
- 处理中即可打开已 materialize 的用户附件；它不会出现在 change set 卡片中。

### 9.4 Artifact 消息

- 保留 Agent 消息中现有 artifact 特殊样式和消息归属。
- artifact 点击后统一在右侧工作区打开，不加入 `TurnFileChangesCard`，也不根据 artifact 目录生成文件变化。

### 9.5 破坏式替换旧路径

Conversation / ACP 主路径完成后删除：

- composer 上方 `AcpSessionAssetsPanel`。
- `ACPArtifactsDialog` 在会话详情中的调用与状态。
- 仅为 composer 上方 session 聚合栏服务的 `allArtifacts / allAttachments / artifacts / attachments` prop 传递；消息内 artifact 和用户附件所需数据必须保留。
- `ConversationRunVm` 与 `ConversationSessionSwitchVm` 中仅为旧聚合栏服务的 asset payload。
- 已无消费方的 `ConversationAssetsBar`、`acp-session-assets-panel.ts` 和对应 i18n key / test。

保留：

- Round 详情和节点排障页使用的 `AssetItemVm`、`showArtifact`、`showAttachment`。
- 用户 composer 的附件选择、校验与发送能力。
- 用户消息附件和 Agent artifact 的消息内 renderer、归属信息与右栏打开入口。
- 现有工作区 live 文件查看和编辑能力。

不增加旧栏 fallback、双写展示或灰度开关。

## 10. 配置、性能与安全

所有阈值进入 `WorkspaceFileConfig` 或独立 `TurnFileCaptureConfig`，至少包括：

- `cardPreviewLimit`
- `attachmentCardPreviewLimit`
- `captureMaxEntries`
- `captureMaxFileBytes`
- `captureMaxTotalBytes`
- `diffTextMaxBytes`
- `diffTextMaxLines`
- `blobCacheMaxBytes`
- `blobRetentionPolicy`

性能规则：

- 会话快照只带 change set summary，不带完整 change list 和正文。
- 卡片展开时拉 change list，打开 Tab 时再拉正文。
- tool diff 到达时只对其携带的 old / new 正文做 CAS 去重和 mutation journal 追加，不访问工作区、不执行 Git。附件 baseline/finalize 只枚举 attempt `attachments/`，遍历总 entry 数受 `captureMaxEntries` 约束，跳过隐藏项、symlink 与非普通文件；不扫描仓库。
- turn finalize 复用一次 attachment delta，折叠当前 turn mutation 后做 O(changes + attachments) canonical-path 分类，并使用 `similar` 计算普通变化的 `+N/-N`；成本与有界附件条目、tool diff 数量和正文大小相关，而不是与仓库大小相关。不新增 watcher 轮询、缓存、队列或第二份 canonical state。
- 文件列表使用稳定 change ID；展开/收起不重建会话全部 Markdown 或 CodeMirror。

安全规则：

- 前端不能提交任意 blob path 或 runtime absolute path，只提交后端签发的 changeSetId / versionId。
- resolver 必须校验 project、attempt、branch ownership 和 version 所属关系。
- CAS 路径由 hash 生成，不拼接用户文件名。
- 图片继续通过短期 preview grant；禁止 WebView 直接加载本地路径。
- symlink、路径穿越、Windows 大小写与 UNC 路径继续复用 workspace file 领域的权威校验。

## 11. 错误模型与 i18n

后端只返回结构化 code + params，不返回对客文案：

| code | 含义 |
| --- | --- |
| `turn-files.change-set-not-found` | change set 不存在或不属于 locator |
| `turn-files.version-not-found` | 不可变版本不存在 |
| `turn-files.version-access-denied` | project / branch / attempt 归属校验失败 |
| `turn-files.capture-partial` | 捕获因上限、截断、崩溃或 mutation 链不连续而降级 |
| `turn-files.capture-limit-exceeded` | 单文件、总量或条目数超限 |
| `turn-files.invalid-tool-diff` | ACP diff 缺少路径或完整前后文本，不能形成可信版本 |
| `turn-files.non-linear-mutation` | 同一路径相邻 mutation 的 after / before hash 不连续 |
| `turn-files.diff-too-large` | 文本 diff 超出渲染阈值 |
| `turn-files.binary-diff-unsupported` | 当前格式不支持内容差异 |
| `turn-files.blob-corrupted` | CAS blob hash 校验失败 |

中英文文案统一维护在前端 i18n；若实现中需要新增 runtime prompt 上下文或 repair 指令，必须同步放入 `src/prompts/zh-CN/...` 与 `src/prompts/en/...`，不能在代码中硬编码长 prompt。本方案预计不需要修改提示词。

## 12. 后续 Git 管理兼容设计

本期就把“文件比较”定义为两个版本端点，而不是“当前文件 + 一段 patch”：

```rust
enum VersionEndpoint {
    CapturedVersion { version_id: String },
    // 后续新增
    GitBlob { repository_id: String, oid: String },
    GitTreePath { repository_id: String, tree_oid: String, path: String },
}
```

未来 Git 功能可增加：

```text
list_git_changes(baseEndpoint, targetEndpoint)
get_git_file_comparison(baseEndpoint, targetEndpoint, path)
```

这样可以覆盖：

- commit A 与 commit B。
- merge-base 与目标 commit。
- 某组 commit 解析后的首尾 tree。
- commit 与当前 worktree。
- 两个 blob。

前端仍消费 `FileChange[]` 和 `FileComparisonVm`，继续使用同一文件卡与 CodeMirror merge viewer。`react-diff-view` 这类“只消费 unified patch 文本”的组件不作为主选，正是为了避免未来多端点时重新设计 UI 数据模型。

## 13. 实施阶段

### Phase 1：领域与持久化（2～3 日）

1. 固化统一 turnId。
2. 新增 change set、version ref、CAS 和 journal。
3. 从标准 ACP tool content 提取 diff，完成幂等 mutation 写入和同路径链折叠。
4. 完成取消、失败、hidden repair、截断输入和崩溃恢复。

### Phase 2：接口与 timeline（2 日）

1. `fileChangeSet` timeline pointer event。
2. change set lazy query、file version read、comparison API。
3. generalized file locator 和权限校验。
4. branch 分页、live event 和 Browser API mock。

### Phase 3：右栏与卡片（2～3 日）

1. 安装官方 `@codemirror/merge`，抽取共享 editor extension/theme factory。
2. 新增 `file-diff` resource 和截图风格的只读 unified Diff Viewer。
3. 使用 shadcn/ui copy-in 组合 `TurnFileChangesCard`。
4. 用户消息附件和 artifact 消息点击统一打开右栏，但不进入卡片。

### Phase 4：删除旧路径与验收（1～2 日）

1. 删除 Conversation / ACP 旧资产展开栏、dialog 和 props。
2. 补齐 Rust、TypeScript、组件、分页和安全测试。
3. 启动真实前端，通过 deep link 验证浅色/深色、宽/窄右栏、根会话/Agent branch、长列表和图片。
4. 清理测试会话与浏览器资源。
5. 同步维护 `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md`、`workspace-files.md`、`shell.md` 与本开发计划的实施状态。

## 14. 接口级测试与验收矩阵

### 14.1 Rust 单元/集成测试

- 标准 ACP diff 的新增、修改、删除分别生成正确 before / after。
- 同一 tool call 多文件、同一文件多个 tool call、同一 tool call 多次 update 均按稳定键幂等持久化。
- 同一路径多次修改按 mutation chain 折叠；`Write A→B` 后 `Edit B→C` 必须持久化并展示 `A→C`，不读取磁盘当前内容。
- 新文件 `Write None→B` 后 `Edit B→C` 必须保持 added，点击打开已捕获版本 C。
- 工具最终报告恢复首个 old hash 时不进入卡片。
- mutation 链 hash 不连续时返回 `turn-files.non-linear-mutation`，不伪造连续链。
- execute / shell / MCP 没有 ACP diff 时不生成文件记录；工作区磁盘变化不被扫描进来。
- 用户附件和 canonical artifact 不进入 change set；artifact 消息和用户消息归属保持不变。
- 文件名含空格、中文、换行和 Windows 大小写差异。
- hidden repair 合并到原 turn；取消/失败仍结算。
- mutation journal 崩溃恢复幂等，不重复生成 change set。
- 大文件、二进制、总量和条目上限返回结构化 limitation code。
- versionId 路径穿越、跨 project、跨 attempt、跨 branch 访问被拒绝。
- 历史 version 在 live workspace 后续变化/删除后仍可读取。

### 14.2 前端接口/组件测试

- change set timeline item 固定跟在所属 turn 最后，并计为一个语义分页块。
- 无变化不显示卡片；partial 显示告警 Tooltip。
- 修改点击打开 diff；新增点击打开历史原文；删除没有 click / focus / hover affordance。
- 相同路径不同 turn 使用不同历史 Tab，内容不串。
- 宽/窄右栏都保持 unified，调整可视上下文宽度但不自动切换双栏。
- CodeMirror unified viewer 只读，语言高亮、主题、搜索、未修改区折叠和 chunk 导航可用。
- 删除行在上、插入行在下；折叠入口显示未修改行数并支持鼠标与键盘展开。
- 新增图片使用 preview grant；二进制 diff 使用 limitation UI。
- 用户消息图片和文本附件点击打开右栏，不再打开旧 modal。
- 根会话不显示 Agent branch change set，Agent branch 不显示 sibling change set。
- 删除旧 `AcpSessionAssetsPanel / ACPArtifactsDialog` 后 composer 上方不再出现聚合栏。

### 14.3 真实 UI 验收

| 场景 | 操作 | 预期 |
| --- | --- | --- |
| 单轮混合变化 | 同一 turn 的 tool call 报告新增、修改、删除多个文件 | turn 末尾只有一张汇总卡，统计与列表一致 |
| 历史稳定性 | 下一 turn 再次修改同一文件，回点上一 turn | 上一 turn 仍显示原 before / after |
| 输入附件 | 发送图片和文本附件 | 只从用户消息入口在右栏打开，不进入 turn 卡 |
| Artifact | Agent 返回 artifact | 保持消息内特殊样式并可在右栏打开，不进入 turn 卡 |
| 无 diff 工具 | Shell 修改磁盘但 ACP 没有报告 diff | 不进入 turn 卡，不扫描工作区补录 |
| 窄窗口 | 收窄右栏或进入 Sheet | 继续保持 unified，工具栏和文件名自适应，不出现水平页面滚动 |
| 长列表 | 变更超过默认预览数 | 默认紧凑，展开后卡内滚动，不抢占整个会话滚动 |
| Agent branch | 根会话与子 Agent 各改文件 | 各 branch 只展示自己的 change set |
| 失败/停止 | 工具已改文件后失败或用户停止 | 已发生的净变化仍在 turn 末尾显示 |
| 无变化 | 纯问答 turn | 不显示空文件卡 |

## 15. 风险与处理

| 风险 | 错误做法 | 方案 |
| --- | --- | --- |
| 旧 turn 读取 live path | 后续修改使历史 diff 变化 | before / after 物化为不可变 CAS version |
| 扫描 workspace 推断本轮变化 | 把用户或并发 session 的写入算入当前 turn | 只消费绑定当前 turnId 的 ACP tool diff |
| 累加每次 tool diff 的行数 | 文件恢复原状仍显示变化，统计失真 | 折叠为最早 tool-reported before / 最后 tool-reported after，再用 `similar` 计算净统计 |
| mutation 更新重复到达 | 同一个 tool call 重复计数 | 使用稳定幂等键并按 tool update revision 合并 |
| 在 timeline 内嵌正文 | 会话分页和 live refresh 变重 | timeline 只存 summary + changeSetId，详情懒加载 |
| 把用户附件或 artifact 塞进 diff 卡 | 破坏消息归属并产生重复入口 | 保持消息内展示，只统一右栏查看器 |
| 为 Git 未来选择 patch-only UI | commit/tree/blob 接入时重写前端 | 使用版本端点 + CodeMirror before/after viewer |
| 保留旧聚合栏 fallback | 两个入口状态和数量不一致 | 开发阶段破坏式删除旧消费路径 |

## 16. 一句话结论

> 用户上传附件和 artifact 保持消息归属并统一在右栏查看；同一 turn change set 一次性、互斥地持久化本轮新增节点附件与普通 ACP tool diff。普通变化以不可变 before / after 版本和 CodeMirror merge 展示，节点附件以受控 live editable 文件展示；后续 Git commit diff 只需增加新的版本 provider。

## 17. 实施结果（2026-08-04）

### 17.1 已落地能力

- Rust `src/acp/turn_files.rs` 实现标准 ACP diff 提取、BLAKE3 CAS、durable mutation journal、同路径 mutation chain 折叠、`similar` 净行数统计、节点附件 baseline/delta 与一次 finalize 互斥分类、partial limitation 和 finalized change set。
- prompt 生命周期使用稳定 `turnId/promptId`；根会话与 Agent branch 按事件 `_meta.conversation.branchId` 独立采集。完成、失败与取消路径都会结算；hidden repair 复用最近可见 turn identity，并以稳定 change set/event ID 更新同一张卡。
- timeline 使用轻量 `fileChangeSet` 指针事件，只保存 summary、attachmentCount、changeSetId 和 limitation codes；完整互斥列表和版本正文分别通过 `get_turn_file_change_set`、`get_file_comparison` 懒加载。
- 查询 command 只接受 attempt locator、branchId、changeSetId/changeId，校验 branch ownership、标识符与 CAS hash；不会接受 blob path 或 live workspace path。
- `resolve_turn_attachment_file` 只接受完整 attempt locator、branchId、changeSetId 与 attachmentId；后端从 finalized manifest 解析相对路径，校验 canonical attachments root 和 symlink 边界后签发精确外部读写 grant，不接受前端附件绝对路径。
- 右侧工作区包含 `file-diff`、`file-version`、`conversation-asset` 与 `turn-attachment` 资源。文本 diff 使用 `@codemirror/merge@6.12.2` 的只读 unified 模式；节点附件通过受控 manifest resolver 复用可编辑普通文件内容组件。
- `TurnFileChangesCard` 对同一份 change set 先渲染新增附件卡、再渲染普通变化卡，不做后置去重。附件默认预览数来自 `attachmentCardPreviewLimit=1`；普通长列表来自 `cardPreviewLimit`。修改打开 diff、普通新增打开捕获原文、删除为不可聚焦非交互行，附件打开可编辑右栏资源。
- 用户消息图片/文本附件与 canonical artifact 保持消息内入口，点击后打开右侧工作区；它们不进入 change set。旧 `AcpSessionAssetsPanel`、`ACPArtifactsDialog`、`MessageImagePreviewDialog`、会话聚合 DTO 字段及其专用 view-model/pref/test 已删除。
- `configs/app-config.toml [turnFiles]` 统一配置卡片预览、捕获条目/字节和 diff 渲染上限。CAS blob 不启用额外内存 cache（`blobCacheMaxBytes = 0`），生命周期跟随 attempt（`blobRetentionPolicy = "attempt"`）。

### 17.2 实际数据与接口边界

- ACP 标准 diff 的 `oldText/newText` 可能是完整内容，也可能只是一次 Edit 的上下文 hunk。同一 tool call 的流式 update 必须取最后 revision，不能将不同上下文范围误串成两次编辑；若后续 update 错把 unified diff 的 `No newline at end of file` 元数据混进文本字段，捕获层按 diff 元数据处理并恢复真实末尾换行，不得写入版本正文。用户附件中的图片继续使用现有安全图片查看链路，ACP diff 的二进制/image endpoint 留给后续协议或 Git provider。
- Conversation 主 DTO 已删除旧 session 聚合 `artifacts/attachments`；Round/节点排障 DTO、`AssetItemVm` 和按名读取 command 保留。
- 普通 live workspace 文件仍使用原 `file` 资源与自动保存；历史 captured version 一律只读，二者不共享内容状态。
- 后续 Git 管理继续以 `FileChange/FileComparison` 为前端契约，新增 Git 版本 provider 即可复用卡片和 unified viewer；本期未调用 Git CLI，也未增加 commit UI。

### 17.3 自动化验证

- `cargo test -p sasuke acp::turn_files --no-fail-fast`：13 项通过，覆盖标准 diff、Write+Edit 折叠、同一 tool call 后续 revision 替换、`No newline at end of file` 元数据清理、schema v2 change set 从 journal 修复、旧 change set 重建、幂等、non-linear、恢复原状、磁盘后续变化不影响历史、路径穿越和条目上限。
- `cargo test -p sasuke hidden_repair_reuses_the_latest_visible_prompt_turn_identity --no-fail-fast`：通过。
- `cargo test -p sasuke-desktop --no-fail-fast`：223 项通过，包含持久化重载后的卡片排序修复和 conversation DTO 契约回归。
- `cargo check -p sasuke-desktop`：通过；仅保留项目既有 warning。
- `npm run web:build`：通过。
- `npm run web:test`：121 个测试文件、804 项测试全量通过；新增覆盖 timeline 卡片顺序、A/M/D 点击语义、删除项无焦点、Agent branch locator、artifact 右栏入口、桌面 command 参数、unified viewer 只读契约、长文件列表共用滚动区和新增文本无底部横线。

### 17.4 浏览器实机验收

- browser preview 增加一组标准 `fileChangeSet` 样例，只复用正式 timeline、查询接口和右侧工作区消费链路，不增加独立展示逻辑。
- 已从会话 deep link 验证：文件卡位于本轮工具活动之后；新增文件打开捕获原文；修改文件打开只读 unified diff，并提供上一处/下一处变化；删除文件不可点击；旧会话顶部附件/产物聚合栏不再出现。

## 18. 真实会话回归修正（2026-08-04）

- 使用 `task-159/run-001/direct-agent/attempt-001` 的真实 timeline、raw frame、mutation journal 和 finalized change set 复核。确认 CAS/change set 已持久化，重启后卡片位置变化源于 pointer 的 `startedSeq` 错绑 prompt 起点；生产端改为卡片终态 seq，读取端同步修复既有错误位置。
- 同一 Edit tool call 在 ACP raw frame 中先后返回不同上下文范围的标准 diff；聚合规则改为先按 `toolCallId + path` 取最后 event revision，再跨工具调用折叠。change set 增加 schema version，旧版本在首次查询时从 durable journal 重建。
- `test2.md` 的 Write 返回了标准 diff，但随后 `Bash rm` 只返回完成状态，没有 diff。继续坚持 tool diff 唯一事实源：不解析 shell 命令、不按工具名猜测、不读取 live 磁盘，因此该删除无法由 sasuke 补推断；provider 若上报标准删除 diff，创建后删除会按净零变化移出卡片。
- 卡片标题调整为“本轮变更 N 个文件”并移除标题告警图标；展开态改为全量文件共用一个 ScrollArea；CodeMirror 新增文字移除默认底部渐变横线。
- UI Stop、provider cancel 和 prompt failure 均调用 turn finalize；本次真实 cancelled session 也已落盘终态 change set。应用进程被强制杀死不等同于用户 Stop，不承诺补写未完成的 pointer。
- 使用 `task-165/run-001/direct-agent/attempt-001` 复核换诗 Edit：同一 tool call 的早期 update 提供正确 `oldText/newText`，后续 update 将 structured patch 的 `No newline at end of file` 标记混入文本。change set schema 提升到 v3，新捕获在写入 CAS 前清理该元数据，schema v2 数据在首次查询时从 mutation journal 重建并生成干净版本；CodeMirror 主题选择器同时修正为命中编辑器根节点的 `&.cm-merge-b`，显式关闭新增片段的默认渐变 background image。

## 19. Diff Viewer 响应式与选中态修正（2026-08-04）

- 根因属于统一 viewer 策略实现不完整：普通文件编辑器已经使用 `EditorView.lineWrapping`，历史 diff viewer 未装载该扩展，CodeMirror 内容最小宽度会被长行撑开；现统一约束 panel、React CodeMirror 容器、editor 和 scroller 的最大宽度，并在 diff/version viewer 启用官方换行能力。
- 深色模式选区不清晰源于 CodeMirror `drawSelection` 把选区画在文本 decoration 下层，merge 的 changed-text 背景会覆盖它。历史只读 diff/version viewer 关闭自绘 selection，直接复用应用原生 `--text-selection` / `--text-selection-foreground`；普通 CodeMirror 自绘选区的背景也改用同一 token，不新增主题硬编码。
- 前端契约测试固定 `lineWrapping`、100% 容器宽度、隐藏横向溢出、只读 viewer 原生 selection，以及 CodeMirror selection token 复用，防止后续扩展重组时回归。

## 20. 变更文件悬浮 Diff 与统一行级强调（2026-08-16）

- 文件变更行增加 shadcn/ui copy-in `HoverCard`：鼠标稳定悬浮 350ms 后打开，离开宽限 150ms；键盘聚焦立即打开。预览收紧为最大 40rem × 24rem、高度约束 44vh 和 36px 标题区，继续根据 viewport 保持安全边界，CodeMirror 正文独立滚动。新增/修改行原点击继续打开右侧工作区，并同步收起当前预览；点击后持续抑制迟到 open 请求，焦点转移、布局位移和合成 pointer 事件不能提前解锁。点击时保存 `clientX/clientY`，只有后续 `pointermove` 的实际客户区坐标发生变化时才恢复悬浮，不使用布局重排时可能失真的 `movementX/movementY`。删除行仍无点击导航，只提供悬浮/聚焦预览。
- 从 `TurnFileWorkspacePanel` 抽取共享 `ReadonlyUnifiedDiff`，悬浮预览与 Turn/Git/Commit/PR 右侧 Diff 统一复用 CodeMirror merge 配置、语言、主题、换行、未修改区折叠和扫描预算，不复制第二套 viewer。
- 新增按完整 attempt/branch/change set/change identity 建 key 的 comparison loader，最多保留最近 2 项并合并相同 in-flight 请求。悬浮后立即点击不会重复读取 before/after；缓存淘汰后的迟到请求不能覆盖新请求。列表加载、收起/展开和普通消息流不读取正文，也不挂载 CodeMirror。
- 统一 Diff 视觉保留 CodeMirror 官方行级与字符级 Diff：新增/删除行维持 10% 语义背景，`.cm-deletedText` / `.cm-changedText` 字符变化层通过共享主题统一降为 12% 语义背景并移除默认 background image。按最终产品决定回滚 merge active line 透明覆盖、单光标 chunk 导航和 `highlightChanges: false`；普通 active line、官方 chunk range 导航与用户主动文本选择均保持原交互。
- 回归范围：Hover Card trigger、键盘 loading、原点击导航、删除项无点击、完整 locator 隔离、并发请求去重、缓存上限、共享 viewer 只读契约、行级背景保留且行内强调降低；执行 TypeScript、前端生产构建，并在内置浏览器验证正常/窄窗口及浅色/深色主题。
- 针对首次打开右栏时仍出现的瞬时闪回，增加可选诊断开关 `sasuke.debug.turnFileHover`。启用后用统一前缀记录行 mount/unmount、HoverCard open request、受控 state commit、Radix Content `data-state`、focus/blur、click、workspace dispatch 和抑制期 pointer 事件；日志为单行 JSON，不包含文件路径、Diff 内容或完整 locator，问题定位后删除诊断探针。
- 用户日志确认闪回发生在导航之前：Radix 在 pointer down 请求关闭后，按钮取得 focus，业务 `onFocus` 误把该鼠标焦点当作键盘聚焦再次请求打开；重新挂载 CodeMirror 产生约 859ms 主线程长任务，导致真正 click 和第二次关闭被显著推迟。修复为行内输入模态 ref：pointer down 到 pointer up/cancel 期间屏蔽 focus 带来的 open request，Tab 等纯键盘聚焦仍立即预览；不新增全局监听、延时或布局状态。
- 最终验收：定向 Vitest 2 个文件 23 项通过，TypeScript 与 Vite 生产构建通过，`git diff --check` 通过；内置浏览器从“右栏尚未出现”的初始态验证，悬浮后第一次点击可立即打开右侧工作区，等待 1.2 秒不重新弹出，真实移走并重新进入后才恢复预览；回归测试额外固定“`movementX/movementY` 非零但 `clientX/clientY` 未变”的布局合成事件不能解除抑制，并固定 pointer down 带来的 focus 不得重新打开预览。紧凑尺寸在 1280×720 正常窗口和 720×700 窄窗口下均位于 viewport 内，窄窗口实测 640×308、标题 36px，无页面横向溢出，控制台无 warning/error。

## 21. 变更卡消息流间距收口（2026-08-16）

- `TurnFileChangesCard` 复用 Tailwind 标准 `mb-3` 间距，为完整变更结果与下一条消息保留额外 12px 底部呼吸空间；不修改卡片内部行高、折叠状态或消息列表全局节奏。
- DOM 契约测试固定变更卡的底部间距类，避免后续样式整理重新造成卡片与下一条消息紧贴。
- 本次只增加静态 CSS utility，不新增组件、状态、依赖、数据加载或渲染订阅，性能影响可忽略。
## 追加实施：Composer 图片预览与图片看板手势

- [x] ACP composer 底部操作收敛为两行：附件/键盘提示为辅助行；模型、思考强度、权限、继续工作流和发送为同一 command bar。
- [x] 快速对话和会话追问新增 `draft-attachment` 右侧工作区资源，输入图片点击不再打开遮罩 Dialog；草稿附件移除、清空或提交时同步关闭对应 Tab。
- [x] 统一 composer 上下文区与正文的水平内容 inset：快速对话使用 `px-0` 对齐命令标签和正文，会话追问使用 `px-3` 对齐 prompt-kit textarea；共享组件不再硬编码 `px-1`。
- [x] 系统文件选择路径通过短期 `sasuke-preview` grant 提供图片预览，不把本地路径直接加载进 WebView；元数据和 revision 计算在 blocking pool 执行。
- [x] 消息图片和草稿图片共用 `WorkspaceImageCanvas`，采用 `react-zoom-pan-pinch`；图片双轴居中并使用语义纯色背景，普通滚轮/双指滚动不缩放，只有 `Ctrl + 滚轮`、浏览器映射为 Ctrl-wheel 的原生触摸板 pinch、触摸屏 pinch 和 shadcn 工具栏按钮缩放，鼠标拖拽负责平移；“适应窗口”恢复 CSS 视口约束下的完整图片尺寸。
- [x] 修复 Windows 原始 wheel delta 导致一步跳到 10%/800% 并在上限附近横跳：禁用库内 wheel step 算法，在不受第三方组件内部 ref 覆盖影响的自有 viewport 上增加原生非 passive Ctrl-wheel 适配器，统一 `deltaMode`、限制单事件增量并使用乘法缩放曲线；以指针为缩放中心，每动画帧最多提交一次 transform。触摸屏 pinch 与拖拽仍由成熟组件处理。
- [x] 修复消息图片工作区承载层缺少 flex 导致看板被压缩到内容高度：标题栏以下保持连续的 `min-h-0 flex-1 flex-col` 尺寸链，手势命中区与垂直居中均以完整可用视口计算。
- [x] 修复 Windows 精确式触摸板 pinch 在 DOM 前被 WebView2 丢弃：桌面窗口创建前开启 WRY `zoom_hotkeys_enabled` / WebView2 `IsPinchZoomEnabled`；应用根节点安装 capture、non-passive 页面缩放 guard，只 `preventDefault` 而不停止传播，使图片 viewport 能消费 pinch 产生的 Ctrl-wheel，同时禁止图片外捏合与 `Ctrl +/-/0` 改变整个 EXE 页面倍率。
- [x] 根据实机手感将 pinch/Ctrl-wheel 指数灵敏度由 `0.002` 调整为 `0.003`（相较上一版提升 50%，相较初版 `0.0015` 提升 100%）；保留 `120px` 单事件限幅，极端输入从 100% 单次最多到约 143%/70%。每帧提交时先同步权威 transform ref，再调用缩放组件，保证连续同位置手势不因第三方回调滞后而复用旧状态、出现停滞。
- [x] 引用提交改为结构化 `ConversationPromptInput { displayText, quotes }`：前端创建消息引用时限定单个已完成 Agent 正文 DOM；后端只校验最多 64 条、唯一且有界的 ID/来源键和 12,000 字符总上限，不读取 timeline 或验证来源存在，再构造 Agent 完整 prompt。canonical/optimistic 用户事件只以 `displayText` 作为气泡正文，并把引用元数据写入 `raw.quotes`。Direct 队列版本升级并保留结构化引用，队列 ViewModel 显示引用计数，编辑正文不再丢失引用。
- [x] 用户消息新增 shadcn Popover “n 条引用”入口；弹层最大高度同时受 24rem 与视口余量约束，标题固定，只有多引用列表内部滚动。展示层只认显式元数据，不解析 `>` 或其他 Markdown，因此用户原生 blockquote 不会误判。新增前端组件/数据契约、canonical event、后端校验与队列持久化单测；发送失败时 keyed 草稿只在输入区仍为空时整体恢复。
- [x] 增加 composer command bar 与 workspace resource key 回归测试；执行前端类型检查、生产构建、Rust `cargo check` 和目标单元测试。

性能结论：图片手势库随右侧图片面板 lazy chunk 加载，不进入不可见 Tab 的首屏路径；Ctrl-wheel 连续输入只保留一个待应用 transform 并按 RAF 合帧，缩放百分比通过局部 ref 更新，不提交 React state。应用级缩放 guard 只有两个稳定监听，每次事件执行 O(1) 条件判断；图片事件计算同样为 O(1)，不扫描 DOM、不缓存历史输入，也不扩大组件订阅范围。引用最多 64 条、总正文最多 12,000 字符，随单条消息一次序列化，详情只在 Popover 打开后渲染；后端校验只线性遍历本次有界引用，不读取、解析或排序 timeline，不增加常驻缓存、轮询或历史 I/O。桌面文件选择只对用户主动选择的至多 10 个附件做一次有界元数据/revision 读取，不引入全量目录扫描、轮询、无界缓存或 N+1 网络请求。
