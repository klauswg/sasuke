# 会话优化

## 0. 当前实现状态

当前 ACP 会话链路已经能工作，并已从轮询刷新切到事件级 push；后续重点是继续压实 timeline / snapshot 作为会话主路径，降低历史读取和检索成本。

当前 sasuke 现状：

- 会话主数据文件为：`acp.session.json`、`acp.events.jsonl`、`acp.raw.jsonl`、`acp.diagnostics.jsonl`。
- `acp.raw.jsonl` 记录完整 ACP inbound / outbound raw frame，见 `src/acp/events.rs:48-52`、`src/acp/client.rs:303`、`src/acp/client.rs:931`。
- `acp.events.jsonl` 当前仍以逐帧归一化 event 为主，`normalize_session_update` 会把整份 `update.clone()` 塞进 `event.raw`，见 `src/acp/events.rs:165-199`。
- 会话详情读取时，后端会再次扫描 `acp.events.jsonl`，把连续 text / thought delta merge 成窗口，再计算 usage、permission 和 session elapsed，见 `src-tauri/src/view_models.rs:3160-3329`、`src-tauri/src/view_models.rs:3595-3616`。
- 前端打开 ACP 会话后常驻订阅 Tauri `sasuke://acp-session-updated`；活跃会话每个后端聚合 timeline item 以单事件 payload 推送到当前窗口，完整 session 快照只用于首次打开、历史查询和命令完成后的最终同步，不再用轮询或每帧重建完整 session 批量刷新。父级 conversation run 订阅通过统一 update plan 分类事件：当前已选中 session 的普通增量事件只进入 ACPChatDialog 局部合并；已存在于 session tree 的后台 session 普通 live event 不触发完整 run refresh；后台非终态 session snapshot 只做轻量运行态 patch；新 session 缺锚点、terminal snapshot、权限/暂停/等待输入等交互态仍必须刷新 run VM，让 activeSessions 与 lifecycle 收敛。前端 text/thought live 合并必须以后端聚合 timeline item 为事实，保证同一 stream 不被旧短帧、空帧或乱序 session hydrate 覆盖；interaction quiet window 只延迟普通 trailing stream flush，边界事件和 `sync` flush 必须先提交 pending text/thought，再进入后续 tool、permission、lifecycle 或 session 状态。
- 会话式运行页在 run 已 running 但首个 attempt 尚未出现在 session tree 前展示 `Agent 调起中`；attempt 已创建但尚无可见 timeline item 时展示 `处理中...`，首个 thought 到达后切换为 `思考中...`。ACPChatDialog 必须消费当前选中 attempt 的 runtime status：选中 attempt 运行中时锁定输入、显示底部运行状态和停止按钮；选中 attempt 已结束时恢复正常追问输入且不显示停止按钮。运行状态标签使用轻量 CSS 圆环旋转，避免 SVG spinner 在高频状态刷新时卡顿。
- run 启动路径已调整为在 `prepare_run` 阶段同步写入首个 `node.json`，保证首个 `round/node/attempt` 在 agent/provider 启动前就能作为稳定 session key 锚点被 conversation VM 读取；无显式 `selectedSessionKey` 时，后端默认选择 current attempt，其次选择 active/running attempt，再回退到最近历史 session。前端仍保留 `activeSessions` 临时兜底，避免极短竞态下首屏缺少运行态、停止按钮和输入锁定。
- 前端完整 `ConversationRunVm` 快照已收口到 `mergeConversationRunSnapshot`：创建、初始加载、live refresh、workflow save、stop refresh、continue refresh、rerun 都经同一入口合并；局部标题编辑、删除置空、手动 session switch 只做局部 patch。合并规则禁止同 run 内的 `unknown` ACP 快照覆盖当前 running/cancelling selected leaf，并保留当前 selected key。`selectedSessionKey` 与 `selectedSession / artifacts / attachments` 必须作为原子一致的 session payload 处理：当 live refresh 或手动切换返回的 payload 不属于最终选中的 key 时，必须丢弃旧 payload，等待当前 key 的切换结果或下一次按 key 拉取，防止不同 attempt 的 timeline 被合并到同一个 ACPChatDialog 窗口。手动 session switch 已先更新 selected key 再异步回填目标 session，因此 ACPChatDialog 在首次补拉目标 session 完成前必须显示 loading shell；补拉完成后仍没有 session/live shell 时才展示 missing ACP session 错误，避免把正常切换竞态误报成 ACP 会话失败。
- ACP 会话壳层新增 `interrupted` 终态：当后端 lifecycle 已为 `paused + process-interrupted`，且当前 attempt 没有 `sessionId`、可展示 metadata 或 timeline item 时，判定为 ACP 初始化从未成功。该状态优先于 initial loading，立即展示无图标单行文案“会话发起中断，请重跑该任务”，停止 readiness 补拉并隐藏 composer；已经建立过 ACP session 的 `process-interrupted` 仍沿用既有可继续会话语义。
- 新 UI 的暂停态分为两类：`waiting-for-user-input` 继续显示 continue 按钮；`process-interrupted`（用户 stop 后）在 ACP session 非 active 后恢复输入框，但发送不会走普通 ACP follow-up，而是仍走原来的 runtime continue 链路，只把默认“继续”替换成用户输入文本。若 run 已暂停但 ACP session 仍 active/cancelling，则 composer 必须继续显示 stopping 并锁定输入。当前状态解释已收口为后端 leaf `lifecycle` 与前端纯 `deriveAcpRuntimeComposerState`：session tree、activeSessions、placeholder、输入禁用、停止按钮、submit target 都从同一套派生结果读取，不再在页面和组件里重复拼接 `runtimeStatus/sessionStatus/pauseReason`。
- Round 详情/会话式运行页首次进入活跃 attempt 时，system prompt 入口不能依赖 conversation attempts 聚合快照已经补齐；只要当前 attempt 详情或首次 session 快照已拿到 `systemPromptAppend`，按钮就应立即可点击，后续再与 conversation 聚合结果合流，避免首节点运行中短暂置灰。
- 历史分页请求已切到 `beforeCursor / afterCursor` 主路径，同时兼容旧 `beforeSeq / afterSeq`；前端聊天列表使用原生滚动容器触发顶部/底部分页，prepend 旧页前捕获当前可见 timeline item 的 DOM 锚点，合并后按同一锚点的新旧 top 差值保位。
- 会话式运行页的 auto-follow reset 需要只在 `runId` 切换时触发；如果 effect 依赖父组件每轮重建的 `onAutoFollowChange` 回调，会在 live refresh 后误重置为 follow=true，导致用户手动查看已完成 session 时被运行中的 session 抢占。
- ACP 会话运行态已补齐三类回归约束：测试/验收节点的 workflow outcome failure 不再触发 runtime-error composer 面板；runtime attempt 已 completed 时会压过 stale ACP `running`、stale local awaiting response 和 stale `provider.pid`，避免“回复生成中”残留；若最后节点的 ACP session 已自然 `completed` 但父级 run snapshot 仍短暂保留 stale `runtime.active=true`，前端 composer 会以选中 ACP session 终态压制该 active 态并主动刷新父级 run；session 选择所有权改为 `auto/manual` 显式模式，手动查看历史 attempt 后 live refresh 只能后台更新，不能抢回最新 running attempt；只有当前选中 session 仍是 active session 且消息列表 pin 在底部时，滚回底部才恢复 session auto-follow。
- sasuke 主题滚动条已从 ACP 会话主滚动区扩展为全局滚动规范：原生 `overflow-auto / overflow-y-auto` 容器默认使用 `--gold-scrollbar-*` token，prompt-kit 聊天容器保留 `gold-themed-scrollbar` 显式类，shadcn/Radix `ScrollArea` 的 track/thumb 也改为同一组 token，并统一为 10px 交互轨道、约 4px 可见 thumb，避免运行模式管理、设置页、弹层和 ACP 会话出现多套滚动条颜色或胖瘦不一致。
- 会话输入卡顿排查发现旧 Workbench 的后台刷新仍会在 Conversation 模式注册 10 秒/30 秒 interval，周期性触发旧任务/工作流/round detail 数据刷新并与输入主线程竞争；当前已约束该后台刷新只在 Workbench 模式启用，切回 Conversation 时清理 interval，Conversation 模式的数据更新继续由会话侧栏加载与 ACP/live run 事件驱动。
- 会话主页 composer 草稿状态已从 App 顶层移入局部 `ConversationComposerDraftBoundary`，App 只通过 `reset` handle 清理草稿；输入按键只更新 composer context 消费者，避免每个字符驱动 App、Shell、侧栏和运行页参与 render。
- session token / cost 既在 runtime 内存里跟踪，也在读取 `acp.events.jsonl` 时重算一遍，见 `src/acp/client.rs:859-880`、`src-tauri/src/view_models.rs:3215-3231`。
- session 净处理耗时依赖 `AcpSessionElapsedState` 在读 timeline/events 时重建，包含 permission wait 扣减逻辑。会话累计只统计每个 sasuke prompt turn 内的处理时间，多次 continue/resume 之间的空闲墙钟间隔不计入；`available_commands_update`、`current_mode_update`、`session_info_update` 等元数据事件不推进本轮最后处理时间；前端 event-only live shell 也使用同一口径，避免在完整 session 尚未 hydrate 时退回 `createdAt -> updatedAt` 跨度。

当前方案的主要问题：

1. `acp.events.jsonl` 与 `acp.raw.jsonl` 重复偏重，`events` 没有真正成为“UI 已加工时间线”。
2. 活跃会话已改为事件级 push，但 session 状态、usage 和检索索引仍需要继续压实权威来源。
3. 读取详情、分页、统计仍有部分逻辑依赖重新扫描和 merge，history 链路仍需要继续从 legacy event 迁到 timeline / snapshot 主路径。
4. ACP stop 的运行态与 provider 退出时机必须解耦：停止请求发出后 UI 先进入 `stopping / cancelling` 中间态，provider 再走优雅退出，超时后 kill fallback；只有当前 attempt 已有 ACP session 时，前端才需要等待 ACP session 非 active 终态后再视为停止完成并刷新 run/session。对于尚未建立 ACP session 的启动阶段停止，应以 runtime 不再 active 作为完成信号，并进入无 composer 的“会话发起中断，请重跑该任务”终态，不再视为可继续会话。会话树 leaf 在 graceful stop 期间必须优先消费 ACP stopping 事实：`cancel-request` 仍存在且 `provider.pid` 未消失、或 session 为 `cancelling/cancel_requested` 时，即使 snapshot 已出现 `cancelled` 也仍展示停止中，避免过早显示“已停止”；一旦 provider 真正退出且 runtime 已落盘为 `paused + resumable`、pause reason 属于 `process-interrupted / waiting-for-user-input / error-blocked`，最终展示必须优先区分 ACP session 是否真正建立：已建立的 session 回归 runtime 的可继续暂停态，从未建立的 session 进入无图标中断终态，不能被 ACP `cancelled` 或空 `unknown` 快照覆盖。后续所有 stop/continue 状态变更必须补充 lifecycle 与前端 session shell 状态矩阵测试，禁止只在组件里局部追加 if。
5. token / elapsed 的权威来源仍需继续压实到 snapshot；elapsed 展示口径已明确为 prompt turn 净处理耗时，禁止用 ACP session 生命周期跨度作为会话累计。
6. 后续跨 session 搜索如果继续纯扫文件，复杂度和性能都会快速恶化。

---

## 1. 核心方向

本次会话优化的核心目标：

1. **活跃会话从 polling 改为 push。**
2. **会话时间线从逐帧 event 改为聚合后的 timeline item。**
3. **token / elapsed 从读时重算改为 runtime 聚合 + snapshot 持久化。**
4. **文件仍然是会话事实源，SQLite 只用于辅助检索。**
5. **旧会话可继续打开，通过 legacy reader / lazy migration 兼容。**

目标分层：

```text
ACP raw frame
  -> live session aggregator
  -> timeline final item
  -> push to UI
  -> rewrite timeline / update snapshot
  -> optional SQLite search index
```

这里必须明确三个边界：

- `raw`：协议排障事实源。
- `timeline`：UI 聚合时间线事实源。
- `SQLite`：辅助检索索引，不承担会话恢复和详情展示的唯一事实源。

---

## 2. 设计原则

### 2.1 会话详情优先服务 live UX

会话主路径必须优先服务：

- 文本流顺滑增长
- tool call 原地刷新
- permission 状态即时切换
- 历史向上翻页不抖动
- 会话恢复后可继续增长

因此主详情链路不能继续依赖“轮询文件 + 再 merge”。

### 2.2 timeline 必须是 UI 已加工结果

`acp.timeline.jsonl` 不再保存 raw-ish event 镜像，而应保存：

- 一条用户消息
- 一条 assistant 文本块
- 一条 thought 块
- 一条 tool call
- 一条 completed plan 块
- 一条 permission 块
- 必要的 system / error / compaction 块

即：一个逻辑 UI item 在内存中可以被多次更新，但落盘后只保留当前最终态，不能退回成 ACP chunk 日志。

### 2.3 文件优先，数据库旁路

当前项目 runtime 已按 attempt 目录组织，会话与 task / run / round / node / attempt 绑定紧密；因此本期不引入“DB 作为会话主存储”。

本期约束：

- `raw / timeline / snapshot` 为主数据。
- SQLite 仅承担跨 session prompt / timeline 检索索引。
- 没有 SQLite 时，详情、恢复、排障都必须继续工作。

### 2.4 破坏式升级，但保留旧会话可读

开发阶段允许替换旧方案，但不能让已有会话不可打开。

因此策略为：

- 新会话走 V2。
- 旧会话走 legacy reader。
- 必要时按首次打开或后台任务做 lazy migration / backfill。

---

## 3. 目标文件结构

每个 ACP attempt 的目标文件结构：

```text
attempt/
  worker-ref.json
  acp.raw.jsonl
  acp.timeline.jsonl
  acp.snapshot.json
  acp.agents.jsonl
  acp.diagnostics.jsonl
  agents/
    agent-<stable-id>/
      timeline.jsonl
      snapshot.json
  provider.pid
```

说明：

- `worker-ref.json`：继续承载 session id、continue 信息、provider handoff 信息，是 session identity 的事实源。
- `acp.raw.jsonl`：完整 ACP inbound / outbound raw frame，只用于协议排障与极端恢复。
- `acp.timeline.jsonl`：根会话规范 timeline；Agent 工具、思考、TODO、权限和正式文字不重复写回根分支。
- `acp.snapshot.json`：当前会话聚合快照、metrics、游标与恢复锚点；用于取代现有 `acp.session.json`。
- `acp.agents.jsonl`：稳定 Agent execution 关系、状态、attention、统计和 latest cursor 物化索引。
- `agents/<stable-id>/timeline.jsonl`：单个 Agent 分支规范 timeline；目录名只使用 sasuke 生成的稳定 ID。
- `agents/<stable-id>/snapshot.json`：分支状态、统计和分页恢复锚点。
- `acp.diagnostics.jsonl`：adapter / protocol diagnostics。

本期不再把 `acp.events.jsonl` 作为 V2 会话主路径。旧会话首次访问时执行一次性分区迁移，之后所有 runtime/query/interaction 路径只使用 branch timeline；旧文件原样保留为审计来源，不维持长期兼容双读或双写。

---

## 4. 目标架构

### 4.1 live session aggregator

后端新增 ACP 会话聚合器（以下简称 aggregator），负责：

1. 接收 ACP raw session updates。
2. 将 update 聚合进内存中的 session state。
3. 生成稳定的 timeline final item。
4. push 给前端。
5. 同步重写 `acp.timeline.jsonl`。
6. 同步更新 `acp.snapshot.json`。
7. 异步更新 SQLite 检索索引。

aggregator 是活跃会话的唯一 live 聚合层。前端不再直接解释 ACP 原始 frame，也不再依赖扫描 timeline 来获得最新 UI 状态。

### 4.2 前端改为 push 消费

活跃会话改为：

- 前端打开会话时订阅该 attempt 的 live channel，订阅生命周期跟随抽屉会话而不是发送按钮锁定状态，避免漏掉调起后的早期帧。
- 后端对每个聚合后的 timeline item 直接推送单事件 payload：新增 item、更新 item、权限请求和工具/文本流状态变化；手动 `send_acp_prompt` 与自动工作流 provider 执行路径都必须接入同一个 live event sink，不能只在命令完成后靠完整 session 快照刷新。
- 前端只 merge 对应 event；命令返回、节点完成或重新打开时再用完整 session 快照做最终同步，不在每个 ACP frame 上全量重拉；后端保留向前端推送完整 session snapshot 的基础通道，当前自动 workflow 只在 run completed 完成态落盘后推送 terminal session snapshot，不能只依赖手动命令返回路径，否则最后节点没有下一跳 event 时父级 run VM 无法收敛；如果 live event 早于完整 session 快照到达，前端用最小 live session shell 承载当前窗口事件，避免首个流式片段因 `session` 为空而不可见；抽屉关闭再打开同一 attempt 时先恢复本地事件窗口并建立订阅，再主动补拉最新 session window，避免等待快照期间空白或旧快照覆盖 live tail。

历史加载和首次打开仍然走 branch 命令查询，但活跃期间的新增变化由 push 主导。ACP Chat 主列表以语义块为最小视觉单元，使用浏览器原生滚动；assistant/user 消息、Activity 摘要和 Agent link 都保持稳定单行/单块身份。Activity 折叠时不构造 thought/tool 审计详情，展开后使用独立局部 cursor；Agent transcript 通过右侧只读 Tab 查询，不进入父消息流或父分页窗口。

### 4.3 history 读取改为 snapshot + timeline

打开会话详情时：

1. 先读 `acp.snapshot.json`，获得摘要、metrics、active 状态、游标。
2. 再从 `acp.timeline.jsonl` 读取最近一页已聚合 item。
3. 如果会话仍活跃，再建立 live subscription。

历史翻页时：

- 基于 timeline cursor / checkpoint 向前或向后读取。
- 不再对 `acp.raw.jsonl` 或逐帧 event 做窗口 merge。

---

## 5. `acp.timeline.jsonl` 设计

### 5.1 设计目标

`acp.timeline.jsonl` 必须满足：

- 支持一行一个最终逻辑 item。
- 支持同一逻辑 item 在内存中多次更新，但文件中只保留当前最终态。
- 支持会话恢复时快速重建最终时间线。
- 支持历史分页。
- 支持与 live item model 保持一致。

### 5.2 item 粒度

timeline item 粒度固定如下：

- `userMessage`
- `assistantMessage`
- `assistantThought`
- `toolCall`
- `plan`
- `permissionRequest`
- `systemNotice`
- `sessionError`
- `contextCompaction`

### 5.3 final item 模型

`acp.timeline.jsonl` 推荐每行一个最终 item，示意：

```json
{
  "item": {
    "id": "assistant-msg-12",
    "kind": "assistantMessage",
    "status": "streaming",
    "content": "我先检查当前工作流节点配置...",
    "startedSeq": 120,
    "endedSeq": 127,
    "startedAt": "1717612345Z",
    "endedAt": "1717612348Z",
    "meta": {
      "attemptId": "..."
    }
  }
}
```

约束：

- `id` 是稳定标识，同一逻辑消息 / thought / tool call 在整个 turn 中保持不变。
- `startedSeq` 表示逻辑 item 首次出现时关联的 source seq。
- `endedSeq` 表示该 item 当前最终覆盖到的最新 source seq。
- `startedAt / endedAt` 用于展示、耗时、恢复和分组。
- 旧 patch log 仅作为 legacy reader 兼容输入，新写入统一采用 final item 结构。

### 5.4 文本 / thought 聚合策略

文本与 thought 必须在写时聚合，而不是读取时 merge：

- 第一帧 `AgentMessageChunk` 到来时，新建一个 `assistantMessage` item。
- 后续属于同一稳定流的 chunk 到来时，更新该 item 的 content / endedSeq / endedAt。
- `AgentThoughtChunk` 同理写入 `assistantThought` item。
- `usageUpdate`、`modeUpdate`、`availableCommandsUpdate` 这类隐藏状态帧不能打断同一文本 / thought 流。
- 只有跨 turn、跨类型、跨 stable stream 时才新开 item。

### 5.5 tool call 聚合策略

tool call 必须按 `toolCallId` 原地聚合：

- `ToolCall` 创建时建立 `toolCall` item。
- `ToolCallUpdate` 到来时按 `toolCallId` 更新同一 item。
- terminal / diff / locations / raw input / raw output 等详情作为该 item 的字段或子结构更新。
- 不能把 `toolCall` 和 `toolCallUpdate` 分裂成多张卡。

### 5.6 permission / plan 聚合策略

- `permissionRequest`：pending -> selected / rejected 更新同一 item。
- `plan`：允许增量更新，但最终 UI 只投影为所属生命周期内的单个 Todo 列表；主会话和每个 Agent 分支分别维护最新快照，不保留 plan 消息卡消费路径。
- `systemNotice` / `sessionError`：一条逻辑提示一个 item，不复用 raw error 行。

### 5.7 前端 Plan/Todo 面板（2026-06-08）

- 主会话顶层 plan 事件不再渲染为消息流中的独立卡片，而是聚合为 composer 上方单一可折叠面板：
  - 默认收起，展开后展示条目列表与状态 Badge。
  - 收起状态显示完成数/总数及进行中任务摘要。
  - 每次 plan 更新实时刷新面板内容，不再在消息流中重复追加块。
- Agent 内部 plan 只写入对应 branch timeline，并在右侧只读 Agent 会话中复用同一 Todo 面板；plan 本身不渲染为大卡片。
- branch 路由在 ACP 归一化边界完成，并为 plan 固化通用 `planOwnership = branch | unscoped`。只有明确 provider relation 或内部 branch 定位可以形成 `branch` ownership；前端不得用 `parentToolUseId`、内容血缘、Agent 名称或事件邻近猜归属。
- 没有 Agent execution 的普通根会话继续展示最新 `unscoped` plan；存在 Agent execution 时根投影必须隐藏 `unscoped` session-wide plan。Agent 分支只消费明确归属本分支的 plan，历史 Agent timeline 中已有稳定非 root branch ID 的 plan 视为明确归属。
- 面板每次 render 读取当前 branch 投影的最新 entries，稳定 branch key 与展开状态不随快照更新变化。
- 顶层 plan 事件仍写入 timeline 文件（保留完整性），但在前端消息流中被过滤。

### 5.8 前端工具调用参数展示（2026-06-08）

- 工具调用卡片展开后以有序参数列表展示输入：
  - 参数来源优先级：rawInput > title 解析 > locations fallback。
  - 同标签参数保留多个不同值，不再用 `Object.fromEntries` 去重。
  - 无结构化参数但有原始 input payload 时回退展示 JSON。
  - 扩展 `toolDetails` 提取来源：增加 `toolCall.input`、`toolCall.fields` 回溯。

---

## 6. `acp.snapshot.json` 设计

### 6.1 定位

`acp.snapshot.json` 是 V2 会话快照文件，用于取代现有 `acp.session.json`。

它不再只是 adapter/status metadata，而是统一承载：

- 会话摘要
- 聚合 metrics
- timeline 恢复锚点
- live session 恢复状态

### 6.2 建议字段

建议结构：

```json
{
  "version": 2,
  "session": {
    "status": "running",
    "restored": true,
    "stopReason": null,
    "title": null,
    "adapterId": "claude-code",
    "adapterDisplayName": "Claude Code",
    "cwd": "...",
    "capabilities": {},
    "models": {},
    "modes": {},
    "configOptions": {}
  },
  "usage": {
    "usedTokens": 12040,
    "contextWindowSize": 200000,
    "inputTokens": 8500,
    "outputTokens": 2340,
    "cachedReadTokens": 1200,
    "cachedWriteTokens": 0,
    "totalTokens": 12040,
    "totalCostUsd": 0.12
  },
  "metrics": {
    "sessionElapsedMs": 523000,
    "currentTurnElapsedMs": 12000
  },
  "timeline": {
    "latestRevision": 184,
    "latestItemCount": 37,
    "latestCursor": "...",
    "lastTimelineOffset": 48213,
    "lastRawOffset": 90312
  },
  "openState": {
    "activeTurn": {
      "turnId": "turn-12",
      "startedAt": "1717612340Z",
      "lastActivityAt": "1717612352Z"
    },
    "pendingInteractions": [{ "kind": "permission", "interactionId": "permission-3", "turnId": "turn-3" }],
    "permissionWaitStartedAt": "1717612348Z",
    "openToolCalls": ["tool-7"]
  },
  "createdAt": "1717612000Z",
  "updatedAt": "1717612352Z"
}
```

### 6.3 读取优先级

V2 会话读取优先级：

1. `acp.snapshot.json`
2. `acp.timeline.jsonl`
3. `worker-ref.json`
4. `acp.raw.jsonl`（仅排障或恢复兜底）

旧 V1 会话读取优先级保持 legacy：

1. `acp.session.json`
2. `acp.events.jsonl`
3. `worker-ref.json`
4. `acp.raw.jsonl`

---

## 7. live push 设计

### 7.1 推送内容

后端向前端推送的消息类型：

- `timelinePatch`
- `sessionStatusPatch`
- `usagePatch`
- `metricsPatch`
- `permissionPatch`
- `sessionErrorPatch`

推荐统一成：

```json
{
  "attemptKey": "...",
  "patchType": "timelinePatch",
  "payload": { ... }
}
```

### 7.2 推送节流

文本流不能按 token 级频率直接刷前端。建议：

- 文本 / thought patch：50~150ms 内合并后推一次。
- tool call / permission / status / error：即时推送。
- snapshot 写盘频率可略低于 UI push，但不得太慢，避免崩溃丢失恢复锚点。

### 7.3 断线恢复

前端重新订阅后：

1. 先取最新 `snapshot + timeline window`。
2. 再接入 live push。
3. 如果服务端保存了最近 patch revision，可在必要时从某 revision 之后补拉。

---

## 8. 历史分页设计

### 8.1 不再使用 `beforeSeq / afterSeq`

V2 不再把 raw frame 数量或规范事件数量作为历史分页单位。cursor 由稳定语义块的 seq 范围编码，但对前端保持不透明。

原因：

- seq 是协议帧粒度，不是 UI item 粒度。
- 一个语义块可能覆盖多个规范事件和 raw seq；连续工具/思考无论展开与否都只算一个活动摘要。
- 活跃会话中 seq 会持续增长，作为 UI 分页 anchor 不稳定。

### 8.2 使用 timeline cursor

历史分页改为：

- `beforeCursor`
- `afterCursor`
- `pageSize`（单位为语义块）

cursor 推荐编码：

- semantic block ordinal / seq range
- byte offset checkpoint
- revision / updatedAt

实现上允许 cursor 是不透明字符串，不向前端暴露内部结构。

### 8.3 checkpoint / index

文件方案下，为避免从头扫描 timeline：

- 每 N 个 timeline item 生成一个 checkpoint。
- checkpoint 可内嵌在 `acp.snapshot.json` 尾部摘要，也可独立为 `acp.timeline.index.json`。

index 最小信息：

- checkpoint item ordinal
- file byte offset
- timestamp
- itemId

这样打开最新页、向上翻历史都不需要全量扫描。

### 8.4 无感丝滑分页

前端分页交互要求：

- 前端会话历史分页的单次加载条数由 `configs/app-config.toml` 的 `acpChatEventPageSize` 控制，默认 96，单位是语义块；一个活动摘要或 Agent link 始终只计一块。内存与原生 DOM 中保留的页数由 `acpChatEventWindowPageCount` 控制，默认 3，窗口上限由两者相乘派生为 288，避免跨页裁剪时滚动跳变。
- 向上翻页只 prepend timeline item，不触发底部自动滚动。
- 保持 scroll anchor，避免闪跳。
- 活跃会话只有在用户位于真实最新窗口、`hasNewer=false` 且没有分页请求时，timeline live patch 才进入页面 latest-wins buffer、合入当前窗口并保持吸底。用户正在读旧历史、`hasNewer=true` 或 older/newer 请求进行中时，cached/paged `loadedEvents` 是可见窗口唯一事实；text/thought/tool/plan 等 live item 完全不进入页面 buffer，只标记 newer，并保留在 Router 的有界 branch replay 与后端 canonical timeline，不创建历史 DOM 或待渲染队列。界面维持“有更新 / 返回最新”，待返回最新或分页收敛后再按稳定 identity、revision 和 cursor 追平。
- 历史阅读期间仍由统一 control reducer 即时投影 timing、usage、Agent branch result、permission/elicitation、terminal lifecycle 与 prompt admission；这些更新不得改变当前 timeline item 集合、制造 seq 缺口、触发相反方向裁剪或破坏 DOM scroll anchor。同会话 prop/session refresh 也不得把 live head 混入 historical window。活跃会话的 live patch 与历史分页最终使用同一 timeline item 模型无缝收敛，不建立第二套消息事实源。
- Activity 展开详情使用独立局部 cursor 和“显示更早活动”，不得改变会话 `beforeCursor/afterCursor/hasOlder`；单条工具 raw output 继续按需读取。

---

## 9. token / cost / elapsed 设计

### 9.1 token / cost

切到 V2 后，不再依赖扫 timeline 全量重算 session usage。

权威来源改为 runtime aggregator + snapshot：

- `usedTokens`
- `contextWindowSize`
- `inputTokens`
- `outputTokens`
- `cachedReadTokens`
- `cachedWriteTokens`
- `totalTokens`
- `totalCostUsd`

规则：

- `usage_update` 先进入 runtime usage state：正数更新确认后的 `usedTokens`，瞬时 `used=0` 只作为 raw 采样或 compact reset 边界，不覆盖 snapshot/UI；`contextWindowSize / totalCostUsd` 独立更新。
- prompt response 结束时补齐 `inputTokens / outputTokens / cached* / totalTokens`。
- 删除 `accumulatedUsedTokens`；上下文占用是 gauge，不能通过正向 delta 推导累计 Token 消耗。累计消耗只采用 Provider 返回的 `inputTokens / outputTokens / cached* / totalTokens`。
- UI 直接读 snapshot 中的 usage，live 期间通过 `usagePatch` 刷新。

timeline 只在需要消息级 token 展示时，给最终 assistant message item 写入：

- `usedTokensAtStart`
- `usedTokensAtEnd`
- `tokenDelta`

### 9.2 elapsed

V2 下 session 净处理耗时必须改为 runtime 维护，不再以“详情页读文件重算”为主路径。

建议 runtime 维护：

- `sessionElapsedMs`
- `currentTurnElapsedMs`
- `activeTurnStartedAt`
- `activeTurnLastActivityAt`
- `permissionWaitStartedAt`
- `permissionWaitAccumulatedMs`
- `pendingPermissionIds`

规则保持与当前口径一致：

1. sasuke 用户 prompt accepted 时开启新 turn。
2. assistant / thought / tool / plan 的有效活动刷新 `lastActivityAt`。
3. permission pending -> selected 期间不计入净耗时。
4. session active 时 `currentTurnElapsedMs` 可按 now 实时增长。
5. turn 结束时把净耗时累加进 `sessionElapsedMs`。

历史读取时：

- 优先读 snapshot 中的 metrics。
- 仅在 legacy 会话、snapshot 缺失或调试校验时，从 timeline / events 回放重建。

---

## 10. SQLite 辅助检索方案

### 10.1 原则

SQLite 在本方案中**只用于辅助检索**，不承担：

- 会话详情渲染主存储
- 活跃会话 live state
- raw frame 排障
- timeline 恢复的唯一来源

文件仍然是权威事实源。

### 10.2 使用场景

SQLite 只承担两类检索：

1. **跨 session 用户 prompt 搜索**
2. **可选的 final timeline item 搜索**

### 10.3 最小表设计

建议最小化为：

- `sessions`
  - `session_id`
  - `attempt_path`
  - `task_id / run_id / round_id / node_id / attempt_id`
  - `created_at`
  - `updated_at`
  - `title`

- `session_prompts`
  - `id`
  - `session_id`
  - `prompt_id`
  - `timestamp`
  - `text`
  - `normalized_text`

- `session_prompts_fts`（FTS5）
  - 对 `text` 建立全文索引

可选：

- `timeline_items`
  - 只存 final timeline item
  - 不存中间流式 revision

### 10.4 写入策略

- 文件写入成功后，再异步写 SQLite 索引。
- DB 写失败不影响会话详情与恢复。
- 可通过后台 backfill 为旧会话补索引。

### 10.5 兼容策略

- 新 V2 会话：写文件 + 写 SQLite 索引。
- 旧 V1 会话：首次命中搜索范围时可 lazy backfill。
- 搜索优先走 SQLite，详情优先走文件。
- 若 `sessions` / `session_prompts` 索引 schema 落后，则启动时只重建这组会话索引表并从文件回填，不做逐列迁移，也不影响 `tasks` 索引。

---

## 11. 旧会话兼容与迁移

### 11.1 版本定义

- **V1**：`acp.session.json` + `acp.events.jsonl` + `acp.raw.jsonl`
- **V2**：`acp.snapshot.json` + `acp.timeline.jsonl` + `acp.raw.jsonl`

### 11.2 兼容读取

读取逻辑：

1. 若存在 `acp.snapshot.json`，按 V2 打开。
2. 否则若存在 `acp.events.jsonl`，按 V1 legacy reader 打开。
3. 若用户显式打开旧会话，可在后台生成 V2 timeline / snapshot。

### 11.3 lazy migration

推荐 lazy migration，而不是强制一次性全量迁移：

- 第一次打开旧会话时：
  - 读取 V1 `events + session + worker-ref`
  - 生成 V2 `timeline + snapshot`
  - 后续优先走 V2

- 正在运行中的旧会话不强切：
  - 已启动的 V1 attempt 继续按旧链路跑完
  - 新建会话统一走 V2

---

## 12. 与 Zed 的可借鉴点

本方案可以直接借鉴 Zed 的以下设计，但不照搬其 GPUI 具体实现。

### 12.1 借鉴点 A：ACP update 直接进入内存线程状态，而不是轮询文件

Zed 的 ACP notification 会直接派发到前台线程状态，而不是靠定时拉取：

- ACP notification handler 挂载：`D:/repos/zed/crates/agent_servers/src/acp.rs:749-751`
- notification 入 foreground dispatch：`D:/repos/zed/crates/agent_servers/src/acp.rs:399-414`

可借鉴点：

- sasuke 活跃会话改 push。
- live patch 直接作用于 session aggregator。

### 12.2 借鉴点 B：AcpThread 本身就是聚合后的 UI thread model

Zed 的 thread entry 不是 raw frame，而是高层结构：

- `UserMessage`
- `AssistantMessage`
- `ToolCall`
- `CompletedPlan`
- `ContextCompaction`

定义见：
`D:/repos/zed/crates/acp_thread/src/acp_thread.rs:165-253`

可借鉴点：

- sasuke timeline item 应直接建模 UI 逻辑对象，而不是保留 chunk 级 event 流。

### 12.3 借鉴点 C：文本 chunk 写时聚合，并对 streaming 文本做平滑缓冲

Zed 在 `handle_session_update` 中直接把 message / thought chunk push 进聚合线程：

- session update 分发：`D:/repos/zed/crates/acp_thread/src/acp_thread.rs:1646-1722`
- user message append：`D:/repos/zed/crates/acp_thread/src/acp_thread.rs:1724-1772`
- assistant message append 与 streaming buffer：`D:/repos/zed/crates/acp_thread/src/acp_thread.rs:1775-1800`

可借鉴点：

- sasuke 文本与 thought 必须写时聚合。
- live patch 应加小粒度缓冲，避免 token 级刷新抖动。

### 12.4 借鉴点 D：tool call 按稳定 id 原地 upsert

Zed 的 tool call 与 tool call update 都按 `tool_call_id` 更新同一 entry：

- `update_tool_call`：`D:/repos/zed/crates/acp_thread/src/acp_thread.rs:2085-2155`
- `upsert_tool_call`：`D:/repos/zed/crates/acp_thread/src/acp_thread.rs:2157-2226`

可借鉴点：

- sasuke `toolCall` / `toolCallUpdate` 必须是同一 timeline item 的 patch，不再由前端读时二次 merge。

### 12.5 借鉴点 E：session list 用 cursor 分页并支持 watch 更新

Zed 的 session list 是 cursor-based，并支持 watch：

- `AgentSessionListRequest { cursor }`：`D:/repos/zed/crates/acp_thread/src/connection.rs:306-317`
- `list_sessions -> next_cursor`：`D:/repos/zed/crates/agent_servers/src/acp.rs:572-609`
- `watch()`：`D:/repos/zed/crates/agent_servers/src/acp.rs:635-640`

可借鉴点：

- sasuke 历史列表、跨 session 归档和检索结果分页，应改为 cursor 思维，而不是继续围绕 raw seq 做窗口分页。

### 12.6 借鉴点 F：结构化 thread 持久化

Zed 本地保存的是结构化 thread，而不是 raw 日志主驱动：

- `ThreadStore::save_thread / load_thread`：`D:/repos/zed/crates/agent/src/thread_store.rs:50-75`
- 恢复 thread 后再注册成 `AcpThread`：`D:/repos/zed/crates/agent/src/agent.rs:1455-1494`、`D:/repos/zed/crates/agent/src/agent.rs:1513-1539`

可借鉴点：

- sasuke 应把 `timeline + snapshot` 视为结构化持久化结果，而不是把 raw frame 当 UI 详情的主恢复链路。

### 12.7 不直接照搬的部分

Zed 的实现与本项目差异：

- Zed 绑定 GPUI entity / subscribe 模型。
- sasuke 是 Tauri backend + Web 前端。
- Zed 自带本地 thread DB；本方案明确只把 SQLite 用于辅助检索。

因此本方案借鉴的是设计思想，而不是直接复制其运行时框架。

---

## 13. 实施步骤

### Phase 1：V2 数据结构落地

1. 定义 `timeline item` / `timeline patch` / `snapshot` Rust 与 TS 类型。
2. 新增 `acp.snapshot.json` 与 `acp.timeline.jsonl` 路径与读写器。
3. 保留现有 `acp.raw.jsonl` 与 `acp.diagnostics.jsonl`。

### Phase 2：live aggregator

1. 在 ACP runtime 中引入 session aggregator。
2. 处理 message / thought / tool / plan / permission 的写时聚合。
3. 生成 live patch 并写 timeline。
4. 维护 token / elapsed / permission wait metrics，并写 snapshot。

### Phase 3：前端 push 化

1. 前端会话详情页接入 live subscription。
2. 活跃会话停止依赖 1.5s 轮询。
3. 历史分页切到 timeline cursor。

### Phase 4：legacy 兼容

1. 保留 V1 reader。
2. 增加旧会话 lazy migration。
3. 新建会话默认走 V2。

### Phase 5：SQLite 辅助检索

1. 增加 `sessions` / `session_prompts` / `session_prompts_fts`。
2. prompt 写入时同步索引。
3. 增加搜索接口和旧会话 backfill。

---

## 14. 风险与权衡

### 14.1 风险

1. live push + history page 混合后，前后端状态同步复杂度上升。
2. timeline item 边界若定义不好，可能出现消息合并错误或 tool call 错位。
3. snapshot 与 timeline 若一致性处理不好，恢复链路会出现偏差。
4. SQLite FTS 对中文 prompt 的检索效果需要单独评估 tokenizer 与标准化策略。

### 14.2 权衡

- 本方案刻意不把 SQLite 升格为主存储，换来较低改动和更稳兼容，但历史查询高级能力会受限于“旁路索引”模式。
- 文件优先意味着仍需做好 checkpoint / index，否则 timeline 超大后历史读取仍会变慢。
- 相比当前方案，本方案实现复杂度上升，但会显著改善流式体验、职责边界与后续搜索扩展性。

---

## 15. 最终结论

本期推荐的会话优化方案是：

1. **以 `acp.timeline.jsonl` 取代 `acp.events.jsonl` 作为 V2 会话主时间线。**
2. **以 `acp.snapshot.json` 取代 `acp.session.json` 作为 V2 会话快照与恢复锚点。**
3. **保留 `acp.raw.jsonl` 作为协议排障事实源。**
4. **活跃会话从轮询改为 push，timeline item 采用写时聚合。**
5. **token / elapsed 改为 runtime 聚合 + snapshot 持久化，不再以读时重算为主路径。**
6. **SQLite 仅用于辅助检索，尤其是跨 session 用户 prompt 搜索，不承担会话主存储。**
7. **旧会话通过 legacy reader 与 lazy migration 继续可打开。**

## 16. 当前实现进度

已完成第一阶段落地：

- runtime attempt 路径已新增 `acp.snapshot.json` 与 `acp.timeline.jsonl`。
- ACP runtime 新建会话已切到仅写 `acp.snapshot.json` + `acp.timeline.jsonl`；不再为新会话创建 `acp.session.json` / `acp.events.jsonl`。
- ACP 会话读取已改为优先走 `snapshot + timeline`，仅旧会话缺失 V2 文件时回退 `session + events`。
- 前端 ACP Chat 活跃会话已切到 Tauri `sasuke://acp-session-updated` push 主路径；轮询兜底已移除，抽屉打开期间常驻订阅，重开会话仍通过 `snapshot + timeline` 初始化，再无缝接入 live push。
- ACP runtime 每次持久化聚合后的 timeline item 后都会推送单事件 payload；手动 ACP prompt 和自动工作流节点执行都接入同一个 live event sink，前端按稳定 event key merge 到当前窗口，命令或节点完成后的完整 session 快照只做最终同步，不再在每个 ACP frame 上重建整段 session。自动 workflow run completed 完成态落盘后会推送完整 terminal session snapshot，保证最后节点没有下一跳事件时 run VM 仍能刷新到终态；暂停、阻塞、人工验收继续以 workflow runtime 状态刷新为主，不新增 session snapshot 触发。停止会话已收到 `cancelled/failed/killed/error` 等 ACP terminal snapshot 后，composer 必须结束 stale stopping 与本地 cancelling 锁定，再按 runtime pause/continue 或普通会话态展示。会话式运行页已把当前选中 full session snapshot 提升到 App 层更新 `ConversationRunVm.selectedSession`，Chat 组件在新 UI 中不再仅凭本地 timeline events 重建 running shell；单元测试固化 selected session payload 所有权：同 key refresh 缺 session payload 不清空既有终态 payload，同 key full snapshot 替换旧 payload，selected session identity 变化时不沿用旧 payload，避免后续重构时重新把 terminal session 复活为 running。
- 会话式主页创建/重跑 run 也必须使用同一个 live event sink，并以后台 run 启动返回初始 VM；前端收到当前 run 的首个 attempt live event 后刷新 session tree 并选中该 session，避免新 UI 只等定时刷新或权限请求时才一次性补齐历史。重跑创建新 run 的编号由 runtime 统一从该 task 已存在 `run-*` 的最大数字后缀递增，并通过原子创建 run 目录处理并发；前端只使用后端返回的 `runId` 导航，不根据当前选中 run 推导下一号。
- 会话式运行页使用 compact composer 时，`AcpUsagePanel` 不得只展示 token 和计时；运行中必须在同一区域展示具体处理状态标签，并优先使用当前步骤语义：发送中、处理中、思考中、工具调用中、响应中、停止中。该状态仍属于 composer，不进入消息流。
- 历史分页请求已支持 `beforeCursor / afterCursor`，同时兼容旧 `beforeSeq / afterSeq`；前端 cursor 从当前已加载窗口的首尾事件计算，避免使用过期 session page cursor 重复拉错段。
- ACP Chat 历史区已改为原生滚动容器；向上接近顶部触发 `beforeCursor` 历史加载，向下到底部且存在较新窗口时触发 `afterCursor` 加载。
- 针对 prepend 历史页和窗口裁剪导致阅读位置漂移的问题，前端在加载前捕获当前可见语义块的 DOM 锚点，合并旧页后按该锚点新旧 top 差值补偿 `scrollTop`；窗口内直接渲染语义块，不使用 `react-virtuoso`、render block 分片、预渲染像素或消息拆段补丁。assistant/user 消息、Activity 摘要和 Agent link 都保持稳定视觉单元。
- 2026-07-31：修复超长 ACP 会话向下回看时跨页整段跳跃。根因是分页大小与 loaded event buffer 使用同一上限，名义上的多页缓冲实际只有一页；`afterCursor` 返回下一页后会把当前页整体裁掉。前端缓冲改为三页、最少一整页的滑动窗口，较新页在距离窗口尾部约 240px 时预取；older/newer 请求统一为单一分页方向状态，禁止双向并发；两个方向换页都捕获同一套可见 DOM item 锚点，并在 `useLayoutEffect` 中同步校正裁剪前后的 `scrollTop`，保证当前页与下一页先共存再滑窗裁剪，消息顺序连续且不闪跳。2026-08-30 先将默认页大小收敛为 96、三页窗口为 288；在静态 Markdown 和跨会话事件热路径完成根因降载并经长会话实测流畅后，参数曾回调为 192/576，用 task-313 的约 470 个语义块继续观察 DOM 容量边界。2026-08-31 结合整机高 Commit 压力下的复现证据，正式配置恢复为 96/3/288，并把 page size 与 window page count 一并纳入 app config。回归固定通用 `pageSize × windowPageCount` 边界、默认 96/3/288、首次合并保留完整当前页以及满窗口后滑窗连续。
- 2026-08-30：长历史渲染边界继续收敛。静态 Markdown 直接展示最终正文，不创建 playback controller 或 `MutationObserver`；只有当前流式 assistant 消息持有播放器，并覆盖 static→streaming baseline 与 streaming→static 先 settle 后释放。`ACPChatDialog` 的恢复窗口使用 canonical identity 的 lazy initializer，普通 render 不再重复 restore/merge。Web 端由 conversation event router 维持唯一原生 ACP 订阅；attempt key 的 Task 分量使用 `taskUuid ?? taskId`，UUID 存在时为 canonical，只有缺少 UUID 的 legacy 事件才回退 `taskId`，完整 key 仍覆盖 project/task/run/round/node/attempt 与 outer locator。Router 先更新 branch replay/lifecycle snapshot，再按该 key 投递当前会话，并向 App 发布侧栏、新 session 锚点、终态和自动跟随所需的轻量全局事件；后台 replay 语义保持不变，其他 attempt 的普通事件不再遍历当前页面 listener。
- 2026-08-31：补全历史窗口到 live head 的 snapshot/replay 水位交接。历史 window gate 保持不变，canonical prompt admission 在 gate 前按稳定 `promptId` 把本地气泡收敛为静态 accepted；terminal 只清仍 pending 的 raw optimistic，canonical 已到后迟到 transport reject 不得回退。完整 session refresh 在历史态经统一 reducer 更新 control metadata，不改写 timeline。返回最新先读取 canonical latest page；newer 分页得到 `hasNewer=false` 且视口仍处 newer edge 时先结束分页 token，再进入同一个 coordinator 做一次 fresh canonical head 交接，避免分页请求飞行期间新增的 recovery 被旧 response 错误结清。交接读取最终 Router cut：更高 timeline generation 至多触发一次 canonical refresh，同代 revision loss 通过 `afterRevision` 补齐且每页按有效配置窗口即时裁剪，当前默认 `96 × 3 = 288`；sequence-only loss 在同一总预算内有界重读 full head。ACK 的 generation/revision/sequence 只由 canonical response eventPage 推进，合并 replay 后的可见 `newestSeq` 不得参与 ACK；ACK 成功才一次提交 session、timeline、`hasNewer=false` 与 at-bottom。timeline generation、Router generation、catch-up cursor 与 canonical `coveredRevision` 分域，代际前进会重置 revision。older/newer/latest 全部用 `{componentInstanceId,eventWindowKey,requestSeq}` 拒绝切会话、卸载和新请求后的迟到 await/finally/layout commit。快照与 replay 不启动 Markdown playback，交接后的新 live 才恢复动画；折叠 Activity 只更新轻量摘要且不读取详情。回归覆盖历史 cache/prop refresh、sending→accepted→canonical、replay-only control metadata、请求期间 replay/代际竞态、旧请求所有权、冻结 RAF 首帧贴底、revision/sequence loss catch-up、visible/canonical watermarks 分域与新 generation 覆盖旧 loss。
- 2026-08-31：继续收紧 session/generation owner 与背压。可见窗口 owner 扩展为 `eventWindowKey + sessionId + timelineGeneration`，Router replay 同步持有 sessionId；同 locator 新 session 原子清旧 retained/loss/head/permission。live envelope 的 generation 始终非空，只有 durable event 携带 revision；Agent branch transient 从自己的 TimelineStore 读取 generation。Router 淘汰无 revision transient 时以最大 `endedSeq/seq` 建立 `lossWatermarkSeq`，sequence fence 不能伪装为 revision delta 或被普通 ACK 清除，必须由匹配 owner/generation 且 `newestSeq` 已覆盖的 canonical head 消费。ACK 改为 prefix ACK并保留 C0 后事件/新 loss；catch-up 固定 C0，最多 4 页或 2 秒，随后只读一次 C1。历史、暂停和 recovery 状态的 timeline 事件不进入页面 buffer；live-head buffer 与 DOM 窗口共用配置容量，默认 288，溢出走统一 canonical-head coordinator。Agent branch envelope burst 只有一个 in-flight 和一个 trailing refresh；跨代分页使旧 cursor 失效，只有显式返回最新可重建。
- 2026-08-31：补齐 lazy detail、非法 generation 与 optimistic owner 的有界边界。Activity 详情复用 40 项 API page，前端保持 3 页/120 项窗口；继续向前时裁掉 newer 端并用真实详情 DOM 锚点补偿滚动，显式“回到最新活动”重新取 latest page。Activity/Tool 请求统一绑定 window owner、session、generation、logical item、observed position 与 request sequence；Tool 额外绑定 `raw/status/content/title` 的语义 source fingerprint，不使用 raw 对象引用。等价 canonical refresh 不失效详情或追加扫描，同 position 真实 source 变化只覆盖一个 trailing；旧响应、错误/finally 和低位置详情不能覆盖新 owner。Router 在 listener 分发前把非法 generation event 降为 `timelineRecoveryRequired` 控制信号，保留 session/lifecycle。Session-keyed Optimistic UI store 是组件唯一 snapshot，组件不再镜像第二份数组/ref；容量跟随有效会话窗口，默认每 session 288 项、最近 12 个 session。新增回归覆盖 Activity 第四页裁剪/回最新、session/generation 迟到详情、Tool 等价/真实 source、锚点补偿、非法 event、transient sequence loss 与 optimistic 单一有界源。
- 2026-08-31：完成 ordinary/recovery handoff 与 lazy detail 背压闭环。普通“返回最新”、newer edge/页面容量 handoff 和非法 generation、replay loss、暂停恢复全部复用同一个 canonical-head coordinator；每个 owner 最多一个 in-flight 加一个可覆盖的 latest trailing intent，recovery 优先，成功只清当前请求已经覆盖的 gate，失败或请求期间新增 loss 继续保留恢复入口。Activity/Tool 每个展开项同样只有一个 in-flight 加一个 latest-trailing intent，中间 revision/source 不并发触发 Tauri IPC 和 JSONL 全文件扫描；无非空 canonical `sessionId` 时不查询，query、后端候选与响应按 session 精确隔离。同 position Tool detail 只补当前 raw 缺失字段，不得以旧 output/status/content/title 覆盖新 live canonical。进入 live pause 时清空暂停前尚未 drain 的页面 buffer，并登记一次 canonical recovery。回归固定 ordinary→recovery 与 recovery→ordinary 竞态、historical malformed→显式/自然分页交接→合法 live、sequence-only 水位、Activity session owner、多 revision 合并、Tool canonical enrichment 与 generation/semantic source 交接。
- 2026-09-01：完整 ACP resource LRU 的会话容量从硬编码 12 收敛为 `configs/app-config.toml` 的 `acpChatResourceCacheSessionCount`，当前默认 8；配置通过 RuntimeConfig 与 Tauri `AppConfigVm` 在 bootstrap 提交前初始化唯一 resource store。Session VM、有限事件窗口、正文 hydrate 标记和 branch view state 继续按完整 resource key 原子淘汰；轻量 Conversation run LRU 与 optimistic prompt session store 仍分别保持既有 12 项边界。本次复用现有 LRU，不新增缓存、队列、订阅或运行期动态 resize；最坏重资源会话常驻数下降约三分之一，代价仅是第 9 个冷会话更早触发 canonical history 重读。
- 2026-07-31：修复分页边界上的已回答 elicitation 被短暂渲染成待回答卡片。历史旧页可能只包含 `elicitationRequest(pending)`，而对应 response / completed tool update 位于较新页；旧实现从当前裁剪窗口反推 pending，导致先出现可操作卡，加载下一页后又消失。pending permission / elicitation 的窗口 fallback 现统一受 session 生命周期约束：terminal session、仍有较新页、当前 timing 未处于对应 `waitReason` 时均不得推断 pending；仅最新 active 窗口可恢复交互卡，历史 AskUserQuestion 继续由工具卡展示。新增单元测试覆盖 terminal 历史窗口、`hasNewer` 历史窗口、wait kind 匹配及 active 非等待态。
- 2026-07-31：继续修复向下分页到 AskUserQuestion 边界时跳到会话末尾。数据窗口与 DOM 锚点已经连续，但 prompt-kit 仍会把裁剪窗口的物理底部识别成会话底部；分页请求等待期间贴底锁可能重新开启，下一页内容增长便触发自动滚到底。前端现在把 `viewportAtBottom && !hasNewerEvents` 作为唯一“真实会话底部”信号，在仍有较新页时持续解除 stick-to-bottom，并在 older/newer 请求返回、写入新窗口前再次 `stopScroll()`，确保最终由 DOM 锚点而不是贴底动画决定位置。单元测试固化裁剪窗口底部不得向 run 级 auto-follow 上报 at-bottom。
- 2026-07-31：修复工作流结束后偶发无法从历史会话底部向上滚动。根因是 session 恢复显式创建了 `ignoreEscapes` 的强制贴底动画，长 timeline 的 Markdown、折叠节点等异步布局会延长动画生命周期；同时第三方组件在底部邻域内继续上报 at-bottom，导致小幅上滑不能表达历史阅读意图。恢复路径改为可中断的瞬时定位；prompt-kit `ChatContainer` 在捕获到任意向上 wheel 输入时立即调用公开 `stopScroll()`、对外上报离开底部，并且只有视口真正返回内容底部才重新启用跟随。回归测试以 `deltaY=-1` 固化“小幅上滑立即脱离，后续内容增高不回拉”的接口行为。
- 2026-08-04：继续修复无用户操作时流式会话偶发脱离自动贴底。真实 attempt 表明 assistant 终态之后还会追加 `fileChangeSet`，文件卡加载详情后再次增高；浏览器可能先发布布局 scroll、再通知 ResizeObserver，第三方 stick-to-bottom 因而把程序性上移误判为用户逃逸。`ChatContainer` 现把 follow/manual 意图提升为包装层状态：只有向上 wheel、键盘、pointer/滚动条拖动以及业务显式 `stopScroll()` 才进入 manual；follow 状态下若第三方几何锁短暂丢失，内容尺寸观察会使用可中断的 instant scroll 恢复真实底部。包装后的外部 `stopScroll/scrollToBottom` 分别表达退出/恢复跟随，保留历史分页和 branch view 锚点语义。接口回归覆盖布局竞态自动恢复、文件卡二次增高、wheel 后不抢回、回到底部后恢复及外部 stop 后不恢复。
- 2026-09-02：流式会话在底部容差区内轻微上滑后仍可能被重新拉回。已确认的实现缺陷有两层：`ChatContainer` 曾只按“距底不超过 2px”恢复 follow，导致明确用户意图被亚像素容差覆盖；首次修复又把任意向下 `scroll` 位移当作用户返回，但流式内容增长、浏览器滚动锚定和第三方补偿也会产生同类位移。包装层现用 ref 保存“已收到明确向下用户输入”的恢复资格：向上输入与外部 `stopScroll()` 清除资格，向下滚轮、键盘或滚动条拖动授予资格，布局与 ResizeObserver 只能读取、不能授予，只有具备资格并到达真实底部或显式 `scrollToBottom()` 才恢复 follow。该方案通过最小 DOM 回归和真实浏览器 smooth 流式增长验证，但真实 EXE 会话仍报告复现，因此暂停继续修改行为，转为采集现场证据。复用默认关闭、2,000 条有界的 ACP streaming diagnostics；上滑后开启 5 秒/160 条精确 trace，以稳定实例 ID 记录 mount/unmount、用户输入、scroll/resize 几何、wrapper 与第三方状态、每次 follow 写入及外部回底 cause，直接判定是重挂载、业务层显式 `scrollToBottom()` 还是第三方内部动画。诊断不记录正文，不新增 React 高频 state、监听器、持久化 I/O、缓存或依赖。
- 2026-09-02：EXE 精确 trace 确认剩余回拉来自 canonical-head layout commit，而非底部容差、组件重挂或第三方动画：用户 wheel 后 wrapper 已稳定进入 manual，视口最远离底 204.1px，但飞行中的 canonical recovery 响应仍先同步写物理底部，再显式调用 `scrollToBottom()`；10 次调用中 9 次发生在 manual，典型回写延迟 401–593ms。根因是 2026-08-31 的 snapshot/replay 水位交接正确地统一了数据恢复，却把“数据窗口已到 head”错误等同成“用户仍要贴底”。`ChatContainer` 现在把每次 follow 写入的 cause 同步透传给 ACP，即使 external stop 已先把布尔值置为 false，后续 `user-wheel/key/scrollbar-up` 仍能建立独立 manual viewport intent；分页/recovery 自己的 `external-stop-scroll` 不改变该意图，显式“回到最新”或真实返回底部才清除。`rejoinLatestHead` 提交时读取最新 manual intent：用户已离底则原子更新 canonical session/window 后复用 branch view 的稳定 DOM anchor + `scrollTop` fallback，保持“回到最新”入口；用户未离底则保留既有 layout-before-paint 贴底。最小 DOM 测试已记录修复前 canonical 内容更新但 `scrollTop 1600 → 1800` 的稳定红灯，修复后保持 1600；相邻用例同时固定未发生用户离底时内容高度增长仍从 1800 跟随到新底部 2000。实现不新增持久状态、缓存、请求、队列或滚动热路径测量。
- 2026-09-03：“回到最新”只在流式期间、持续上滑初段高频闪烁，上一份 trace 并未出现 120px 阈值附近的高频往返，因此当时暂停继续按“阈值抖动”修改行为，先补可审计诊断。ACP 只在按钮显隐真实转换时记录来源与几何，并用稳定 callback ref 记录 ref attach/detach/mount sequence；普通 session metadata 刷新必须保持同一按钮 DOM identity。现有诊断桥增加当前页面可逆的 `normal / no-backdrop / no-transition` A/B 模式，用于区分 `sticky + backdrop-blur` 的 WebView 合成重绘与 Button transition；默认 `normal` 不改现有样式，切换不持久化。记录继续受 2,000 条内存上限约束且不含消息正文，不新增 observer、React 高频 state、IPC、缓存、队列或依赖。最小测试先稳定红于缺失诊断事件/接口，再确认显隐 source/geometry、元数据刷新 DOM identity、点击后的对应 detach 及样式注入可逆。
- 2026-09-03：2,000 条 EXE 现场记录推翻了视觉层假设并确认单阈值缺陷：`no-backdrop` 与 `no-transition` 均未消除闪烁；同一组件/session、manual intent 始终为 true 且没有自动回底调用时，按钮仍在 `120.71px 显示 → 116.71px 隐藏`、`120.14px 显示 → 108.71px 隐藏` 之间由 `viewport-scroll` 反复切换。流式 `scrollHeight` 持续增长而历史会话高度固定，解释了为何只有流式期间明显。随新按钮出现的 0ms `attach → detach → attach` 来自根节点 `React.StrictMode` 对 callback ref 的开发态复查，不能视作实际 DOM 重挂。显隐投影改为同一 O(1) 双阈值判定：隐藏态达到 120px 才显示，显示态进入 48px 近底区或重新 following/at-bottom 才隐藏；不新增状态、监听器、定时器、缓存或依赖。最小 DOM 测试先稳定红于按钮在 116px 被卸载，修复后固定 116px 保持同一 DOM、48px 隐藏、再次越过 120px 后出现、metadata 刷新保持 identity 及点击回底收敛。
- 2026-09-03：双阈值首轮修复消除了上滑闪烁，但 48px 几何退出仍会在下滑时与流式高度增长竞争：按钮先隐藏，下一批内容把距离重新推过 120px 后又出现，说明动态几何不适合作为该状态的退出事实。最终显隐改为阅读生命周期门闩：隐藏态由明确 manual intent 或 canonical `hasNewerEvents` 历史窗口在 120px 激活；一旦显示，距离缩小到 48px、流式增长重新扩大距离或 newer gate 收敛都保持同一 DOM，只有 following/at-bottom、显式回最新、会话切换或 canonical 重新归属才隐藏。最小测试先红于 48px 卸载，修复后固定 `200px → 116px → 48px → 流式增长至 168px` 全程 identity 不变，点击回最新才 detach；相邻历史分页、跨 compaction 隔离和 cursor 失效测试固定无 wheel 的 canonical 历史窗口仍可激活入口。实现删除隐藏阈值，不新增状态、监听器、定时器、缓存或依赖，scroll RAF 仍为 O(1) 且减少 React 显隐提交。
- 2026-09-03：EXE 下滑现场 trace 补全了门闩的退出缺陷：序列 8532、9127 均在 `viewportAtBottom=true + hasNewerEvents=true` 时由 `at-bottom-change` 隐藏按钮，随后 newer window/流式布局扩展又把距底推至 155px/126px 并重新显示；`hasNewerEvents=false` 后真正到底的隐藏才是正确收敛。根因是按钮 helper 仍把有限加载窗口的物理底部当成 canonical head，与 run auto-follow 已使用的 `viewportAtBottom && !hasNewerEvents` 契约不一致。显隐投影现复用同一 `isAcpConversationAtBottom` 判定：按钮在仍有 newer page 的物理底边保持同一 DOM，分页扩展后继续显示，直到抵达 canonical head 或其他明确生命周期边界。最小接口测试先稳定红于 `true/true` 被错误隐藏，再转绿；相邻 DOM 用例覆盖历史窗口物理底边的按钮 identity。实现不新增状态、阈值、监听器、请求、缓存或依赖，scroll RAF 保持 O(1)。
- 2026-09-03：后续 13 秒 EXE trace 证明按钮显隐门闩已稳定，但暴露出独立的 follow intent 热路径争抢：307 个 `scroll` 期间产生 351 次 `external-stop-scroll`；到达底部时 `user-wheel-down: false→true` 会在同一毫秒被 `external-stop-scroll: true→false` 推翻。根因是 2026-07-31 为历史 newer 分页保护 DOM 锚点而加入的 `hasNewerEvents` scroll-frame stop，在 `hasNewerEvents` 后来同时承载流式 replay/recovery 闸门后作用域过宽。通用 scroll handler 不再写 follow intent；明确向上输入、older/newer 分页开始与响应提交、branch view 恢复及返回最新继续保留既有一次性 stop/scroll 边界。最小诊断 DOM 测试先稳定红于 3 个远离分页阈值的 scroll 产生 3 次 stop，修复后同一用例为 0；相邻分页测试继续负责固定 loaded-window 底部的按钮和 canonical newer 行为。实现不新增状态、监听器、阈值、请求、缓存或依赖，scroll RAF 仍为 O(1) 且减少第三方贴底状态写入。
- 2026-09-03：修复切换到其他应用后返回时权限卡已出现、但 Timeline 仍为空白 Logo 或停留在旧正文的问题。`acp.raw.jsonl`、物化 index 与 session query 证明 canonical 正文未丢失；缺陷来自 permission control projection 与可见 Timeline window 分路恢复，且后台布局形成的 `hasNewer / 非底部` 几何被误当成用户正在阅读历史。前端现在在初始查询完成后比较 pending permission/elicitation envelope 与 canonical query `generation / revision` 水位：同 owner 且领先时复用既有单飞 canonical-head coordinator；初始 query、delta 与成功 handoff 统一推进 owner-scoped coverage，相同 revision 的重复权限不再读取。完整 pending session 已领先当前窗口时，即使窗口几何被判为 historical，只要没有明确 wheel/key/scrollbar/pagination manual intent，也原子提交最新正文并保留权限卡。真正手动历史窗口继续保持锚点并展示“回到最新”。最小 DOM 回归先稳定复现空正文和旧正文均不触发读取，再固定两者只增加一次 canonical 读取、Logo 退出、权限卡保留，并验证重复权限不会产生第二次读取；另覆盖非手动 stale session 直接收敛和手动历史不自动覆盖。未新增 focus 监听、轮询、延时、缓存、队列、持久字段或依赖。
- 2026-09-03：继续修复流式会话中“回到最新”在向上、向下滚动接近底部时闪烁。最小容器测试先稳定红于向下滚动刚触底便把 following 从 `false` 改为 `true`，ACP DOM 测试同步红于按钮在 `scrollend` 前被卸载，证明问题不是 backdrop、transition 或单纯阈值，而是 manual 生命周期用连续几何事件提前退出；相邻自然 newer pagination 测试又确认 canonical branch restore 的幂等 `external-stop-scroll` 会覆盖同一次未结束的向下恢复意图。`ChatContainer` 现只在明确向下输入时登记恢复资格，并在原生 `scrollend` 到达、视口仍处于真实底部后统一恢复 following；manual 状态下分页/恢复重复调用 `stopScroll()` 不再调用底层 stop，也不清除待完成的用户恢复资格，明确向上输入、展开交互与首次脱离 following 仍会取消资格。容器与 ACP DOM 回归固定滚动期间按钮保持同一 DOM、滚动结束后稳定隐藏，并覆盖 canonical head 在滚动期间提交和自然分页到 head 的顺序。复用现有 prompt-kit、`use-stick-to-bottom`、DOM 锚点和分页窗口，不新增阈值、定时器、依赖、持久状态或第二状态机；scroll 热路径减少重复底层 stop，新增的 `scrollend` 处理仅在低频生命周期边界执行 O(1) 几何检查。
- 2026-09-03：最新 EXE 全程 trace 推翻了 `scrollend` 仍在反复切换按钮的假设：按钮可见的 10.3 秒内显隐只发生一次进入和一次退出，following 也只发生一次脱离和一次恢复；同期流式内容分批增高 360px，并记录 58 个 Long Animation Frame、累计约 7.5 秒。当时据此推断 viewport 级“回到最新”与 composer 位于持续增长的 `ChatContainer.Content`、通过 sticky footer 参与滚动和流式重排是主要来源；这确实偏离了 `use-stick-to-bottom` 将内容与 ScrollToBottom/ChatBox 分层的组件契约，但后续 EXE 复测已证明它不足以解释闪烁。`ConversationViewport` 现提取显式 footer slot，将其作为滚动内容之外的稳定 overlay sibling；滚动 viewport 继续占满可用高度，timeline 通过 footer block size 对应的局部 CSS 变量保留底部 padding，按钮仍锚定 composer 上方。单一 `ResizeObserver` 只观察 footer 尺寸并直接写 CSS 变量，不读取 scroll 几何、不提交 React state；最小 DOM 测试固定流式父更新期间 footer identity、滚动归属和 96px 动态占位。该调整作为独立布局改进保留，不再标记为闪烁 Bug 的完整根因或修复证据。
- 2026-09-03：上述把 sticky/footer 重排认定为闪烁完整根因的结论被真实 EXE 再次复测否定：完成 viewport overlay 分层后，流式期间仍会高频闪烁，因此该布局调整只能作为独立结构改进，不能作为 Bug 已修复证据。当前停止修改显隐、滚动和布局行为，转为补齐视觉层可观测性：按钮出现后建立独立 `probeId`，有界关联每个视觉采样窗口内的按钮/viewport/content/footer/composer/timeline element identity、rect、computed style、中心点 hit-test 栈、活动 animation、DOM mutation、ResizeObserver、transition/animation 事件及 React commit。visual frame 以 32ms 最小间隔采样，首次挂载记录 10 秒，滚动输入后续延长 4 秒，单 probe 最多 720 条；正常 diagnostics 关闭时不创建任何额外观察器或 RAF，日志不包含按钮文案和消息正文。最小测试先稳定红于缺少 visual probe 模块，随后固定完整 snapshot、统一 probe identity、commit/frame/stop 关联和隐私边界；真实根因仍以新一轮 EXE 日志为准。
- 2026-09-03：visual probe 的 1,589 条 EXE 现场记录确认“回到最新”并未反复显隐或更换 DOM；13 次 opacity transition 中 7 次被中途取消，变化固定发生在 `opacity: 1 ↔ 0.5`，并与 `disabled` 属性及 `pointer-events-none` 同步。根因属于正确的自动追页/手动回最新设计中状态所有权不完整：进入 240px newer-page 预取区后，自动分页抵达 canonical edge 会调用统一 handoff，但该后台路径错误写入只应表示用户点击命令的 `loadingLatest`；流式更新持续重建 newer edge，于是按钮被反复禁用、启用，历史完成会话则没有这条循环。实现将状态收窄为 `returnToLatestPending`：只有用户显式点击时置位，既有 canonical-head single-flight 及 trailing intent 全部结算后清除；自动追页不再写 UI pending，会话切换继续按 identity 清理。最小测试先稳定红于自动 handoff 把按钮禁用、同时证明手动点击测试为绿，修复后两条均为绿。删除已证伪的 `no-backdrop / no-transition` 注入模式，保留默认关闭、有界且不记录正文的 visual probe 供真实 EXE 验收。未新增依赖、请求、队列、持久字段或高频 React state，滚动与分页热路径复杂度不变。
- 2026-09-04：修复自动 canonical recovery 在固定预算耗尽后，后续 terminal/subscription snapshot 已覆盖缺口但页面仍停留旧正文，直到用户下滑才突然补齐并恢复 sibling auto-follow 的问题。现场 raw timeline、物化 index 与 session identity 证明后续 assistant、runtime JSON 和 sibling session 均已正确持久化，缺陷位于前端恢复事务：初始 catch-up 失败只保留 `hasNewerEvents`，没有登记可被后续 coverage 前进唤醒的 recovery intent；canonical window 已提交到 ACK 后、外层 Promise 尚未 settle 的微窗口又仍保留 `paginationDirection=newer`，此时到达的 live tail 会被误归为历史增量。实现复用既有 loss gate、coverage watermark 与 one-in-flight/one-trailing coordinator：失败出口保留自动 recovery intent；subscription envelope 与父级 `session` prop 共用同一 snapshot 唤醒判定，generation/revision/seq 任一权威水位前进且无明确 manual intent 时启动下一轮有界恢复，revision-only 前进不再依赖可见 seq 变化；成功 rejoin 在同一同步提交中安装 loaded window、结算 gate 与 pagination token，已有剩余 replay 则合并一个 trailing recovery。最小回归先固定 4 次 revision delta 后仍保留旧正文与 `requiresCatchUp`，再分别通过 subscription、prop 注入正文和 `newestSeq` 均不变的 covering revision snapshot，验证第二次 canonical 读取在 ACK 前保持 gate、ACK 后自动收敛；独立 microtask 竞态测试固定 ACK 与 handoff settle 之间的 tail 不再滞留。过度设计评审：未新增状态机、缓存、轮询、定时器或依赖。性能评审：水位比较为 O(1)，无前进的重复 snapshot 不读接口，每轮恢复继续受 4 页 / 2 秒预算及 single-flight 背压约束，不引入 N+1、无界队列或额外渲染订阅。
- 2026-08-04：修复贴底时展开 Activity 或隐藏运行上下文看起来向上生长的问题。原因不是 shadcn Collapsible 的展开方向，而是内容增高时贴底锁同步推进 `scrollTop`，使卡片底部保持不动。`ChatContainer` 提供 token 化 content-expansion context/hook：从 follow 状态打开首个会话内披露内容时暂停贴底并固定标题位置，详情沿正常文档流向下展开；多个披露同时打开时，关闭任意一个后若实际 viewport 已到底便立即恢复，否则等最后一个收起后在布局帧恢复。任何明确滚动或外部 `stopScroll()` 都会作废强制恢复 token，原本处于历史阅读位置的用户不会被拉回，但实际回到底部仍恢复 follow。Activity 专用 props 链已删除，Activity 与隐藏上下文直接消费共享 hook，只上报打开/收起、不自行操作滚动位置；回归测试覆盖两类组件接线、向下展开、部分收起即到底、最终收起恢复及用户滚动后不强制恢复。
- 2026-07-31 的前端递归 Agent 分组方案已于 2026-08-02 被 branch 持久化与右侧工作区破坏式替换。当前不再建立 `ownedChildKeys`、provider parent ID 索引、seq 生命周期窗口或 plan 内容血缘；Agent 工具、文字、TODO 与权限在规范化时写入唯一 owning branch，祖先仅获得 lifecycle/attention 聚合。
- 2026-06-11：ACP live 渲染链路增加性能保护：
  - 前端对 `textDelta / thoughtDelta / plan` live event 做短窗口合并，同一稳定 item 只保留最新帧；非文本类更新会先 flush pending 文本，再即时处理，确保 tool、permission、error、terminal 状态不被延迟。
  - 正在流式增长的 assistant 文本先用轻量纯文本草稿渲染，避免每个 chunk 都重新执行 `react-markdown + remark-gfm`；消息稳定后继续使用 Markdown。
  - `buildAcpTimeline` 之后增加 timeline item 引用稳定化，未变化的历史消息、Activity 摘要、工具审计行和 Agent link 复用旧对象引用，提升 memo 命中率。
  - Raw frames 折叠态只展示行摘要；展开单条 frame 时才做 JSON pretty print 和长段落换行。
  - 后端 timeline 写入由每次重写整份 `acp.timeline.jsonl` 改为 append `timelinePatch/upsert`，每 128 个 revision compact 一次；读取端支持 final snapshot 与 patch 混合文件。
  - 后端 live push 与 timeline patch 写盘都对 streaming timeline item 做约 75ms 合并；非 streaming item 前会 flush pending update，session 写入、shutdown 和 runtime drop 也会强制 flush。
- 2026-06-13：ACP 进行中打开系统提示、产物预览或工作流 sheet 时，前端暂停 text/thought/plan 的非关键 UI flush，只缓存同一稳定 item 最新帧；覆盖式交互关闭后用低优先级 flush 补齐。普通 streaming flush 窗口从 50ms 放宽到约 125ms，并对同一稳定 event key 的更新走原位替换路径，减少数组重排和 React 高优先级 commit。系统提示弹窗和工作流 sheet 关闭时不再解析大 prompt / workflow JSON，打开后再 memo 化计算。
- 2026-06-14：针对 ACP 流式期间打开抽屉、工作流 Sheet 和运行图动画卡顿继续收敛：
  - 前端把 live event flush 策略抽成纯函数测试；text/thought/plan 仍只做短窗口合并，但从固定定时器升级为 interaction-aware scheduler。覆盖式交互、消息列表用户滚动与 wheel 输入都会更新同一个 interaction quiet window，并取消已排队 timer，避免旧 flush 在用户滚动期间提交大段消息更新。权限、错误、工具终态和 session 终态仍走即时路径，且不会强制 flush cached streaming text；滚动门控不得用容器级 pointer/touch 起手事件覆盖所有按钮点击，避免与 shadcn/Radix composed refs 交互链路互相放大。
  - `ConversationRunPage` 稳定 workflow sheet 回调和 active session key；`GraphView` 将运行图布局与运行态分离，布局 memo key 只包含节点 id/order 与边 from/to/label，ACP selected session 或节点 running/current 刷新不再重复执行布局。
  - 清理 `[gb-acp]` 与 `[gb-session-follow]` 持续性 console 日志，删除上一轮运行图映射排障日志；当前只保留 `[gb-ui-error]` 的 `Maximum update depth exceeded` 专项诊断，输出 active element、最近 pointer 目标与截断 stack，避免流式期间日志本身制造主线程压力。
  - `Button` 恢复为标准 Radix/shadcn ref 转发组件，并直接使用 `@radix-ui/react-slot`；TooltipTrigger / CollapsibleTrigger / PopoverTrigger / DialogTrigger / SheetTrigger / DropdownMenuTrigger / AlertDialogTrigger / SelectTrigger 等项目封装的 Radix trigger wrapper 也统一改为 `forwardRef`，让所有 `asChild` 触发器共享稳定 DOM ref，避免 composed refs 在消息流重绘和窗口按钮交互后进入重复 setRef。
  - 根据 `[gb-ui-error]` component stack 进一步定位到 `PromptInputAction`，composer 输入框工具栏改为无状态原生 title 提示，不再在流式热路径里使用会维护 trigger ref state 的 Radix TooltipTrigger。
  - 针对后续诊断栈暴露的 `AcpSessionConfigBar -> SelectTrigger`，不再继续单点替换组件，而是按热/冷路径统一收敛：新增 ACP session config view model/signature 工具，模型与权限配置栏只消费归一化后的配置签名；`ACPChatDialog` 的配置写入和选择 handler 改为稳定 callback，内部读取最新 session ref。普通 text/thought/plan live event 不再携带模型/权限下拉框重渲染，只有配置签名、会话 scope 或 handler 实际变化时才更新配置栏。
  - 修复 completed session timeline/events cache 只按路径命中的问题：cache 条目记录文件长度与修改时间，文件尾部 flush、追加或 compact 后下一次 `get_acp_session` 必须重新解析，避免 snapshot 已 completed 但 timeline 最后一批消息稍后写入时，前端切换会话仍拿到缺尾部消息的旧缓存。
  - 消息滚动容器的 `scroll` 事件不再同步读取布局；滚动输入只排一个 rAF，贴底状态、历史分页判断和 `isAtBottom` 更新在 rAF 中合并完成。timeline 更新后的自动贴底也受 interaction quiet window 约束，用户滚动期间不抢 `scrollTop`，降低流式输出时滚动区 forced reflow 风险。
  - `toolCall / toolCallUpdate` 的非终态 live event 纳入同一套低优先级 flush 策略：按 attempt/session + `toolCallId` 在 pending 队列中合并，保留工具开始帧的 title/content/raw input，并以最新 running update 覆盖状态；completed/failed/error/cancelled 等工具终态仍即时应用，应用前吸收同工具 pending 帧，避免工具卡片参数丢失或终态后再被旧 running 帧打断。
  - Conversation run 订阅层新增统一 live update plan：当用户手动查看已完成 session 时，后台运行 session 的普通 text/thought/tool live event 不再每 120ms 拉整份 `ConversationRunVm` 和 sidebar；已知后台 session 的非终态 session snapshot 只更新 session identity / activeSessions 等轻量运行态，terminal、权限、暂停、等待输入和未知 session 锚点仍触发完整 run refresh，保证会话切换、节点完成和暂停继续能力不受影响。
  - React Flow 自定义 node / edge 组件改为 memo 化；运行图边恢复 success/failure 等本地化 label 和 CSS 虚线流动效果，但不得通过 React state、JS timer 或布局重算驱动画布动画。running edge 保留更高亮的流动节奏，running node halo 改为 opacity/transform 伪元素动画，降低 paint 压力。
  - 新增前端回归测试覆盖 flush 暂停语义、非终态工具事件低优先级合并、滚动 quiet window 自动贴底门控、运行图拓扑签名、ACP 配置 view model 签名、UI 错误诊断匹配规则、Button/ref trigger 转发契约与 PromptInputAction title 派生；新增 Rust 回归测试覆盖 completed timeline/events 文件变化后的 cache 失效，防止后续把流式 token 或工具运行态更新重新绑回布局、配置下拉、覆盖式交互动画、持续性 console 日志、不稳定 composed refs 或过期后端 timeline cache。
- 2026-06-29：ACP permission 决策终态已收敛到后端 permission lifecycle：`write_permission_response_if_pending` 写入 Allow / Reject response signal 时同步 upsert `permissionRequest(status=selected)` 到 V2 timeline / legacy events，并保留原 pending event 的 session、tool call、title 和 options；runtime waiter 消费 response 后再次确认终态落盘，再清理 signal 文件。这样正常授权、停止取消和非活跃 fallback 共用同一套 pending -> terminal 状态机，重进完成会话不会把历史 pending 权限重新弹出。
- 2026-06-29：继续补齐 ACP permission live runtime 内存事实源：live waiter 消费 response 后不再只改磁盘 timeline，而是构造继承 pending 上下文的 terminal permission event 并走 runtime `persist_event`，让内存 `timeline_items` 同步替换旧 pending。这样 shutdown / final compact 不会把 command/helper 写入的 selected 终态覆盖回 pending。
- 2026-07-06：修复 AI-DYNAMIC 内部节点 continue ACP session 首屏可能显示 partial 会话的问题。会话式运行页不再允许 `runtimeActive` 绕过 `allowEventOnlySessionShell=false` 创建 event-only live shell；初始化阶段即使已有 timeline/thought live event，只要 base session 尚未具备 `systemPromptAppend`、模型/权限配置和 sasuke 用户消息，就继续展示 loading 并等待 session-ready snapshot，避免出现“无 session id / 无系统提示 / 无权限模型”的运行中空壳。新增前端单元测试覆盖 runtime active 禁用 event-only fallback、partial base session loading、ready session 才可展示等策略。
- 2026-07-08：统一 runtime active 但 ACP session 详情暂为空的主区域空态。会话式运行页选中下一内部节点后，若 leaf lifecycle 已 active 但 `selectedSession/effective session` 尚未 hydrate，继续展示通用 `加载中...`，不把 `conversation.runtime.runtimeActive` 等内部状态 key 暴露给用户，也不误显示为上一会话的“拉起下一节点中”。新增 `resolveAcpSessionShellState` 单元测试固化该加载态。
- 2026-06-29：ACP elicitation 响应终态同步收敛到后端 elicitation lifecycle：`write_elicitation_response` 写入 accept / decline response signal 时同步 upsert `elicitationResponse(status=completed)` 到 V2 timeline / legacy events；runtime waiter 消费响应后再次确认终态落盘，再清理 signal 文件。停止、关闭应用或非活跃 fallback 只需要写 response signal，不能再绕过 lifecycle 单独维护 timeline，避免重进页面恢复旧的 `elicitationRequest(pending)`。
- 2026-07-05：ACP permission pending 的 composer 展示收敛为运行态锁定，而不是替换输入框：前端继续渲染 prompt-kit `PromptInput`，textarea、附件和发送按钮保持禁用，placeholder 使用 runtime controlled 文案，hint 显示“等待权限决策”，停止入口继续可用。`deriveAcpRuntimeComposerState` 将 pending permission 视为 session active，确保关闭应用后从持久化 pending permission 恢复时仍不可输入，并通过前端 composer state 矩阵测试固化。
- 2026-07-05：修复关闭客户端后已关闭 ACP session 仍恢复 pending permission 的问题：connection manager 在 bounded `session/close` 前统一取消 pending permission / elicitation，并在 close 后把 snapshot/session metadata 写为 `cancelled`；会话 view model 读取 active snapshot 时增加 raw log 熔断，只在 `acp.raw.jsonl` 中存在同一 JSON-RPC id 的 `session/close` request 与成功 result 时将旧 snapshot 收敛为 `cancelled`，同时 terminal session 不再返回 pending permission。新增 Rust 单元测试覆盖关闭边界结算、raw close 配对检测和 active snapshot 熔断，防止已关闭 session 重启后恢复成可点击权限申请。
- 2026-07-05：继续修正 raw close 熔断边界：如果 `session/close` 成功后又有新的 `session/load` / `session/prompt`，说明同一 ACP session 已被恢复到新的运行态，旧 close 不得再次把 running snapshot 熔断为 cancelled。权限响应命令也不再因为 stale snapshot 为 terminal 就删除刚写出的 response signal，`acp.permission-response.*.json` 仍由 live waiter 消费后清理，避免用户点击允许后 runtime 永远等不到 response 而停在工具调用中。
- 2026-07-08：新 UI 会话页补齐 run-level runtime 业务异常横幅。`workflow_control_limit_exceeded` / `max_rounds_exceeded` 等控制流终局失败不再依赖 ACP session diagnostics 或 `error-blocked` pause reason；后端 `ConversationRunVm.runtimeErrorMessage` 复用旧工作台的 canonical control failure 解析结果，前端 ACP 顶部错误横幅直接展示该消息，但不把 composer 切到锁定型 `runtime-error`，避免已完成失败会话无法继续追问。新增 Rust VM 单元测试覆盖 round 超限消息暴露，新增前端单元测试覆盖 runtime error 优先进入会话横幅。
- 2026-07-09：补齐 workflow control 终局失败的 live refresh。`max_rounds` / `max_attempts` 等超限路径在持久化 `workflow_control_limit_exceeded` 与 terminal run state 后，统一发送 `RunCompleted(Failure)` lifecycle，让会话页立即刷新 run VM 和 sidebar，从 `launching-next-node` 收敛到错误横幅；新增 Rust 单元测试覆盖控制失败落盘后 lifecycle bus 发出 run completed 事件。
- 2026-07-22：ACP assistant 文本流式呈现从“累计快照直接替换 + 活跃期纯文本草稿”升级为“事实状态与呈现状态分层 + 实时 Markdown”：
  - 保留后端约 75ms live snapshot、前端 interaction-aware 事件合并、稳定 event id、乱序保护和 timeline 最终一致性；视觉层新增局部 presentation controller，以 canonical target、visible offset 和速率 carry 管理唯一活跃尾部。约 32ms 的有界帧按积压量自适应推进可见前缀，两次 snapshot 之间继续消费队列，避免视觉速度被 125ms flush 和偶发大批次直接支配。
  - 取消“完整 snapshot 一次性入 DOM + opacity 字符 stagger”方案，并进一步移除 Streamdown 全字符动画插件。可见 offset 已负责逐步出现，重复的 opacity/stagger 会在 block 重建时重播历史字符，长消息还会因累计 delay 形成顶部闪烁与底部透明占位。现在 Streamdown 只解析当前可见 Markdown 前缀，不再维护第二套字符可见状态。
  - 现有 prompt-kit `Markdown` copy-in 内部继续使用模块化 `streamdown` 核心，流式前缀通过 syntax guard 跳过纯 `**`/反引号后缀、未完成链接地址等无可见进度区间，再由 streaming mode 修复未闭合 Markdown。曾经进入流式状态的组件在完成后关闭 incomplete repair，但保持同一 block renderer DOM，避免 terminal 时再整棵切换 static；重新加载的历史消息仍可直接 static。历史消息没有 presentation timer，业务页面仍不直接消费第三方 renderer API。
  - `thoughtDelta` 删除纯文本展示旁路，与 assistant `textDelta` 共用实时 Markdown 和同一呈现节奏；活跃 key 改为按最新 `endedSeq/seq` 生命周期事件判定，tool/plan/permission 到达后旧消息不再被误标为 streaming。真实 ACP 数据确认 Claude Code 的独立 thought chunk 不携带换行，后端 accumulator 现对前后均为完整 strong block 的 chunk 写入 `\n\n`，token 级 thought chunk 保持连续。前端不重写旧 timeline canonical。
  - active thought 折叠期间使用 prompt-kit `ChainOfThoughtContent(preserveMount)` 对 Radix Content `forceMount`，closed 状态只做 `hidden`，Markdown presentation timer 和 visible offset 继续存在；重新展开复用同一实例。thought 结束后不再 forceMount，历史折叠内容恢复按需挂载。
  - 没有启用 Streamdown 的 Shiki、Mermaid、KaTeX 插件；构建验证使用模块化 2.x 版本，避免 AionUi 所用旧版 1.x 把完整高亮语言包、图表与公式资源带入聊天主包。
  - 删除活跃 assistant 的纯文本 `StreamingTextDraft` 分支，同一 `Markdown` 组件通过 `streaming` 属性完成流式/静态切换，减少组件树替换和完成态布局跳变。
  - 新增接口级单元测试覆盖静态 Markdown、可见前缀不预布局后续 block、无 opacity 动画节点、未闭合语法门控、积压自适应推进、thought 活跃流选择、完整 thought block 分段、token chunk 连续累计，以及 active thought closed 时保留挂载且不占布局。
- 2026-07-27：统一修复 ACP 斜杠命令选择后的 composer 焦点与标签解除生命周期。`useSlashCommandController` 在命令文本提交并关闭菜单后发出共享输入焦点请求，快速对话与会话详情分别通过原生 textarea ref 和 prompt-kit `PromptInputTextarea` 外部 ref，在下一动画帧恢复焦点并把 selection 放到当前可见文本末尾；若输入框已禁用则不抢焦点。刚选中的标准 `/${name} ` 标签按第一次 Backspace 时由 controller 明确转换为普通 `/${name}` 文本并恢复末尾光标，第二次 Backspace 再走 textarea 原生删除，不再依赖浏览器焦点后的偶然 selection。鼠标与键盘选择继续复用同一个 `selectByIndex`，两类 composer 复用同一状态机，不增加 DOM 查询或页面专用分支。`slash-command.test.ts` 固化延迟聚焦、末尾光标、disabled 门控、第一次解除标签和后续原生退格边界，生产构建与真实前端交互验证通过。
- 2026-07-30：修正 ACP attempt Token 展示口径。新增 `AcpPromptTokenUsage` 与 `AcpAttemptTokenTotals`，把 Provider 每轮 `PromptResponse.usage` 作为增量累加到当前 attempt totals；同一 attempt 内停止后继续、runtime 重建、节点完成后追问、Direct 多轮均从同一累计状态恢复或续算，新 attempt 即使 resume 相同 Provider session 也重新计数。snapshot 新增 `attemptInputTokens / attemptOutputTokens / attemptCachedReadTokens / attemptCachedWriteTokens / attemptTotalTokens`，会话 VM 只把这些累计字段投影到 `AcpUsageVm`；原 `inputTokens / outputTokens / cached* / totalTokens` 暂时保留最后一轮值，指标上报链路不在本次调整范围。Rust 单元测试固化“后一轮非缓存输入下降但 attempt 累计继续增长”、runtime 重建后续算、缺少 total 时按分项求和，以及 VM 不得投影最后一轮快照。
- 2026-07-31：补齐 ACP attempt Token 的崩溃一致性。attempt 新增 durable WAL `acp.prompt-usage.jsonl`，按 `attemptBaseline → promptStarted → promptCompleted` 记录每轮事务；完成记录先于 snapshot checkpoint 落盘，并使用稳定 turn ID 幂等去重。应用若在 raw 响应落盘后、snapshot 更新前崩溃，重开时会把未完成 start 与 `acp.raw.jsonl` 成功响应配对并补写 completion；raw 已滚动时只接纳 baseline 之后的数据，禁止把已有累计降低。snapshot 调整为可重建物化缓存，旧 attempt 首次启用 journal 时以已有字段建立不下降 baseline。durable JSONL 追加在锁内执行 `flush + sync_data`，同时修复崩溃留下的半行尾部。接口级测试覆盖 checkpoint 崩溃窗口、二次恢复幂等、复用 RPC ID 的 task-118 序列、旧 snapshot 迁移 baseline 与 torn-tail 修复。
- 2026-08-02：完成 Agent 分支会话与语义分页重构：
  - 根会话和 Agent 使用统一 branch 查询接口，根 timeline 只保留根语义和 Agent link，Agent timeline 独立写入 `agents/<AgentExecutionId>/timeline.jsonl`；`acp.agents.jsonl` 和 snapshot 作为关系、生命周期、统计与 cursor 物化索引。
  - Agent projection 改为严格直属关系：根只返回顶层 Agent，Agent branch 只返回 `parentAgentExecutionId` 指向自身的孩子，全部后代不再平铺。Plan projection 使用显式 `branch/unscoped` ownership；存在 Agent execution 时根会话 fail-closed 排除 session-wide 聚合 Todo。
  - 分页单位从规范事件数改为语义块。连续工具/思考只形成一个活动摘要，Agent 子分支事件不影响父分支 `hasOlder`。根和 Agent 共用 cursor、有限三页 event buffer、原生滚动与真实 DOM item 锚点补偿。
  - Activity 详情与会话分页分离。折叠时不请求详情；展开时按明确 start/end seq 顺序扫描轻量 header，仅保留最近 `limit + 1` 个候选并只反序列化所选事件；工具 raw input/output 使用独立单条查询。已决 permission 不进入审计详情。
  - 前端 `ConversationEventRouter` 只建立一个 Tauri 订阅，并按 branch key 投影轻量 status/attention snapshot；只有激活 Tab 挂载完整 `ConversationViewport`。事件窗口、滚动/贴底状态和后端 timeline/Agent index 均使用有限 LRU，未变化 timeline item 复用对象引用。
  - 开发阶段旧 Agent 嵌套分组、launch anchor 和前端 Claude metadata 消费已删除。`acp.events.jsonl` 只作为一次性迁移来源保留；迁移后 runtime、permission、elicitation 与 storage query 只读写 branch timeline。

- 2026-08-15：收紧 workflow session auto-follow 的 Runtime 控制权边界。实时 ACP 事件选择、完整 run 刷新以及会话底部恢复跟随统一读取既有 `lifecycle.control.mode`；只有当前选中 session 与目标 active session 都是 `runtime-controlled` 时，才允许从已完成节点自动切到后续节点。已完成节点中的用户普通追问属于 `non-runtime-controlled`，追问进入 ACP accepted 边界后以 `manual-follow-up` 原因通过既有 runtime-control cursor 的 prepare/commit + CAS 事务同步持久化到 snapshot/session；未接受不切换、重复追问幂等、迟到提交不覆盖 stop/continue，ViewModel 不再因 orchestrated attempt 缺少 cursor 而误回退为 Runtime 控制。追问结束后继续停留在当前 session，session 内消息贴底不受影响。Rust 回归覆盖持久化、accepted 边界、幂等/CAS 和 ViewModel 投影；前端接口回归覆盖 dev 追问结束时 clean 仍运行但不得抢焦点、目标为 NonRuntime 时不得切换，以及正常 Runtime 节点推进继续自动跟随。
- 2026-08-15：完成会话导航即时切换与品牌加载态。删除“等待目标 run 与文件变更预取后继续显示旧会话”的呈现策略：点击侧边栏后立即提交完整 run locator；同一 run 内选择 session 时立即提交 `selectedSessionKey` 并清除旧 session 正文投影。目标 run/session 内容异步加载期间统一展示引用 `/logo.svg` 的 sasuke 呼吸加载组件，reduced-motion 下静态显示。ACP 首屏门禁收敛为明确的 `loading / success / error` 正文请求状态：首次 `getAcpSession` 完成前，摘要、缓存、established session 和 live shell 都不得跳过加载态；请求成功后才直接进入 timeline 或空态，失败进入错误态。turn 文件变更的 12 项有界预取移出导航关键路径，页面快照提交后后台执行。前端单元测试固化跨 run 即时 loading、session locator 即时切换、迟到响应拒绝、正文请求未完成时所有摘要/壳投影不得提前显示、请求失败错误态和 Logo 真源。
- 2026-08-15：补齐会话返回访问的 stale-while-revalidate 缓存语义。App 层新增最多 12 项的轻量 `ConversationRunVm` LRU，导航时按完整 run identity 同步恢复；ACP 现有 12 项 resource LRU 新增只能由成功正文查询写入的 `contentHydrated` 来源标记，summary/session/live 投影不得冒充正文命中。run 和当前 event window 同时命中时立即恢复完整会话，后台继续执行原有请求；背景刷新失败不遮蔽已有内容。冷命中的 run 与 ACP 两阶段加载由 `ConversationRunPage` 的页面级门禁合并为一个连续品牌 Logo，不再暴露会话框架内第二个加载态。单元测试固化 run 完整 identity、LRU 淘汰、正文 hydrate 来源、缓存命中直接展示和旧 session 状态不得跨 identity 复用。
- 2026-08-15：现场验证发现侧边栏 task/run 点击仍直接调用 `setConversationPage`，绕过上述缓存感知导航边界，导致 run LRU 虽已写入却永远不恢复。两个侧边栏入口现统一调用 `onSelectConversation`，接口回归测试禁止再出现直接写 run page 的旁路。
- 2026-08-15：消除会话内容出现后“先显示 timeline 顶部、再瞬间跳到底部”的首屏闪动。原因是第三方 `scrollToBottom(animation=instant)` 仍经过内部调度，不承诺在首次 paint 前完成。prompt-kit `ChatContainer` 包装层现在首次挂载且 follow 开启时，于 `useLayoutEffect` 内只执行一次同步底部对齐；ACP 业务层的历史锚点恢复、分页补偿和后续贴底状态机保持不变。单元测试固化 `scrollHeight - clientHeight` 的首屏同步对齐接口。
- 2026-08-15：收紧会话详情 composer 底部功能区。复用既有 prompt-kit `PromptInputActions`、shadcn Button 与 ACP 配置触发器，仅新增共享布局常量，将操作行顶部间隔和上下内边距由 8px 收紧为 4px，并把模型、权限及静态配置展示统一为 32px；停止、继续和发送继续使用同一 32px 基线。快速对话保留原 36px 触控规格和容器查询换行策略，输入区两行默认高度、内容自增高与用户拖拽高度均不改变。DOM/布局契约固定两类 composer 的尺寸边界；本次没有新增状态、监听、测量或渲染订阅，单次布局仍为 CSS O(1)。
- 2026-08-16：修复 Activity 审计工具行在超长命令或 Windows 路径下覆盖右侧状态的问题。根因是 prompt-kit `Tool` 的工具名节点使用不可收缩布局，provider 未提供独立 summary 时整段 title 会越过主内容边界。复用现有 copy-in 组件，将工具触发行统一为 `minmax(0, 1fr) + auto` 两列 grid，图标/工具名/摘要位于可收缩且裁剪的主列，状态与展开按钮位于稳定 auto 列；工具名与摘要单行截断，完整参数继续只在展开详情中安全断行。新增 DOM 布局契约测试覆盖“只有超长 title”和“工具名 + 超长 summary”两类输入；未增加状态、测量、监听或渲染订阅，布局仍由 CSS O(1) 完成。
- 2026-08-16：完成 Workflow / AUTO `continue session` 的单写入 attempt 约束。后端复用 workflow snapshot + round trace、dynamic graph `continueFromNodeId` 和 durable worker-ref 投影最终 session owner，链式 continue 直达末端 attempt；旧 attempt 的 composer VM 统一返回 `session-superseded + supersededBy`，prompt preflight 使用同一 resolver 返回结构化 `conversation.session-superseded`，Direct 与 `session:new` 不受影响。前端复用 prompt-kit composer，在旧 attempt 输入区展示带真实 `<a href>` 的只读提示，链接复用 Markdown 文件链接的主题链接色与紧凑字号，并禁用 textarea、拖放、附件、发送和停止；新增普通与 AUTO dynamic 完整 leaf deep link，点击后 URL、`selectedSessionKey`、Session Switcher 与正文走同一切换事务。VM 构建仅线性读取轻量 runtime metadata，提交前只解析当前 round，不扫描 timeline、不新增缓存/轮询/队列。Rust 回归覆盖 Workflow 链式 owner、AUTO 显式 continue source 与 stale submit 防线；前端回归覆盖 composer 锁定、链接点击与主题样式、相同 attemptId 不串 leaf 及两类路由 round-trip。
- 2026-08-18：修复 ACP 大量 session update 积压时停止长期停留在“正在停止”的控制面饥饿问题。`session/prompt` 首次观察到 `CancelRequested` 时先启动统一 10 秒 deadline，再发送 cancel 并按 64 帧 / 25ms 时间片有界消费可用事件；每批后优先检查 deadline 与 prompt response，不再等待队列完全清空。provider response 后的 route watermark / terminal quiet drain 复用同一剩余 deadline；到期沿用现有 cancelled 终态与 attempt session 隔离，不要求刷完未消费的本轮增量，迟到响应不得恢复 lifecycle。未取消的普通流式处理保持原路径，不新增状态机、缓存、轮询或共享 provider 进程 kill。Rust 回归固定大积压仍按批让权、取消总上限为 10 秒、terminal route 超时归一化为取消终态。
- 2026-08-19：补齐 lifecycle-only 停止终态的前端 Activity 收敛。停止控制面不再等待或查询正文；前端以已按 revision 合并的 lifecycle 计算 Activity 有效状态，`idle + terminal latestTurnStatus` 立即归档旧工具活动，避免旧 `running` 正文快照持续显示“正在调用”。本地新 prompt 接纳期间仍优先保持运行投影，较低 revision 的迟到终态不能通过既有 lifecycle 合并逻辑覆盖新 turn。新增单元测试覆盖 stale body + terminal lifecycle 与新 prompt admission 两个边界；不新增轮询、缓存、timeline 解析或状态机。
- 2026-08-19：修复 `runtime-abnormal` 暂停时结构化错误只进日志、不进会话横幅的问题。根因是消费链断裂：orchestrator 已把 `RuntimeErrorInfo` 写入 `run_paused` 事件的 `controlFailure.runtimeError`，但 Conversation VM 的 `runtime_error_message` 只处理 `error-blocked` 与终态 failure，非终态暂停返回空；前端会话壳随后回落到 provider-neutral 的“检查 Agent adapter”提示。VM 仅在 canonical `run.json` 为 `paused/runtime-abnormal` 时，读取与 `updatedAt` 对齐的当前 `run_paused` 审计事件，投影结构化 `runtimeError`（code/params/diagnostic）与 `runtimeErrorMessage`；恢复、其它暂停和终态不继承历史错误。事件读取限制为末尾 256 KiB，避免会话 VM 反复扫描和解析完整历史；未新增持久字段、缓存、轮询或状态机。前端横幅与初始化失败态优先显示该错误，并按 `acp.session-config-value-unavailable` 错误码本地化：`category=thought_level` 且无可选值时提示当前模型不支持思考强度，有可选值时列出可选档位，其它 category 使用通用配置项文案。Rust 回归覆盖当前暂停错误投影、恢复后清空与当前暂停事件不回退历史错误；前端回归覆盖三类文案映射与未知错误码回落。
- 2026-09-10：修复 Direct 续问期间旧 `runtime-abnormal` Run 错误重新出现的问题。根因是 ACP 横幅在收到 `latestTurnStatus=none/running/cancelled` 时仍把 Run-level error 当作 fallback；当前 ACP snapshot 已清空 `turnError` 且产生新 turn 后，旧的“Reconnecting... 5/5”仍被显示。横幅现以当前 ACP lifecycle 为权威：只有当前 turn `failed` 且没有自身错误时才使用 Run fallback；新 turn 运行、取消或完成都清除旧错误。新增前端回归固定运行和取消两种状态，不增加状态、请求、缓存或历史扫描。
- 2026-08-21：收紧 ACP canonical timeline 回归契约：permission 响应测试同时写入 pending signal 与权威 pending timeline item，固定 CAS 结算不能被 terminal snapshot 旁路；Agent launch 在索引中明确归类为独立 standalone 语义块，测试显式写入 `_meta.agentTranscript.agentLaunch = true`，固定两个 Agent link 各计一块且子分支 500 条历史不进入父分页。workspace identity 迁移断言改按路径组件区分 legacy slug 与 canonical `slug--hash` sibling，避免字符串前缀误报；Windows 旧索引中的非 canonical 大小写 ID 继续 fail-closed，不借用当前 workspace 展示名。复用现有索引与路径类型，不增加状态、缓存、扫描或 I/O。
- 2026-08-25：修复 Direct 首轮后及其他 `non-runtime-controlled` 工作树会话输入 `/` 不再出现命令菜单的问题。根因不是 lifecycle 门控，而是会话详情按实际 `providerCwd` 查询 `{agentType, projectId}` 目录时，把仅由 Doctor 预热的持久缓存误当成完整事实源；新工作树没有缓存便直接返回空。后端查询现把缓存限定为 ACP 原生命令缓存，在锁内只克隆快照、锁外按当前物理 workspace 扫描全局/项目 Skill；缓存未命中也返回瞬时 skill-only 目录，不复制全局 Skill、不写工作树缓存，也不把可能分叉的工作树归一回主目录。前端以当前 session 的 `availableCommands` 作为 ACP 原生命令投影，置于查询目录之前按名称不区分大小写去重，并在原始 JSON 边界过滤非法条目。Rust 回归覆盖工作树缓存未命中扫描及原生命令同名优先级；前端回归覆盖 `non-runtime-controlled + providerCwd + ACP 命令 + 工作树 Skill` 下输入 `/` 展示两类条目及异常载荷。过度设计评审：复用现有目录扫描、session VM 和 slash controller，未新增缓存、监听器、状态机或依赖。性能评审：每次查询最多投影 512 条元数据，扫描不持有共享锁；输入热路径只合并已加载的两个有界数组，未增加 timeline 订阅或消息流重渲染。
- 2026-08-25：完成会话分支选择、消息详细时间和 workspace 路径提示。快速对话与主工作区会话详情复用 shadcn Command/Popover 和源码管理 Git runner，新增轻量 branch snapshot 与 typed `switch / create-and-switch`；revision 校验只约束真实 checkout mutation，选择阶段只保留分支名。用户点击发送后，后端在既有 repository/workspace 锁内校验当前分支并读取该时刻最新 HEAD，再固化为 Run 的 `forkCommit`，因此同分支发送前新增 commit 合法、实际换分支则结构化失败；新 Worktree 从该固定提交创建 Run 专属分支，已创建 Run Worktree 只读。Agent 复制按钮右侧复用 timestamp 显示 `HH:MM + 相对时间 + MM/DD[/YYYY]`，头像下方仍为 `HH:MM`；普通与置顶 workspace 标题 Tooltip 展示 absolute path。用户验证发现路由重挂时 branch picker 重复闪回 loading、可编辑长分支缺少 Tooltip 后，补充 App 会话期 24 项有界 snapshot LRU，以 `projectId + workspacePath` 隔离并采用同步恢复、后台校准；可编辑触发器复用溢出测量，仅真实截断时展示全称。后续现场反馈继续补齐契约：Popover 与触发器默认左对齐，分支列表独立滚动且创建 action 固定底部；session VM 将 `worktreePath + worktreeBranch` 作为同一 workspace catalog 投影，AI-DYNAMIC child 不再错误复用外层 Run branch；会话信息栏分支图标与名称显式使用和累计时间数值一致的 `foreground/80` 主题层级，不再继承浅灰辅助文案色。接口回归覆盖公共 API facade、桌面 invoke、浏览器 preview、branch picker mutation/只读/锁定/缓存首帧/截断 Tooltip/固定 action/session 颜色、发送时 fork point、dynamic workspace path+branch、时间格式、消息 action row 和两类 sidebar 标题。过度设计评审：除解决重复 I/O 与视觉闪动所需的有界轻量 snapshot Store 外，未新增持久字段、全局状态机、队列、轮询、消息定时器或 Run identity；Store 不复制完整源码管理状态，也不成为 Git 事实源，dynamic branch 复用既有 catalog 字段。性能评审：picker 只读取 status 计数、refs、HEAD、worktree 占用与 revision，单次复杂度随本地分支数线性增长；不加载 numstat/history/diff，锁只覆盖 checkout mutation CAS，或发送时的当前分支校验与 HEAD 读取；缓存命中避免重复 loading且后台校准仅更新选择器作用域；session branch 与 path 在既有 O(nodes + workspaces) VM 构建扫描中一并解析，不新增 Git I/O；颜色修正只改变静态 utility class，不增加渲染、订阅或 I/O。发送时 fork point 只读取 Git operation marker、`symbolic-ref` 与 `HEAD`，不扫描工作区文件、refs 或 worktrees。

本阶段仍未完成：

- 2026-09-10 补充修复：task-056 的横幅在结束时消失、追问运行时重新出现，源于错误分类要求 submitTarget=acp-prompt；执行时切换为 queue-prompt 后旧 Run 错误被误升为高优先级 runtimeError。删除该依赖，仅依据 Direct、控制权、异常暂停及 blockingError 分类。task-058 为 process-interrupted，没有同类 Run 错误投影，作为对照场景。新增分类与横幅组合回归覆盖完成、追问、停止、取消，以及阻塞错误和 Runtime 控制隔离。用户要求不编译、不运行测试，本次回归代码尚未执行；未启动验证服务。复用既有生命周期，O(1) 判断，不新增依赖、状态、I/O、缓存或队列。

- SQLite 辅助检索尚未开始落地。

该方案兼顾了：

- 新 UI 的流式体验
- 当前 runtime 文件体系
- 后续跨 session 搜索
- 对旧数据的可兼容性
- 开发阶段允许破坏式升级但不丢历史会话的需求
