# sasuke 新会话式 UI 完整方案

## Context

当前 sasuke 的主界面是工作台式任务编排，入口集中在 `web/src/pages/TaskListPage.tsx`、`web/src/pages/WorkflowPage.tsx`、`web/src/pages/RoundDetailPage.tsx`，用户需要理解 task / workflow / run / round / node / attempt 后才能进入会话。新方案的目标是新增一个以 chat bot 为主心智的会话式 UI，让用户从“输入需求 → 校验配置 → 创建 task + run → 进入当前 session 对话”开始，同时保留现有工作台 UI 并通过显式 toggle 切换。

本方案按用户澄清后的决策执行：默认进入会话模式、toggle 选择持久化、旧 task/run 仍显示但完整体验以新会话为主、左侧是会话列表、右侧主区域永远展示当前具体 session 对话，round/node/attempt 只作为 session 切换层级。

## Product Shape

### App shell

- 根路径统一进入会话形态；Workbench/Conversation toggle 不再作为产品入口，旧工作台只保留显式 deep link 供开发观察。
- 会话应用壳采用左侧导航、中间主工作区、右侧辅助工作区三段式结构。中间区域承载当前一级页面，右侧使用通用多 Tab 资源模型。
- 右侧首个资源为 Agent 分支会话；文件、Diff、产物和日志只预留资源联合类型，本阶段不实现具体页面。
- 三段式布局使用 shadcn `Resizable` copy-in 与 `react-resizable-panels`，紧凑宽度使用同一资源内容的 shadcn Sheet；不维护第二套页面和手写 mousemove 拖拽。

### Conversation sidebar

左侧栏结构：

1. 顶部：品牌区 + Workbench/Conversation toggle。
2. 快捷入口：快速对话、搜索、Agent 管理、上下文管理、运行模式管理。
3. 置顶区：显示用户置顶的会话，支持手动拖拽排序；置顶会话仍在原工作空间下重复显示。
4. 工作空间/项目区：支持多个 workspace 并行，每个 workspace 下列出 task/conversation。

会话行只显示三类信息：

- 标题
- 状态小圆点
- 相对时间

状态与排序规则：

- 普通工作空间会话统一按 task `lastActivityAt` 倒序；该值取会话元数据活动时间与最近更新 run 的 `updatedAt` 最大值，Direct、Workflow、AUTO 不再使用不同排序口径。
- run 子列表按 `updatedAt` 倒序，最近有活动的 run 同时作为 task 的 `latestRun`，确保状态、展示时间和排序一致。
- 后端比较时间前统一解析 Unix 秒、RFC 3339 和历史本地日期时间格式，不直接比较时间字符串。
- 运行中不显示时间；已结束才显示分/时/天/周/月/年。
- 状态使用最新 run 的最终状态，复用现有 run status/outcome：成功为绿，失败/异常/停止为红，暂停为黄。

### Conversation home

- 新会话输入框支持任意需求文本。
- 新会话创建前可选择 workspace、运行模式 AUTO / WORKFLOW。
- AUTO 模式下显示 agent 与模型选择；agent 来自 Agent 管理，模型来自 agent 支持模型列表。
- 首次进入会话主页时，如果运行模式配置不完整，优先引导用户配置运行模式。

创建规则：

- 标题 = 输入内容首行前 12 个 Unicode 字符；无文字时使用 i18n `New Task / 新任务`。
- 自动标题只在第一次创建时生成；生成完成后用户才能手动改名，手动标题不再被自动覆盖。
- description 为空，requirement/content 是完整输入。
- 提交前先校验 workspace、运行模式、agent、模型、AUTO 设置、workflow 模板有效性。
- 校验通过：创建 task + run，并进入该 run 的当前 session。
- 校验失败：展示缺失项和恢复路径，例如跳转 Run Mode Management、Agent 管理、上下文管理或选择模型。
- 初始附件绑定 task，作为原始 task 输入的一部分；重跑复用这些附件。
- 继续会话时新增附件进入当前 ACP session，可先通过 ACP 传给 agent，不额外做 task 级持久化。
- task/run 创建后不可再移动 workspace。

### Runtime window

- 左侧选中最小单位是 run。
- 右侧主区域永远显示当前/选中 session 的具体对话，不做完整 run 时间线主视图。
- session switcher 放在会话窗口顶部信息展示区，显示路径如 `round-001/dev/attempt-002`。
- 点击 switcher 后展示 round → node → attempt/session 的层级树，用户可切换具体 session。

默认 session 选择：

- 如果用户已有选中 session 且仍有效，保持选中。
- 有多个 session 时默认最近 session。
- run 结束时显示到达 end 状态的 session；若无法判定，则回退到最近 terminal session。

自动切换规则：

- 上一个 session 完成，且消息窗口在底部时，才自动切换和折叠历史。
- 用户不在底部表示正在看历史，不自动切换、不折叠。
- 只有一个 session 正在运行时，自动展开该 session。
- 多个 session 正在运行时，在主对话上方显示几个折叠行，展示 session 名称和实时状态；用户点击后进入该 session。
- 多个运行 session 中完成的行消失；若只剩一个运行 session，则按自动展开规则处理。

Composer 与顶部操作：

- 输入框内状态互斥：正常输入、继续按钮、工作流无效修复按钮、运行错误提示/操作。
- `continueRun` 继续同一个 session，不再进入工作流 runtime；复用现有实现。
- 停止并重跑是顶部操作区按钮，常显。
- 当前 run 运行中点击重跑：弹窗二次确认，确认后停止当前 run 并创建新 run。
- 当前 run 已结束点击重跑：直接创建新 run。
- run 历史始终保留，重跑只新增 run 并切换过去。
- WORKFLOW 模式显示编辑工作流 icon，复用现有 workflow drawer；修改只影响未来 run，不影响当前 run snapshot。
- composer 下方显示当前 session 的产物和附件信息区，有内容才显示，点击查看详情。

### Run Mode Management

新增运行模式管理页，只管理：

- AUTO模式
- 工作流模式

不合并：

- Agent 管理
- 上下文管理

AUTO 本质上生成一个只有 AI-DYNAMIC 节点的工作流，因此仍复用现有 workflow validation、snapshot、runtime 执行能力。每个 workspace 记忆上次选择的运行模式。

## Data / Storage Plan

### Config and state

在 `src/config/mod.rs` 的状态/设置结构中新增独立的新 UI 状态，避免污染旧工作台字段：

- `desktop_ui_mode`: `conversation | workbench`
- `conversation_workspaces`: 多 workspace 列表
- `last_conversation_workspace`: 新会话默认 workspace，也是新旧 UI 形态切换时的当前 workspace；仅成功创建/重跑会话后提升，查看历史 run 或切换 composer 草稿目标不提升；侧栏按该 workspace 置顶
- `conversation_pins`: 有序置顶 task refs
- `conversation_run_modes`: workspace 级运行模式记忆与 AUTO模式配置

推荐使用 `projectId` 作为前端 URL 与会话引用标识，避免 workspace path 进入 URL，也避免跨 workspace task id 冲突。后端提供 `projectId -> workspacePath` resolver。

### Conversation metadata

为会话 UI 创建的 task 增加 task 级 metadata，例如：

`~/.sasuke/projects/{project_id}/tasks/{task_id}/authoring/conversation.json`

内容：

- `version`
- `source: "conversation-ui"`
- `runMode: "auto" | "workflow"`
- `workflowTemplateId`
- `autoConfigSnapshot`
- `initialAttachmentNames`
- `titleAutoGenerated`
- `createdAt`

旧 task 没有该文件时仍可显示，但以兼容展示为主；能找到最新 run 就打开 latest run，找不到可显示最小恢复态并提供打开工作台入口。

### Task-level attachments

新增 task 初始附件目录：

`~/.sasuke/projects/{project_id}/tasks/{task_id}/authoring/attachments/`

创建时上传的附件写入此目录，run 启动时复制或引用到首个 attempt 的附件上下文。重跑复用 task-level attachments。

### SQLite search

`src/storage/sqlite.rs` 已有 `tasks` / `sessions` / `session_prompts` 与 FTS。新 UI 搜索只搜 task/conversation 记录，并跨 workspace。

实施前需要修正多 workspace 索引键：

- 现有 tasks 表以 `task_id` 为主键，跨 workspace 可能冲突。
- 调整为 `task_path` 主键，或增加 `(project_id, task_id)` 唯一键。
- 给搜索结果增加 `projectId` / workspace 展示信息。
- SQLite 是辅助索引，可通过 schema version bump + full backfill 处理。

## Backend / Tauri API Plan

在 `src-tauri/src/commands.rs` 增加命令，`src-tauri/src/main.rs` 注册，并在 `web/src/api/client.ts`、`web/src/api/desktop.ts`、`web/src/api.ts`、`web/src/api/browser.ts` 暴露。

### New commands

- `save_desktop_ui_mode(mode)`：持久化 `conversation | workbench`。
- `remove_recent_workspace(workspace)`：旧 UI 最近列表移除指定非当前 workspace 记录；只更新用户级最近列表，不切换当前 workspace，也不影响新 UI conversation workspace 列表。当前 workspace 与唯一有效 recent 项返回结构化错误并由前端禁用入口。
- `get_conversation_sidebar()`：返回 workspace 列表、置顶列表、按 workspace 分组的 task rows、最新 run summary。
- `choose_conversation_workspace()`：选择并加入会话 UI workspace 列表，更新默认 workspace。
- `get_conversation_task_runs(projectId, taskId)`：展开 task 行时获取 run 列表。
- `search_conversation_tasks(query, limit)`：包装现有 `search_tasks`，返回 projectId、workspace、task、latestRun 等富结果。
- `get_conversation_run(projectId, taskId, runId, selectedSessionKey?)`：返回 run 级 session tree、选中 session、active sessions、task/run 摘要、artifacts/attachments、workflow 状态。
- `validate_conversation_create(input)`：创建前校验并返回缺失项与恢复路径。
- `create_conversation_run(input)`：创建 task、写 conversation metadata、保存初始附件、启动 run、返回 `{ task: ConversationTaskRowVm, run: ConversationRunVm }`；创建完成无需再全量刷新侧边栏。
- `rerun_conversation_task(projectId, taskId, currentRunId?, stopActive)`：必要时停止当前 active run，再创建新 run。
- `update_task_metadata(projectId, taskId, title, description?)`：保存标题修改并重建/更新 SQLite 索引，返回持久化后的权威 `ConversationTaskRowVm`；失败时前端不得先行改写标题。
- `pin_conversation` / `unpin_conversation` / `reorder_pinned_conversations`。
- `get_conversation_run_mode(projectId)` / `save_conversation_run_mode(projectId, settings)`。

### Reuse existing backend code

- `create_task` / task authoring persistence from `src/app/mod.rs` and `src-tauri/src/commands.rs`。
- `start_run` / `continue_run` / `pause_run` from existing runtime commands。
- `WorkflowTemplateStore` CRUD from `src/app/mod.rs` and `commands.rs`。
- workflow validation and agent doctor checks, especially `ensure_workflow_agents_doctor_ready`。
- `run_summary_vm`、`round_summary_vm`、`acp_session_vm`、`dynamic_acp_session_vm` patterns in `src-tauri/src/view_models.rs`。
- SQLite `search_tasks` / index retry helpers from `src/storage/sqlite.rs`。

### Run-level session aggregation

在 `src-tauri/src/view_models.rs` 新增 run 级会话 VM 构造逻辑：

- 遍历 run 下所有 round / node / attempt。
- 对 AI-DYNAMIC 动态节点遍历 dynamic attempt 目录，不只看 latest。
- 每个具体 ACP session 作为 leaf：`roundId`、`nodeId`、`attemptId`、`outerNodeId`、`outerAttemptId`、`pathLabel`、`status`、`outcome`、`current`、`startedAt`、`finishedAt`、`sessionId`、`artifactCount`、`attachmentCount`。
- `get_conversation_run` 只加载选中 session 的完整 `AcpSessionVm`，不要一次加载全 run 所有 events。

### AUTO workflow generation

AUTO 模式在创建 task 前生成标准 `WorkflowDsl`：

- 一个 `ai-dynamic` 节点作为 entry。
- 成功 edge 指向 end。
- agent、model、permission、allowed profiles、global goal、control limits、allowed workflows 来自 workspace 级 AUTO 设置。
- 生成后走现有 workflow validation、snapshot、runtime。

### Agent model list

在 `ManagedAgentVm` 中增加 `supportedModels?: AcpModelVm[]`。

- 优先从 provider doctor capabilities 读取。
- 不支持模型列表的 agent 返回空数组；是否必选模型由 agent capability/配置决定。
- 如果模型会影响运行，需要在 node resolved config / ACP invocation 中传入 selected model。

## Frontend Plan

### Routing and app state

修改 `web/src/routes.ts`：

- 保留现有 workbench route。
- 新增 conversation 页面对象的路径解析/构建，但页面最终采用哪种 shell 由 toggle 决定。
- 推荐仍保留清晰的内部路径，方便刷新和返回：
  - `/chat`
  - `/chat/projects/:projectId/tasks/:taskId/runs/:runId`
  - `/chat/agents`
  - `/chat/contexts`
  - `/chat/run-modes`

修改 `web/src/types.ts`：

- `DesktopUiMode`
- `ConversationPage`
- `ConversationSidebarVm`
- `ConversationWorkspaceVm`
- `ConversationTaskRowVm`
- `ConversationRunVm`
- `ConversationCreateResultVm`
- `ConversationSessionTreeVm`
- `ConversationSessionLeafVm`
- `ConversationRunModeVm`
- `ConversationCreateInput`
- `ConversationSearchResultVm`
- `AcpModelVm`

修改 `web/src/App.tsx`：

- 新增 `uiMode`、`conversationPage`、`conversationSidebar`、`conversationRun`、`conversationRunMode`、`selectedConversationSessionKey`、`conversationSearchOpen` 等状态。
- workbench refresh 保持现有逻辑。
- conversation home refresh sidebar + run mode validation。
- conversation run refresh sidebar + selected run VM。
- Agent / Context 页面可复用现有页面，但在 conversation shell 中渲染。
- 后台刷新不能重置 selected session 或滚动位置。
- 标题状态只存在于 `ConversationTaskRowVm`；`ConversationRunVm` 与 run cache 不包含标题。页头按完整 `projectId + taskId` 从 sidebar Task 状态派生，创建/重命名只应用命令返回的单条 Task 快照，并同步普通列表与置顶投影。

### Shell

重构 `web/src/components/Shell.tsx`：

- 会话主路径由 `WorkspaceShell` 统一承载左导航、中间页面和 `RightWorkspaceDock`。
- 页面 profile 集中声明中间最小宽度；自动折叠按左栏、右栏顺序和迟滞状态机执行，右栏宽度写入会话 preference。
- 自动折叠右栏只隐藏 Dock；用户在紧凑宽度显式点击资源链接时，才以共享 Tab state 在 Sheet 打开。
- 右工作区按稳定 `resourceKey` 去重、相邻关闭、横向滚动和溢出列表，只挂载激活 Tab DOM。

可复用组件：

- shadcn `Button`、`ScrollArea`、`DropdownMenu`、`Popover`、`Sheet`、`AlertDialog`、`Tooltip`。
- Tailwind utilities；避免自研基础控件。

### New pages / components

新增页面：

- `web/src/pages/ConversationHomePage.tsx`
- `web/src/pages/ConversationRunPage.tsx`
- `web/src/pages/RunModeManagementPage.tsx`

新增/抽取组件：

- `web/src/components/conversation/ConversationSidebar.tsx`
- `web/src/components/conversation/ConversationComposer.tsx`
- `web/src/components/conversation/ConversationRunHeader.tsx`
- `web/src/components/conversation/ConversationSessionSwitcher.tsx`
- `web/src/components/conversation/ActiveSessionChooser.tsx`
- `web/src/components/conversation/ConversationComposerStatus.tsx`
- `web/src/components/conversation/ConversationAssetsBar.tsx`
- `web/src/components/conversation/ConversationSearchDialog.tsx`
- `web/src/components/conversation/ConversationViewport.tsx`
- `web/src/components/conversation/InterventionLayer.tsx`
- `web/src/components/conversation/AcpConversationComposer.tsx`
- `web/src/components/workspace/WorkspaceShell.tsx`
- `web/src/components/workspace/RightWorkspaceDock.tsx`
- `web/src/components/workspace/AgentConversationPanel.tsx`
- `web/src/components/workspace/right-workspace-context.tsx`
- `web/src/components/run-mode/RunModeManagement.tsx`
- `web/src/components/workflow/WorkflowTemplateManager.tsx`，从 `TaskListPage.tsx` 的创建抽屉中抽取模板管理能力复用。

### Chat reuse

复用 `web/src/components/acp/ACPChatDialog.tsx` 和 prompt-kit：

- `ChatContainerRoot` / `ChatContainerContent`
- `Message`
- `PromptInput`
- `Markdown`
- `ChainOfThought`
- `Tool`
- `AcpUsagePanel`

给 `ACPChatDialog` 增加可选能力，而不是重写聊天基础控件：

- `hideInternalHeader`
- `hideInternalStop`
- `onAtBottomChange`
- `externalComposerState` 或类似方式让外层控制 composer 状态
- 保持 existing live update / pagination / optimistic event 逻辑。

需要强化滚动行为：

- 暴露 `isAtBottom` 给 `ConversationRunPage`。
- 用户不在底部时，不自动切换 session，不强制滚到底。
- 历史 prepend、tool/thought 展开折叠要保持锚点。

### Runtime actions

`ConversationRunPage` 顶部操作：

- title inline edit → `update_task_metadata`。
- rerun → `rerun_conversation_task`；active run 先 `AlertDialog` 确认。
- workflow edit icon → 复用 `WorkflowPage.tsx` 的 workflow drawer + `WorkflowEditor`。
- graph/workflow icon 可打开现有 `GraphView` 视图用于查看 runtime graph。

Composer 状态：

- normal input → `sendAcpPrompt`，继续当前 ACP session，不进 workflow runtime。
- paused/resumable → 复用 `continueRun` 和 `createAcpPromptId` / `optimisticUserEvent` / `updateAcpOptimisticEvents`。
- invalid workflow → 修复按钮打开 workflow drawer 或 Run Mode Management。
- runtime error → 显示现象 + 行动，不展示实现解释。

### Search

新增 `ConversationSearchDialog`：

- shadcn `Dialog` + `Command`。
- 调用 `search_conversation_tasks`。
- 只展示 task/conversation 结果。
- 选中结果时获取/定位最新 run，行为等同点击 task。

### i18n / error codes

更新 `web/src/i18n.ts`，中英文同步新增：

- `conversation.*`
- `conversation.sidebar.*`
- `conversation.home.*`
- `conversation.runtime.*`
- `conversation.search.*`
- `conversation.rerun.*`
- `runMode.*`

后端业务错误继续返回 `{ code, params }`，前端 `displayAppError` 渲染。新增错误码类别：

- workspace required
- content required
- agent required
- model required
- auto config incomplete
- workflow template required
- workflow invalid
- task/run/session/project not found
- attachment too large / unsupported
- search index unavailable / query failed

## Documentation Plan

按项目规则，每次代码修改同步维护产品设计文档和开发计划文档。本次应新增，不覆盖旧工作台文档。

新增产品设计文档：

- `docs/sasuke/产品设计文档/interaction/app/conversational-home.md`
- `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md`
- `docs/sasuke/产品设计文档/interaction/app/run-mode-management.md`

新增开发计划文档：

- `docs/sasuke/开发计划/新UI/会话式主页实施计划.md`

这些文档记录：信息架构、创建流程、运行时 session switcher、状态/错误文案规则、运行模式管理、MVP 验收。

## Implementation Phases

### Phase 1 — 基础形态与文档

- 新增产品/开发计划文档。
- 新增 `DesktopUiMode`、conversation route、持久化 UI mode。
- 重构 `Shell` 为 workbench/conversation 两种 shell。
- `/chat` 可进入新 shell，toggle 可持久化，工作台不受影响。

### Phase 2 — Conversation sidebar 与多 workspace 基础

- 新增 conversation workspace / pin state。
- 新增 `get_conversation_sidebar` 与 VM。
- 修正 SQLite task key 以支持跨 workspace。
- 实现会话侧边栏、workspace 分组、置顶重复展示、状态点、时间显示。

### Phase 3 — Run Mode Management 与创建流程

- 新增 Run Mode Management 页面。
- 抽取 workflow template manager。
- 实现 AUTO 设置与 AUTO workflow 生成。
- 实现 `validate_conversation_create` / `create_conversation_run`。
- 实现首次引导、创建校验、task + run 创建后进入 runtime。

### Phase 4 — Conversation runtime 与 run 级 session tree

- 新增 `get_conversation_run`。
- 构造 run 级 session tree。
- 新增 `ConversationRunPage`。
- 实现 session switcher dropdown。
- 复用 `ACPChatDialog` 渲染选中 session。

### Phase 5 — 运行中行为打磨

- 暴露并使用 at-bottom 状态。
- 实现自动切换/不自动切换规则。
- 实现多 active session chooser。
- 实现互斥 composer 状态。
- 实现产物/附件信息区。
- 确保继续输入沿用现有 same-session continue，不进入 workflow runtime。

### Phase 6 — 搜索、置顶、重跑、标题与附件

- 实现 cross-workspace task search。
- 实现 pin/unpin/reorder。
- 实现 rerun active confirmation / ended direct rerun。
- 实现 title inline edit 和 SQLite reindex。
- 实现 task-level initial attachments 与 rerun 复用。

### Phase 7 — 验证与收敛

- 补齐 i18n。
- 补 browserApi mock。
- 跑 Rust / TS 构建与测试。
- 启动前端，用 agent-browser 验证主路径和边界场景。

## Critical Files

Frontend:

- `web/src/App.tsx`
- `web/src/routes.ts`
- `web/src/types.ts`
- `web/src/components/Shell.tsx`
- `web/src/i18n.ts`
- `web/src/api.ts`
- `web/src/api/client.ts`
- `web/src/api/desktop.ts`
- `web/src/api/browser.ts`
- `web/src/pages/ConversationHomePage.tsx`
- `web/src/pages/ConversationRunPage.tsx`
- `web/src/pages/RunModeManagementPage.tsx`
- `web/src/components/acp/ACPChatDialog.tsx`
- `web/src/pages/TaskListPage.tsx`，用于抽取 workflow template 管理能力
- `web/src/pages/WorkflowPage.tsx`，用于复用 workflow drawer 模式
- `web/src/components/WorkflowEditor.tsx`

Backend:

- `src/config/mod.rs`
- `src/runtime/mod.rs`
- `src/storage/mod.rs`
- `src/storage/sqlite.rs`
- `src/app/mod.rs`
- `src/dsl/mod.rs`
- `src-tauri/src/commands.rs`
- `src-tauri/src/view_models.rs`
- `src-tauri/src/main.rs`
- `src-tauri/src/state.rs`

Docs:

- `docs/sasuke/产品设计文档/interaction/app/conversational-home.md`
- `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md`
- `docs/sasuke/产品设计文档/interaction/app/run-mode-management.md`
- `docs/sasuke/开发计划/新UI/会话式主页实施计划.md`

## 2026-08-02 右侧工作区与 Agent 分支里程碑

- 后端统一 `ConversationBranch` 查询：`get_acp_session` 通过 `branchId` 返回根或 Agent 的同一 `AcpSessionVm/eventPage`；Activity 审计和单工具详情使用独立按需命令。
- Agent execution 使用 sasuke 稳定 UUID 派生 ID，扁平存储 parent relation。根 projection 只返回顶层 Agent，Agent branch 只返回直属孩子；后代不平铺。根/Agent timeline 单一归属，permission、elicitation 和统计均绑定 branch ID；Plan 额外使用 `branch/unscoped` ownership，存在 Agent execution 时根会话不展示无法确认范围的 session-wide Todo。祖先只接收 attention 聚合。
- 主会话已删除嵌套 transcript Collapsible，只渲染 `AgentLinkRow`。右侧 Agent 面板复用同一消息视口与 intervention，完全不挂载 composer/停止/继续/重试控件。
- 单一 `ConversationEventRouter` 按 attempt + branch key 路由 live event。非激活 Tab 不挂载 viewport，普通后台 streaming 不刷新全部 Tab；有限 LRU 保存 branch event window、滚动锚点、贴底和状态摘要。
- 会话分页按语义块计算；Activity 展开详情和工具 raw output 分两级延迟读取。继续使用原生滚动、有限 DOM window 和真实 item 锚点，不采用动态高度虚拟列表。
- 旧 Agent launch anchor、前端 Claude metadata、根/Agent 共用事件窗口和 `acp.events.jsonl` 运行时双读写已删除；旧会话仅执行一次性分区迁移。

## Verification Plan

### Backend / Rust

- `cargo fmt`
- `cargo check`
- targeted tests for:
  - UI mode persistence defaults
  - conversation workspace and pin state
  - workspace-scoped command resolution: `create_conversation_run` / `rerun_conversation_task` / `get_conversation_run` / `validate_conversation_create` / `switch_conversation_session` / `update_task_metadata` / `show_conversation_attachment` all resolve the correct workspace-scoped `App` by `projectId` instead of using the global `DesktopContext.repo_root`
  - unknown `projectId` returns `workspace.not-found` structured error
  - `remove_conversation_workspace` cascade cleans pins, run modes, and falls back `last_conversation_workspace`
  - frontend composer `selectedProjectId` correctly reflects the user's workspace selection and is carried into the create payload even on immediate submit after switching
  - SQLite cross-workspace task key/backfill/search
  - AUTO workflow generation validation
  - create conversation task + run + metadata + attachments
  - rerun active and ended cases
  - run-level session tree selection, including dynamic AI-DYNAMIC sessions

### Frontend

- TypeScript / Vite build via existing package scripts.
- Browser mock coverage for new RuntimeApi methods.
- Manual UI verification with dev server + agent-browser, required by project rules.

Agent-browser scenarios:

1. Open `/chat` and confirm conversation shell/default mode.
2. Toggle to workbench and back; refresh/restart restores the persisted toggle shape correctly.
3. In workbench, open the workspace picker, switch to conversation without choosing a workspace, and confirm the conversation home is shown instead of the workbench picker.
4. In workbench workspace picker, confirm the current/only recent workspace remove action is disabled; when another non-current recent workspace exists, remove it and confirm it disappears without switching workspace or deleting files.
5. First-entry guide appears when run mode is incomplete.
6. Configure run mode; create a conversation from text input.
7. Validate that missing agent/model/workflow/workspace shows recovery actions.
8. Confirm sidebar recent ordering, pinned duplication, status colors, time display.
9. Search task and open latest run.
10. Open runtime, verify selected session dropdown hierarchy.
11. Verify multiple active sessions chooser behavior.
12. Scroll up while session completes; confirm no auto-switch/fold.
13. Scroll to bottom; confirm auto-switch/fold happens.
14. Continue input stays in same session and does not re-enter workflow runtime.
15. Rerun active run asks confirmation; rerun ended run starts directly.
16. Workflow edit affects future runs only.
17. Switch zh-CN / en and confirm new text is localized.

## MVP Acceptance

MVP 完成标准：

- 用户可从 `/chat` 输入需求并成功创建 task + run，进入当前 session 对话。
- 会话侧边栏按 workspace、置顶、统一最近活动时间、状态点正确展示；混合时间格式和旧 run 后续更新均不得造成视觉时间与列表顺序不一致。
- 点击 task/run 后默认展示正确 session，session switcher 能切换 round/node/attempt。
- 运行中自动切换遵守 at-bottom 规则，不打断用户查看历史。
- Run Mode Management 能让发送前校验通过，并与 Agent/Context 管理保持边界。
- 工作台 UI 仍可通过 toggle 进入，现有 `/tasks` 路径不回归。

## Direct 模式增量验收

- 快速会话固定展示 `Direct / 工作流 / AUTO`，Direct Agent 使用 icon picker。
- Direct Agent picker 按可用性稳定分区：可用 Agent 连续展示在前、不可用 Agent 连续展示在后，组内维持 Agent 注册表顺序；两组同时存在时仅显示一条低对比竖线分隔，并保留不可用项的诊断 tooltip 与禁用状态。
- Direct 模型/权限位于 composer 右下角，并按 workspace + Agent 恢复记忆。
- 普通快速对话 composer 顶部使用工作空间信息区承载 workspace 与工作位置；底部工具栏只保留附件、模型/权限与发送。定时任务创建模式继续在底栏展示 workspace，并固定主工作区。窗口变窄时配置与操作按 composer 自身宽度换行，禁止固定宽度控件互相覆盖；会话主页使用响应式页面 gutter 保证最小窗口仍有边距。
- Direct 复用 ACPChatDialog，但隐藏 workflow/session tree/run outcome；切页后 lifecycle、计时、token 和停止能力可恢复。
- Direct 侧边栏和搜索使用 Agent icon 与 `lastActivityAt`，Workflow/AUTO 的状态点规则保持不变。
- Direct prompt 必须通过 RawAgent envelope 证明 system prompt 为空且没有 sasuke hidden/runtime/profile/output/repair 泄漏。

### 2026-07-23 响应式补充验收

- Vitest 固化 composer 工具栏可换行、左右控制组存在弹性 basis、Direct 下拉不再使用不可收缩固定宽度。
- 标题栏窗口控制组通过 `w-max + flex-none` 固化整体 intrinsic width，三个窗口按钮分别使用 `flex-none` 固化 `w-11 / w-11 / w-12`，禁止嵌套 Flex 在窄窗口下压缩 hover 区域；关闭按钮在默认宽度和最小宽度下均贴紧右侧窗口边缘，不保留末尾 gutter。
- Direct 组合页头的标题外层使用 `flex-none + max-width: 40%`：短标题按 intrinsic width 完整显示，不得被 Agent/sessionId 兄弟项压缩到单字符；标题 trigger 不使用会从 intrinsic width 扣减空间的负外边距，避免其 `max-width: 100%` 反向裁剪文字；长标题只在超过上限时截断，编辑态与常态保持相同布局槽。
- 删除 `body` 的 `min-w-[1040px] / min-h-[680px]`；Tauri `minWidth/minHeight` 是桌面最小尺寸的唯一事实源，Web 层在任何实际 viewport 下都继续响应式重排，不能停止变化后让宿主窗口裁切右侧内容。
- 生产构建必须通过，确认 Tailwind v4 响应式 utilities 被正确生成。
- 手工验证至少覆盖默认桌面宽度和最小窗口宽度；Direct 模型、权限、发送按钮在任何宽度下都不与工作空间区域重叠。

### 2026-08-03 会话主页最窄宽度控件对齐

- [x] 将 composer 从“两个 flex 组自由换行”改为命名容器驱动的明确布局阶段，修复最窄窗口中模型独占一行、权限与发送挤在残缺行、工作空间宽度与下方控件边缘不一致的问题。
- [x] 最窄容器固定为附件/工作空间一行，模型、权限、发送分别整行；中等容器使用模型/权限两列与整行发送；桌面容器才恢复模型/权限/发送同排，更宽时再把前后控制组合并为单行。
- [x] 共享 ACP 模型与单项配置选择器新增布局层 `triggerClassName` 接口，保留 shadcn/Radix 触发器的可访问行为和基础样式，由 composer 负责 `w-full / max-w-none`，不复制下拉组件。
- [x] 处理模式和 Agent 选择区复用同一个 composer 容器断点；最窄宽度标签上置、Tabs 占满可用宽度，Agent picker 超出时横向滚动。
- [x] 修正 Agent picker 的双轴溢出契约：滚动容器占满剩余宽度，列表保持单行，仅在 Agent 数量超出时横向滚动；显式 `overflow-y-hidden`，避免 Tabs 伪元素触发 Windows 原生竖向滚动条。
- [x] 收紧 Agent 行垂直节奏：外层取消额外上下内边距，滚动容器内部保留 `py-1` 安全空间，在 40px 紧凑行高中完整容纳 32px Agent 药丸及焦点边缘。
- [x] Vitest 固化命名容器、三阶段工具栏、最窄整行发送、配置触发器样式扩展接口及模式/Agent 堆叠契约；生产构建和 320px、最小桌面窗口、常规桌面宽度的前端验证通过后完成交付。

### 2026-08-10 主页 Agent 列表滚动条收敛

- [x] 复用原有单行横向滚动结构，不改动 Agent Tabs 数据和选中交互。
- [x] 为主页 Direct Agent 滚动容器增加专用隐藏滚动条工具类，同时保留鼠标滚轮、触控板与键盘导航能力。
- [x] Vitest 固化 Agent Tabs 同时具备横向滚动和隐藏滚动条的样式契约。

### 2026-07-24 全前端容器响应式审计

- 设置页 section、主题摘要列表、单张主题摘要与主题抽屉使用命名 container query；整窗宽但实际内容区窄时不得提前双列，主题名称不得逐字纵排，预览与操作按钮不得覆盖名称。主题抽屉在自身内容宽度达到 `@2xl` 时切换为双列视觉样本墙，窄于该阈值保持单列。
- 工作区选择页默认单列，仅在完整页面宽度足够时恢复双列；最近工作区列表在单列模式使用受控高度，页面允许纵向滚动。
- Run 详情默认堆叠 Rounds 与 Events，仅在页面容器达到双栏阈值后恢复 `320–420px + 1fr`；工作流编辑器同样按自身容器宽度决定 inspector 是否右置，画布浮动工具栏允许换行。
- 数据表、日志列和 workflow canvas 等确需固定内部尺寸的区域保留横向滚动或画布交互，不强行改造成压缩列；审计重点是消除无降级策略的固定分栏与 viewport-only breakpoint。
- Vitest 固化上述命名容器、升列阈值、默认单列和工具栏换行契约；生产构建后在默认宽度与最小窗口宽度逐页验证设置、工作区选择、Run 详情、工作流编辑器。

### 2026-07-24 会话侧栏分隔线收敛

- 根因是侧栏 resize handle 同时承担命中区和视觉反馈，`w-1` 热区在 hover / active 时整块使用 primary 背景；高 DPI 显示缩放下会形成明显的粗色带，而不是稳定的分隔线。
- 保留透明的 8px 拖拽命中区，删除 primary hover / active 填充；可见边界继续由主内容区低对比 1px border 与左上圆角统一绘制，避免双线、圆角断裂和拖动时突然变粗。
- Vitest 固化 resize handle 为透明命中层，并禁止恢复 `hover:bg-primary/40` / `active:bg-primary/60` 这类整段强调色填充；默认深色、终端黑和浅色主题均需实测侧栏边界。

### 2026-07-23 Windows 窗口 resize 黑带修复

- 根因不是 React 页面布局，而是 Windows Win32 外层窗口与 WebView2 opaque controller 在连续边缘 resize 时不同步；单独修改 CSS 阴影或设置 WebView 背景无法消除旧边界与新边界之间的黑带。
- Windows 平台启用 Tauri `transparent` composition controller，页面根节点继续绘制不透明主题背景；仅同步 Window 宿主背景，不调用 WebView background API 破坏 composition 路径。
- 主窗口配置为 `visible: false`，bootstrap 与主题 surface 准备完成后由前端调用 `show()`；该策略参考 AionUi Electron 的 `show: false + ready-to-show` 生命周期，解决 transparent workaround 的启动闪烁副作用。
- Windows 的 undecorated native shadow 按系统能力启用：Win11 开启，由 DWM 提供系统圆角与边框；Win10 关闭，避免 TAO 为无边框阴影保留左、右、下 frame thickness、顶部保持 0 inset 后形成三侧黑线。transparent composition 独立负责消除 resize 黑带。macOS 显式恢复原生 shadow；Win11 应用根壳不绘制整窗 border、CSS 圆角或 `::before` 合成层，避免与 DWM 形成双层轮廓。
- Vitest 固化 Web 根节点不存在桌面固定最小宽高、窗口按钮整体和单项均不可收缩，并检查 Tauri resize listener 在 React effect cleanup 中释放。
- Vitest 固化主题到宿主 surface 的颜色映射、Windows composition 配置、隐藏启动/显示权限和无 overlay 外轮廓；同时运行 Web 构建、Rust `cargo check` 与真实 Windows 左右边缘 resize 验证。

### 2026-07-28 Windows 10 无边框窗口外轮廓修复

- 根因是窗口外壳把外轮廓完全交给 Win11 DWM，但产品仍支持不提供原生圆角的 Windows 10；这是平台能力建模缺失，不通过恢复所有系统通用 CSS 圆角处理。
- 桌面 bootstrap 新增结构化 window chrome policy。Windows 使用成熟的 `windows-version` crate 调用 `RtlGetVersion` 获取真实 build：build 22000 及以上返回 `{ frameStyle: native-compositor, nativeShadow: true }`，更低版本返回 `{ frameStyle: app-outline, nativeShadow: false }`；禁止使用 Win10/Win11 都可能报告 `Windows NT 10.0` 的浏览器 UA 判断。
- `app-outline` 只在应用根壳绘制主题化 1px 内侧边界，并叠加 8px 低透明度柔和内阴影；两层效果均使用随主题切换的语义 token，不占用布局、不引入伪元素层、不裁剪 WebView，也不伪造圆角。禁止使用会在 viewport 边缘被裁切并与 native resize frame 形成不一致视觉权重的 CSS `outline`。`native-compositor` 保持无额外外框，继续由 Win11 DWM 提供圆角和边界。
- 单元测试固定 Windows build 分界、bootstrap 序列化值、Win10 关闭 native shadow、Win11 保留 native shadow、两套桌面 Shell 的 policy 绑定，以及 Win10 四边一致的 1px inset frame 与柔和内阴影，不恢复旧的高层级 `::before` 方案。
- 最大化窗口拖拽还原只保留一个鼠标手势入口：Tauri `data-tauri-drag-region` 注入脚本负责 mouse drag 与双击最大化，WebView2 `app-region: drag` 只补充触摸/触控笔；删除 React `onMouseDown -> startDragging()` 和手动 `onDoubleClick -> toggleMaximize()`，避免 Win10 对同一次按下连续派发两次 `WM_NCLBUTTONDOWN(HTCAPTION)` 后把还原窗口移动到工作区外。
- Vitest 固化共享顶栏继续声明 Tauri drag region，但实现源码不得再直接调用 `startDragging()` 或注册手动拖拽/双击处理器。

## 快速会话工作树增量计划（2026-08-17）

- 数据先行：在 `RunState` 增加可选 `RunWorktreeState { path, branch, fork_commit }`，把当前 run 的工作目录归入 run 生命周期；conversation metadata 只保留 `work_location` 创建/重跑意图，前端偏好按 `projectId` 保存。
- 接口：快速会话输入增加 `workLocation: main | worktree`；运行详情增加可选 worktree 投影；后端准备接口区分主工作区与 worktree，但复用同一 prepared-run guard、后台启动和结构化 Git preflight。
- 实现：普通快速对话 composer 顶部显示 workspace 与“主工作区 / 新工作树”，使用 28px 高、较输入面更深的主题栏；包含左右侧边在内的顶部操作栏总宽固定为输入框宽度的 80% 并整体居中，中间实体宽度按总宽扣除左右各 48px 侧边计算。两侧分别使用固定 48×28px 的镜像 SVG 背景，从输入框上边缘内侧端点沿约 50° 的直线斜边向上收窄，仅在斜边接近顶部时通过切线连续的圆角接入主体顶边，使梯形顶部内角约为 130°，形成顶部圆润、底部无回弯的圆角梯形；不保留从底部水平起步的 Bézier、“S / ∫”轮廓、竖直脖子、拼缝或折点，也不得通过压缩侧边本身实现总栏收窄。信息栏、侧边与输入面相邻但不重叠，禁止负 margin、z-index 叠放、向外鼓出的实心圆角或侧边伸入半透明 composer，确保壁纸场景没有底层色带。非 Git 点击复用现有 GitRequirementDialog。worktree 从 `HEAD` 创建到 sasuke 用户数据目录，Worker cwd 指向 worktree，adapter workspace 保持原项目路径。
- 焦点：工作空间 Select 沿用 Radix 的键盘焦点归还；仅在最后一次选择交互来自 pointer 时阻止关闭后的自动回焦并 blur 触发器，避免鼠标切换完成后残留 focus ring。不得全局关闭 `focus-visible`，也不得让键盘选择失去连续焦点。
- 对齐：将 80% 宽的完整圆角梯形作为顶部信息栏布局根节点，中间矩形表面内收左右各 48px，两侧 SVG 放回根节点内部；工作空间与工作位置触发器从外轮廓左侧 32px 的安全平顶位置开始排列，比矩形左边缘提前 16px。组间外部间距为 0，触发器水平内边距与内部图文间距为 6px，同时保留 28px 高点击热区；不得用负 margin、transform 或绝对定位移动交互控件。
- 密度：输入区后的 composer 主区块间距最终收至 6px；处理模式、Workflow、AUTO 外层使用 4px 上下内边距，处理模式 Tabs 与 Workflow / AUTO 选择器统一为 28px；Direct Agent 行最小高度为 40px、Agent 药丸为 32px，AUTO 目标输入最小高度为 36px。focus ring、横向滚动与文本完整性保持不变。
- 主页纵向布局：欢迎标题使用 `text-3xl`；欢迎标题、输入框、处理模式和当前模式操作栏恢复为同一个正常文档流内容组，通过 64–80px 响应式底部留白在可用主区内光学居中。不得拆成底部停靠区，也不得使用 fixed、absolute、transform 或视口测量补丁。
- 图标：Workflow 模式选择栏复用运行模式入口与管理页的 Lucide `Route` 图标，保留既有尺寸和语义色，不新增图标映射状态。
- 生命周期：复用 `PreparingWorkspace -> StartingNode -> RunningNode`；创建期间停止后重新读取 durable run，不启动 Agent并保留已创建目录；AI-DYNAMIC 继续以外层 run 的实际 workspace 作为 main workspace。
- 验收：单元测试覆盖偏好 schema、快速页内收低栏的内容宽度/双侧曲线/主题层级契约、详情页 composer 外壳独立、内部 usage 行不重复声明 composer surface、非 Git 错误码、旧 run 兼容、HEAD/幂等创建、停止竞态与 invocation cwd；执行前端完整测试/生产构建及 Rust 定向测试，并在内置浏览器验证正常/窄宽、明暗主题、Tooltip、菜单与非 Git 对话框。
- 约束：不新增依赖、缓存、队列或第二套状态机；每次启动只读取当前 run 的小型状态文件，worktree 路径必须处于受管目录且在使用前经 Git 验证。

## 会话侧栏跨工作空间状态点收敛计划（2026-08-17）

- 复用既有 `sasuke://conversation-run-state-updated` canonical terminal event，在 App shell 建立唯一全局订阅；事件身份必须使用 `projectId + taskId + runId`，不得绑定当前选中页面或只比较末级 ID。
- 前端只在 `ConversationSidebarVm` 的目标 workspace task、run 与置顶副本中投影 `status/outcome`，保持工作空间列表、偏好、非目标 workspace、时间和会话正文 identity 不变；terminal 状态拒绝迟到的非终态回退。
- 当前 run 详情继续按事件局部读取 `ConversationRunVm`；后台 run 不读取详情，run 终态事件不再调用全量 `getConversationSidebar()`，避免每次完成都扫描所有 workspace/task/run。
- Vitest 固化跨工作空间同名 task/run 隔离、普通区与置顶副本同步、非目标数据 identity 保持、terminal 单调收敛、全局唯一监听和详情刷新不调用侧栏接口；完成后执行前端类型检查、生产构建并在桌面客户端验证跨工作空间后台完成。

## Direct 会话侧栏 activity 全局收敛计划（2026-08-18）

- 根因：`sasuke://acp-session-updated` 仅由当前打开 run 的 effect 订阅，后台、跨工作空间或监听建立前结束的短会话会遗漏终态 `activity: null`，导致 Direct Agent icon 持续呼吸；run-state 事件不能替代该事实，因为 completed run 仍允许 same-session follow-up，而 `session/new` 尚未返回即停止时 run 也可能按可恢复语义保持 `paused / process-interrupted`。
- 数据：保留 prompt control registry 为 live activity 的 canonical source；事件负载从单 attempt 查询改为在 task 根下聚合全部 attempt，只有 task 无活动 prompt 时才投影 `null`，防止一个 attempt 结束误清另一个 prompt。
- 接口与实现：App shell 建立唯一 ACP session update 订阅，按完整 `projectId + taskId` 只更新目标 workspace task 与置顶副本的 `activity`；当前 run 详情通过稳定 ref 消费同一事件，删除页面级重复订阅。相同 activity 重复到达时返回原 sidebar identity。
- 验收：Vitest 固化跨工作空间同名 task 隔离、置顶同步、显式终态清除优先于暂停/停止中的 lifecycle、重复事件 identity 以及全局唯一订阅；Rust 单测固定 task 下多 attempt 聚合及 retained control 已 `stopped` 时不再计为活动。执行定向测试、前端生产构建和桌面客户端短 Direct 会话实测，确认正常 `end_turn` 与 pre-accept `process-interrupted` 后都只收敛 icon 呼吸态，侧栏展开、滚动、选中和排序不重载。
- 约束：不新增依赖、轮询、缓存、队列或第二套生命周期；每次事件仅扫描目标 workspace 摘要与置顶列表，后台不读取 session tree、timeline、raw frame 或 Markdown。

## ACP 历史反向分页重叠区间修复（2026-08-30）

- [x] 回溯 task-321 现场，确认首条用户消息为稳定逻辑项 `[startedSeq=2, endedSeq=511]`，后续 Warning 为 `[4,4]`；原 indexed `beforeSeq` 查询按 `newestSeq < cursor` 过滤，导致累计消息被永久跳过，前端最终错误收敛为 `hasOlder=false`。
- [x] 在独立 Rust integration test 中以 3 个语义块最小复现：修复前只返回 Warning，修复后按阅读顺序返回用户消息与 Warning，并固定 `hasOlder=false / hasNewer=true`。
- [x] 反向分页改为按 semantic block `oldestSeq` 选择更早项；`newestSeq/lastRevision` 继续只承担累计更新和增量水位。保留现有 materialized index、有限页面、前端三页窗口与 DOM anchor，不调整页大小、滚动阈值或可见消息结构。
- [x] 完成 timeline 独立接口回归、前端 ACP 分页/分支滚动测试、Rust 生产检查和前端生产构建；等价重叠区间数据已验证连续反向分页可到达首条用户消息。内置浏览器本次无可用实例，真实页面手工上滑留待可用客户端构建复核，不作为接口根因转绿证据的替代。
- 性能边界：查询仍只筛选轻量 semantic block index 并读取选中页 locator，复杂度、I/O 数量、锁范围和内存上限不增加；不引入全量 timeline 正文读取、N+1、缓存、队列或新依赖。
