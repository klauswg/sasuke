# sasuke 功能点 Todo 列表

| 状态 | 概要 | 说明 | 方案链接 |
|---|---|---|---|
| 完成 | 会话输入历史翻阅 | 空 composer 上下箭头回填本会话原文，最早向上循环至最新，最新向下恢复空输入；保护多行光标、输入法、斜杠菜单与异步草稿，按既有 Timeline 索引分页并限定缓存。未标记原文来源的旧记录不纳入。 | `docs/sasuke/开发计划/acp接入/composer-input-history.md`; `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md` |
| 完成 | 前端基础控件复用契约 | UI 交互规则已明确产品选择控件必须复用 shadcn/ui、prompt-kit 或项目共享封装，禁止直接使用原生 `<select>`；现有 AST 契约测试扩展为同时阻止原生 `<select>` 和浏览器 `title` Tooltip 回流。 | `docs/sasuke/rules/ui-interaction.md`; `web/tests/native-title-tooltip-contract.test.ts` |
| 完成 | 未首提 Git 仓库源码管理 | 将 porcelain v2 的 `(initial)` HEAD 在 Git 领域解析入口规范化为空，初始化后的仓库可直接读取未跟踪文件并返回空历史，不再误报仓库无法读取；真实临时仓库接口回归覆盖 snapshot 与 history。 | `docs/sasuke/产品设计文档/interaction/app/source-control.md`; `docs/sasuke/开发计划/新UI/右侧源码管理与GitHub集成技术方案.md` |
| 完成 | 右侧源码管理与 GitHub 集成 | 已完成 Git snapshot、porcelain v2 状态、refs/worktree/stash 读取、历史分页、统一文本 comparison、project/worktree 路由校验、repository/workspace 共享写锁、stage/unstage/commit、branch/tag/worktree typed mutation，以及 stash/fetch/pull/push/push-tag 可取消长操作和仓库操作 UI；GitHub 已完成 capability/login、PR/Issue 查询、带 ahead/发布/已有 PR 预检的可取消 PR 创建及 PR 文件 Diff，PR body 复用 Atomic 编辑器并通过 stdin 传入，PR 文件点击复用现有 `file-diff` viewer。提交 DAG 已通过本地 adapter 接入 HTML Grid renderer，支持 300 条分页、复杂 merge、窄栏和任意 Checkbox 多选，300/1000/5000 commits PoC 已固化；单提交详情、多提交关系、目标包含状态和两点文件 Diff 已完成。snapshot/history、内部 Tab、分页、多选、详情和 commit 草稿已迁入按 repository/workspace 隔离的 24 项 LRU 会话缓存，Diff 往返不再重新加载；显式刷新、Git 操作终态和匹配 watcher 事件会立即或去抖刷新，并阻止 stale response。本地 Git、GitHub 登录和 PR 创建长操作完成状态均已改为 Tauri typed event subscription，普通文件事件复用 workspace watcher，HEAD/index/refs/packed-refs 由 repository/workspace-scoped `GitStateMonitor` 监听，linked worktree watcher 与缓存均隔离。 | `docs/sasuke/产品设计文档/interaction/app/source-control.md`; `docs/sasuke/开发计划/新UI/右侧源码管理与GitHub集成技术方案.md` |
| 完成 | SKILL 多 Agent 实例级管理 | 已实现按 `directoryPath` 区分同名原生 skill；sasuke 自管 SKILL 根目录统一切换到 `~/.sasuke/skills` 与 `<project>/.sasuke/skills`；同步目标以全局已配置 agent 为准，项目级同步会自动覆盖 Claude/Codex/Cursor/Gemini/OpenCode 等已配置 agent 并按目标集合对账支持取消同步，创建/编辑抽屉提供同步目标“全选 / 全不选”批量操作，范围下拉统一使用项目 shadcn/ui Select；同目录重名与同步目标原生同名都会直接阻止；原生 agent skill 的同步、冲突检测和同步状态识别统一改为按真实目录名；管理页补齐删除链路，并把卡片左下角改为展示“来源 agent + 已同步 agent”的图标并集，`.sasuke` 自建 skill 展示 sasuke 图标；列表新增按已配置 agent 的可用性筛选；编辑已有 skill 时保存位置提示改为按真实目录展示，且原生 agent skill 不再允许勾选同步到自身；保存流程已收敛为后端统一预检、目录移动/写入、同步链接 reconcile 与失败回滚，rename 会真实移动目录并按新目录名重建多 Agent 同步链接；卡片底部已升级为来源 icon + 可切换同步目标 icon，已同步目标原色并带绿色状态点，未同步目标灰色展示，点击通过 `update_skill_sync_targets` 快速同步/取消同步且不重写正文；SKILL 卡片顶部信息区和底部 agent/action 区已固定高度，避免描述行数不同导致底部列表跳动；项目 SKILL 的 workspace 下拉会记忆上一次有效选择并自动恢复加载；创建/编辑抽屉已拥有局部表单状态，正文输入不再驱动背后的 SKILL 卡片网格重渲染；SKILL 与角色管理已统一使用共享 YAML frontmatter 解析模块，支持 `description: >`、`description: |`、`summary: >` 等 block scalar，并兼容 LF/CRLF frontmatter 分隔符；编辑已有 SKILL/角色时只覆盖已知字段和正文，保留 `compatibility` 等未知 frontmatter 字段与原 block scalar 风格，避免多行 frontmatter 被误读为 `>`、被视为无描述或在保存后被单行化。 | `docs/需求/sasuke-SKILL-多Agent管理改进-任务文档.md`; `docs/sasuke/产品设计文档/MCP-SKILL管理-完整设计方案.md` |
| 完成 | Agent 目录配置归一化 | 移除封闭的 `ManagedAgentType` 运行模型，改用稳定 `ManagedAgentId` 与内置 preset registry；`ManagedAgentConfig` 直接保存主 Agent 目录和兼容 Agent 目录，删除 `skillsDirOverride` 与 Claude/Codex 类型特判。主目录作为 sasuke Skill 读写同步目标，兼容目录只读；上下文管理的全局/项目列表扫描完整读取目录并按物理路径去重，因此 Codex 可同时发现 `.codex/skills` 与 `.agents/skills`，同步写入仍只落主目录。所有路径统一通过 resolver 追加 `skills`。Agent 管理抽屉支持编辑、规范化并回填两类目录。旧 `settings.json` 通过 `settingsSchemaVersion` 在统一加载入口一次性迁移并原子写回，当前版本后续启动不重复迁移，解析失败不再静默回退默认配置。 | `docs/sasuke/产品设计文档/interaction/app/agent-management.md`; `docs/sasuke/产品设计文档/MCP-SKILL管理-完整设计方案.md` |
| 完成 | 定时任务 | 已支持 Direct、Workflow、AUTO 的定时触发；包含 At、重复预设、每周多选、每隔分钟/小时、Cron、队列保护、Task/Run 生命周期、会话 AlarmClock 标记和全局 CRUD 管理页。错过时间点的 `missed` 记录与补跑策略暂未实现。 | `docs/sasuke/开发计划/定时任务/定时任务完整设计与开发计划.md`; `docs/sasuke/产品设计文档/runtime/scheduled-task.md`; `docs/sasuke/产品设计文档/interaction/app/scheduled-task-management.md` |
| 完成 | ACP 斜杠命令菜单 | 快速对话与会话详情输入框支持 `/query` 命令菜单；每个 Agent 分离维护 Skill 写列表与读列表，Doctor 将 ACP 原生命令和用户级/workspace 级读目录中的 `SKILL.md` 元数据合并，ACP 优先并按名称去重后按 Agent + workspace 持久化；同名 SKILL 的展示元数据使用确定性优先级 `主目录实体 > 主目录软链接 > 兼容目录实体 > 兼容目录软链接`，但选择后仍只发送普通 ACP 命令文本，由 Agent 自己解析实际 Skill。自动/手动 doctor、live update 和 SKILL 同步异步刷新。Codex 默认读取 `.codex + .agents`，Claude 只读 `.claude`，不做 prompt injection。 | `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md`; `docs/sasuke/开发计划/acp接入/acp-ui.md` |
| 完成 | 上下文管理与会话侧栏加载解耦 | 角色管理首次进入只加载 Profile；Agent registry、SKILL 和 workspace 元数据延迟到 SKILL Tab。新增轻量 `get_conversation_workspaces`，不再用完整会话侧栏为项目下拉供数。Profile、Agent registry、MCP、全局/项目 SKILL 与完整侧栏等文件读取入口统一通过 async command + `spawn_blocking_command` 执行；侧栏按 task 一次、run 一次读取，不再执行 workflow/Profile 校验或重复扫描 run，损坏的单 task run 历史降级为空而不隐藏整个 workspace。 | `docs/sasuke/产品设计文档/MCP-SKILL管理-完整设计方案.md`; `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md` |
| 完成 | CLI 顶层命令骨架 | 已实现 `task / run / artifact` 三类顶层命令与统一 `RuntimeConfig` 注入，满足 CLI-first MVP 主入口。 | `docs/sasuke/产品设计文档/interaction/cli.md:31-52, 54-128` |
| 完成 | `task show` | 已支持按 task id 读取并输出 `task.json`。 | `docs/sasuke/产品设计文档/interaction/cli.md:54-60` |
| 待办 | `task list` | 设计文档定义了 task 列表能力，当前 CLI 仍未实现。 | `docs/sasuke/产品设计文档/interaction/cli.md:54-60` |
| 完成 | `run start` | 已支持读取 task workflow、校验 DSL、创建 run/round/attempt 并从 entry node 开始执行。 | `docs/sasuke/产品设计文档/interaction/cli.md:63-86`; `docs/sasuke/产品设计文档/product/overview.md:49-53`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:255-287` |
| 完成 | `run status` | 已支持读取并校验 `run.json`，输出 run 当前状态。 | `docs/sasuke/产品设计文档/interaction/cli.md:66-72`; `docs/sasuke/产品设计文档/runtime/state/run.json.md:14-149` |
| 完成 | `run continue` | 已支持两类 continue：恢复被中断 provider 会话，或对 `completed + invalid` attempt 重新结算。 | `docs/sasuke/产品设计文档/interaction/cli.md:87-120`; `docs/sasuke/产品设计文档/runtime/control.md:337-360` |
| 完成 | `run retry` | 已支持对当前 node 新建 attempt 并重新执行，默认使用新 session。 | `docs/sasuke/产品设计文档/interaction/cli.md:87-120`; `docs/sasuke/产品设计文档/runtime/control.md:361-374` |
| 完成 | `run kill` | 已支持显式结束 run，并同步落盘 run/round/node 的 `killed` 终局状态。 | `docs/sasuke/产品设计文档/interaction/cli.md:87-120`; `docs/sasuke/产品设计文档/runtime/control.md:390-417` |
| 完成 | `run open-session` | 已支持从 `worker-ref.json` 解析 provider-native 打开命令，并在不支持时显式报错。 | `docs/sasuke/产品设计文档/interaction/cli.md:111-121`; `docs/sasuke/产品设计文档/provider/worker-ref.md:76-104` |
| 待办 | `run events` | 设计文档定义了 run 级 events 查询命令，当前 CLI 尚未实现。 | `docs/sasuke/产品设计文档/interaction/cli.md:66-72` |
| 完成 | `artifact list` | 已支持列出 attempt 下 canonical artifacts。 | `docs/sasuke/产品设计文档/interaction/cli.md:122-128`; `docs/sasuke/产品设计文档/runtime/layout.md:392-425` |
| 完成 | `artifact show` | 已支持读取 attempt 下指定 artifact 内容。 | `docs/sasuke/产品设计文档/interaction/cli.md:122-128`; `docs/sasuke/产品设计文档/runtime/layout.md:392-425` |
| 完成 | `artifact show-path` | 当前代码已额外支持按绝对路径直接查看 artifact 文件。 | `docs/sasuke/产品设计文档/interaction/cli.md:122-128` |
| 待办 | `artifact export` | 设计文档定义了 artifact 导出命令，当前 CLI 尚未实现。 | `docs/sasuke/产品设计文档/interaction/cli.md:122-128` |
| 待办 | `inspect` 命令空间 | 设计文档定义了 inspect 系列命令，当前 CLI 尚未暴露。 | `docs/sasuke/产品设计文档/interaction/cli.md:130-136` |
| 待办 | `provider` 命令空间 | 设计文档定义了 provider list/show/doctor/test，当前 CLI 尚未暴露。 | `docs/sasuke/产品设计文档/interaction/cli.md:138-145` |
| 完成 | Workflow DSL 基础解析 | 已实现 `WorkflowDsl`、节点/边模型与结构化反序列化。 | `docs/sasuke/产品设计文档/dsl/overview.md:1-999`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:113-126` |
| 完成 | Workflow DSL 基本校验 | 已实现 version、entry、节点唯一性、edge 合法性、开启 AI 输出验证的 worker 节点唯一性等校验。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:113-126`; `docs/sasuke/产品设计文档/runtime/overview.md:43-65` |
| 完成 | `session=continue` DSL 约束 | 已对显式 `session=continue` 做 provider 能力边界校验，当前仅允许真实节点目标，`$end` / `$new-round` 不允许 continue。 | `docs/sasuke/产品设计文档/product/overview.md:66-70`; `docs/sasuke/产品设计文档/provider/adapter.md:119-127` |
| 完成 | Worker JSON 输出验证 | `worker` 支持 `output.kind=json` 与 `success_condition.path/equals`，runtime 根据指定字段值产生 success/failure；schema 输出不合法时自动隐藏追问修复。 | `docs/sasuke/产品设计文档/dsl/nodes/worker.md`; `docs/sasuke/开发计划/新增流程/新增任务&工作流.md` |
| 完成 | `$new-round` 边目标 | workflow edge 可显式指向 `$new-round`，控制引擎返回 `OpenNewRound`。 | `docs/sasuke/产品设计文档/dsl/overview.md`; `docs/sasuke/产品设计文档/runtime/layout.md` |
| 完成 | `worker.显式 edge` 合法性校验 | 已校验 `worker` 只能引用 worker 节点；需要产物校验的来源 worker 通过 `output.artifact` 声明 canonical artifact。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:194-204`; `docs/sasuke/产品设计文档/runtime/control.md:187-220` |
| 完成 | runtime 顶层目录布局 | 已实现 `.sasuke/` 下 logs、tasks、runs、rounds、attempt、artifacts、attachments、raw stream 等路径模型。 | `docs/sasuke/产品设计文档/runtime/layout.md:98-445` |
| 完成 | 用户级 profile 路径解析 | profile 已收敛为内置 + 用户级两类来源，自定义角色统一存储到 `~/.sasuke/context/profiles/<name>-<id>.md`；不再读取项目级 profile 目录，也不提供迁移或双读 fallback。另外：profile 目录读取已改为容错跳过，单个文件缺少合法 front matter 时只记录 warn 并跳过，不再中断整个 profile 列表；front matter 解析入口统一剥离 UTF-8 BOM，避免 Windows 下带 BOM 文件被误判为缺少 front matter。 | `docs/sasuke/产品设计文档/runtime/overview.md`; `docs/sasuke/产品设计文档/runtime/layout.md` |
| 待办 | workflow preset 解析链路 | 设计文档要求支持项目/用户预设 workflow，当前仅支持 task workflow 与 CLI `--workflow` 覆盖。 | `docs/sasuke/产品设计文档/interaction/cli.md:74-86`; `docs/sasuke/产品设计文档/runtime/layout.md:153-160` |
| 完成 | `run.json` 状态模型 | 已实现 run 级状态结构与一致性校验。 | `docs/sasuke/产品设计文档/runtime/state/run.json.md:14-149` |
| 完成 | `round.json` 状态模型 | 已实现 round 级状态结构与一致性校验。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:127-138`; `docs/sasuke/产品设计文档/runtime/control.md:254-289` |
| 完成 | `node.json` 状态模型 | 已实现 attempt 级状态结构、resolved config 快照与一致性校验。 | `docs/sasuke/产品设计文档/runtime/state/node.json.md:14-145` |
| 完成 | `worker-ref.json` 状态模型 | 已实现 provider-specific 会话引用结构与最小校验。 | `docs/sasuke/产品设计文档/provider/worker-ref.md:30-104` |
| 部分完成 | `task.json` 扩展元数据 | 当前已实现最小 `id/title/description`，满足 console task picker 展示；更完整 task 管理字段仍待后续补齐。 | `docs/sasuke/产品设计文档/runtime/state/task.json.md:1-999`; `docs/sasuke/产品设计文档/runtime/layout.md:200-214` |
| 完成 | workflow snapshot 落盘 | 已在 run 启动时落盘 `workflow.snapshot.json`。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:21-24, 131-138`; `docs/sasuke/产品设计文档/runtime/layout.md:217-250` |
| 完成 | resolved workflow / provenance 落盘 | 已在 task authoring 目录落盘 `workflow.resolved.json` 与 `provenance.json`。 | `docs/sasuke/产品设计文档/runtime/layout.md:203-210`; `docs/sasuke/产品设计文档/runtime/overview.md:62-65` |
| 完成 | ACP provider 默认实现 | 已实现基于 ACP registry ID 的 `claude-acp` 默认 provider、doctor、worker 调用、sessionId 提取、open command 构建。 | `docs/sasuke/产品设计文档/provider/overview.md:20-46`; `docs/sasuke/产品设计文档/provider/implementations/claude-code.md:17-39, 147-238, 275-341` |
| 完成 | provider 能力暴露 | 已实现 `supports_open_session / supports_continue_session / supports_raw_stream` 能力模型。 | `docs/sasuke/产品设计文档/provider/adapter.md:27-141`; `docs/sasuke/产品设计文档/provider/implementations/claude-code.md:303-324` |
| 完成 | prompt bundle 渲染 | 已实现 system/user prompt 组织、冷数据索引、feedback summary 注入。 | `docs/sasuke/产品设计文档/provider/prompt-bundle.md:1-999`; `docs/sasuke/产品设计文档/provider/implementations/claude-code.md:71-145` |
| 完成 | worker 节点执行 | 已支持 `worker` 节点按 resolved config 调用 provider，并生成 output artifact / worker-ref。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:264-270`; `docs/sasuke/产品设计文档/runtime/control.md:102-128` |
| 完成 | 开启 AI 输出验证的 worker 节点执行 | 已支持 output validation 通过 provider 通道运行，自动收集证据并产出 `验收输出产物`。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:278-282`; `docs/sasuke/产品设计文档/runtime/control.md:141-152` |
| 完成 | worker 节点执行 | 已支持读取 `节点输出产物`、串行执行命令、生成 `节点输出产物`。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:272-276`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:194-204` |
| 完成 | `节点输出产物` 规范化落盘 | 已支持对 provider 返回的 `节点输出产物` 做解析、校验与规范化写入。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:151-164`; `docs/sasuke/产品设计文档/runtime/layout.md:394-406` |
| 完成 | `节点输出产物` 规范化落盘 | 已支持 `节点输出产物` 生成、校验与 canonical 写入。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:151-164`; `docs/sasuke/产品设计文档/runtime/control.md:129-140` |
| 完成 | `验收输出产物` 规范化落盘 | 已支持 `验收输出产物` 解析、校验与 canonical 写入。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:151-164`; `docs/sasuke/产品设计文档/runtime/control.md:141-152` |
| 完成 | outcome/status 分离 | 已在 run/round/node 状态模型中实现 `status` 与 `outcome` 分离约束。 | `docs/sasuke/产品设计文档/runtime/overview.md:66-80`; `docs/sasuke/产品设计文档/runtime/control.md:62-98` |
| 完成 | 运行态 UI 状态统一派生 | 已在 Tauri VM 层新增 `runtimeDisplay`，由后端统一根据 `status/outcome/pauseReason/current/resumable` 派生 `code/tone/icon/terminal/resumable/reasonCode`；GraphView、会话顶部选中栏、Session Switcher 和 AI-DYNAMIC 内部节点不再各自推断状态，动态节点状态来源统一为 dynamic graph 节点事实状态而非 ACP attempt 会话目录；会话 composer 的错误态文案按 `runtimeDisplay.code` 显式映射，`error-blocked / killed / failure / invalid` 不再回退到暂停文案；用户主动停止 ACP 会话时，即使 provider 超时被 kill，也按 `process-interrupted` 可继续暂停处理。 | `docs/sasuke/产品设计文档/interaction/app/conversational-runtime.md`; `docs/sasuke/产品设计文档/interaction/app/round-detail.md` |
| 完成 | 控制流决策引擎 | 已实现基于 node outcome 的 `TransitionToNode / OpenNewRound / PauseRun / CompleteRun` 决策。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:206-237`; `docs/sasuke/产品设计文档/runtime/control.md:155-180` |
| 完成 | attempt 限制 | 已支持 `worker.failure` 经显式 edge 回到 worker，并按当前 round 内修复/重试跳转统计 `max_attempts`；正常 success 前进与 schema 隐藏修复不消耗次数。 | `docs/sasuke/产品设计文档/runtime/control.md`; `docs/sasuke/产品设计文档/dsl/control.md` |
| 完成 | `output.schema` 自动修复 | 已移除 `invalid` edge；声明 `output.schema` 的 worker 输出不合法时同 attempt 隐藏追问修复，最多 3 次，仍不合法则 workflow failure。 | `docs/sasuke/产品设计文档/runtime/control.md`; `docs/sasuke/产品设计文档/dsl/control.md` |
| 完成 | acceptance loop | 已支持 `worker.failure + auto_loop` 新建 round，并将最新 output validation 反馈带回 entry。 | `docs/sasuke/产品设计文档/runtime/control.md:236-307, 414-416`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:423-424, 447-449` |
| 完成 | `$end` 终止语义 | 已支持命中 `$end` 时按 success/failure 完成 run。 | `docs/sasuke/产品设计文档/runtime/control.md:176-181, 446-449`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:403-405, 447-449` |
| 完成 | run 级 progress 快照 | 已实现 `run-progress.json` 最小快照写入。 | `docs/sasuke/产品设计文档/interaction/progress.md:58-110, 142-156`; `docs/sasuke/开发计划/可观测性plan.md:80-112` |
| 完成 | run 级 events 时间线 | 已实现 `events.jsonl` 写入与关键生命周期事件记录。 | `docs/sasuke/产品设计文档/interaction/progress.md:112-156`; `docs/sasuke/开发计划/可观测性plan.md:80-112` |
| 完成 | attempt 级 raw stream 归档 | 已实现 provider stdout/stderr 采集并落盘 `raw.stream.jsonl`。 | `docs/sasuke/产品设计文档/interaction/progress.md:40-57, 142-156`; `docs/sasuke/开发计划/可观测性plan.md:113-145` |
| 完成 | runtime debug 日志 | 已实现 user project runtime 下的 `logs/runtime.log` 与 observability 初始化。 | `docs/sasuke/开发计划/可观测性plan.md:19-47, 162-176`; `docs/sasuke/产品设计文档/runtime/layout.md` |
| 待办 | `progress.events.jsonl` 规范化事件流 | 当前仅保留路径与设计占位，尚未完整实现 attempt 级规范化进度事件。 | `docs/sasuke/产品设计文档/interaction/progress.md:47-57, 112-156`; `docs/sasuke/开发计划/可观测性plan.md:113-145, 217-218` |
| 完成 | DSL loop 参数合法性校验 | 已校验可选 `max_attempts`、`max_rounds` 必须为正整数；留空表示不限制。 | `docs/sasuke/产品设计文档/dsl/control.md` |
| 待办 | 无 output validation 场景的 acceptance 配置校验 | 需校验没有 `worker` 节点时，`failure 边` 不能作为有效控制配置单独存在。 | `docs/sasuke/产品设计文档/dsl/control.md` |
| 待办 | 节点保留字与 `$end` 合法性校验 | 需补齐节点 id 保留字冲突校验，以及 `to = "$end"` 时仅允许 `success/failure` 的约束。 | `docs/sasuke/产品设计文档/dsl/control.md:44-53, 336-337, 325-330` |
| 待办 | `session=continue` 目标能力校验 | 需按目标节点/provider 校验 continue 能力，而不只做当前的基础限制。 | `docs/sasuke/产品设计文档/dsl/control.md:349-350`; `docs/sasuke/产品设计文档/provider/adapter.md:124-126` |
| 完成 | repair / continue 的 session lineage | 已明确并实现 `session=continue` 回到目标 worker 时复用该目标节点当前最新 attempt 的 worker-ref。 | `docs/sasuke/产品设计文档/runtime/control.md` |
| 待办 | `node.json.resolvedConfig.sessionMode` 对齐 | 需在 attempt 元信息中记录实际启动的 `sessionMode`，保证 continue/new 语义可追溯。 | `docs/sasuke/产品设计文档/runtime/state/node.json.md:83-104`; `docs/sasuke/产品设计文档/runtime/control.md:337-374` |
| 待办 | output validation 证据包边界完善 | 需补齐 output validation 对当前 round 最新 `节点输出产物`、上游 worker output artifact、显式暴露 attachments 的证据收集规则。 | `docs/sasuke/产品设计文档/provider/invocation.md:173-188, 206-213`; `docs/sasuke/产品设计文档/runtime/layout.md:408-425` |
| 待办 | cold attachments 暴露链路 | 需增加 attachments 的发现、筛选、暴露到 provider invocation 的完整链路。 | `docs/sasuke/产品设计文档/provider/invocation.md:206-213`; `docs/sasuke/产品设计文档/runtime/layout.md:408-425` |
| 待办 | prompt bundle 完整契约 | 需补齐 role contract、artifact schema prompt、cold attachment index 与更完整 runtime context 注入。 | `docs/sasuke/产品设计文档/provider/prompt-bundle.md:97-243` |
| 待办 | `worker-ref.json` 完整 schema 校验 | 当前仅有最小校验，仍需补齐字段、枚举、布尔类型、`openCommand` 结构等完整约束。 | `docs/sasuke/产品设计文档/provider/worker-ref.md:30-69` |
| 待办 | runtime 状态文件 schema 命名对齐 | 需统一 `task.json`、`run.json`、`node.json`、`worker-ref.json` 的落盘字段命名与文档 schema。 | `docs/sasuke/产品设计文档/runtime/state/task.json.md:15-43`; `docs/sasuke/产品设计文档/runtime/state/run.json.md:16-50`; `docs/sasuke/产品设计文档/runtime/state/node.json.md:16-53`; `docs/sasuke/产品设计文档/provider/worker-ref.md:30-45` |
| 待办 | `节点输出产物.source.json` sidecar | `worker` attempt 目录仍需补齐 `节点输出产物.source.json`，与 layout 设计保持一致。 | `docs/sasuke/产品设计文档/runtime/layout.md:339-355` |
| 待办 | `run-progress.json` stage 枚举对齐 | 需统一 `currentStage` 的命名格式与文档约定，并明确兼容策略。 | `docs/sasuke/产品设计文档/interaction/progress.md:95-104` |
| 待办 | `RuntimeConfig.default_provider` 生效链路 | 需让 runtime 默认 provider 真正由配置驱动，而不是只停留在配置结构中。 | `docs/sasuke/产品设计文档/runtime/overview.md:81-92` |
| 待办 | 用户目录 `~/.sasuke` 解析 | 需将用户级目录解析收敛为 home 语义，而非固定机器路径。 | `docs/sasuke/产品设计文档/runtime/layout.md:48-68` |
| 待办 | CLI 参数契约统一 | 需统一 `run/*`、`artifact/*` 命令的参数定位模型，并与文档示例保持一致。 | `docs/sasuke/产品设计文档/interaction/cli.md:61-72, 122-128` |
| 待办 | `open-session` handoff 语义收敛 | 需明确并实现 `run open-session` 是实际打开 provider 会话，还是只输出 provider-native command。 | `docs/sasuke/产品设计文档/interaction/cli.md:111-121`; `docs/sasuke/产品设计文档/provider/worker-ref.md:76-93` |
| 待办 | inspect / provider 输出 schema | `inspect` 与 `provider` 目前只有命令设计，仍需补齐输出结构与交互契约。 | `docs/sasuke/产品设计文档/interaction/cli.md:130-145` |
| 完成 | Tauri 2.x 桌面端 MVP 原型对齐 | 新增 `src-tauri/` 与 `web/`，以 Tauri commands 复用 Rust core，并已按 `interaction/app/原型` 对齐应用壳、任务列表 Task Preview、工作流 execution history、Round 三块工作台和设置页本地偏好控件。 | `docs/sasuke/产品设计文档/interaction/app/overview.md:1-999`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:55-65` |
| 完成 | 桌面端浏览器调试 fallback | `web/src/api.ts` 在非 Tauri 浏览器环境使用 mock view model，支持用 `npm run web:dev` 直接检查任务列表、工作流、Round 详情和设置页布局；Tauri 环境仍使用真实 commands。 | `docs/sasuke/产品设计文档/interaction/app/overview.md:1-999`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:55-66` |
| 完成 | 桌面端 workspace 选择与记忆 | 已支持启动时恢复最近 workspace、从 `src-tauri/` 向上识别项目根、通过原生目录选择器切换 workspace，并在左侧应用壳展示切换入口。 | `docs/sasuke/产品设计文档/interaction/app/shell.md:27-140`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-63` |
| 完成 | 任务列表宽度与刷新反馈 | 任务列表已改为固定比例列宽并在单元格内截断；手动刷新保留低对比度局部反馈，后台自动刷新静默更新，避免刷新按钮和表格顶部出现品牌色闪烁；首次加载使用骨架屏；未实现动作以显式禁用按钮展示。 | `docs/sasuke/产品设计文档/interaction/app/task-list.md:84-177`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-65` |
| 完成 | 桌面端 Tailwind/shadcn UI 重构 | 桌面端 UI 已从自定义全局 CSS 一次性迁移到 Tailwind CSS v4 + `shadcn@latest`，基础控件使用 shadcn/ui 生成组件，保留原有 IA 与运行操作合约。 | `docs/sasuke/产品设计文档/interaction/app/overview.md:138-167`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-65` |
| 完成 | 桌面端任务编排三页 IA 收敛 | 任务详情并入任务工作流页，run 详情并入 workflow run 分组，Round 详情作为唯一执行详情页；侧边栏“任务编排”一级入口始终回到任务列表根页面。 | `docs/sasuke/产品设计文档/interaction/app/overview.md:49-68`; `docs/sasuke/产品设计文档/interaction/app/task-list.md:8-12`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md:1-185`; `docs/sasuke/产品设计文档/interaction/app/round-detail.md:1-234` |
| 完成 | 桌面端真实工作流图 | 工作流卡片列表已替换为 React Flow 节点-边画布：任务工作流页支持作者态工作流编辑，Round 详情页保留运行态快照节点选中、双击详情和右键菜单。 | `docs/sasuke/产品设计文档/interaction/app/overview.md:152-177`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md:169-181`; `docs/sasuke/产品设计文档/interaction/app/round-detail.md:218-232` |
| 完成 | 桌面端渠道化更新 | 已接入 Tauri updater，default / wb 按构建渠道隔离内置更新地址和 public key；default 使用 GitHub Release latest.json，并由 release-please 同一 workflow 确保 git tag 后附加安装包、签名和 updater manifest；wb 使用内网占位地址；设置页高级 tab 支持覆盖 URL、恢复内置地址、后台状态和手动检查更新。 | `docs/sasuke/产品设计文档/interaction/app/settings.md`; `docs/sasuke/产品设计文档/interaction/app/overview.md`; `docs/sasuke/开发计划/sasuke-mvp-plan.md` |
| 完成 | 桌面端更新分层红点提醒 | 当后台发现当前可更新版本时，左侧 Settings、设置页 Advanced tab、Advanced 内 Updates 标题都会显示红点；进入设置页只清 Settings 红点，进入 Advanced 只清 Advanced 红点，Updates 红点仅在当前无可更新版本时消失，且提示与版本号绑定。 | `docs/sasuke/产品设计文档/interaction/app/settings.md`; `docs/sasuke/产品设计文档/interaction/app/shell.md` |
| 完成 | 桌面端更新公告区 | 右侧主内容区顶部新增一次性更新公告：每个新版本首次发现时展示，可关闭、可点开提示弹窗并引导用户前往 设置 → 高级 → 更新；公告关闭状态和可用更新快照都跨重启持久化。 | `docs/sasuke/产品设计文档/interaction/app/settings.md`; `docs/sasuke/开发计划/sasuke-mvp-plan.md` |
| 完成 | 创建任务与工作流作者态编辑 | 任务列表只保留“创建任务”；创建任务 Sheet 支持导入 txt/md requirement、基于已配置 Agent 创建默认 workflow，模板下拉支持删除非默认模板与保存修改到当前非默认模板，并在任务工作流页继续编辑保存。 | `docs/sasuke/开发计划/新增流程/新增任务&工作流.md`; `docs/sasuke/产品设计文档/interaction/app/task-list.md`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md` |
| 完成 | 默认轻量工作流 | 已新增“开发测试”内置角色和 `拷问 -> 开发测试 -> 验收` 内置轻量模板；验收失败从开发测试开启新 Round，次数由与完整模板一致的 `max_attempts=10`、`max_rounds=3` 控制；完整/轻量模板分别保留采访/拷问可选入口开关，模板只用 `isBuiltIn` 区分是否内置；定时任务冻结有效入口选择。Rust 编译与专项接口测试、Web 全量测试、生产构建和页面验收已通过；根 crate 全量 Rust 测试在 10 分钟工具窗口内未结束且无失败输出。 | `docs/sasuke/开发计划/新增流程/默认轻量工作流实施方案.md`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md`; `docs/sasuke/产品设计文档/interaction/app/run-mode-management.md` |
| 完成 | 上下文管理 / Profile 管理 | 侧边栏“知识库”升级为上下文管理，支持内置与用户级 profile 默认加载、搜索、新增、修改、查看和工作流 profile 选择器；自定义角色不再区分项目级。 | `docs/sasuke/开发计划/新增流程/新增上下文管理.md`; `docs/sasuke/产品设计文档/interaction/app/shell.md`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md` |
| 完成 | 桌面端测试问题修复 | 已补齐 workflow control 展示、前后端 i18n、Round trace 真实路径图、Round 动态 Tabs、任务/工作流列表分页排序与统一滚动布局；工作流蓝图页 control 规则条与画布分层展示，并限制 GraphView 自动放大，避免少量节点撑满画布。2026-05-08 验收补充：设置中的字体切换现已覆盖导航栏、面包屑与 requirement 正文；工作流页正序排序不再把全部 run 自动展开，且 run 内 round 改为最新在上；工作流页与 Round 详情页进一步收紧纵向留白。 | `docs/sasuke/测试问题/测试问题.md:1-20`; `docs/sasuke/产品设计文档/interaction/app/settings.md:84-136`; `docs/sasuke/产品设计文档/interaction/app/task-list.md:172-189`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md:169-189`; `docs/sasuke/产品设计文档/interaction/app/round-detail.md:229-255`; `docs/sasuke/产品设计文档/runtime/state/round.json.md:14-112` |
| 完成 | 任务编排首页视觉与溢出修正 | 已收敛首页 summary cards、间距与工具条视觉层级，Task Preview 改为固定 header + 内部滚动正文，执行统计窄栏单列展示，避免底部统计贴边或超出卡片。 | `docs/sasuke/产品设计文档/interaction/app/task-list.md:65-190`; `docs/sasuke/产品设计文档/interaction/app/overview.md:162-173`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-72` |
| 完成 | 任务列表抽屉式预览交互 | Task Preview 已改为 shadcn/ui Sheet 右侧抽屉：初始不打开，单击任务行滑出，单击其他任务行切换内容；抽屉上方展示完整需求框，支持框内滚动和复制 icon，底部固定单一“工作流”按钮，不展示执行统计或查看产物入口。 | `docs/sasuke/产品设计文档/interaction/app/task-list.md:139-215`; `docs/sasuke/产品设计文档/interaction/app/overview.md:175-184`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-73` |
| 完成 | Round 详情抽屉式详情查看 | Round 详情页右侧 Detail Viewer 已改为 shadcn/ui Sheet 详情抽屉，释放工作图和信息流宽度；双击节点、右键详情/会话、点击信息流条目打开抽屉；固定时切换为右侧占位面板，主工作区自动收窄；信息流拆为上下文、编排事件、节点进度，固定态不复用 Sheet Portal，并用 `contextNodeId` 统一避免全局详情打断 node 上下文。 | `docs/sasuke/产品设计文档/interaction/app/round-detail.md:23-249`; `docs/sasuke/产品设计文档/interaction/app/overview.md:186-195`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-74` |
| 完成 | 工作流运行记录紧凑分组列表 | 工作流运行记录已从 Run/Round 混合表格改为紧凑分组列表：Run 行只保留当前 Round 与必要操作，Round 明细只保留状态、结果、当前节点和查看入口；2026-05-09 起补齐稳定列头、运行记录 Header 主操作和更清楚的展开层级。 | `docs/sasuke/产品设计文档/interaction/app/task-workflow.md:90-203` |
| 完成 | 需求详情抽屉交互收敛 | 工作流页删除无效 Tabs 与继续/停止/禁用查看需求入口；任务列表 Task Preview 直接展示完整需求框，长内容框内滚动并提供复制 icon，不再使用截断预览 + 二级完整需求视图。 | `docs/sasuke/产品设计文档/interaction/app/task-workflow.md:42-185`; `docs/sasuke/产品设计文档/interaction/app/task-list.md:124-223`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-84` |
| 完成 | 浏览器调试 Deep Link | 桌面 Web 调试模式已支持 `/tasks`、`/tasks/:taskId/workflow`、`/tasks/:taskId/runs/:runId/rounds/:roundId`、`/settings`，便于 agent-browser 直达页面验证。 | `docs/sasuke/产品设计文档/interaction/app/overview.md:195-207`; `docs/sasuke/开发计划/sasuke-mvp-plan.md:54-76` |
| 完成 | 任务编排顶部 Header 统一 | 任务列表、任务工作流和 Round 详情页已统一使用 Page Header 承载面包屑、标题、说明/状态、操作与低对比 stats，内容区不再重复顶部上下文。 | `docs/sasuke/产品设计文档/interaction/app/task-list.md:16-63`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md:21-61`; `docs/sasuke/产品设计文档/interaction/app/round-detail.md:23-47` |
| 完成 | 多 ACP agent 接入 | Agent 管理已支持 ACP registry 固定集 `claude-acp`、`codex-acp`、`cursor`、`gemini`、`opencode`，工作流只允许选择 doctor 成功的已配置 agent，Cursor 图标已纳入 `web/public/agent-icons` 打包路径。 | `docs/sasuke/产品设计文档/interaction/app/agent-management.md`; `docs/sasuke/产品设计文档/interaction/app/task-workflow.md`; `docs/sasuke/开发计划/新增流程/agent管理.md` |
| 完成 | 运行模式管理与 ACP 模型切换收敛 | 已修复配置页模型 Select 定位/点击、AUTO 模板保存反馈与校验、Agent/工作流不可选原因展示、composer 下方持久 AUTO 错误、ACP follow-up 模型 override 传递，以及快速会话创建同步阻塞导致的卡顿；补齐工作流模板另存后刷新 store、另存生成新 `workflow-{uuid}`，并将工作流/AUTO 模板统一迁到用户级 `~/.sasuke/context` 跨 workspace 持久化；AI-DYNAMIC 动态 Agent 策略补齐 bootstrap 模型选择、可选 Agent 模型清空和“决策指南 / 模型锁定”二选一规则；运行模式管理去掉页面级保存，切换 tab、工作流模板和 AUTO 模板立即应用且不清空另一侧配置；AUTO 操作栏收敛为“保存修改 / 新模板名称 / 另存模板”，未使用模板时保存修改直接持久化默认 AUTO 配置，AUTO 校验反馈不再泄漏到工作流模板 tab，保存期间展示“保存中…”且成功横幅自动消失，模板操作行提交按钮统一使用主题色；运行模式管理页已支持按新 UI conversation workspace 选择项目，进入默认选中当前打开项目，切换项目加载该项目自己的运行模式记忆，空记录回到默认 AUTO，避免跨项目继承上一个 workspace 的配置。 | `docs/sasuke/产品设计文档/interaction/app/run-mode-management.md`; `docs/sasuke/产品设计文档/interaction/app/conversational-home.md`; `docs/sasuke/开发计划/新UI/会话式主页实施进度.md` |
| 完成 | 右侧工作区入口、Tab 与 Diff 视觉收敛 | 空态资源入口改为按工作区可用宽度整行点击；复用 shadcn/Radix DropdownMenu，将溢出 Tab 列表收敛为 32px 单行选项，展示资源图标、截断标题和当前 Tab 勾选态；diff/version 只读浏览不再因内部限制码显示黄点，修改文件图标改为主题 `gold-running` 蓝色 token。补齐入口、Tab 与限制 diff 的 DOM 契约。 | `docs/sasuke/产品设计文档/interaction/app/workspace-files.md`; `web/tests/right-workspace-dom.test.tsx`; `web/tests/turn-file-changes.test.tsx` |
| 待办 | provider 命令级诊断入口 | provider `doctor()` 已有代码实现，但 CLI 侧尚未提供 list/show/doctor/test 命令。 | `docs/sasuke/产品设计文档/provider/adapter.md:27-46`; `docs/sasuke/产品设计文档/interaction/cli.md:138-145` |
| 待办 | 项目索引 `index.json` | 目录设计中定义了项目级索引，当前 storage 尚未落地。 | `docs/sasuke/产品设计文档/runtime/layout.md:109-150` |
| 待办 | 高级调度 / 多 run 并发 orchestration | 设计文档明确列为后续能力，当前未实现。 | `docs/sasuke/开发计划/sasuke-mvp-plan.md:46-53` |
