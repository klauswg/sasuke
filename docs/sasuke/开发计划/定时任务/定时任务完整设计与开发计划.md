# sasuke 定时任务开发计划

## 目标

为 Direct、Workflow、AUTO 增加统一的定时触发能力，复用现有 Task/Run/ACP 生命周期，并提供 composer 创建入口和左侧定时任务管理页。

## 已确认范围

- 定时任务是触发层，不是第四种运行模式。
- 支持 `At`、重复预设、固定间隔 `Every`、自定义 Cron。
- `Every` 只支持分钟和小时；首次启用记录 `anchorAt`，暂停不补跑，重新启用或修改间隔时重新起算。
- Workflow/AUTO 只能新会话；Direct 支持新会话和持续会话。
- 默认启用队列保护；关闭后按 30 秒间隔最多重试 3 次。
- 错过执行不补跑。
- 会话使用 AlarmClock 图标标注，不增加任务名称字段。

## 2026-08-03 可靠性与 Direct 对齐修订

本修订覆盖 AionUi 当前 Direct 定时任务能力，并修正上一版计划将“启动后台执行”误认为“执行完成”的问题。Direct 必须补齐立即执行、任务详情/执行历史、诊断字段、用户执行配置透传、missed、崩溃恢复、lease/heartbeat、完成通知和保持唤醒；Workflow/AUTO 复用同一 occurrence 生命周期，但保留 sasuke 自身的内容指纹、Task/Run、Workflow snapshot 和 AUTO authoring 规则。

定时任务不增加独立名称字段，展示标题取 instruction 第一条非空行。Scheduler source of truth 是用户级 `core.db` 中按 `project_id` 分区的 SQLite 表；不保留双写、旧数据迁移或 runtime fallback。

无人值守处理固定为：permission request 以 `failed + SCHEDULED_PERMISSION_REQUIRED` 结束；`AskUserQuestion` 以 `attention_required + SCHEDULED_USER_INPUT_REQUIRED` 结束并保留可恢复 Run。两者都必须释放 scheduler lease，不能无限等待。

## 实施任务

## 2026-08-19 `core.db` 统一存储与 project identity 收敛

本次按开发阶段破坏式更新实施，不迁移已有定时任务：

- [x] `SasukePaths::scheduler_db_path()` 直接复用用户级 `core_db_path()`，删除 scheduler legacy path 和 JSON store。
- [x] `scheduled_jobs` 使用 `(project_id, id)` 主键；`scheduled_occurrences` 增加 `project_id`，使用 `(project_id, id)` 主键、`(project_id, job_id)` 级联外键和 `(project_id, job_id, scheduled_at, trigger_kind)` 唯一约束。
- [x] repository、service、coordinator、lease、heartbeat/active guard registry、recovery、retention、notification 与 attention lookup 全部显式传递 canonical `project_id`；不再使用 `workspaceKey` 或仅凭末级 ID 全局查找。
- [x] Scheduler 复用 `core_schema`，以独立 `component = 'scheduler'` 管理 schema version，并与 Runtime recovery 表共存。
- [x] 删除旧 `scheduled-tasks.db` / JSON 的 import、migration marker、schema upgrade 和 fallback 路径；旧文件即使存在或损坏也不得打开。
- [x] 增加接口层定向测试，覆盖跨项目相同 ID 隔离、definition project mismatch 拒绝、Scheduler/Runtime recovery 共库及旧文件不读取。

方案评审：问题来源于 workspace identity 与物理数据库边界耦合。当前 `project_id` 已是应用内唯一 workspace identity，因此应让约束和接口显式表达作用域，而不是继续依赖每 workspace 一个文件。实现复用现有 `core.db`、`core_schema`、SQLite 事务和 coordinator；没有新增 aggregate、缓存、队列、并发机制或迁移抽象，不过度设计。

性能评审：enabled deadline、active occurrence、history 和 status history 索引均以 `project_id` 为首列；共享数据库后的协调、恢复、分页和 retention 仍只访问单项目范围，不引入全库扫描、N+1、无界加载、额外缓存、扩大锁范围或事件线程阻塞。验收限定为 Rust 编译、scheduler/storage/scheduled runtime/service 定向测试、格式与 diff 检查，不运行全量回归或前端验证。

定向验收结果（2026-08-19）：`cargo check --lib` 与 desktop `cargo check` 通过；scheduler repository 45 项、core-state 6 项、scheduler path 1 项、scheduled runtime 87 项、scheduled service 26 项、scheduled attention 2 项全部通过；`git diff --check` 通过。Scheduler 改动文件已按 rustfmt 格式化；仓库级 `cargo fmt --all -- --check` 仅剩本任务范围外未提交文件 `src-tauri/src/image_actions.rs` 的既有枚举排版差异。按本次约定未执行全量回归、前端构建或前端/EXE 启动验证。

### 1. 领域模型与持久化

- [x] 在 Rust runtime 增加 `ScheduledTaskDefinition`、`ScheduleSpec`、`EverySpec`、`OverlapPolicy`、`ScheduledTrigger` 等类型。
- [x] 将 definition/occurrence 收敛到 `core.db` 的 project-scoped scheduler tables，使用 occurrence 唯一约束、原子认领、lease、heartbeat 和输入附件快照；旧数据直接废弃。
- [x] 实现 `scheduled-task.json` 校验：模式与 session policy 约束、IANA 时区、Every 正整数和分钟/小时枚举。
- [x] 为 task content 建立稳定指纹；Workflow/AUTO 的 Agent 身份、Agent 策略和可用 Agent 集合属于 authoring 并参与指纹，只有 model、thought level、permission 和 Direct session policy 不参与指纹。
- [x] 为 schema 添加 Rust 接口层单元测试，覆盖非法单位、非法模式、空指纹和重复启用。

### 2. 调度计算器

- [ ] 使用成熟 Cron 解析/计算库实现 `At`、真正的每个整点 Hourly、每天、工作日、每周多选和自定义 Cron。
- [x] 实现 `Every` 的 anchor 计算、暂停/重新启用重置和间隔编辑重置。
- [ ] 明确系统默认时区、DST wall-clock 计算；`Every` 使用绝对时间间隔。
- [ ] 实现 missed 记录和下一个未来时间点计算，不生成补跑 run。
- [ ] 为边界时间、周日跨周、多选星期、DST 和 Every 重置行为添加接口测试。

### 3. Scheduler 与队列保护

- [ ] 用每任务 timer 替换每秒全量轮询；启动和系统唤醒显式恢复 lease、标记 missed，并只安排未来时间点。
- [ ] 对 running、permission waiting、AskUserQuestion、waiting-for-user 和 resumable paused 统一识别 active，并通过 occurrence claim 防止跨进程重复执行。
- [ ] `skip_when_running` 和 `retry_when_busy` 作用于 occurrence；busy 重试有明确上限，错误执行不无限重试。
- [ ] 调度器只创建/认领 occurrence 并调用 App 既有 Task/Run API；真实完成事件必须带 occurrence ID，不能把进程启动标记为成功。
- [ ] 覆盖启动恢复、lease 到期、并发 claim、唤醒 missed、permission、AskUserQuestion、run-now 和进程关闭测试。

### 4. Task/Run/ACP 集成

- [x] 增加创建、更新、启用、暂停、删除和查询 scheduled task 的基础命令接口。
- [ ] 增加立即执行、occurrence 历史、诊断字段和 attention 状态的命令接口。
- [x] Workflow/AUTO 根据 content fingerprint 复用 task 或在下一触发物化新 task；每次内容不变的触发创建新 run。
- [x] Direct new 每次创建 task/run/session；Direct continuous 复用 session 并发送新 prompt。
- [x] 运行时冻结 Workflow/AUTO 的 authoring 快照，并将执行配置写入 run/session metadata。
- [x] 为 Direct Agent 不可修改、Direct workspace/content 变化、Workflow/AUTO Agent/authoring 变化和 model/thought level/permission 变化分别增加回归测试；验证 Workflow/AUTO Agent/authoring 变化后下一次触发创建新 Task，仅执行配置变化时复用 Task 并创建新 Run。

### 5. 前端 composer 与管理页

- [x] 修改 `web/src/components/conversation/ConversationComposer.tsx`：拆分发送菜单、定时创建状态、齿轮重开配置和取消动作。
- [x] 使用 shadcn/ui Dialog、Select、Switch、ToggleGroup 等组件实现定时配置，不重复已有模型/权限/Agent/workspace 控件。
- [x] 增加 `web/src/routes.ts` 和 `web/src/App.tsx` 的定时任务管理路由。
- [x] 在 `ConversationSidebar.tsx` 增加 AlarmClock 导航项、会话行标记和管理列表入口。
- [x] 管理页采用紧凑列表布局，支持启用开关、编辑抽屉和最近触发状态。
- [x] 使用 prompt-kit 现有 composer/message 基础组件，不自研消息容器。

### 6. 会话标记与 ViewModel

- [x] 扩展 `src-tauri/src/view_models_conversation.rs` 返回 scheduled-task 标识和最近触发信息。
- [x] 会话页头使用 Lucide `AlarmClock`；Direct continuous 定时触发使用分隔线。
- [x] Workflow/AUTO、Direct new、Direct continuous 的侧栏显示和 run 展示分别回归验证。

### 7. 验证与发布门槛

- [ ] Rust scheduler repository、runtime adapter 和命令接口测试全部通过。
- [ ] Web tests、生产构建和定时任务管理 deep link 覆盖详情、历史、立即执行和 attention 通知。
- [ ] 启动前端开发服务器，验证 Direct 全部能力以及 Workflow/AUTO 共用 occurrence 生命周期。
- [ ] 验证完成后清理本次启动的测试资源。
- [x] 更新产品设计文档、开发计划和相关导航链接后再进入实现提交。

### 2026-08-06 Scheduler contract baseline

- [x] 移除 scheduler 生命周期测试事件构造中的失效字段，并确认 scheduler 库测试可编译。
- [x] 固化 `MigrationConflict`、`CoordinatorUnavailable`、`PowerInhibitorFailed`、`NotificationFailed` 和 `SkillValidationFailed` 的稳定 `SCHEDULED_*` 序列化契约。
- [x] 将 queue retry（30 秒 / 最多 3 次）、late-fire grace（60 秒）和 occurrence retention（默认 30 天、范围 1-3650 天、每批 500 条）集中为 `src/scheduler/queue.rs` 公共常量并回归测试。

## 相关文档

### 2026-07-30 创建链路修复

- [x] 统一前后端调度协议为扁平的内部标签结构（`{ kind: "At|Repeat|Every|Cron", ... }`），兼容 Composer 的 camelCase 字段。
- [x] 定时创建复用 Direct、AUTO、Workflow 的会话创建前置校验，并展示具体校验原因。
- [x] 增加 `Every.anchorAt` 的 Rust 接口回归测试和前端定时创建校验测试。
- [x] 后台 scheduler loop、触发后 Task/Run/ACP 物化已实现；`missed` 记录和补跑策略仍保留为后续阶段。

- [定时任务运行时设计](../../产品设计文档/runtime/scheduled-task.md)
- [`scheduled-task.json` 规范](../../产品设计文档/runtime/state/scheduled-task.json.md)
- [定时任务交互设计](../../产品设计文档/interaction/app/scheduled-task-management.md)

## 2026-08-03 实施阶段（待执行）

### Phase 1：Occurrence 与 SQLite 调度底座

- [x] 新增 `scheduled_jobs`、`scheduled_occurrences` schema；当前唯一约束为 `(project_id, job_id, scheduled_at, trigger_kind)`。
- [x] 实现 `create_or_get_occurrence`、`claim_occurrence`、`renew_lease`、`finish_occurrence`、`recover_expired`、`mark_missed` 和历史查询接口。
- [x] 编写接口测试：并发 claim、重复时间点、lease 到期恢复、真实终态回写和跨项目隔离。
- [x] 删除旧 JSON definition/trigger 导入和冲突检测入口；runtime 只读写 `core.db`。

### Phase 2：Direct AionUi 能力对齐

- [x] 不做 Direct full-auto 能力预检或 Agent 能力映射，创建和触发原样使用用户选择的执行配置。
- [ ] Direct/new、Direct/continuous 的 occurrence completion hook。
- [ ] permission request -> failed、AskUserQuestion -> attention_required、通知和详情 deep link。
- [ ] run-now、详情/历史、next run/last error/run/retry diagnostics、完成通知和 keep-awake。

### Phase 3：Workflow/AUTO 和时间语义完善

- [ ] Workflow/AUTO 接入统一 occurrence adapter，保留 content fingerprint 和 Task/Run 复用规则。
- [x] 修复 Hourly、系统默认时区、DST 非法/重复本地时间和 Every anchor 编辑语义。
- [ ] 增加每任务 timer、启动/唤醒 missed 检测和跨工作区全局调度恢复。
- [ ] 完成 Rust、Tauri、Web、前端 deep-link 和手动运行验收，并同步产品设计和本计划。
## 实现进度（2026-07-30）

- 已完成调度领域模型、重复预设、多选星期、Every 分钟/小时、IANA 时区与队列保护策略。
- 已完成项目级 JSON 存储、任务列表查询与启停命令。
- 已完成 Composer 发送按钮拆分及“创建定时任务”配置对话框，支持 At、重复、Every、Cron、队列保护和 Direct 会话策略。
- 已完成创建命令的校验、指令快照、执行配置快照与附件复制。
- 已按 Composer 原型对齐入口交互与配置版式：下拉菜单、定时创建态、AlarmClock 摘要、齿轮配置、三段式计划标签和重复频率面板。
- 已接入后台 scheduler loop，并在到点时复用现有 Task/Run/ACP 创建链路；`missed` 记录和补跑策略暂未实现。
### 2026-08-03 Phase 3 runtime lifecycle implementation

- [x] Switched scheduled runtime source of truth to per-workspace SQLite definitions and occurrences; legacy JSON import is startup-only when the database is empty.
- [x] Added transactional create/claim/lease/heartbeat/finish flow, startup missed-point handling without backfill, and bounded busy retry/skip behavior.
- [x] Bound Direct/new, Direct/continuous, Workflow and AUTO execution to occurrence IDs propagated through background App clones and lifecycle events.
- [x] Mapped permission requests to `failed + SCHEDULED_PERMISSION_REQUIRED` and user questions to `attention_required + SCHEDULED_USER_INPUT_REQUIRED`, releasing the lease before update notification.
- [x] Synchronized scheduled task create, update, enable/disable and delete commands with SQLite definitions.
- [x] Added runtime regression tests for completion, failure, intervention, missed startup points, manual trigger scheduling invariance, and lifecycle origin propagation.

### 2026-08-03 Phase 4 frontend occurrence diagnostics

- [x] Added Tauri and browser facade contracts for occurrence history, diagnostics, run-now, and occurrence update subscriptions.
- [x] Added browser-preview occurrence history and manual-run behavior, including running and succeeded update events.
- [x] Added management-page run-now action, execution history, status/error/retry diagnostics, and live occurrence refresh.
- [x] Added web regression tests for browser occurrence state, desktop command forwarding, and management-page controls.
- [ ] Start the web dev server and verify the scheduled-task deep link plus attention/failed detail states in a desktop viewport; remove the temporary test process afterward.

### 2026-08-03 Phase 5 workspace isolation and startup migration（历史，已由 2026-08-19 方案取代）

- [x] Scoped scheduler SQLite storage to each workspace runtime directory instead of the shared user root.
- [x] Filtered scheduler definitions by the current workspace project ID and rejected mismatched execution contexts before Task/Run materialization.
- [x] Added idempotent startup migration for definitions and occurrence history from the former shared scheduler database.
- [x] Kept migration scoped to scheduler data; existing Task/Run/ACP history is not relocated.
- [x] Emitted scheduled-task updates after successful Task/Run materialization so the conversation sidebar refreshes immediately.
- [x] Added Rust regression tests covering path isolation, project-scoped migration, workspace mismatch rejection, and successful materialization notifications.

### 2026-08-03 Phase 6 Direct session association semantics

- [x] Preserved `task_id` when a Direct task's instruction, attachments, or session policy changes.
- [x] Defined Direct/new as new Task/Run/session per trigger and Direct/continuous as reuse of the associated Task/session chain.
- [x] Preserved the association across `New -> Continuous` and `Continuous -> New`; the runtime policy determines whether the next trigger continues or materializes a new Task.
- [x] Prevented Direct content fingerprint changes from forcing Task materialization; mode changes between Direct and Workflow/AUTO still reset the association.
- [x] Added Rust regression tests for Direct authoring/policy edits and Direct fingerprint changes.

### 2026-08-04 Phase 7 queue protection and lifecycle hardening

- [x] Extended queue protection to inspect active ACP prompt activity for every associated current attempt, including dynamic-node attempt directories.
- [x] Kept `RunPaused` as a non-terminal lifecycle event so `RunPaused -> InterventionRequested` can finish the same occurrence and release its lease correctly.
- [x] Applied the same ACP-aware busy decision to scheduled and manual run-now triggers.
- [x] Added Rust regression tests for prompt activity after Run completion and the paused-to-intervention lifecycle.
- [x] Kept user-initiated session termination from disabling the schedule, rewriting `task_id`, or auto-creating a replacement Task; a paused associated Run is handled by the configured skip/bounded-retry policy until resumed or disabled.

### 2026-08-04 Phase 8 management surface UX separation and disabled-state suppression

- [x] Suppressed `next_at` for disabled tasks in both `ScheduledTaskVm` and the diagnostics command so the list no longer shows a phantom next-run time.
- [x] Enriched `ScheduledTaskUpdatedEventVm` with a full `ScheduledTaskVm` snapshot so the frontend can merge a single row locally instead of reloading the entire task list.
- [x] Replaced the management page full-reload subscription with an in-place local merge that preserves scroll position and selection state.
- [x] Moved execution details (diagnostics, occurrence history, run-now, edit, enable/disable, delete) into a dedicated `ScheduledTaskDetailPage` with deep-link routing at `/chat/scheduled-tasks/:id` and a back button to the management list.
- [x] Kept the management page focused on list, filtering, and quick actions (run-now, enable/disable, edit, delete, open detail).
- [x] Added frontend regression tests for management-page local merge, detail-page scoped event handling, and the new occurrence/diagnostics/run-now API contracts.

### 2026-08-05 Phase 9 codex gate, task_id preservation, management UI, and scheduled context

- [x] 2026-08-24 删除按 ACP mode 列表判断定时执行资格的旧 gate；内置与自定义 Agent 一律原样使用用户配置。
- [x] Changed `set_scheduled_task_enabled` to load from SQLite DB instead of JSON store, preserving `task_id` across enable/disable toggles.
- [x] Removed the workspace column from the management list (filter dropdown retained), keeping workspace info in the detail page only.
- [x] Filtered `Skipped` and `Missed` occurrences from execution history; added Chinese error code labels.
- [x] Injected `ScheduledTaskContextInfo` into the `App` clone and rendered it as a hidden context section via `scheduled_task_context.md` prompt templates (zh-CN + en).
- [x] Added prompt templates under `src/prompts/zh-CN/runtime/scheduled_task_context.md` and `src/prompts/en/runtime/scheduled_task_context.md`.
- [x] Ran full Rust and frontend test suites with no regressions.

## 2026-08-05 Phase 10-14 统一完善计划（待实施）

本节是当前实施基线；上方早期阶段中尚未勾选、但已在后续进度完成的条目只保留历史，不作为当前状态判断。Phase 10 重新确认并移除命令层仍存在的 JSON/SQLite 双写，因此不能把早期“runtime 已切换 SQLite”理解为 CRUD 已经只有一个权威数据源。

### Phase 10：基线与 SQLite 唯一权威

- [x] 修复 scheduler 测试中生命周期 event 构造仍传递已移除字段导致的编译失败，并固化基线结果。
- [x] 为所有 CRUD 命令补充 SQLite-only 接口测试，再删除活跃 JSON read/write/sync/fallback。
- [x] 增加持久化 migration marker、重复启动 no-op、冲突回滚和跨工作区迁移测试。
- [x] 保留旧 JSON 作为迁移证据，但确保运行时不可重新读取或写入；启动注册只通过 migration marker 驱动一次性导入。

#### 2026-08-06 SQLite repository 实施进度

- [x] 将 scheduler schema 从 v1 有序迁移到 v2，新增 job `revision`、`next_run_at`、enabled deadline partial index 和 `scheduler_migrations`；迁移事务按 definition 回填 enabled deadline，高于当前支持版本时拒绝写入。
- [x] 将 legacy JSON definition/trigger 先读为值对象，再用单个 immediate transaction 导入并最后写入 `legacy-json-v1`；冲突时 definition、occurrence 和 marker 一起回滚。
- [x] 将旧 shared SQLite 按当前 project 隔离复制，并用 `legacy-shared-db-v1` 保证每个项目数据库只执行一次。
- [x] 增加 project-scoped get/delete、基于 `updated_at` 的乐观更新、revision 冲突返回、enabled job 查询和 runtime projection 更新接口；authoring 时间令牌必须至少推进 1ms，runtime projection 同步推进 JSON/SQL 时间令牌，使旧 authoring 写稳定冲突。
- [x] 将到期 scheduled occurrence 的插入/去重、下一计划点推进和 revision 增长放入同一事务；陈旧 callback 不物化，manual occurrence 不推进 `next_run_at`。
- [x] 增加有界 occurrence retention，batch 必须大于零，保留 `attention_required`、非终态记录、活动 Run 链接及全部 Task/Run/ACP 文件。
- [x] 固化 schema 迁移、marker 原子性、空库 marker、shared DB、项目隔离、乐观冲突、deadline/manual 和 retention repository 回归测试。
- [x] CRUD/startup 消费者已切到 repository/service 接口，命令层不再读写 JSON，也不再用“空库才导入”判断；legacy store 仅作为 marker 保护的一次性迁移输入。

### Phase 11：事件驱动 coordinator 与统一队列

- [x] 使用每 job deadline 的 scheduler coordinator 替换一秒全量轮询，支持 CRUD 后增量 add/reset/remove。
- [x] 统一 startup、system resume 和 overdue timer 的 reconcile、lease recovery 与 missed 语义；missed recovery 每 turn 使用 `MISSED_RECONCILE_BATCH_SIZE` 有界推进并重新入队，避免占住 coordinator。
- [x] 删除运行时重试次数/间隔硬编码，统一调用 `src/scheduler/queue.rs`；公平 `tokio::select!` 防止命令洪峰饿死 deadline、heartbeat 或 clock check。
- [x] 使用 paused time 测试 timer 重排、停用取消、陈旧 callback、run-now、休眠恢复、控制命令积压和大规模 missed backlog。
- [x] 固化“创建只保存定义、显式 run-now 立即执行 manual occurrence 且不改变 `next_run_at`”的命令接口回归测试。

### Phase 12：三模式适配器与桌面能力

- [x] 固化 Direct/new、Direct/continuous、Workflow、AUTO 的 Task/Run/session 适配器接口测试。
- [x] 增加全局 keep-awake setting、`SystemSleepInhibitor` 和 enabled-job 引用状态机；默认关闭并允许显示器休眠。采用 `keepawake 0.6.0` 统一支持 Windows System Power API、macOS IOKit 与 Linux inhibit，退出握手释放 guard，平台失败不阻断调度。
- [ ] 在 macOS CI 或真机执行 desktop crate 编译与 keep-awake 开关 smoke test；当前 Windows 环境已确认 `keepawake` 的 IOKit/`objc2-io-kit` 依赖路径和跨平台状态机实现，但不把 Windows 测试记录为 macOS 编译证据。
- [x] 扩展现有 native notification/intervention 管线，覆盖完成、失败、attention、missed 聚合与 deep link；后端只发结构化事件，前端中英文 i18n 生成文案，同 occurrence/kind 复用既有去重器。

#### 2026-08-07 租约取消与队列状态收敛

- [x] 以测试固化 heartbeat in-flight renew 场景：取消发布后不得继续执行 release/state-inspect；`stop()` 等待 worker，`Drop` 不 abort blocking SQLite 操作。
- [x] 将 claim-handoff、终态/attention 生命周期和 shutdown 统一为“先停止并等待 guard，再落库”的所有权顺序；成功 handoff 仍持有 guard 直到真实 lifecycle 事件。
- [x] 固化 Direct/new、Direct/continuous、Workflow、AUTO 的统一执行适配器接口和 occurrence binding；启动阶段不调用模型。
- [x] 将 manual/scheduled queue decision 统一映射到 ActiveExecution，并以 occurrence attempt 作为重试判断输入。
- [x] elicitation response 在写入响应文件前恢复同一 attention occurrence 的 claim 与 heartbeat，后续终态继续回写原 occurrence。
- [x] 为 manual 与 scheduled 入口收敛 `ActiveExecution` 状态识别，并且只通过 `decide_queue` 生成 skip/retry/start 决策。
- [x] 接通 30 天终态 occurrence 清理策略：启动 reconcile 与终态 occurrence 后按 500 条分批并在批次间 yield，保留 attention/非终态/活动 Run 与全部 Task/Run/ACP 历史。

### Phase 13：历史、时区与 i18n

- [x] 详情页恢复展示 `skipped`、`missed`，提供状态筛选，并增加携带 Task/Run/Round/Attempt 的会话跳转。
- [x] 时区控件使用完整 IANA 列表和系统默认值，以 `@vvo/tzdb` 作为回退；管理页、详情页、创建弹窗和 Composer 的定时任务可见文案已迁入 `web/src/i18n.ts`。
- [x] `ScheduledTaskVm` 替换旧 `scheduleLabel/timezoneLabel/lastTriggerLabel`，改为 typed schedule、原始时区与 RFC 3339 时间；浏览器 facade、桌面接口和所有消费者同步切换，不保留旧字段兼容层。
- [x] 设置页使用 `ScheduledRuntimeSettings` 覆盖 keep-awake 配置值/生效值、完成通知和 `1..=3650` 天 occurrence 保留期；2026-08-09 按交互收敛要求从管理页移除重复入口。
- [x] 定向 Web 验证通过：8 个 scheduled-task/routes 测试文件共 22 个测试；`npm run web:build` 通过。Rust 接口测试已分别证明完整 occurrence 状态映射和 typed ViewModel 序列化。
- [x] 窄屏 UI 验证发现并修复 Shell 固定侧栏挤压管理页的问题：`<=767px` 自动收起侧栏，管理页 header 可换行；390px 截图复验通过。创建弹窗已实际验证完整 IANA 时区搜索（`Pacific/Apia`），设置页已验证同一运行时设置入口。

### Phase 14：接口回归与 UI 验收

- [x] 完成 repository、coordinator、adapter、power、notification、retention 和 Web facade 接口测试：Rust scheduler 86/86、desktop scheduled runtime 77/77，Web 全量 91 files / 578 tests。
- [ ] 全量 Rust gate 尚未通过：`cargo test --workspace --no-fail-fast --offline` 运行至测试阶段后有 2 个未修改的 `src-tauri/src/view_models.rs` 测试失败；其中 permission timeline 用例单独重跑通过，dynamic round graph 用例的业务断言通过后在旧的临时目录清理 `unwrap()` 处失败。定时任务定向测试和 `cargo check -p sasuke-desktop --tests --offline` 均通过。
- [x] Web 全量测试和 production build 已通过；构建仅保留既有的大 chunk warning。
- [x] 启动前端并 deep link 到管理页、设置页；验证 keep-awake、运行时设置、时区和中文窄屏布局。详情历史、Round/Attempt target、Direct/Workflow/AUTO 生命周期以接口测试覆盖；浏览器 facade 不创建持久 occurrence，因此没有用人工页面冒充真实历史数据。
- [x] 验证结束后已停止前端开发服务器；未创建持久 scheduled definition、Task、Run 或 occurrence 测试数据。
- [x] 同步维护产品设计文档、开发计划和最终实现进度；Task 6 的 macOS 编译与 keep-awake 真机 smoke test 保持独立未完成项，不以 Windows 结果代替。

### Phase 15：详情白屏与通知所有权修复（2026-08-09）

- [x] 根因确认：`App` 在 `onSelectConversation` 初始化前求值详情分支，直接传递该回调触发 JavaScript 暂时性死区，URL 已切换但 React 根节点卸载并显示白屏。
- [x] 将会话导航回调初始化移动到页面内容求值之前，并新增真实 `App` 详情 deep link 服务端渲染回归测试；测试已先复现同一 `ReferenceError` 再转绿。
- [x] 根因确认：定时 occurrence 的 lifecycle 同时进入专用定时通知和通用会话通知订阅器；专用通道尊重完成开关，但通用 `RunCompleted` 忽略 `scheduled_occurrence_id` 后额外发送完成通知。
- [x] 通用通知转换统一排除带 `scheduled_occurrence_id` 的 `RunCompleted`、`InterventionRequested` 与 `AcpTurnFinished`；定时任务运行时继续负责 occurrence 终态、完成开关、失败/attention/missed 策略和原生通知发送。
- [x] 新增 Rust 回归测试证明定时完成事件不生成通用通知，且普通非定时任务完成事件仍生成通知。
- [x] 第二轮桌面 WebView 复现确认真实剩余根因：详情有可跳转 occurrence 时渲染 Radix `Tooltip`，但页面不在 `TooltipProvider` 下，抛出 ``Tooltip` must be used within `TooltipProvider` `` 并卸载 `#root`。
- [x] 新增应用级 `AppProviders`，由根部统一提供 Tooltip 上下文并移除设置页局部所有权；新增 provider 行为回归测试，已按 RED（模块不存在）到 GREEN 执行。
- [x] 完成 Web 全量测试（93 files / 580 tests）、production build、Rust 通知模块测试（8/8）和真实桌面 WebView 交互复验；列表进入含历史详情后 `rootChildren=1`、历史与 Run 跳转均存在，本次操作新增控制台错误为 0。

### Phase 16：时间输入与即时校验（2026-08-10）

- [x] 将创建/更新命令的 `ScheduledScheduleInput` 与查询/持久化 `ScheduleSpec` 分离；At 提交本地日期、时间、IANA 时区和 Earlier/Later，由 Rust 在任何副作用前权威规范化。
- [x] 使用 `@js-temporal/polyfill` 识别普通、DST 不存在和 DST 重复本地时间；新建默认系统时区，解析失败回退 `UTC`，编辑按原时区恢复 wall-clock 字段。
- [x] 使用 `cron-parser` 即时校验六字段 Cron；Weekly 至少一天，Every 只接受正整数；统一 validation result 控制保存按钮，错误文案维护中英文资源。
- [x] 固化 Rust 领域层、Tauri 服务边界、Web payload/校验接口测试，并完成 production build 与桌面/移动端 UI 验收后再标记本阶段完成。

验收记录：Rust scheduler 领域测试 20/20、Tauri scheduled service 测试 15/15、Web 全量测试 95 files / 595 tests 与 production build 均通过。真实 UI 已在 1280×900 和 390×844 验证系统时区默认值、非法 Cron、空 Weekly、Every 空/零/负数/小数、保存禁用状态与无横向溢出；DST 缺口、重叠及 Earlier/Later UTC 映射由 Temporal、Rust 和 Tauri 三层自动化测试固定。对话框同时显式关闭空 `aria-describedby` 关联，避免 Radix 描述警告。

详细设计见 [`2026-08-05-scheduled-task-unified-runtime-design.md`](../../../superpowers/specs/2026-08-05-scheduled-task-unified-runtime-design.md)。

### Phase 17：管理列表与 Composer 创建交互收敛（2026-08-11）

- [x] 删除管理页 `max-w-6xl` 和 `min-w-[980px]` 的冲突约束，列表改为占满主工作区，并以 `minmax(0, fr)` 分配桌面列宽；窄屏收敛为任务摘要、开关和更多菜单。
- [x] 普通发送和创建定时任务统一为同背景 split-button，箭头区收窄为 24px 并删除中间分隔线，不再以 `secondary` 大色块割裂主操作；两个主操作复用 `canSubmit`，空正文时统一禁用，模式切换箭头与配置入口保持可用。
- [x] 定时创建态补齐 ChevronDown 菜单，可无提交切回普通发送；配置齿轮继续作为显式编辑入口。
- [x] 新增 `scheduled-task-config` 右侧工作区资源与稳定 scope key；Composer 注册资源渲染器，新建流程删除模态弹窗状态；管理页与详情页的既有任务编辑统一迁移到可调整宽度的右侧 Sheet 抽屉。
- [x] 新增 `scheduled-task-create` 类型化页面状态和 `/chat/scheduled-tasks/new` deep link；管理页创建入口直接初始化 Composer 定时模式与右侧配置 Tab，退出或创建成功后返回普通 `/chat`，不再丢失创建意图。
- [x] 创建成功后复用 Composer 现有内联状态链接模式，用一句话提示并导航到 `/chat/scheduled-tasks`，提示 5 秒后自动消失；不新增 Toast 组件或通知链路。
- [x] 统一页面类型到侧栏主导航的派生映射：`scheduled-task-create` 归属“快速对话”，列表与详情归属“定时任务”，不再由入口来源残留错误选中态。
- [x] 修复通用侧栏恢复回归：删除 compact resize 将 `sidebarCollapsed=true` 写入手动偏好和 localStorage 的路径，响应式折叠重新只由工作区状态机持有，保留“右侧优先、左侧随后”的恢复顺序。
- [x] 使用 shadcn Input/Popover/ScrollArea/Button 组合替换原生时间输入：保留 `HH:mm` 键盘直输与 Enter/失焦规范化，时钟按钮提供小时/分钟点选；选中态统一消费主题 `accent`，消除 Chromium 分钟循环断层、系统蓝色文本选择和 picker 回滚。
- [x] 新增定时任务时区偏好：首次使用电脑本地 IANA 时区，用户选择合法时区后记忆并作为后续新建默认值；编辑仍优先任务原时区。
- [x] 固化工作区资源去重、split-button 对称切换、右侧 Tab/Sheet 呈现、时间控件、时区偏好、侧栏意图隔离和管理页响应式宽度接口测试；Web production build 通过。
- [x] 启动前端，通过 `/chat/scheduled-tasks` 与 `/chat` deep link 完成宽屏/窄屏、双向模式切换和右侧配置 Tab 的实际 UI 验收，完成后清理测试进程。

验收记录：内置浏览器在 1280×720 下通过 `/chat` 与 `/chat/scheduled-tasks` deep link 验证同色 split-button、24px 箭头区、无分隔线、普通发送/定时创建双向切换、右侧配置 Tab、主题 `accent` 时间选中态、`00/01` 连续行距、`01:01` 选择结果与时区跨新建流程记忆；控制台无错误。从管理页点击创建入口后进一步确认进入 `/chat/scheduled-tasks/new`、Composer 自动处于定时创建模式且右侧“定时任务设置”Tab 自动打开；从菜单切回发送后返回 `/chat`，直接刷新 `/chat` 仍是普通发送模式且不残留右侧配置。窄屏列表收敛、临时折叠不污染手动侧栏偏好，以及从窄到宽“先右后左”的恢复顺序由响应式状态机测试覆盖；既有任务编辑抽屉由页面组件接口测试覆盖。Web production build 与 8 个相关测试文件（52 tests）均通过，浏览器页面和本次 Vite 进程已清理。

补充验收：时间控件恢复键盘直输后，真实页面验证 `9:5`、`905` 均规范化为 `09:05`，`24:00` 显示错误态且不覆盖已提交时间，时钟按钮点选 `20:30` 后输入框与下一次执行摘要同步；输入聚焦不再自动展开并遮挡后续字段。相关 2 个测试文件（15 tests）与 Web production build 通过，控制台无新增警告或错误，浏览器页面、Vite 进程和临时日志已清理。

导航归属补充验收：从 `/chat/scheduled-tasks` 点击创建后进入 `/chat/scheduled-tasks/new`，侧栏由选中“定时任务”准确切换为仅选中“快速对话”；页面类型到主导航的纯派生映射由 3 个相关测试文件（31 tests）固定，Web production build 通过，真实页面控制台无警告或错误。

### Phase 18：定时 Workflow 执行配置与 Task 身份解耦（2026-08-15）

- [x] 将更新判断从完整 `ScheduledTaskContentSnapshot` 相等比较改为规范化语义身份比较；模型、权限、config options、binding/definition revision 与内部 execution slot 不再触发 Task 重建。
- [x] 为新 `modelBindings` 结构补齐按 Worker node ID 投影的 Agent 身份；Agent、Workflow 定义、instruction、附件或 workspace 变化仍生成新 fingerprint，并在下一次触发物化新 Task。
- [x] 仅执行配置变化时保留关联 `task_id` 和既有 fingerprint，避免已有 Task metadata 因指纹算法升级失配；完整冻结快照仍更新为下一次定时执行的权威输入。
- [x] 增加类型化 `prepare_run_with_authoring` 路径：先校验 authoring definition，再注入本机绑定并校验 executable，最后只将完整执行配置写入新 Run snapshot；Task authoring 不被定时更新覆盖。
- [x] 定时 Workflow 复用 Task 时读取定时定义的冻结 authoring；普通手动重跑继续读取 Task authoring，当前正在执行的 Run 不受后续编辑影响。
- [x] 增加 Rust 回归测试，覆盖当前绑定结构的 Agent/执行配置指纹语义、旧 fingerprint 保留、语义变化清空 Task、Run snapshot 使用新模型、Task authoring 字节不变及手动重跑隔离。

性能与过度设计复核：语义投影和绑定注入均为 `O(nodes + bindings)`，不增加全量扫描、额外持久化 I/O、缓存、队列、状态机、数据库字段或第三方依赖；复用现有 `serde_json`、`sha2`、authoring validation、binding injection 和 Run snapshot。典型 Workflow 为个位数到几十个节点，新增开销相对文件写入与 Run 启动可忽略，并减少错误新建 Task 造成的磁盘和侧边栏数据增长。

验收记录：scheduler fingerprint 测试 7/7、scheduled service 测试 19/19、scheduled runtime 测试 55/55、Run authoring override 定向测试 1/1 通过；`cargo check -p sasuke-desktop --tests --offline` 与 `cargo fmt --all -- --check` 通过。编译仅保留既有的 4 条可见性/未使用代码 warning。本阶段无前端、路由、布局或 IPC schema 变化，不需要 UI 验收。

### Phase 19：定时创建 Composer 工作空间样式归一（2026-08-18）

- [x] 定时创建态删除 composer 底栏的独立 workspace 胶囊，复用普通快速对话输入框上方的 80% 宽圆角梯形信息栏及同一个 shadcn `SelectTrigger`。
- [x] 信息栏按创建能力只展示 workspace，不渲染“主工作区 / 新工作树”工作位置入口；定时任务提交协议、持久化定义和运行时继续固定主工作区，不新增 `workLocation` 字段或工作树准备链路。
- [x] 组件接口与布局契约测试固定两种模式共享信息栏外壳、定时模式无工作位置触发器且提交对象不携带 `workLocation`；定向 Vitest 4 个文件 34 项及 TypeScript 检查通过。
- [ ] 按用户要求，本次提交不继续执行生产构建或内置浏览器 deep link 验收；页面实测留待后续独立验收。

性能与过度设计复核：仅减少一个条件分支中的重复 workspace 控件并复用现有信息栏，不新增请求、状态、effect、持久字段、缓存、队列、订阅或后端 I/O；现有 composer 与定时任务输入协议已足以表达“可选 workspace、固定主工作区”，无需提前引入工作树 identity 或调度生命周期扩展。

### Phase 20：用户执行配置透传与定时草稿跨页面恢复（2026-08-24）

- [x] 删除创建、更新、计划触发和手动立即执行中的 Direct full-auto gate，并删除 Agent mode 名称识别及对应拒绝测试；不在 registry 增加定时任务能力投影。
- [x] 保持 `directConfig` 为用户选择的 Agent、model、permission mode 与 config options 权威快照；新增服务接口回归，固定 `permissionMode = plan` 可以创建且原样持久化。
- [x] 将 composer 提交方式收敛为 `send | scheduled-task(config)` 判别状态，与正文和附件一起由现有 `ConversationComposerDraftBoundary` 持有；页面卸载/重挂不再退回普通发送或丢失已确认计划。
- [x] 主动退出定时创建只清理定时模式和配置并保留普通正文/附件；创建成功继续通过统一 reset 清理整个 composer 草稿。
- [ ] 完成 Web 全量测试、生产构建、Rust 定时任务定向测试，以及内置浏览器跨页面返回和创建流程验收。

过度设计复核：删除错误的能力推断和四处 gate，复用既有 ACP 执行配置、权限生命周期及 composer draft owner；没有新增 registry 字段、持久化 schema、缓存、状态机、依赖或兼容层。

性能复核：创建与触发路径减少 provider diagnostics/mode 扫描；草稿仅增加一个常量大小的判别联合与定时配置对象，不增加 I/O、请求、订阅、轮询、全局 Context 或历史消息渲染范围。

### Phase 21：Composer 模式切换菜单语义补齐（2026-08-25）

- [x] 保留现有 shadcn DropdownMenu、split-button 和 `scheduledMode` 权威状态，只把两个对称菜单项统一为“辅助小字 `切换为` + 目标模式图标 + 目标动作”。
- [x] 新增 `scheduled.composer.switchTo` 中英文资源，中文为“切换为”、英文为“Switch to”；实现代码不硬编码对客文案。
- [x] 接口回归固定两个菜单项都消费同一 i18n key、保持辅助层级样式，并按“前缀 → 图标 → 动作”顺序渲染；i18n 回归固定两种语言值。
- [x] 用户实测反馈混合字号未对齐后，将 12px 辅助文案的行盒统一为目标动作使用的 20px；保留字号层级与 shadcn 原生居中布局，不使用像素位移补丁，并以组件契约测试固定两个对称入口。
- [ ] 完成定向测试、TypeScript/生产构建和内置浏览器普通态/定时态双向切换验收。

性能与过度设计复核：只增加两个常量文本节点和一个翻译 key，不新增状态、组件抽象、依赖、请求、I/O、缓存、队列、订阅或 effect；菜单打开时仍只渲染单个目标项，时间与空间复杂度均为 `O(1)`，现有状态模型足以表达模式切换。

### Phase 22：侧栏低频管理入口收纳（2026-09-04）

- [x] 复用现有 shadcn/ui `Collapsible`，在运行模式下增加独立“更多”导航行，将定时任务与需求管理收纳为按需显示的同级入口；不把三个点附着到运行模式，展开项不增加子级缩进并与一级导航左对齐。
- [x] 默认折叠且只保留组件运行期展开状态；路由进入定时任务列表/详情时自动展开并保持子项选中，定时任务创建页继续归属快速对话。
- [x] 增加侧栏组件接口测试，固定默认折叠、手动展开、子项导航和 deep link 自动展开契约。

性能与过度设计复核：只增加一个局部布尔展开状态和两个常量子项，折叠时不挂载子项 DOM，进入页面前不加载定时任务数据；不新增持久化字段、聚合页、路由、请求、订阅、缓存、依赖或跨领域状态，时间和空间复杂度均为 `O(1)`。
