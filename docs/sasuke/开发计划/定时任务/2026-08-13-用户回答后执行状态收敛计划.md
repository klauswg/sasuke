# 用户交互后执行状态收敛计划

**目标：** 定时任务因 AskUserQuestion 或 Workflow 人工验收进入 `attention_required` 后，用户完成交互时让原 occurrence 立即恢复为 `running`，并在原 Run 结束后继续收敛为 `succeeded` 或 `failed`。

**架构：** occurrence 仍是唯一权威生命周期。AskUserQuestion 与 ManualCheck 共用统一恢复边界，在写入 durable elicitation response 或提交人工判定前，通过完整的 Task/Run/Round/Attempt locator 请求 scheduler 恢复原 occurrence；scheduler 完成数据库 claim、heartbeat 注册、definition 投影和 `running` 事件后返回原 occurrence ID。ManualCheck 将该 ID 注入后台 App，使后续 lifecycle 终结同一记录。普通非定时交互没有匹配 occurrence 时维持现有流程；已识别为定时恢复但 claim 失败时返回结构化错误，不推进业务状态。

**性能边界：** 每次交互仅增加现有复合 locator 的单行查询、单行 claim、既有 revision/CAS 投影更新和一次事件发布；不增加轮询、历史扫描、N+1、无界缓存或扩大的锁范围。

## Task 1：恢复结果与权威事件

涉及文件：

- `src/scheduler/db.rs`
- `src-tauri/src/scheduled_runtime.rs`

- [x] 先写回归，固定 `attention_required -> running` 后返回带完整链接的权威 occurrence。
- [x] 先写回归，固定 running 更新事件携带原 occurrence ID、任务 ID、Run ID 和空错误码。
- [x] 抽取统一 occurrence 更新事件构造/发布函数，终态与恢复态复用同一事件契约。
- [x] scheduler 恢复成功后立即发布 `running`，再允许回答信号落盘。

## Task 2：交互命令失败语义

涉及文件：

- `src-tauri/src/commands.rs`
- `web/src/i18n.ts`

- [x] scheduler DB 不存在时普通非定时交互零 I/O 继续；定时 occurrence 存在且无法 claim 时返回稳定结构化错误。
- [x] 恢复 claim 冲突、存储或 coordinator 失败时不写 elicitation response，也不提交人工验收判定；前端恢复交互控件并通过既有错误区域展示本地化反馈。
- [x] ManualCheck 在 scheduler 恢复前执行当前 paused node/attempt 预检，后台提交时重复领域校验；过期判定不会制造孤立的 `running` occurrence。
- [x] ManualCheck 复用 RuntimeContinue 单次启动握手；command 只在人工判定及其控制转移都已持久化后返回最新 Run。握手前校验、加载、持久化、线程创建或 panic 失败先收敛 `Paused + RuntimeAbnormal`，并用原 occurrence ID 发出终止 lifecycle event，再返回 `runtime.continue-launch-failed`。
- [x] ManualCheck 在 scheduler 恢复 occurrence 之前获取 Run 级 single-flight reservation；并发或相反判定只能有一个进入恢复/后台提交边界。握手后失败携带精确 execution identity，只在原 execution 仍拥有 active attempt 时做 CAS 暂停与 lifecycle 收敛，迟到失败不覆盖新执行。
- [x] 握手前失败使用原 Run/Round/Node/Attempt locator，在 attempt 锁内确认仍是 paused + `manual_check_pending` 的原 attempt；若 stop、retry 或其他控制流已替换 current attempt，只终结原 occurrence，不改写 replacement Run。
- [x] attempt 失败收敛显式区分 `Converged`、`Superseded` 与持久化错误；只有明确 identity mismatch 才抑制迟到事件。无 execution ID 的 active attempt 也必须同时匹配 running locator 和 durable Node 空 identity，禁止无条件写。Run 已写成 `Paused + RuntimeAbnormal` 而 Round/Node 写失败或收敛重入时，仍携带原 occurrence ID 发布终止事件并保留持久化错误。
- [x] 完整 desktop 回归暴露的图视图测试共享进程级临时目录已改为独占 `TempDir`，避免并行夹具清理导致业务断言通过后仍以 `NotFound` 假失败；不改变生产行为。

## Task 3：文档与验收

涉及文件：

- `docs/sasuke/产品设计文档/interaction/app/scheduled-task-management.md`
- `docs/sasuke/产品设计文档/runtime/scheduled-task-runtime-implementation.md`
- `docs/sasuke/开发计划/定时任务/定时任务运行时实现补充.md`
- 本计划

- [x] 同步明确 `attention_required -> running -> succeeded/failed` 的 UI 与运行时语义。
- [x] 运行 scheduler、desktop scheduled runtime/commands 回归、Rust fmt/check 和 diff 检查。
- [x] 启动前端，用内置浏览器验证详情页接收 running 事件后从“需要处理”刷新为“执行中”，完成后清理页面与 dev server。

## 验收记录（2026-08-13）

- Rust：`cargo fmt --all -- --check`、两条 scheduled attention locator 回归、两条 resumed attention 状态投影/事件回归及 `cargo check -p sasuke-desktop --bin sasuke-desktop` 通过。
- Rust 测试基础设施：移除停止会话回归中与 `TempDir` 生命周期重复的手工目录清理，避免生产路径已清理目录时因 `NotFound` 产生假失败；不改变生产行为。
- Web：`npm run web:test` 共 168 个测试文件、1089 条测试通过；`npx tsc -p web/tsconfig.build.json --noEmit` 与 `npm run web:build` 通过。
- Rust desktop：完整 `cargo test -p sasuke-desktop --bin sasuke-desktop -- --test-threads=1` 共 389 条通过；scheduler core 95 条、ManualCheck 6 条筛选回归、background-drive 3 条及 fixed-continue stale execution 回归通过。fresh core lib 全套共 757 条，742 通过、15 条既有失败；失败仍只属于共享 profile 目录、Windows temp path、Git 子进程/worktree 隔离和并行 JSONL 四组，没有新增 scheduler/ManualCheck 失败。
- ManualCheck 启动边界：`manual_check_background_converges_a_precommit_failure` 先在旧实现上稳定失败，再验证握手前后台校验失败会同步返回结构化错误、Run 收敛为 `RuntimeAbnormal`，且终止事件保留原 occurrence ID。
- ManualCheck 并发与迟到失败：reservation 回归覆盖同一 Run 第二个判定被拒绝且释放后可重试；`stale_background_drive_error_does_not_override_a_newer_execution` 先在无条件 terminalize 实现上稳定失败，再验证 execution A 的迟到失败不会暂停 execution B 或发出错误终止事件。
- ManualCheck 组合竞态：`manual_check_precommit_failure_does_not_pause_a_replacement_attempt` 先复现 reservation 外部状态转换后 replacement Run 被错误暂停，再验证旧失败只按原 locator 终结 occurrence；`background_drive_persistence_failure_still_finishes_the_occurrence` 用真实 Round 路径故障复现 Run 部分写入，验证终止 lifecycle 不再因持久化错误被误判为 superseded。
- UI：内置浏览器 deep link 进入定时任务列表和详情页，确认 `running` 权威状态显示为“运行中”独立状态 badge，页面无截断、重叠或控制台 warning/error；列表显示 `1-10 / 14`，详情运行记录显示 `1-5 / 10`，分页控件可用。交互后的即时 `running` 事件与原 occurrence identity 由接口级回归固定，避免依赖浏览器 mock 中瞬时完成的执行时序。
- 清理：验证页面与本次启动的 Vite dev server 已关闭，5173 端口无残留监听。
- 性能：定时交互恢复增加一次带完整 locator 且 `LIMIT 1` 的查询、一次单行 claim/状态更新、既有 revision/CAS 投影更新和一次增量事件；普通交互在 scheduler DB 不存在时不创建数据库。未引入全量扫描、轮询、N+1、无界缓存、扩大锁范围或额外历史加载。
- 握手性能：每次 ManualCheck 增加一个进程内一次性 channel 和一次常数级消息传递，不增加数据库读写、轮询、队列积压、锁范围或渲染刷新；command 只等待本就必须完成的本地持久化，不等待后续 provider 长执行。
- 并发性能：single-flight registry 仅保存正在启动的 Run key，lease 在握手返回时释放；execution CAS 复用 attempt 细粒度锁，不持锁执行 provider，不增加扫描、全局锁或无界状态。
- 失败路径性能：仅在收敛写入本身失败时增加一次精确 Run/Node 重读；正常路径、正常失败路径的 I/O 数量、attempt 锁范围和 provider 并发度不变。
- 最终静态验收：`cargo check -p sasuke-desktop --bin sasuke-desktop`、`cargo fmt --all -- --check` 与 `git diff --check` 通过；编译仅保留仓库既有 warning。
