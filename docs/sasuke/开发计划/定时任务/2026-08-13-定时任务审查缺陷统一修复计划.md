# 定时任务审查缺陷统一修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development task-by-task. Each production behavior must be preceded by a failing regression test.

**Goal:** 修复定时任务精确对账、终态副作用、deadline 事件、设置并发和管理错误闭环，并将 occurrence 历史改为每页 20 条的稳定游标分页。

**Architecture:** occurrence 使用完整 Task/Run locator 对账，lifecycle 与恢复共用终态收敛管道；SQLite 以三字段 keyset cursor 提供有界历史页。前端以目标实体级请求状态和 mutation generation 管理异步结果，错误保留当前 canonical 数据。

**Tech Stack:** Rust 2024、rusqlite、Tauri 2、React 19、TypeScript、Vitest、shadcn/ui、Tailwind CSS。

---

### Task 1: 精确 Run 对账与统一终态

**Files:**
- Modify: `src-tauri/src/scheduled_runtime.rs`
- Test: `src-tauri/src/scheduled_runtime.rs`

- [x] 写失败测试：同 Task 中 Run A completed、Run B running 时，Run A occurrence 返回 `Succeeded`，不得被 Run B 保持为 active。
- [x] 写失败测试：恢复对账成功后同时更新 definition 最近状态、发出 task/occurrence event、调用通知和 cleanup seam。
- [x] 将 active probe 改为 `task_id + run_id` 精确读取，只在目标 Run 的 attempt tree 探测 prompt activity。
- [x] 提取共用 terminal convergence 输入，按 stop guard -> owner CAS -> projection -> events -> notification -> cleanup 顺序执行。
- [x] 运行 `cargo test -p sasuke-desktop --bin sasuke-desktop scheduled_runtime`，83 项测试全部通过。

### Task 2: deadline 事件单一数据源

**Files:**
- Modify: `src-tauri/src/scheduled_service.rs`
- Modify: `src-tauri/src/scheduled_runtime.rs`
- Test: `src-tauri/src/scheduled_service.rs`

- [x] 写失败测试：create/update/enable 的 coordinator notify event 包含数据库 record 的 `next_run_at`。
- [x] 将 service coordinator 通知契约从 definition 改为 `ScheduledJobRecord`，删除事件仍只携带定义身份。
- [x] 事件 ViewModel 只从 record 构造 `nextAt`；不得为非删除事件传 `None`。
- [x] 运行 desktop scheduled service/runtime 测试，scheduled service 19 项、scheduled runtime 83 项全部通过。

### Task 3: occurrence 游标分页仓储与 API

**Files:**
- Modify: `src/scheduler/db.rs`
- Modify: `src-tauri/src/view_models_conversation.rs`
- Modify: `src-tauri/src/commands_conversation.rs`
- Modify: `web/src/types.ts`
- Modify: `web/src/api/client.ts`
- Modify: `web/src/api/desktop.ts`
- Modify: `web/src/api/browser.ts`
- Modify: `web/src/api.ts`
- Test: `src/scheduler/db.rs`
- Test: `src-tauri/src/commands_conversation.rs`
- Test: `web/tests/api.test.ts`
- Test: `web/tests/browser-scheduled-task-api.test.ts`

- [x] 写失败测试：25 条相同/相邻时间记录按 `(scheduled_at, created_at, id)` 稳定倒序返回 20 + 5，页间无重复。
- [x] 写失败测试：第一页之后插入更新记录，旧 cursor 的第二页仍不重复或跳过原快照边界后的记录。
- [x] 增加复合索引和 `OccurrenceCursor`；SQL 使用三字段 lexicographic `<` 条件并读取 `PAGE_SIZE + 1`。
- [x] 将命令与 web facade 改为 `ScheduledOccurrencePageVm { items, nextCursor }`，拒绝客户端自定义超大 limit；可选状态条件在 SQL 中先筛选再分页。
- [x] browser preview 实现同一 cursor/20 条契约，并以 occurrence ID 锚点避免新记录插入造成 offset 重复。
- [x] 运行 scheduler、commands 和 API 测试：scheduler 95 项、desktop commands conversation 20 项以及 Web API 回归全部通过。

### Task 4: 设置缓存并发与输入一致性

**Files:**
- Modify: `web/src/components/scheduled-tasks/useScheduledRuntimeSettings.ts`
- Modify: `web/src/components/scheduled-tasks/ScheduledRuntimeSettings.tsx`
- Modify: `web/src/i18n.ts`
- Test: `web/tests/scheduled-runtime-settings-cache.test.ts`
- Test: `web/tests/scheduled-task-settings.test.ts`

- [x] 写失败测试：stale GET 先发、save 后完成、GET 最后返回时缓存仍保持 save 响应。
- [x] 缓存增加 mutation generation 和订阅通知；fetch 只在捕获 generation 未变化时提交。
- [x] 保存请求串行化，三个设置始终从最新 server snapshot + 当前 draft 生成。
- [x] retention 使用 server/draft/dirty，未编辑时跟随 refresh，非法值显示 `1..=3650` 字段错误。
- [x] 运行设置回归、全量 Web 测试与 TypeScript 检查，全部通过。

### Task 5: 管理与详情错误闭环、分页和响应式

**Files:**
- Modify: `web/src/pages/ScheduledTaskManagementPage.tsx`
- Modify: `web/src/pages/ScheduledTaskDetailPage.tsx`
- Modify: `web/src/i18n.ts`
- Test: `web/tests/scheduled-task-management-page.test.ts`
- Test: `web/tests/scheduled-task-crud.test.tsx`
- Test: `web/tests/app-scheduled-task-detail.test.ts`
- Create: `web/tests/scheduled-task-history-pagination.test.tsx`

- [x] 写失败测试：首次加载失败显示错误，刷新失败保留旧 rows；启停/run/edit/delete rejection 显示反馈且删除确认保持打开。
- [x] 写失败测试：状态筛选显示“已启用”并按 `enabled` 筛选，不再声称“运行中”。
- [x] 写失败测试：历史每页 20 条、下一页/上一页使用 cursor stack，筛选保留，非第一页 occurrence event 不强制跳页。
- [x] 用目标 task ID 的 pending action union 禁止重复提交；成功使用后端实体局部更新，失败保留现有实体。
- [x] 历史窄屏改为纵向行，桌面使用四列 grid；页头 `flex-wrap`，不产生页面横向滚动。
- [x] 运行定时任务相关 Web 测试及全量 Vitest，168 个文件、1089 项测试全部通过。

### Task 6: 文档、全量验证与性能验收

**Files:**
- Modify: `docs/sasuke/产品设计文档/runtime/scheduled-task-runtime-implementation.md`
- Modify: `docs/sasuke/产品设计文档/interaction/app/scheduled-task-management.md`
- Modify: `docs/sasuke/开发计划/定时任务/定时任务运行时实现补充.md`
- Modify: this plan

- [x] 运行 `cargo fmt --all -- --check`、scheduler lib、desktop scheduled runtime/service 和 commands 测试；scheduler 95 项、runtime 83 项、service 19 项、commands conversation 20 项全部通过，`cargo check -p sasuke-desktop --bin sasuke-desktop` 通过。
- [x] 运行相关 Web 测试、全量 Vitest、`npx tsc -p web/tsconfig.build.json --noEmit` 和 `npm run web:build`；168 个测试文件、1089 项测试通过，生产构建通过，仅保留既有 sourcemap/chunk-size 提示。
- [x] 使用内置浏览器 deep link 验证管理页、详情第一页/下一页和 390×844 布局：21 条记录稳定显示为 20 + 1，第二页边界正确；移动端 `clientWidth = scrollWidth = 390`，历史行、筛选和翻页控件无溢出；页面 warning/error 日志为空。验证页面与 5174 dev server 已清理。错误态由接口级回归覆盖。
- [x] 用 `EXPLAIN QUERY PLAN` 仓储测试确认状态分页命中 `idx_scheduled_occurrences_status_history`，分页固定读取 21 行；精确对账测试确认不读取同 Task 的其他 Run。
- [x] 将计划 checkbox、实现接口、测试数量和性能结论同步回产品设计与开发计划。

### 验收结论

- 正确性：精确 Run 对账、统一终态副作用、持久化 deadline 事件、设置并发、管理错误闭环和历史游标分页均有接口级回归。
- 性能：历史分页固定最多读取 21 行，状态筛选命中专用复合索引；诊断计数使用 SQL 聚合且只加载最新 20 条明细。未引入全历史加载、轮询、N+1、无界缓存、扩大的锁范围或渲染范围。
- 兼容性：既有 scheduler、desktop runtime/service、commands 和 1089 项前端测试全部通过；生产构建、Rust check、格式与 diff 检查通过。
