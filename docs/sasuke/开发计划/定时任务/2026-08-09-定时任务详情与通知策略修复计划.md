# 定时任务详情与通知策略修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复定时任务详情页初始化白屏，并确保定时任务完成通知开关能够唯一控制成功完成通知。

**Architecture:** 会话导航函数必须在任何页面内容求值前完成初始化，避免详情分支读取暂时性死区变量。带 `scheduled_occurrence_id` 的 lifecycle 事件由定时任务运行时转换为专用通知，通用会话通知订阅器不再重复消费，普通会话通知保持原行为。

**Tech Stack:** React 19、TypeScript、Vitest、Rust、Tauri、Cargo test。

---

### Task 1: 定时任务详情页渲染回归

**Files:**
- Create: `web/tests/app-scheduled-task-detail.test.ts`
- Modify: `web/src/App.tsx`

- [x] **Step 1: 写失败测试**

使用详情 deep link 初始化 `App`，通过 React 服务端渲染验证不会抛出 `Cannot access 'onSelectConversation' before initialization`。

- [x] **Step 2: 验证测试按预期失败**

Run: `npm run web:test -- web/tests/app-scheduled-task-detail.test.ts`

Expected: FAIL，错误指向 `renderConversationContent` 读取尚未初始化的 `onSelectConversation`。

- [x] **Step 3: 实现最小修复**

将 `onSelectConversation` 的初始化移动到 `content` 求值之前；保持现有页面契约和路由行为不变。

- [x] **Step 4: 验证测试转绿**

Run: `npm run web:test -- web/tests/app-scheduled-task-detail.test.ts`

Expected: PASS。

### Task 2: 定时任务通知唯一所有权

**Files:**
- Modify: `src-tauri/src/notifications.rs`

- [x] **Step 1: 写失败测试**

新增 lifecycle 通知转换测试，要求 `scheduled_occurrence_id` 非空的 `RunCompleted` 不生成通用 `InterventionNotification`，并验证普通 `RunCompleted` 仍生成通知。

- [x] **Step 2: 验证测试按预期失败**

Run: `cargo test --manifest-path src-tauri/Cargo.toml notifications::tests::scheduled_lifecycle_completion_is_owned_by_scheduled_notification_policy -- --exact`

Expected: FAIL，当前通用通知转换仍返回通知。

- [x] **Step 3: 实现最小修复**

提取纯函数统一完成 lifecycle 到通用通知的转换；函数首先检查 `scheduled_occurrence_id`，存在时返回 `None`，由定时任务运行时继续完成 occurrence 投影并按全局开关发送专用通知。

- [x] **Step 4: 验证定向测试转绿**

Run: `cargo test --manifest-path src-tauri/Cargo.toml notifications::tests::scheduled_lifecycle_completion_is_owned_by_scheduled_notification_policy -- --exact`

Expected: PASS。

### Task 3: 文档与完整验证

**Files:**
- Modify: `docs/sasuke/产品设计文档/interaction/app/scheduled-task-management.md`
- Modify: `docs/sasuke/开发计划/定时任务/定时任务完整设计与开发计划.md`

- [x] **Step 1: 同步设计边界**

记录定时 occurrence lifecycle 的通知所有权、完成开关语义和通用通知订阅器的排除规则。

- [x] **Step 2: 同步开发计划与回归项**

记录详情白屏与重复通知根因、修复状态以及新增的前后端回归测试。

- [x] **Step 3: 执行完整验证**

Run: `npm run web:test`

Run: `npm run web:build`

Run: `cargo test --manifest-path src-tauri/Cargo.toml notifications::tests`

Expected: 全部退出码为 0。

- [x] **Step 4: 前端交互复验**

启动 `npm run web:dev`，创建并执行一个浏览器预览定时任务，点击任务行进入详情；验证页面非空、URL 正确且控制台无渲染异常。验证结束后关闭测试页面和开发服务。

> 当前工作区包含用户尚未提交的定时任务开发改动，本计划不自动创建 Git commit，避免把既有修改混入提交。

### Task 4: 真实历史详情页 Tooltip 上下文修复（2026-08-10）

**Files:**
- Add: `web/src/components/AppProviders.tsx`
- Modify: `web/src/App.tsx`
- Add: `web/tests/app-providers.test.ts`

- [x] **Step 1: 通过桌面 WebView2 协议复现并获取真实错误**

从定时任务列表点击存在执行历史的真实任务后，URL 正确切换，但 `#root` 子节点变为 0；控制台错误为 ``Tooltip` must be used within `TooltipProvider` ``。

- [x] **Step 2: 写失败测试并确认 RED**

新增应用 provider 行为测试；测试因 `AppProviders` 尚不存在而按预期失败。

- [x] **Step 3: 将 Tooltip 上下文提升到应用根部并确认 GREEN**

应用根部统一挂载 `AppProviders`，设置页不再独占 provider。`app-providers` 与原有详情 deep-link 测试均通过。

- [x] **Step 4: 完整验证与真实任务点击复验**

运行 Web 全量测试、production build、Rust 通知模块测试，并在桌面实例中再次执行列表到真实详情的点击流程，确认根节点保持挂载且控制台无 Tooltip 错误。

结果：Web 93 files / 580 tests、production build、Rust 通知 8/8 全部通过。真实桌面任务详情包含 50 条 occurrence，`#root` 保持挂载，Run 跳转图标正常渲染，点击流程新增控制台错误为 0。
