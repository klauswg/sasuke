# 定时任务运行时实现补充

## 已完成基线

- [x] 管理页使用 instruction 内容摘要，不展示内部 scheduled ID，也不要求独立名称。
- [x] 管理页显示结构化调度摘要、时区、下次执行时间和基础启停状态。
- [x] 创建命令保存 instruction、执行配置、调度定义、队列策略、会话策略和附件快照。
- [x] Direct、Workflow、AUTO 已接入现有 Task/Run/ACP 创建链路的基础路径。

以上是最初功能基线。当前活跃定义已经收敛到 SQLite，`lastTriggerAt` 游标驱动和每秒全量轮询已经由 deadline coordinator 取代。

## 待实现可靠性阶段

- [x] 使用独立 SQLite scheduler store 保存 job 与 occurrence。
- [x] 对 `(job_id, scheduled_at, trigger_kind)` 建唯一约束，实现原子 claim、owner、lease、heartbeat 和过期恢复。
- [x] 将真实 Task/Run/ACP 完成事件与 occurrence ID 绑定；进程启动不再直接写成功。
- [x] 启动和系统唤醒显式标记 grace 外的 `missed`，不补跑历史时间点；每个 enabled job 使用独立 deadline。
- [ ] 增加 run-now、occurrence 历史、诊断字段、完成通知和 keep-awake。
- [x] 删除 Direct full-auto 预检和 Agent 能力映射；创建、更新与触发原样使用用户配置，permission request 进入 `failed`，AskUserQuestion 进入 `attention_required`。
- [ ] Workflow/AUTO 接入同一 occurrence 生命周期，不破坏内容指纹和 Task/Run 复用规则。

## 验证要求

- [ ] Rust repository/runtime/command 接口测试覆盖并发 claim、lease 恢复、真实终态、missed、busy retry、permission、AskUserQuestion、run-now 和迁移。
- [ ] Web 测试覆盖任务列表、详情/历史、立即执行、启停、错误诊断和 attention 通知 deep link。
- [ ] 启动前端开发服务器验证 Direct 全部 AionUi 对齐能力，以及 Workflow/AUTO 的统一生命周期；完成后清理测试资源。

## 2026-08-06 Task 4：deadline coordinator

- [x] 引入 `tokio-util::time::DelayQueue`，同一 enabled job 只保留一个计时项；更新替换旧 deadline，暂停、删除和 workspace 移除取消 deadline。
- [x] CRUD/service 保持创建只持久化定义和输入快照；提交成功后只通知 coordinator 重排，不在创建动作中调用模型。
- [x] timer 注册和触发均重新读取 SQLite `revision`、`enabled` 与 `next_run_at`；陈旧 callback 不物化 occurrence。
- [x] `JobDisabled`/`JobDeleted` 只作为失效通知，命令循环重新读取 SQLite 最终状态；乱序到达的旧通知不会取消已经重新启用或重建的 job。
- [x] 首次到期使用事务型 `materialize_due_occurrence` 创建 scheduled occurrence；run-now 通过显式 `RunNow` 命令立即创建 manual occurrence，且不推进 `next_run_at`。成功执行后的 Task 关联、最近结果等元数据持久化允许推进 `revision`，coordinator 按最新记录刷新原 deadline。
- [x] workspace 启动注册执行 schema/幂等迁移、过期 lease 恢复和 reconcile；旧 JSON/共享数据库在读取或打开前先检查 durable marker，已完成迁移时忽略损坏的废弃源。
- [x] pending/retrying 通过 SQLite partial index 直接查询最老一条，不再受 10,000 条历史扫描上限影响，然后才处理后续计划点。
- [x] 早于 `LATE_FIRE_GRACE` 的时间点标记为 `missed`；单个 coordinator turn 最多处理 `MISSED_RECONCILE_BATCH_SIZE` 条后返回 command/deadline select，并以持久化的过去 `next_run_at` 立即续排下一批，grace 内近迟到点保持可执行。
- [x] coordinator 在 Tauri async runtime 中启动；系统 resume 触发 reconcile，退出触发 shutdown；删除全局一秒轮询。
- [x] paused-time 测试覆盖精确注册、替换、取消、stale revision、乱序 disable/delete、`SchedulerCoordinatorHandle::RunNow`、manual deadline 保持（不要求执行元数据写入后 revision 不变）和 missed/grace 边界。
- [x] 所有 runtime definition 回写统一携带 claim 时的 `ScheduledJobRecord.revision`，通过 `update_job_runtime_projection` CAS 持久化；并发 authoring 的 `Conflict` 与删除后的 `NotFound` 均按 stale no-op 处理，不覆盖也不复活 job。生命周期回写使用 `project_id + job_id` 精确查询，不扫描全表。
- [x] repository 增加 project-scoped recoverable-job 查询，合并 enabled job 与拥有 pending/retrying/running occurrence 的 job，并返回最早 running `lease_until`。coordinator 对 retry、lease 和业务 `next_run_at` 取最早唤醒时间，deadline 回调先执行 expired-lease recovery；disabled one-shot 的非终态 occurrence 同样可以恢复。
- [x] graceful `Shutdown` 将当前 owner 仍持有的 running occurrence 释放为 `retrying + SCHEDULED_LEASE_LOST`；Task 5 再把现有 process-level heartbeat 拆成 per-occurrence guard。
- [x] `RegisterWorkspace` 瞬时失败使用命名退避常量去重重试；新 registration 构建成功后才替换旧 registration，失败时保留旧 deadline。正常成功或 workspace 移除会使待到达的旧 retry 命令失效。
- [x] 使用命名 interval/tolerance 比较 process-level wall clock 与 monotonic clock，仅检测到跳变时消费 `ReconcileReason::TimerDrift` 做全量 reconcile，不恢复一秒 job polling。
- [x] 新增真实 paused-time command-loop 验收：`RegisterWorkspace/JobCreated -> Tokio advance + 可控 wall clock -> deadline -> SQLite materialize/process -> re-register`；同时覆盖 lease 精确到期、首次注册失败后成功、旧 registration 保留、TimerDrift 和 shutdown release。
- [x] 将 workspace refresh 收紧为 replace-on-success：候选 registration 在独立 reconcile 完整成功后才替换旧对象；open/migration/query/process 任一步失败都会按原 `revision + wake_at` 重建旧 deadline 集合，2 秒去重重试成功后才提交候选。
- [x] `SchedulerCoordinatorHandle::shutdown()` 增加 oneshot ack 并持有唯一 coordinator `JoinHandle`；真实 command loop 严格按 release owned leases -> ack -> exit 执行，调用方在 ack 后继续 join task。Tauri `ExitRequested` 使用原子 started/completed 状态防重入，完成前持续 `prevent_exit`，完成后只允许程序化重试退出。
- [x] manual run-now 与 missed recovery 全部切换到 project-scoped、require-existing-definition 的 occurrence 仓储接口；delete 竞态返回 not found，不允许 `ensure_job_row` 在活跃 runtime 中复活 job 或产生 ghost occurrence。placeholder helper 仅保留给核心仓储的隔离单测。
- [x] 新增接口级回归：候选 reconcile 中途失败保持旧 registration/deadline 且 2 秒重试后才替换；shutdown ack 返回前 release 已调用；due materialize 后 delete 再 missed、stale run-now record 后 delete 均不会重建任何 job/occurrence。
- [x] deadline 回调弹出注册项后保留原 `revision + scheduled_deadline`；DB、materialize、process 或 refresh 任一步失败都用 `DEADLINE_FAILURE_RETRY_DELAY` 自重装，下一次回调重新读取 SQLite，不依赖外部命令唤醒。
- [x] manual/scheduled claim 到真实执行接受之间统一使用 `ClaimToHandoffGuard`；任意早退错误自动移除 active 并释放为 retrying，即时终态显式 disarm，成功执行后 handoff 给 lifecycle。
- [x] `Shutdown` ack 升级为 `ScheduledServiceResult<()>`，release 失败返回结构化错误；handle 无论 ack 成功、失败或丢失都先 join coordinator task，再优先返回原 release/ack 错误。
- [x] `ClockDriftDetector` 改为累计 wall/monotonic residual，只有累计超过容差或墙钟倒退才触发 reconcile 并重置基线；正常单调推进不重复触发。
- [x] `TimerDrift` reconcile 使用 coordinator-owned pending 状态；失败时保留 pending 并在下一个 `CLOCK_DRIFT_CHECK_INTERVAL` 重试，只有完整成功才清除，不因 detector 已重置基线而丢失恢复机会。
- [x] coordinator 使用公平 `tokio::select!`，控制命令洪峰不会无限饿死 deadline、lease heartbeat 或 clock check；scheduled busy retry 改为唯一调用 `scheduler::queue::decide_queue`，不再复制重试次数或间隔。
- [x] 新增接口级回归：deadline process failure 无外部信号 2 秒后重试成功；manual/scheduled active probe 错误均释放 claim；shutdown release 与 join 双失败时仍完成 join 并优先返回 release/ack 错误；多段单次低于阈值但累计越界的漂移只在越界时触发；TimerDrift reconcile 首次失败后会在下一次时钟检查成功重试。
- [x] 新增响应性回归：600 个 missed 点的 reconcile 单轮不会穷尽 backlog；控制命令积压时到期 deadline 仍可执行。

## 2026-08-11 主线生命周期合并收口

- [x] occurrence 只覆盖 scheduler 发起的一个 prompt turn；Direct/continuous 自动派发后续排队用户消息前，显式清除 `scheduled_occurrence_id` 与定时 prompt context，后续消息按普通会话处理。
- [x] 本次合并维持停止语义：定时 Workflow 中途停止后保留 `Paused + ProcessInterrupted`，不自动恢复 Runtime，也不改写为 cancelled；后续触发按 `ResumablePaused` 和 overlap policy 跳过或有限重试，只有用户显式“继续工作流”才恢复。
- [x] 删除 scheduler 独立的顶层 Tauri exit 状态机；`desktop_lifecycle` 作为唯一 `RunEvent` owner，在 ACP/runtime 清理前等待 scheduler lease release ack 与 coordinator join，并统一转发系统 resume reconcile。
- [x] 通知按 occurrence 归属互斥：普通事件保留主线 Direct 队列批次通知，定时事件只走 scheduled notification；原生通知导航统一进入带 `conversation/scheduled` 类型的去重待处理队列。
- [x] settings schema 升级到 v4，覆盖主线 v3 与定时分支 v3 两种来源，迁移时同时补齐 scheduler defaults 与 managed-agent capabilities，保留来源分支的显式值。
- [x] 接口级回归已固化 occurrence 上下文边界、通知归属/批次去重、停止后的 paused 分类、shutdown ack/join/lease release、desktop exit 防重入与 v3 双来源迁移。

## 2026-08-11 设置页加载体验优化（SWR 缓存）

- [x] 问题：`ScheduledRuntimeSettings` 每次挂载都从 `null` 重新拉取 `get_scheduled_runtime_settings` 并渲染「加载中…」；Radix `<TabsContent>` 卸载非激活标签页、`SettingsPage` 因 `key` 变化重挂载，导致反复进入「通用 → 定时任务」都出现加载闪烁。
- [x] 方案：新增 `web/src/components/scheduled-tasks/useScheduledRuntimeSettings.ts`，以模块级缓存实现 stale-while-revalidate——有缓存立即渲染、新鲜期内不重复请求、过期或保存成功后静默刷新；App 启动在 `getAppBootstrap` 完成后调用 `prefetchScheduledRuntimeSettings()` 预填缓存，使首次进入也免闪烁。
- [x] 领域归属：该设置混合了运行时状态（生效情况、启用任务数、电源错误码），不并入启动静态快照 `AppBootstrapVm`，独立维护运行时缓存。
- [x] 改动：`ScheduledRuntimeSettings.tsx` 改用 hook 并在保存成功后 `replace` 写回缓存；移除未在生产使用的 `initialSettings` prop（测试改用 `writeScheduledRuntimeSettingsCache` 预填，retention 改为从缓存 lazy 初始化以兼容 SSR 渲染）。
- [x] 验收：新增 `web/tests/scheduled-runtime-settings-cache.test.ts` 覆盖缓存读写、新鲜期判断、inflight 去重、失败重试与预取静默；调整 `web/tests/scheduled-task-settings.test.ts` 适配缓存注入；全量前端回归 156 文件 / 1003 用例通过，`tsc -p web/tsconfig.build.json` 通过。
- [x] 启动前端并用 agent-browser deep link 到 `/settings`：「定时任务」区域完整渲染（保持系统唤醒 / 完成通知 / 历史保留天数），全程无「加载中…」，验证预取 + 缓存命中让首次打开设置页即免闪烁。切换标签页因 Radix Tabs 在 CDP 下不响应合成事件未能直接触发，但 `useState` 同步取缓存渲染控件的行为已由 SSR 渲染测试（`scheduled-task-settings.test.ts`）覆盖。

## 2026-08-12 next_run_at 单一维护者、next_at 单一数据源、running occurrence 主动对账

针对「定时任务 occurrence 卡 running、详情页 next_run_at 停在已过的触发点而外层列表显示正常」的一组耦合问题，做三处本质修复。

- [x] 第1点 next_run_at 单一维护者：`ScheduledTaskService::update` 对比新旧 schedule，未变则保留 scheduler 已推进的 `next_run_at`（编辑 instruction/附件/非调度字段不再扰动调度），只有 schedule 变化或启停才用 `derived_next_run_at` 重算。`derived_next_run_at` 保留 `last_trigger_at`/`created_at` 基准（一次性 `At` 与 legacy 导入仍保留触发点），倒退由「编辑保留」覆盖消除。
- [x] 第2点 next_at 单一数据源：list VM 与 diagnostics VM 统一用持久化的 `record.next_run_at`；`from_definition[_in_workspace]` 增加 `next_run_at` 参数，`service.list` 返回 record，`scheduled-task-updated` 事件携带 record 的 `next_run_at`。外层列表与详情页不再出现 next_run_at 分歧。
- [x] 第3点 running occurrence 主动对账（本质修复，非运行时长上限）：`handle_registered_deadline` 在 `recover_expired` 后用 `reconcile_running_occurrence_outcome` 核对当前 job 的 running occurrence——Task/Run 仍 active 则保留（不误杀长任务），已 completed/缺失则收尾为 succeeded/failed 并记日志。辅助：`launch_prepared_run_background` 用 `catch_unwind` 包 `drive_from_node`，panic 也走 terminalize；`handle_lifecycle_event` 三个静默 bail 加 warning 日志。
- [x] 验收：新增 `derived_next_run_at_for_every_schedule_always_yields_future_point`、`update_preserves_next_run_at_when_schedule_is_unchanged`、`scheduled_task_vm_next_at_uses_persisted_next_run_at_not_realtime_recompute`、`list_running_occurrences_for_job_returns_only_running`，以及三个 `reconcile_running_occurrence_*`（completed⇒succeeded / missing⇒failed / active⇒保留）。scheduler 测试套件全绿；两个无关的 `view_models.rs`/`commands.rs` 测试在改动前同样失败（Windows 临时目录残留），非本次回归。

## 2026-08-12 重新启用停用的定时任务不再回填 missed 记录或发 missed 通知

- [x] 问题：Repeat/Cron 任务停用后再启用，会显示停用期间「错过的记录」并发出 missed 通知。根因是 `set_enabled` 的 enable 分支只重置了 `Every` 的 anchor，Repeat/Cron 的 `next_run_at` 仍用 `derived_next_run_at`（基准为停用前冻结的 `last_trigger_at`）算出停用期间的一个过去点；coordinator 立即当作 missed 回填（`reconcile_missed_deadlines` 每周期落一条 missed occurrence）并 `notify_missed` 发通知。Workflow/Auto 用户常配 Repeat/Cron，所以更易撞上；与 run mode 本身无关。
- [x] 修复：`set_enabled` 在 disabled→enabled 转换时，对所有 schedule 类型都用 `definition.schedule.next_occurrence_after(now)` 计算下一次（停用期间错过的点按既有「grace 外不补跑历史」语义跳过），不再用 `derived_next_run_at`。移除仅 Every 的 anchor 重置特例。disable 路径不变（derived 对 disabled 返回 None）。
- [x] 验收：新增 `reenabling_repeat_job_schedules_next_run_from_now_not_from_stale_last_trigger`（last_trigger_at 设为 30 天前 → disable → enable → 断言 next_run_at ≥ now）。scheduler 测试套件 108 用例全绿。

## 2026-08-13 deadline 毫秒精度与 revision 空转修复

- [x] 根因：`Every::next_occurrence_after` 使用整秒差计算周期，而 schedule anchor 可包含纳秒、SQLite `next_run_at` 只保存毫秒；数据库往返后的 deadline 可能被重算为同一毫秒，导致 materialize 每次成功但 deadline 不前进，coordinator 以零延迟持续重排并约每秒写入 65 次 revision。
- [x] 设计约束：scheduler 以持久化层的毫秒精度为统一时间边界；周期计算必须在该精度上严格单调前进。实现使用 checked `i128` 毫秒算术，保持 O(1)，不增加扫描、轮询或额外 I/O。
- [x] 仓储防护：相同 definition 的 `save_job_definition` 变为幂等 no-op，不为无业务变化的数据增加 revision；coordinator 遇到没有 runnable recovery 工作的 stale 过去 deadline 时至少按 `DEADLINE_FAILURE_RETRY_DELAY` 退避，防止异常路径重新形成零延迟自旋。process 失败已留下 runnable occurrence 时不叠加第二次退避，仍按原 2 秒恢复 SLA 执行。
- [x] 接口验收：`every_sequence_advances_after_a_persisted_millisecond_deadline` 固定现场纳秒/毫秒样本；`due_materialization_advances_a_millisecond_persisted_every_deadline_once` 固定一次物化推进 3 分钟、同一 `now` 再次物化返回 `NotDue` 且 revision 不再增加；`saving_an_unchanged_definition_is_revision_idempotent` 和 `stale_past_deadline_is_rearmed_with_failure_backoff` 分别覆盖两层防护。
- [x] 性能验收：消除单 job 约 65 次/秒的 SQLite 写入、CAS 干扰和 coordinator 立即唤醒；正常任务只在真实状态或 deadline 转换时写入，算法复杂度和锁范围不变。

## 2026-08-13 Direct continuous 终态收敛修复

- [x] scheduler 创建 Direct/continuous 执行 App 时统一调用 `configure_conversation_runtime_callbacks`，不再手工安装不完整的 live/session callback；Direct 成功后延迟发出的 `AcpTurnFinished` 能携带稳定的 `scheduled_occurrence_id`。
- [x] prompt queue drain 显式携带发起当前 turn 的 App，不再从 `DesktopState` 重新构造并丢失执行来源；scheduled turn 收尾后，自动派发的用户消息只清除 scheduled occurrence/prompt context，保留普通会话的 prompt lifecycle callback 以继续驱动队列。
- [x] `CoordinatorRuntimeDriver for ScheduledRuntime` 显式接入主动 running-occurrence 对账；底层 Task/Run 已终态时，先移除并等待 per-occurrence heartbeat guard 停止，再由当前 owner CAS 写入 occurrence 终态，避免历史修正后残留内存 heartbeat。
- [x] 接口回归覆盖 `direct_queue_drain_keeps_the_originating_scheduled_turn_context`、`queued_user_turn_clears_only_the_scheduled_origin`、`reconciled_terminal_occurrence_stops_and_removes_its_active_guard`，并保留 completed/missing/active 三类对账结果测试。
- [x] 性能评审：正常完成链路只增加常数级 App clone/callback 配置；恢复仍只在既有 deadline 唤醒时查询单个 job 的 running occurrences，不增加轮询、全表扫描、跨 workspace N+1、无界缓存或锁范围。

## 2026-08-13 审查缺陷统一收口计划

- [x] 先写 Rust 回归，覆盖同一 Task 的 Run A 已终态、Run B active 时只收敛 A；对账终态必须触发 definition 投影、task/occurrence event、通知和 cleanup；CRUD update event 必须携带持久化 `next_run_at`。
- [x] 将 active probe 收紧到 occurrence 的 `task_id + run_id`，并让 lifecycle 与 reconcile 共用完整终态函数；保持 owner CAS、heartbeat stop 顺序和既有通知去重策略。
- [x] 仓储增加 occurrence keyset cursor 查询，使用 `(scheduled_at, created_at, id)` 稳定倒序；状态筛选先于分页并使用专用复合索引；Tauri/browser facade 返回 `{ items, nextCursor }`，固定每页 20 条。
- [x] 先写 Web 回归，覆盖列表加载失败不清空、CRUD 失败反馈与防重复、筛选“已启用”语义、20 条分页、窄屏无横向溢出、设置迟到 GET 不覆盖 save、retention draft 跟随权威版本及非法范围提示。
- [x] 管理页和详情页使用显式请求状态机；错误时保留 canonical entity。详情第一页响应实时事件，其他页保持浏览位置；分页和筛选不触发全量历史加载。
- [x] 设置缓存增加 mutation generation 与订阅式缓存提交，保存操作串行化；retention 维护 server value / draft / dirty，禁止迟到响应或并发保存造成字段回滚。
- [x] 验证全量 Web 168 文件 / 1089 项、TypeScript、生产构建、Rust fmt/check 和定时运行时/命令回归均通过；内置浏览器在同一 SPA 会话中验证 21 条历史按 20 + 1 翻页，390×844 下页面无横向溢出，页面日志为空，服务与页面已清理。失败态由接口级回归固化。
- [x] 性能验收：精确 Run 对账不扫描同 Task 全部历史；单页 SQL 最多取 21 行且由 `EXPLAIN QUERY PLAN` 验证状态复合索引；诊断总数使用 SQL 聚合，前端只保留当前页和有限游标栈，不引入全历史加载、轮询、N+1、无界缓存或 coordinator 阻塞。

## 2026-08-13 用户交互后 occurrence 恢复执行中

- [x] AskUserQuestion 回答和 Workflow 人工验收提交前，共用 Task/Run/Round/Attempt 精确查询与恢复边界；普通交互在 scheduler DB 不存在时直接继续，不创建数据库或产生迁移 I/O。
- [x] 恢复成功后原 occurrence 原子转换为 `running`、重新注册 heartbeat，并发布既有 occurrence 更新事件；definition 投影同步变为 `running`、清空用户输入错误和 retry 状态。
- [x] coordinator 向命令层返回恢复后的原 occurrence ID；ManualCheck 将其注入后台 App，使后续 lifecycle 终结同一 occurrence。人工验收成功与失败判定使用相同路径。
- [x] ManualCheck 在恢复 occurrence 前先验证 Run 仍暂停在请求的当前 node/attempt，后台提交前复用同一领域校验；过期判定不会先把 occurrence 改成 `running`。
- [x] coordinator、存储、workspace registration 或恢复竞态失败时不写 durable elicitation response，也不提交人工验收判定；后端返回结构化 scheduler 错误码。
- [x] Run 后续终态仍通过原 lifecycle 回写同一 occurrence 为 `succeeded` 或 `failed`，不新建历史记录。
- [x] 接口回归覆盖 coordinator 返回原 occurrence ID、恢复态投影与 running 事件、普通交互 missing-DB 快路径和存储错误映射。
- [x] ManualCheck 后台提交复用 RuntimeContinue 单次握手；人工判定和控制转移持久化后 command 才返回最新 Run，握手前失败或 panic 先以原 occurrence ID 发出 `RuntimeAbnormal` 终止事件并同步返回结构化错误，避免 heartbeat 对应的 occurrence 永久停在 `running`。
- [x] command 在 scheduler reclaim 前持有 Run 级 single-flight reservation，拒绝同一 Run 的并发人工判定；握手后失败使用 Run/Round/Node/Attempt/execution ID 条件收敛，迟到失败不覆盖后来执行。
- [x] 握手前 fallback 改为原 locator 条件收敛：attempt 锁内必须仍是 paused + `manual_check_pending` 才允许写 `RuntimeAbnormal`；replacement attempt 保持权威状态，原 scheduled occurrence 仍收到失败终止事件。
- [x] attempt 收敛结果区分 `Converged`、`Superseded` 和持久化错误；无 execution ID 的 active attempt 仅在 running locator 与 durable Node 空 identity 同时匹配时允许收敛。持久化错误会重读精确 execution/locator，只有确认被新执行替换才抑制事件，Run 已部分写入、收敛重入或归属无法排除时继续终结 occurrence 并记录组合错误。
- [x] 接口回归 `manual_check_background_converges_a_precommit_failure` 固定“scheduler 已恢复、后台提交尚未持久化即失败”的窗口，覆盖命令错误、Run canonical state 和 occurrence lifecycle identity 三个边界。
- [x] reservation 回归覆盖并发提交拒绝与 lease 释放后可重试；`stale_background_drive_error_does_not_override_a_newer_execution` 覆盖 execution A 的迟到失败不得暂停 execution B 或发布错误终止事件。
- [x] 组合竞态回归覆盖 reserve 后 current attempt 被替换，以及 Run 写成功后 Round 路径故障；两条用例均先在旧实现稳定失败，再验证 replacement state 不被覆盖且 occurrence 不残留 `running`。
- [x] 性能评审：每次定时交互只增加一次带 `LIMIT 1` 的完整 locator 查询、一次单行 claim、既有 revision/CAS 投影更新和一次增量事件；普通交互 DB 不存在时零 I/O；不增加轮询、扫描、N+1、无界状态或锁范围。
- [x] ManualCheck 握手仅增加一个进程内一次性 channel 和一条消息；command 等待本地持久化但不等待 provider 执行，不增加 I/O 数量、轮询、队列、缓存、锁范围或前端刷新范围。
- [x] single-flight registry 只保留握手中的 Run key，execution CAS 复用 attempt 细粒度锁；provider 执行不在锁内，不引入全局串行化、扫描或无界状态。
- [x] 仅收敛持久化失败时增加一次精确 Run/Node 重读；正常路径与正常失败路径不增加扫描、轮询、I/O、锁范围或 provider 等待。
- [x] 最终验收：ManualCheck 6 条筛选回归、background-drive 3 条、fixed-continue stale execution、scheduler core 95 条和 desktop 389 条全部通过；desktop check、Rust fmt 与 diff check 通过。core lib 全量 757 条中 742 通过，15 条残余仍属于既有 profile/temp/Git/并行 JSONL 隔离问题。

## 2026-08-17 PR #100 合并审查缺陷收敛

- [x] 基于最新 `origin/main` 在隔离 worktree 合并 PR #100；7 个内容冲突按主线 Workflow authoring/runtime phase 与 PR scheduled occurrence/恢复协调能力并集机械解决，没有引入业务语义二选一。
- [x] Scheduler schema 升级到 v3：新库直接建立最终结构；v1 在单事务内串行升级到 v3；v2→v3 只执行一次 history/status-history 索引重建；当前 v3 open 只读版本后返回，不再开启写事务或重复 DDL。
- [x] 固化 migration 接口回归：v1 job/occurrence/deadline 数据无损升级，v2 旧索引升级为稳定 keyset 索引，v3 重开前后 SQLite `schema_version` 不变化，新于 binary 的 schema 在 mutation 前拒绝。
- [x] 收紧 `next_run_at` 转换：非调度编辑保留已推进 deadline；Repeat/Cron/Every 的真实 schedule 编辑与 disabled→enabled 均从 `now` 求下一点；enabled→disabled 清空；同状态启停直接返回当前 record，不增加 revision 或 coordinator command。
- [x] 新增服务接口回归：stale `last_trigger_at` 下 Repeat/Cron/Every 编辑不回退到过去；重复 enabled/disabled 保留 record、deadline、revision 与通知计数；既有非 schedule 编辑和重新启用用例继续通过。
- [x] 详情页以页面专用 refresh coordinator 统一 generation、history/diagnostics 提交和 single-flight 尾随刷新；foreground 翻页、筛选、任务切换会取消 queued refresh 并使旧成功、失败、finally 整体失效。
- [x] 新增前端接口回归：burst events 合并为当前请求加最后一次 follow-up；旧失败不显示错误；旧 diagnostics/history 不覆盖新页；任务切换只接受新 generation。协调器复用 Promise/ref 语义，不增加依赖、canonical state、轮询或无界队列。
- [x] 回归通过：SQLite repository 54 条、scheduler core 100 条、orchestrator 71 条、desktop scheduled/runtime 128 条、scheduled service 24 条、Web 全量 212 文件 / 1369 项；Rust fmt、TypeScript、Web 生产构建与 `cargo check -p sasuke-desktop` 均通过。
- [x] 内置浏览器 deep link 验证列表、详情、手动执行、状态筛选与 21 条历史的 20 + 1 翻页；诊断计数单调收敛，390×844 与默认宽度均无横向溢出，页面控制台无 warning/error，测试页与 1421 开发服务已清理；本条随 merge commit 一并落盘。
