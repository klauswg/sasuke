# 角色批量导入实施方案

## 目标

让用户能把现成的角色 Markdown 文件批量导入平台，而不必逐个手动填写。用户选择一个文件夹后，客户端递归解析其中所有 `.md`，按宽松兜底策略导入为用户级角色，并以结果报告呈现。

## 设计结论

这是 profile 导入能力缺失导致的功能缺口。复用现有 front matter 解析（`parse_optional_frontmatter_document`）和单角色创建（`create_profile`）能力；目录发现使用成熟的 `walkdir`，不维护自写递归遍历。

数据与生命周期：

- 新增 `ImportProfilesInput`（`folder_path`、`dynamic_template`）、`ImportProfilesResult`、`ImportedProfileRecord`、`ImportRecordStatus`、`ProfileFieldFallback`、`ImportProfileError` 与 `ImportProfileErrorCode`。
- 导入一律创建 `User` scope 角色，id 由 `next_profile_id` 保证全局唯一，文件名由 `sanitize_profile_name`-id 生成；因此冲突只发生在显示名层面，用自动重命名解决，不会覆盖内置角色。
- 批量导入忽略文件自身声明的 `dynamicTemplate`，统一由导入开关决定；开启时复用 `profile_template_validation_contexts` + `render` 做模板校验，失败则降级为静态并标注。

解析与兜底：

- 用 `parse_optional_frontmatter_document` 取 fields 与 body；front matter 缺失时标 `frontmatter-missing`。
- `name = fields.name ?? fields.title ?? file_stem`；兜底标 `name`，仍空则失败。
- `summary = fields.summary ?? fields.description ?? 正文首段`；兜底标 `summary`，仍空用 name 兜底，确保通过 `ensure_profile_input`。
- 动态模板开关开启且校验失败标 `dynamic-template-downgraded`。
- 单文件失败通过 `ImportProfileError { code }` 返回，区分读取失败、front matter 无效、空文件、名称缺失与创建失败。
- 文件夹遍历关闭符号链接跟随、按文件名稳定排序；遍历不完整时在创建任何角色前返回 `profile.import.folder-read-failed`。

接口：

- Tauri 命令 `import_profiles_from_folder`（async + `spawn_blocking_command`），经 app facade 转发到 `profiles::import_profiles_from_folder`。
- 前端三层 API：`RuntimeApi.importProfilesFromFolder`、`desktopApi` 实现、`api.ts` 导出；浏览器预览提供空结果 stub 以满足接口。
- 错误码统一在 `profile.import.*`，前端按码本地化，后端不拼对客文案。
- 导入设置、请求、结果与结果内编辑由 `profileImportReducer` 统一管理为 `closed / settings / result / editing` 四个表面状态；失败始终显示在当前可见抽屉，编辑保存后按 `importedId` 用接口返回值同步结果记录，再恢复原批次结果。

## 实施项

- [x] 后端新增导入数据结构与 `import_profiles_from_folder`，复用 `parse_optional_frontmatter_document` 与 `create_profile`。
- [x] 使用 `walkdir` 实现稳定、无链接跟随的目录发现（5000 上限），遍历错误显式返回。
- [x] 接通 app facade、Tauri command 与 `generate_handler!` 注册。
- [x] 前端新增类型、三层 API 与浏览器预览 stub。
- [x] 角色管理页新增「导入角色」按钮；导入设置、结果报告和结果内单项编辑统一为可连续返回的右侧抽屉工作流。
- [x] 同步中英文 i18n 文案与导入错误码文案。
- [x] 后端单元测试覆盖完整/兜底/字段兼容/重命名/空文件/动态开关各场景（11 例，profiles 模块 21 passed）。
- [x] 增加前端 reducer 与 desktop API 接口测试，覆盖错误可见性和命令参数透传。
- [x] 前端类型检查、src-tauri 编译、内置浏览器 UI 验证。
- [x] 导入设置与结果改为共享尺寸记忆的自适应 Sheet；标题/底部操作固定，中间列表独立滚动，长名称、Windows 路径和错误信息安全断行，窄宽度下编辑操作自动换行；编辑角色后自动返回同一结果批次。
- [x] 结果内编辑保存后按稳定 `importedId` 回写最新角色名称，保留源路径、兜底标记和失败记录；reducer 测试固化目标更新与批次恢复语义。

## 验收矩阵

| 场景 | 验证 | 预期 |
| --- | --- | --- |
| 完整合规文件 | 导入有 front matter 且含 name+summary 的 md | Imported，无兜底标注 |
| 缺失 front matter | 导入纯 Markdown 文件 | 导入成功，标注 frontmatter-missing/name/summary |
| 缺 name / 缺 summary | 导入缺字段文件 | 分别用文件名 / 正文首段兜底 |
| 外部字段名 | 文件用 title/description | 正确读为 name/summary |
| 同名冲突 | 导入与已有角色同名文件 | 自动改名 -2/-3，不覆盖内置 |
| 动态开关关闭 | 正文含模板语法 | 按静态导入，不校验 |
| 动态开关开启 | 正文为合法模板 | dynamic_template=true |
| 动态开关开启 | 正文含未知变量 | 降级 false 并标注 |
| 空文件 | 导入空 md | 失败，error.code=empty-file |
| Front matter 无效 | 导入 YAML 格式错误的 md | 失败，error.code=invalid-frontmatter |
| 目录链接 | 文件夹包含指向外部或自身的符号链接 | 不跟随链接，不重复或越界导入 |
| 文件夹无效 | 选择不存在/非目录/不可完整读取/无 md 文件夹 | 返回 profile.import.* 错误码 |
| 导入失败 | Tauri command 返回错误 | 设置抽屉保持打开并展示本地化错误 |
| 结果内编辑失败 | 读取已导入角色失败 | 结果抽屉恢复并展示错误 |
| 连续修正 | 从结果中编辑任意成功项并保存或返回 | 自动恢复原批次结果，可继续编辑下一项 |
| 修改名称 | 从结果中编辑成功项、修改名称并保存 | 返回结果后目标行立即显示接口返回的新名称，其他记录与导入诊断不变 |
| 大批量结果 / 长路径 / 小窗口 | 导入多条深层 Windows 路径并缩小窗口高度与宽度 | Sheet 保持在窗口安全范围内，仅结果列表滚动，无横向溢出，关闭按钮始终可见 |

## 验证命令

```powershell
cargo test -p sasuke --lib profiles::
cargo check --manifest-path src-tauri/Cargo.toml
npm run web:test
npm run web:build
```

UI 验证：启动 `npm run web:dev`，进入“上下文管理 → 角色管理”，点击「导入角色」确认设置 Sheet（动态模板开关 + 选择文件夹按钮），完成导入后确认结果 Sheet；从结果进入角色编辑并保存或返回，确认恢复原批次且可继续编辑其他项。验证完成后关闭开发服务器与浏览器测试页面。

2026-08-11 验收记录：内置浏览器确认“导入角色”打开唯一右侧设置 Sheet，关闭后正确卸载且控制台无警告或错误；`profileImportReducer` 回归测试固定 `settings -> result -> editing -> result` 生命周期，并证明编辑期间批次结果不清空。相关 3 个测试文件共 12 tests 与 Web production build 均通过。
