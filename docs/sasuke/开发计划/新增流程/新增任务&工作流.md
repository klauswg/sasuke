1.首页任务列表，去掉导入requirements按钮，只保留创建任务
2.点击创建任务按钮，弹出创建任务弹窗
3.弹窗中支持手动填写任务标题、描述和详细需求内容，也支持导入现有需求（支持txt\md格式）覆盖当前编辑内容
4.填写或导入需求后，需要创建工作流
5.工作流可以新增节点和边，为每个节点配置不同的agent，agent来源为agent管理页面的agent卡片
6.工作流保存后持久化为json，在任务详情页的工作流中可以修改工作流，round详情中的工作流则是每一次工作流的运行时快照
7.节点前后需要有环境的生成和销毁(比如释放agent等)

关于工作流的修改与新增：最好是能做到画布式拖拽生成工作流，是否有现成组件可以使用？
关于工作流中的节点与边：
现有的节点类型现在可能要去掉execute节点，和execute-plan产出节点

节点的本质类型只有三种。1.普通节点 2.是否需要人工check？（人工check逻辑你先留白，本期不实现） 2.是否需要验证AI输出结果？ 2.1 输出的DSL配置（也就是AI要输出的指定类型，目前支持json） 2.2 DSL成功条件（json中指定字段的值） 2.3 成功流向（去哪个节点） 2.4 失败流向（去哪个节点），边可以设置一些属性，比如成功边，失败边，边可以指向其他节点（则节点生成新的attempt），或者new round（则生成新的round），边指向节点时，如果是回路（即A-C--》D--》E-->...-->->B-->A）,在B节点指向A的边中可以提供一个选择，是开启新的session，还是继续A刚刚的session？（这里涉及到环判断，可能比较复杂，如果实现困难，可以先用简单方式，即验证输出结果类型的节点，失败边提供这两个选项即可）
工作流这里可以提供一套默认工作流给用户选择：
interview（普通节点，访谈角色，产出 interview-spec.md，由人工 check 判定成功后继续） --> plan（普通节点，方案角色） --> 开发（普通节点，开发角色） --> review（普通+验证输出结果，DSL中输出review是否通过，成功指向测试，失败指向开发，默认是继续开发原session） --> 测试（普通+验证输出结果，DSL中输出test是否通过，默认是继续开发原session） --> 验收（普通+验证输出结果，DSL中输出verify是否通过） --> cleanup（普通节点，清理角色，无 AI 输出验证） --> 结束

## MVP 实现状态

已实现：
- 任务列表移除独立“导入 requirements”入口，只保留“创建任务”。
- 创建任务 Sheet 采用编辑态，支持填写 title / description / requirement 正文，并可选导入 `.txt` / `.md` requirement 覆盖当前编辑内容；保存任务入口固定在 Sheet 标题栏右侧。
- 工作流编辑器基于 `@xyflow/react`，拓扑布局与节点 / 边展示投影已经分离：只有入口、节点身份或边关系变化才重算布局，Agent icon 和结果出口能力等展示信息只刷新轻量投影，其他 Inspector 编辑不触发整图布局。画布工具条复用 shadcn/ui `DropdownMenu`，以单个 `+` icon 新增 Agent、AI-DYNAMIC、结束和 New Round 节点，右键菜单保留为次要快捷方式；所有真实节点提供 success 语义把手，单出口垂直居中，开启 AI 输出验证或人工 check 的 worker 节点都把 success / failure 把手按上下对称位置展示，并提供对应 `NodeToolbar` 快速新增后继。
- 工作流编辑器已完成响应式工作台：宽容器复用 shadcn/ui `ResizablePanelGroup` 调整画布 / Inspector，只保留一条可拖拽分隔线，移除两侧 surface 的重复完整外框；窄容器改为画布 / 配置面板切换，不再纵向堆叠；首次在稳定尺寸上 fit，最小缩放为 `0.3`，pan / zoom 在画布、JSON 与响应式模式切换后保留。删除低价值 MiniMap 及其图边界计算、显隐状态和尺寸观察器，导航统一使用 React Flow 现有平移、缩放与 fit view controls。工作流模板管理页已建立完整 `min-h-0` 高度链，编辑器占满模板操作栏以下的视口剩余高度，并移除该模式的页面底部 padding，使画布直达页面底部；Inspector 内容在自身 `ScrollArea` 内滚动。组件保留默认高度策略并允许使用方通过 `className` 接管高度。
- 作者态已支持真实节点、结束节点、New Round 节点和边的键盘选择与删除、50 步有界 Undo / Redo；节点、终点与边共用画布顶部删除操作，选中边时按钮切换为“删除边”，Edge Inspector 不再保留重复删除按钮。终点节点不显示 `NodeToolbar`，删除时同步清除所有入边；终点与真实节点复用一致的选中颜色、外发光与渐变反馈，仅保留形状差异。打开或替换 workflow、Undo、Redo 以及删除当前节点后会统一清空瞬时画布选择、待聚焦目标和编辑器内 DOM focus，不再以第一个节点作为选择兜底；新增节点与校验问题定位仍保留主动聚焦。Inspector 以“当前选择”组织：无选择或仅选中边、终点时，直接展示“工作流控制”卡片，不再使用“工作流设置”折叠外壳；选中真实节点时，同一控制卡片作为节点 Inspector 的同级业务分区。节点配置和边配置仍可折叠，编辑态移除外层重复 Attempt / Round 摘要。高频 Inspector 草稿和父级通知合并发布；节点重命名会与尚未发布的 goal、model、output 等字段 patch 原子合并后一次写回，避免 React 批处理覆盖前一笔草稿。JSON 只在进入 JSON 模式时生成。
- 保存前校验改为持续、可点击的问题摘要，点击可选择并聚焦对应节点 / 边，保存阻断弹窗继续兜底。作者态所有边统一通过 routed renderer 输出，作者态与运行态标签固定在不透明前景层；按产品决定保留全部边持续流动的 CSS 动画。
- 节点 id 输入采用本地草稿，中文输入法组合输入期间不实时改写 workflow DSL，失焦、Enter 或组合结束后再提交；作者态画布普通节点直接展示原始节点 id，不对 `test` 等名称做本地化替换。
- 作者态画布允许编辑过程中临时存在多条同类型出边，便于先拖拽连线再修改边类型；创建任务 Sheet 标题栏的保存任务按钮与模板保存/另存按钮都会校验同一来源节点的同一结果类型只能有一条出边，并校验 failure 边只能来自已开启 AI 输出验证或人工 check 的 worker 节点；仅当节点不再具备 failure outcome 能力时才同步移除 failure 边。任务详情修改 workflow 时由保存工作流按钮触发同一校验。作者态和运行态已复用 `@tisoap/react-flow-smart-edge` 的障碍感知正交路由：failure、回退和跨节点分支边绕开所有节点矩形并保留安全间距，success 主链继续紧凑直连；主链标签和已放置分支标签通过组件原生 `avoidAreas` 纳入障碍模型，分支标签从真实路径中选择不与节点或其他标签重叠的清晰线段；路径与标签布局只在拓扑变化时执行，失败时回退原生 smooth-step。
- 所有内置与自定义工作流已完成“定义 + 本机模型绑定”双域改造。作者态普通 Worker 只保留稳定 `executionSlotId`，具体 Managed Agent、模型、权限和 Agent capability 声明的全部 session config options 由独立绑定持有；AI-DYNAMIC 及其内部节点保持运行时统一配置。创建 Run 前统一校验并注入完整执行字段，详细实现见 `内置工作流本机模型绑定实施方案.md`。
- 稳定 ID `default` 的展示名调整为“默认完整工作流”，拓扑保持 `interview -> plan -> dev -> review -> test -> accept -> cleanup -> $end`；`interview` 通过人工 check 判定结束，accept failure 新建 Round 后从 `dev` 重开，节点 goal 由后端按桌面语言生成。新增 `default-lightweight`“默认轻量工作流”，拓扑为 `grill -> dev-test -> accept -> $end`；`grill` 同样通过人工 check 判定结束，accept failure 新建 Round 后从 `dev-test` 重开。两个入口节点都不声明 AI 输出验证；两个模板均使用 `control.max_attempts=10`、`control.max_rounds=3`，并来自后端持久化内置 workflow JSON；前端不维护本地生成器。
- 工作流模板通过 `isBuiltIn` 布尔值声明是否内置，通过独立的 `optionalEntryStage` 声明可选入口，不定义完整/轻量类型枚举。composer 选择完整模板时提供采访开关，选择轻量模板时提供拷问开关，默认开启并按 workspace/template 持久化；自定义模板隐藏开关且拓扑不受影响。关闭后只变换本次创建的 workflow 副本，模板编辑器仍展示完整模板。详细方案见 `默认轻量工作流实施方案.md`。
- 默认角色由客户端内建资源直接提供访谈、方案、开发、审查、测试、验收、清理七个角色，再把对应 profile `id` 绑定到默认 workflow 节点；cleanup 是普通 worker 节点，不启用 AI 输出验证。
- 内置角色与默认工作流模板保持一致：可在 UI 中打开并编辑草稿，但不能直接保存覆盖，只能另存为新的普通角色；删除角色时若仍被 workflow 模板、任务 authoring workflow 或可继续/可重试/人工 check 的运行时快照引用，首次点击删除会在弹窗内提示引用范围以及删除后需要修复关联 workflow / snapshot，用户二次确认后仍可删除。
- 默认 review/test/accept 使用简化 AI 输出结构 `{"reason":"String","result":"boolean"}` 与 `$.result == true` 成功表达式；前端默认模板按钮应用后端模板，不维护独立默认 schema/expression。
- worker 节点支持 JSON 输出验证：`output.kind=json` + 简化 `output.schema` + `success_condition.expression`，并兼容旧 `success_condition.path/equals` 条件形式；旧完整 JSON Schema 不再兼容。
- JSON 输出约束输入采用本地草稿：停止输入约 2 秒或失焦后再校验并写入 workflow DSL；格式化改为输入框右上角悬浮按钮手动触发，避免输入中途被自动 beautify 打断。字段问号说明以及 profile 标签旁帮助统一使用圆形问号 icon + 随主题变化的浅色 shadcn/ui `Tooltip`，悬浮或聚焦即可出现；原本带明确语义的其他说明 icon（如 profile summary）保持原语义，不强行替换成问号。
- worker 节点的 AI 输出验证与人工 check 是二选一的结果判定方式，开启其中一种会自动关闭另一种。
- 保存 workflow 时前端会先聚合校验必填字段、角色绑定、角色存在性、成功表达式路径，以及当前 `workflow.id` 是否与模板库内其他模板重复；错误以一次弹窗集中展示，关闭后错误字段红色高亮并显示原因；后端仍会在保存时做同样业务约束的最终裁决，并通过 `code + params` 返回结构化错误，由前端 i18n 负责可见文案。
- 边可指向普通节点、`$end` 或 `$new-round`；工作流至少需要一条边最终指向 `$end`，且所有节点都必须从入口节点可达，否则不能保存。指向普通节点时会生成新 attempt，指向 `$new-round` 时打开新 round；`success` 边不能指向 `$new-round`，作者态目标下拉会隐藏该选项，保存与 DSL 校验都会拦截。
- 节点会话按边上的 session 策略展示：`session=new` 的 attempt 始终是独立可切换 conversation；`session=continue` 挂回被继续的 provider session，并在同一 conversation 内用 attempt 分隔行展示。
- 工作流控制项支持可选的 `max_attempts` 与 `max_rounds`：前者在单个 round 内限制 `failure` 触发的修复/重试跳转次数，正常 `success` 前进和 schema 隐藏修复不消耗次数；后者限制 `$new-round` 打开的新 round 次数；留空表示不限制，超限时 workflow 失败并展示结构化停止原因。默认完整工作流和默认轻量工作流都显式配置 `max_attempts=10`、`max_rounds=3`。
- 模板创建 Task 时复制 `WorkflowDsl` 和整份模型绑定；Task 作者态继续复用同一 `WorkflowDsl`，绑定独立管理。新建 Run 前严格校验并把普通 Worker 绑定注入 DSL，再冻结为完整 `workflow.snapshot.json`；Task 可保存绑定不完整或失效的状态，但不能启动 Run。历史 Run、Round 和 occurrence 快照保持原格式且不回写。
- 用户级 context 存储维护跨 workspace 工作流模板列表 `~/.sasuke/context/workflows.json`，通过 `templates` 数组支持多个命名 workflow，并记录上一次创建任务使用的 workflow；同一模板的本机模型绑定在各 workspace 共用。创建任务 Sheet 中上次使用的命名模板以“上次创建使用的是：xxx”提示展示在下拉框旁，点击模板名后选中该模板并隐藏提示，不作为下拉选项；模板下拉顶部支持新增模板并进入空白画布，行内支持删除非默认模板。内置与自定义模板的主按钮均为“保存修改”：内置定义仍不可覆盖且必须在全部普通 Worker 绑定有效后保存，自定义模板允许保存不可运行的绑定状态；存在内置定义改动时只能另存全部修改，或“还原其他修改”后保留绑定草稿。模板保存/删除成功提示短暂展示后自动消失，错误提示展示在工作流模板操作区下方并持续保留，不占用创建任务顶部全局位置。
- 普通 Worker 通过内部 `executionSlotId` 关联模型绑定：修改 node id 保留绑定，复制节点生成新槽位并复制完整绑定，另存工作流保留槽位并由新 template ID 隔离。普通导出不包含模型绑定或槽位；导入生成新槽位并保持未配置。可编辑模板、Task 与定时任务采用一次性破坏式迁移，历史运行快照不迁移。模板绑定嵌入 `WorkflowTemplate`；Task 的 `{ workflow, modelBindings }` 聚合写入 `authoring/workflow.json`；定时任务把同一聚合冻结到 content snapshot；Run 只保存注入完整执行字段后的 `workflow.snapshot.json`。
- 2026-08-21：修复节点快捷新增路径遗漏 `executionSlotId`，导致 Inspector 的 Agent 选择事件没有稳定槽位可写入。顶部新增与节点快捷新增统一复用 Worker 创建函数，在节点创建事务内生成唯一 UUID 槽位；模型绑定继续按槽位局部 upsert，不新增旁路状态、兼容层、依赖或额外 I/O。接口回归固化“新增 Worker 自带槽位并可立即写入 Agent binding”以及节点 ID 冲突后槽位仍保持独立。该变更只增加 O(1) UUID 生成与单次现有绑定更新，不触发整图布局或全量加载。
- 2026-09-01：修复 JSON 作者态只有保存入口补齐 `executionSlotId`，实时合法草稿与切回画布会把缺槽位 Worker 写入 canonical workflow 的调用链缺口。实时解析、画布切换和保存统一复用 JSON→作者态规范化契约，以当前 canonical 草稿复用或稳定生成槽位，并保留显式重复值供校验。未新增持久字段、状态、依赖或 I/O；每次合法 JSON 投影仍为 O(nodes + edges)，与既有实时解析的数据规模一致。
- Managed Agent 删除影响统计已收敛为用户级聚合接口：工作流模板只统计用户级 store 一次，Task/定时任务遍历全部已注册会话 workspace 与当前 Workbench workspace，并按稳定 `projectId` 去重；磁盘与 SQLite 扫描通过 async command 的 blocking pool 执行。删除确认弹窗只有在统计成功后才启用删除，失败时保留结构化原因并支持原位重新统计，迟到请求不得覆盖新选择。
- “另存为新的工作流模板”时后端会自动重置 `workflow.id` 为新的分布式唯一值，不向用户暴露内部碰撞；如果用户手动编辑 JSON 导致模板库内 `workflow.id` 与其他模板重复，则保存模板会失败并返回 `workflow.duplicate-id`。
- 工作流编辑器 JSON 视图支持直接编辑完整 workflow DSL，便于用户手动修复 `workflow.id` 冲突等模板级问题。
- AI-DYNAMIC 保存时会额外校验其引用的所有模板；只要被引用模板存在 `workflow.id` 与其他模板重复的脏数据，当前工作流就不能保存，直到用户修复对应模板 JSON。
- 节点执行前后增加 environment setup / teardown 事件与基础资源清理。

## 2026-06-15 创建任务草稿跨页面保留

- 创建任务 Sheet 的编辑草稿上提到 `App.tsx` 任务编排模块状态统一管理，避免点击“角色管理”进入上下文管理后组件卸载导致标题、描述、requirement 和 workflow 草稿丢失。
- 草稿只负责保留编辑态，不改变保存任务校验；提交时仍走现有必填校验、`validateWorkflowForSave`、`createTask` 链路。
- 草稿清理时机限定为创建任务成功、切换 workspace 或用户确认放弃草稿；普通跨页面导航不清空。

本期约束：
- 人工 check 已落地为 worker 节点的可选能力；worker 上与 AI 输出验证互斥，开启后 ACP 会话自然结束时暂停在 `WaitingForUserInput`，由会话面板“成功”“失败”按钮决定节点结果。作者态为人工 check 同样开放 failure 出口和分支配置；没有匹配的 failure 边时，runtime 等价执行隐式 `failure -> $end`，以 failure 完成工作流。
- 人工 check 适用于 `worker` 节点，与 AI 输出验证互斥；后端 `worker` / `节点输出产物` 兼容保留，避免破坏既有 workflow 与测试；新建工作流 UI 不再默认生成。
- 成功表达式只支持受限 JSONPath-like 标量比较，不执行任意脚本；支持多级对象路径、数组下标与引号宽松的标量比较，JSON 输出约束统一使用简化结构而不是完整 JSON Schema。

