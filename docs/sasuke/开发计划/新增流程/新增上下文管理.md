现在我们处理下侧边栏的上下文管理tab（现有的“知识库”，更名叫上下文管理）
上下文管理中顶部也是一个tab选择页，提供多类上下文管理，本次我们仅支持profile管理（做i18n,中文叫角色管理，英文叫profile管理（？或其他的））
profile管理的就是工作流中的profile
profile 支持两种来源：内置与用户级。用户级存储在 `~/.sasuke/context/profiles/<name>-<id>.md`；内置角色由客户端内建资源提供，不写入 `context/profiles` 目录。
内置角色包含方案、开发、审查、测试、验收、清理 6 个角色，workflow 继续使用 profile `id` 做引用，角色名称允许重复。历史已经写入 `context/profiles` 的同名默认角色继续按普通用户角色处理，不做迁移兼容。
profile 管理中，可以搜索、新增、修改、查看、删除 profile。列表拆成“自定义角色 / 内置角色”两个 tab：自定义角色 tab 承载用户级角色，并独立维护搜索和分页；内置角色 tab 只展示客户端内建角色，并独立维护自己的搜索状态，不与自定义角色列表串扰。卡片展示名称、id、摘要、scope、创建时间、更新时间；查看时显示完整 Markdown 内容。新增时要求用户填写名称、摘要、完整内容，id 由系统分配为分布式唯一 id；创建时间和更新时间使用系统本地时区的 `YYYY-MM-DD HH:MM:SS`。内置角色可打开并编辑草稿，但不能直接保存覆盖，只能通过“另存为新角色”创建新的用户级角色；删除仅对非内置角色开放。若仍被 workflow 模板、任务 workflow 或可继续运行快照引用，第一次点击删除时不直接拦截，而是在删除确认弹窗内明确列出当前仍被哪些引用占用，并提示删除后关联工作流需要重新修改、可继续运行快照也可能需要修复后才能继续；用户二次确认后仍可执行删除。删除相关提示应直接显示在删除确认弹窗内，而不是退回到页面级通用横幅。桌面端的状态写入类接口必须直接调用 Tauri 命令；浏览器 mock 仅用于非 Tauri 预览模式，不能参与桌面端的前置存在性判断，也不能在桌面端先行改写本地 fallback 状态。针对这条边界，前端增加 Vitest 回归测试，覆盖 runtime 选择、facade `force` 透传、desktop 直达 Tauri 调用与 browser preview mock 语义。
工作流定义中，用id做profile的引用，工作流配置面板的profile项做成下拉栏，支持关键字（名称、id、摘要、描述等）索引，下拉栏中每一项展示名称、id、摘要（截取、悬浮展示全文）、创建时间、更新时间，下拉栏默认加载内置角色和用户目录下的所有 profile，选中后下拉栏的当前栏显示名称，名称后跟着一个info图标，悬浮显示名称、id、摘要、创建时间、更新时间。工作流配置面板profile标题，后面跟一个info，悬浮提示，profile可在上下文管理中进行管理，跟一个超链接，点击跳转到上下文管理-->profile管理标签页

## 2026-07-23 角色卡片响应式补充

- 角色列表列数改由列表容器宽度决定：小于双列阈值时单列，中等宽度双列，仅在每张卡片拥有足够操作空间时升为三列；侧边栏展开/收起和 Windows 显示缩放变化不得依赖整窗断点碰运气。
- 内置角色与自定义角色卡片底部操作区统一允许换行；详情、修改、删除按钮的文案增长或字体缩放不得造成相邻卡片覆盖。
- 新增前端布局契约测试，固定容器查询与操作区换行规则；结合生产构建和最小窗口手工验证完成验收。

## 2026-07-30 角色批量导入

- 角色管理操作区新增「导入角色」入口，位于「刷新」与「新增角色」之间；点击后打开导入设置抽屉，用户选择一个本地文件夹，客户端递归遍历其中所有 `.md` 文件并批量解析为用户级角色。
- 解析采用宽松兜底策略，尽量保留用户内容：缺失 front matter 时用文件名兜底 `name`、用正文首个非空段落兜底 `summary`；字段名兼容外部常见写法（`name`/`title`、`summary`/`description`）；正文与名称都无法获得时才标记为失败。
- 导入设置抽屉提供全局「启用动态模板」开关（与新增角色表单一致）：关闭时本批全部按静态角色导入；开启时对每个角色按动态模板校验，正文不合法的自动降级为静态并在结果中标注。批量导入不再继承单个文件声明的 `dynamicTemplate`，统一由本次开关决定。
- 同名处理采用自动重命名：与已有角色（含内置角色）或同批已导入角色同名时，自动追加 `-2`、`-3` 后缀；导入始终创建用户级角色，不会覆盖内置角色。
- 导入完成后同一右侧抽屉切换为结果报告，分组展示已导入（含兜底字段标注，可点编辑修正）与导入失败（含结构化原因）；扫描文件数超过上限（5000）时提示已截断。文件夹不存在、非目录或无 Markdown 文件等整体性错误通过结构化错误码返回，前端本地化展示。
- 目录发现使用 `walkdir`，关闭符号链接跟随并保持稳定顺序；目录读取不完整时整批停止，不能静默漏扫后继续写入。单文件失败使用 `{ code }` 结构，区分读取、解析、空内容、名称和创建阶段。
- 导入设置、执行、结果及结果内编辑由同一状态机管理：导入失败时设置抽屉保持打开；结果内编辑时保留批次数据并切换到角色编辑层，保存成功后按 `importedId` 使用接口返回的最新名称同步目标结果行，返回或关闭后恢复结果抽屉，可继续修改下一项；任意时刻只有一个 Sheet 生效。

## 2026-08-09 SKILL 卡片 Agent 聚合与作用域来源修正

- 将 SKILL 卡片统一调整为可容纳两行 Agent 的固定高度；Agent 区先完整使用一行和第二行，两行放不下时才显示 `+N`，右侧详情、编辑、删除按钮保持固定。
- 抽取可复用的 Agent 溢出组件和纯容量计算接口，使用 `ResizeObserver` 响应卡片实际宽度，不按当前 Agent 数量写死阈值。
- `+N` 使用 shadcn/ui Popover 展示隐藏项，按直接读取与同步设置分组；隐藏同步项继续调用现有同步/取消同步接口，浮层允许连续操作。
- 修正 `skillSourceAgents()` 的作用域目录匹配：全局读取 `primaryAgentDir`，项目读取 `projectPrimaryAgentDir ?? primaryAgentDir`，兼容目录保持两端通用，覆盖 Pi `.pi/agent` 与 `.pi` 的拆分目录。
- 修正已配置 Agent 集合的数据源：以 `AgentRegistryVm.agents` 的实际配置为准，Catalog 只用于内置排序，自定义 Agent 必须追加并进入 SKILL 来源、筛选、同步表单和卡片展示。
- MCP 卡片接入相同的两行 `+N` 策略，隐藏 Agent 在 Popover 中继续展示兼容状态，未知状态允许触发单 Agent 诊断；SKILL 与 MCP 共用容量计算及测宽 Hook。
- 增加容量边界、SKILL/MCP Popover 点击操作及保持打开、自定义 Agent 纳入与修改配置优先、Pi 全局/项目来源识别以及错误作用域不匹配的前端回归测试；通过前端生产构建与上下文管理 deep link 实测验收。
