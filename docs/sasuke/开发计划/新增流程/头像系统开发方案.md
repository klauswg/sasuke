# 头像系统开发方案与实施记录

## 1. 方案判断

原系统只有会话内硬编码 Bot / User 图标，没有头像领域模型与持久化，因此需求来源于能力模型缺失，而不是单个页面样式不完善。本次建立统一头像偏好、存储和接口，再由设置页与会话详情共同消费，不采用页面 localStorage 或针对消息组件的路径补丁。

复用方案：

- shadcn/ui Avatar、DropdownMenu、Dialog、Button、Slider。
- `react-easy-crop` 负责拖动、缩放和圆形/方形取景框。
- 浏览器原生 file input 打开系统文件选择器。
- 既有 Preferences / Bootstrap 数据流负责跨页面同步。

## 2. 实施切片

- [x] 定义 Agent / 个人头像、形状、当前选择与最近 10 条数据结构。
- [x] 增加用户级头像元数据和图片文件存储，限制大小并校验图片签名。
- [x] 增加保存头像、选择最近头像、保存形状三个结构化 Tauri 接口。
- [x] 将设置页“外观”tab 更名为“个性化”，新增头像设置区。
- [x] 接入上传、1:1 裁剪、缩放、圆形/方形取景框和 WebP 输出。
- [x] 会话详情使用共享头像组件，头像增大到 36px。
- [x] ACP 思考/工具/计划/权限结构化行改为等宽无头像占位。
- [x] 同步中英文 UI 文案与结构化错误码映射。
- [x] 将头像区移动到外观和字体之后，并将双头像卡片收敛为可响应换行的紧凑横向设置行。
- [x] 增加清空当前头像接口与悬浮删除入口，恢复默认头像时保留最近头像历史。

## 3. 验证记录

- Rust：头像模块 3 项接口测试通过，覆盖默认值、上传持久化、最近 10 条、历史选择、形状切换和非法图片拒绝。
- Web：生产构建通过；头像偏好浏览器接口测试覆盖上传、最近选择、形状和上限。
- UI deep link：`/settings` 验证个性化布局、默认头像、最近头像菜单、文件选择、圆形裁剪弹窗、应用预览和方形切换。
- 会话 deep link：`/tasks/task-001/runs/run-003/rounds/round-007` 验证文本消息显示 36px 头像，思考、工具与权限行不显示头像但保持横向对齐。
- 2026-07-30 密度与顺序优化：个性化页调整为“外观 / 字体 / 头像”；头像预览缩为 48px，移除冗余“头像框”标签，形状按钮与头像信息同行，并通过容器查询在窄宽度下换行。
- 2026-07-30 头像菜单展开方向优化：下拉菜单改为左边缘对齐触发头像，优先向右展开；继续使用 Radix 碰撞检测处理窄窗口边界。
- 2026-07-30 头像删除能力：新增 `clear_desktop_avatar(kind)`，只清空对应 profile 的 `selectedAvatarId`；设置页在已设置头像悬浮或聚焦时显示独立删除按钮，避免与头像菜单触发器互相嵌套。
- 2026-07-30 删除入口视觉修正：移除容器 `focus-within` 显示条件，避免选择最近头像后因触发器残留焦点持续显示；删除入口收敛为 18px 中性减号，仅悬浮时切换危险色。
- 2026-07-30 头像操作状态域修正：移除四个头像回调对应用级 `busy` 的切换；上传继续使用弹窗内保存状态，历史选择、形状与删除改用 `AvatarEditor` 内无 UI 的 ref 并发锁，不再切换头像或形状按钮的 disabled 透明态，将可见更新缩小到头像预览或实际发生变化的形状按钮。

## 4. 回归边界

- 修改 `PreferencesVm` 时必须保留 `avatars`，浏览器 preview clone 必须深拷贝最近头像。
- 头像文件仅存用户目录，不写入 task/run/session 文件，不随工作空间删除。
- 最近列表裁剪后必须同步清理淘汰图片文件，避免无界增长。
- 新增图片格式时必须同时补前端 accept、裁剪输出策略、后端 MIME 白名单与文件签名校验。
