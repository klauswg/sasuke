# 个人数据分析内置能力开发计划

## 1. 目标与当前状态

从“帮助”菜单提供个人数据分析入口，用户选择可用 Agent、模型与思考强度后，客户端后台分析 `.maling/projects` 的全部历史，最终在应用内原生页面展示可追溯报告。

当前进度（2026-08-22）：

- [x] 完成真实数据目录只读盘点和指标边界确认。
- [x] 完成中英文 system/user/repair 固定提示词及严格渲染单元测试。
- [x] 完成产品设计、规格和本开发计划同步。
- [x] 实现统计投影、evidence locator manifest 和有界语义批次。
- [x] 实现 `AnalyticsOperation` 后台生命周期、取消、持久恢复和一次报告 repair。
- [x] 实现帮助菜单直达入口，以及原生统计页面内的 Agent 选择和启动操作。
- [x] 完成核心接口、前端生产构建、真实数据性能与浏览器页面验收。
- [x] 报告契约升级为 `2.1.0`，Agent 输出收窄为分章节洞察，客户端组装确定性报告。
- [x] 增加最近 Workflow/AUTO 任务、质量、耗时/Token TOP10、节点统计、Agent 与显式 Skill 使用段落。
- [x] 累计执行耗时改用节点 attempt 的 `sessionElapsedSeconds`，包含重试；历史缺失按 0 统计并公开补零数量，Run 墙钟时间不再参与耗时。
- [x] 最近任务和累计执行耗时 TOP10 增加 Token 列，报告内所有 Token 统一使用 K/M 紧凑格式。
- [x] 完成 2026-08-18 日期筛选、SQLite 派生索引、章节导航和 Agent 洞察解耦的优化设计评审；实现仍待启动。
- [x] 2026-08-19 实现现有 SQLite 库内 8 表分析索引、逻辑视图、fingerprint 增量同步和单调 `indexRevision`。
- [x] 拆分 `sync_personal_analytics` 与 `query_personal_analytics_report(range)`，日期切换只读 SQLite 聚合。
- [x] 实现全部/今天/近 7 天/近 30 天/自定义日期、章节导航和 `IntersectionObserver` 当前区。
- [x] 拆分独立 Agent 洞察生命周期，洞察绑定 range/schema/indexRevision/agentType 并按章节展示。
- [x] 2026-08-19 报告契约升级为 `2.2.0`：收窄章节目录、任务摘要携带稳定会话导航身份，最近任务与两类 TOP10 可跳转，Token TOP10 同步展示累计执行耗时。
- [x] 2026-08-20 索引 schema 升级为 `2`：node、snapshot、usage 按同一 attempt 合并来源贡献，usage turn 去重，usage-only 记录不再伪造节点重试或执行次数。
- [x] 2026-08-20 完成数据一致性与竞态加固：报告同 SQLite snapshot 读取、Agent 输出版本校验和前端报告请求 generation 防迟到覆盖。
- [x] 2026-08-20 索引 schema 升级为 `3`：counter 保存自身活动时间并按事件时间筛选，修复同一任务跨日期串数；历史起止改为显式 min/max。
- [x] 2026-08-20 两套分析 runtime 改为 durable-first 原子提交；范围报告按完整 range 身份展示，查询失败不再把旧范围报告伪装为新范围结果。
- [x] 2026-08-20 复审修复：timeline 按 `itemId + 最高 revision` 物化计数；会话按 canonical conversation 来源统计；语义 eligible 与 sampled 分离并执行 120 项/72,000 字符双上限；`Cancelling` 禁止回写 `Completed` / `Failed`。
- [x] 2026-08-20 索引 schema 升级为 `4`：Direct `run.json` 归一为 `direct-session`，每次回复改由 attempt usage journal 的 `promptStarted/promptCompleted + turn_id` 统计；删除 `turn.json`、根目录 usage、无 `kind` usage 和 baseline totals 的旧事实路径，根目录 usage 同时降为非 eligible source。
- [x] 2026-08-20 收紧页面刷新边界：进入页面最多自动同步一次，日期范围切换只查询 SQLite；终局可靠性只在专属章节展示，不再与使用概览重复。
- [x] 2026-08-21 索引 schema 升级为 `5`：持久化 node outcome 与 workflow invocation child run locator；AUTO child run 不再进入 outer 可靠性、终局汇总和最近任务状态，“重试后恢复”改由恢复后的 canonical outcome 计算。
- [x] 2026-08-21 索引 schema 升级为 `6`：AUTO dynamic leaf 的 Node/Snapshot/Usage 统一归一到 leaf 权威目录，按 `DynamicNodeState` 字段解析身份；并行 sibling leaf 不再合并为重试，已有 schema 5 派生索引一次性重建以清除历史拆分行。
- [x] 2026-08-22 索引 schema 升级为 `7`：SQLite 洞察表收窄为纯 completed cache；新增专用 `AgentInsightOperation` durable lifecycle、跨 operation generation 和 cache commit 崩溃恢复。
- [x] 2026-08-22 修复范围任务活动口径：`lastActivityAt` 只聚合范围内 conversation/run/attempt/counter，范围外最新 run 仅用于状态与导航兜底。
- [x] 2026-08-21 调整报告信息架构：终局可靠性固定紧邻使用概览，章节导航与内容分区保持“使用概览 → 终局可靠性 → 最近任务”同序。
- [x] 2026-08-31 个人分析标题操作区复用 composer 模型选择栏；`modelId` 贯穿报告查询、洞察 operation、ACP model override 与 completed cache identity。增量同步入口改名“刷新”并使用独立刷新图标，删除输入区上方的 “Agent” 标签。
- [x] 2026-08-31 修复模型选择上线后的交互与执行缺口：标题操作区改为容器换行；Help Tooltip 在菜单导航后显式关闭并抑制焦点回开；使用版本化偏好记忆最近 Agent 与每个 Agent 的有效模型；standalone 洞察 attempt 写入 `acp.storage.json` 后再进入通用 ACP runtime，消除 `acp.attempt-state-missing`，同时保留损坏工作流的严格拒绝。
- [x] 2026-09-01 完成本轮接口验收：个人分析/标题栏 Web 37 项、核心个人分析 21 项通过且 2 项真实历史基线按需忽略、桌面个人分析 16 项测试及 TypeScript、production build 通过。
- [x] 2026-09-01 补齐思考强度链路：复用 `AcpModelThoughtSelects` 的 `thought_level` 能力，按 Agent 记忆 Provider 原始 option ID/value，冻结到洞察 operation，并用于 analysis/repair 的 ACP `config_options`；索引 schema 升级为 `9`，completed cache 按思考强度隔离。analysis/repair attempt 继续使用 Hidden prompt，不进入用户会话或消息流。
- [x] 2026-09-01 修复个人分析首次进入的状态投影与查询时序：同步期间不并发查询日期报告，完成后自动恢复洞察入口；历史 failed/cancelled operation 不再伪装为当前错误，当前 active operation 仍可恢复；Agent 执行 identity 变化后清理旧提交错误。Help 导航关闭菜单时阻止焦点回到触发器，并由目标页面接管焦点。
- [x] 2026-09-01 完成本轮 live deep-link 视觉复验：Codex 内置浏览器未提供可用实例，改用真实 Chromium deep link 进入 `/chat/personal-analytics`；验证思考强度菜单、按 Agent 持久化与 reload 恢复，并确认 720x900 下页面无横向溢出、重叠或裁切。测试页面、浏览器会话与开发服务均已清理。
- [ ] EXE 原生窗口专项验收；本次功能没有仅 EXE 可见的交互，已按规则使用内置浏览器完成 WebView 等价页面验收。
- [x] 2026-08-19 完成后端接口测试、前端页面测试、类型检查、主题构建、生产构建与浏览器 1280px/720px、浅色/深色验证；首次全量索引 `19.330s` 未达到历史 `6.032s` 门槛，详见第 9 节。

本计划只覆盖个人数据分析专用能力；通用 `SkillSource::BuiltIn` 平台不在范围内。

## 2. 现有能力与组件复用

不新增依赖，优先复用：

- MiniJinja strict 模板：渲染所有固定提示词。
- serde/schemars/jsonschema：DTO、schema 生成和报告校验。
- 现有 storage/runtime/domain/ACP parser：读取 canonical 数据并原子保存快照。
- 现有 Agent registry、doctor capability 和 ACP runtime：执行专用 analysis/repair attempt。
- shadcn/ui、Tailwind、prompt-kit 既有交互组件和 Recharts 图表组件。

该领域的指标语义、证据边界和 repair 协议是产品特有规则，没有可直接替代的开源 Skill；使用固定 prompt + JSON Schema 是成熟的 structured-output 实践。

## 3. 数据基线

对 `C:\Users\user\.maling\projects` 的 2026-08-17 只读盘点：

| 项目 | 数量/大小 |
| --- | ---: |
| 项目目录 | 4 |
| 文件总数 | 3334 |
| 总大小 | 374,080,616 bytes |
| JSONL | 924 / 360,459,085 bytes |
| JSON | 2041 / 6,590,815 bytes |
| `acp.raw.jsonl` | 238 / 253,540,256 bytes |
| `acp.timeline.jsonl` | 235 / 93,439,596 bytes |
| `node.json` | 263 |
| `run.json` | 101 |
| `conversation.json` | 72 |
| `turn.json` | 仅发现 2 份旧格式文件；schema 4 起明确不索引 |

72 个 conversation 的模式样本为 AUTO 27、Direct 26、Workflow 19；101 个 run 的状态样本为 completed 79、paused 21、running 1。数据会持续增长，不能使用全文件内存加载、无界 Agent 上下文或每次物理全量重扫。

## 4. 实现顺序

### 阶段 A：数据与 schema

1. 定义 `AnalyticsOperation`、`AnalyticsProjection`、`ContentManifest`、`SemanticBatchManifest`、`AnalyticsReport` 和 `MetricDefinition`。
2. 明确每个字段的 canonical 来源、缺失语义、schema version 和迁移策略。
3. 固化三个 metric ID：
   - `direct.reply_completion_rate`
   - `workflow.run_terminal_success_rate`
   - `auto.outer_run_terminal_success_rate`
4. 定义结构化错误，仅返回 `code + params`。

### 阶段 B：读取与投影接口

1. 从配置化 sasuke home 解析 projects root，前端不提交绝对路径。
2. canonicalize root 和候选路径，拒绝越界、symlink/reparse point 和排除源。
3. 按 project/task/run/attempt 流式解析 canonical 文件，局部坏数据写入 coverage。
4. 默认跳过 `acp.raw.jsonl`、doctor/、诊断、DB/WAL、ZIP、PID、class 和二进制。
5. 生成确定性全历史投影、evidence locator 清单和内联有界语义批次；客户端不按 Agent 返回的 locator 回读原文件，prompt 约束 Agent 不主动扫描原始目录。通用 ACP/provider 已有文件能力不做技术隔离，按“永不修复的风险接受项”处理。
6. 使用 project/task 完整 locator 关联 task、conversation、run、node 和 usage；标题只用于展示。
7. 最近任务过滤 Direct；Workflow 使用 run 终局，AUTO 只使用 outer run 终局。
8. 使用完整 run/attempt locator 读取 `acp.snapshot.json.timing.sessionElapsedSeconds`；任务和终局 Run 累计全部节点 attempt，AUTO 并行节点求和表示累计 Agent 执行时间。
9. Direct 的 `run.json` 表示会话容器；回复完成率使用 attempt usage journal 中按 `turn_id` 去重的 started/completed 事实，不使用旧 `turn.json`。

### 阶段 C：后台 operation 与 Agent

1. 实现 `start_personal_analytics({ agentType })`、`get_personal_analytics()`、`cancel_personal_analytics(...)`。
2. 同一用户只允许一个 active operation，返回稳定 operationId 和单调 revision。
3. 复用现有后台 Agent run，使用本计划已落地的中英文固定 prompt。
4. Agent 只输出 `PersonalAnalyticsNarrative`；客户端校验并与确定性投影组装最终报告。失败时 repair 一次，再失败返回 `analytics.report-invalid`。
5. 新执行失败时保留上一次成功报告。
6. 附件投影遵循 runtime 配置的有界策略；洞察 JSON 只从最新有界 ACP 消息按稳定身份契约提取。

### 阶段 D：原生页面

1. 在帮助菜单新增与用户反馈同级入口。
2. 帮助菜单直接导航到独立页面，在标题操作区使用 shadcn `Select` 放置 Agent 选择，并复用 composer `AcpModelThoughtSelects` 放置模型与思考强度组合选择；增量同步使用独立“刷新”按钮。操作区按容器宽度换行，不能以固定最大宽度假设 Agent/模型/思考强度/按钮总长。
3. Agent、模型与思考强度使用 schema 2 版本化偏好按 Agent 成组保存；恢复时校验当前可用 Agent、doctor model capability 与 `category=thought_level` 的原始 option ID/value，失效身份回退到可用 Agent 或 Agent 默认值。accepted 后在当前页面展示 scanning/analyzing/validating/terminal 状态；洞察运行中锁定三类选择并禁止重复启动。
4. 使用聚合 DTO 和原生页面组件渲染使用概览、最近任务、质量、效率、Token 消耗、上下文与技能使用，末尾展示数据覆盖；覆盖字段名称通过 shadcn Tooltip 提供精简含义。最近任务和耗时 TOP10 同时展示累计执行耗时与 Token，所有 Token 使用 K/M。
5. 实现取消、失败恢复、保留旧报告的重新生成、窄窗口和浅色/深色主题。

## 5. 固定提示词交付

已新增以下镜像模板：

- `src/prompts/{zh-CN,en}/personal-analytics/system.md`
- `src/prompts/{zh-CN,en}/personal-analytics/user.md`
- `src/prompts/{zh-CN,en}/personal-analytics/repair_system.md`
- `src/prompts/{zh-CN,en}/personal-analytics/repair_user.md`

分析 prompt 只允许 Agent 使用投影、内容授权清单和语义批次清单，不允许直接扫描 `.maling/projects`；客户端也只主动附加这三个有界资源。该约束不隔离通用 ACP/provider 已有的文件系统能力，相关风险按“永不修复的风险接受项”处理。确定性统计覆盖全部历史，Agent 只输出按 `quality`、`efficiency`、`token-usage`、`context-and-skills` 分区的洞察。repair 只修结构，不重算、复述确定性统计或新增事实。

## 6. 测试与验收计划

### 已固化

- [x] 八个模板在 MiniJinja strict context 下成功渲染。
- [x] 缺少执行变量时严格渲染失败。
- [x] 中英文 system prompt 同时包含三个准确指标、evidence locator、sampleCount、confidence 和 raw frame 排除规则。
- [x] 中英文 repair prompt 禁止重新分析、猜测和新增洞察。

### runtime 接口测试

- [x] Direct/Workflow/AUTO 使用独立统计单位，raw frame 不参与指标。
- [x] 最近任务只包含 Workflow/AUTO，AUTO child workflow 不重复进入 outer run 口径。
- [x] 节点 snapshot `sessionElapsedSeconds` 是累计执行耗时唯一事实源；完整 locator 关联后汇总全部 attempt（含重试），缺失按 0 进入样本和平均值并记录补零数量。
- [x] Run 墙钟跨度不参与累计执行耗时；测试以跨数十天 Run 和 60 秒 session timing 固定该契约，AUTO 并行节点按累计 Agent 执行时间求和。
- [x] task/run/node/usage 使用完整 locator 关联，TOP10 和节点聚合保持有界。
- [x] 失败、取消、未达终局、running 和 unknown 保留在 coverage/终局分类。
- [x] 单文件损坏局部隔离，单项语义文本与总批次有明确上限。
- [x] canonical root、相对 locator 和 symlink/reparse point 不突破读取边界。
- [x] active operation 冲突、取消幂等、单调 revision 与终态防迟到覆盖已实现。
- [x] schema invalid 只 repair 一次；最新报告独立持久化。跨文件提交窗口可能被失败执行污染，按“永不修复的风险接受项”处理。

### 前端与客户端验收

- [x] accepted 后立即返回 queued 快照，扫描运行于 blocking pool，页面订阅 operation update。
- [x] Agent 不可用、取消、失败、完成、重启中断恢复和重新生成均有结构化状态。
- [x] 原生页面在正常/720px、浅色/深色主题无重叠和横向溢出。
- [x] 使用 `/chat/personal-analytics` deep link 复验页面内 Agent 选择和启动；Help 菜单无需 Dialog 直接进入页面，1280px/720px 与浅色/深色均无重叠或横向溢出。
- [x] 页面 DOM 测试覆盖无可用 Agent、active operation、防重复提交、accepted 快照合并和长 Agent 名称收缩约束。
- [x] 页面 DOM 测试覆盖六个主要报告段落、Workflow/AUTO 最近任务和无显式 Skill 调用状态。
- [x] 页面 DOM 测试覆盖最近任务与耗时 TOP10 的 Token 列，以及 0、百位、千位 Token 的 K/M 格式。

## 7. 性能影响评审

- 算法和 I/O：每次 O(total eligible canonical bytes) 流式扫描；始终分析全部历史，不维护增量缓存。节点 snapshot 已在既有扫描中读取，本次耗时关联不增加第二次目录扫描。
- 内存：JSONL 逐行解析、有界 channel 和语义批次；不保留完整 timeline/附件集合。
- 报告：最近任务和任务排行固定 TOP10，节点/工具/Agent/Skill 固定 TOP12，不向 React 传递全量历史明细。
- 调用频率：用户主动触发；active operation 去重，避免并发重复扫描。
- UI：只订阅当前 operation 和聚合 DTO；Agent 注册表复用 App 已有权威状态，不为页面重复请求或建立缓存。
- 存储：先复用现有数据库，不建立第二个索引或无界缓存。

2026-08-17 release 实测：`3334` 文件、`374,130,152` bytes，投影耗时 `6.032s`，峰值工作集 `8.2 MiB`，解析 `1509`、跳过 `1822`、语义样本 `82`。该结果低于 10 秒/32 MiB 的 MVP 验收目标，因此不增加数据库索引、并行扫描或无界缓存。

## 8. 过度设计复核

- 必要新增：一个跨扫描、Agent attempt 和报告校验的 `AnalyticsOperation`，因为现有 conversation run 无法表达该专用生命周期。
- 明确不新增：通用 BuiltIn Skill 平台、第二套 Agent runtime、第二个数据库、报表历史中心、服务端同步、金额模型和 HTML viewer。
- 页面内选择与启动复用既有 shadcn 控件和 App Agent registry；删除无独立确认价值的 Dialog，不新增第二套表单或导航状态。
- 在真实性能基线证明有需要前，不增加额外队列、并行扫描或缓存层。

## 9. 2026-08-18 优化实施计划（实施中）

本节记录已批准设计的实施进度。E-H 代码主体已落地，I 阶段测试、性能与页面验收完成后更新最终验收结果；不做兼容旧入口的旁路。

### 阶段 E：SQLite 分析索引

1. 在现有 `sasuke.sqlite` 中增加 8 张 `analytics_*` 物理表、必要索引、迁移记录和逻辑视图，由独立 `AnalyticsIndex` 模块管理。
2. 定义源文件 fingerprint、解析状态、稳定 locator 和 `index_revision`；所有派生写入使用事务和幂等 upsert。
3. 项目字段冗余在任务表、Token 汇总归属 attempt、事件计数进入带 CHECK 约束且保存自身 `activityEpoch` 的通用 counter 表；timeline 按单条事件时间写入，项目、usage、事件计数和洞察明细只用视图重建，不建立第二份物理事实。
4. schema 9 延续 schema 8 的 `analytics_attempts` 来源与 identity 口径及纯 completed `analytics_insight_cache`，在已有可空 `modelId` 基础上增加可空 `thoughtLevelOptionId + thoughtLevelValue`，完整隔离不同模型与思考强度。普通节点使用物理 attempt 目录，AUTO dynamic leaf 使用 `dynamic/nodes/<leaf-id>` 权威目录，固定的 ACP attempt 子目录不单独形成统计身份。Dynamic Node 按 `id/provider/outcome` 解析，sibling leaf 按各自 locator 分组。Node 贡献身份、outcome 和 workflow invocation 的完整 child run locator，Snapshot 贡献耗时，attempt usage 的 `promptCompleted` 贡献 Token 与去重 prompt 数。`promptStarted/promptCompleted + turn_id` 先投影为模式中立的 `prompt-status` counter，报告查询再按 canonical `runMode=direct` 生成 Direct reply 指标；usage-only 行不计入执行 attempt。旧 `turn.json`、task 根目录 usage、无 `kind` usage 和 baseline totals 不进入事实表。
5. 首次执行全量扫描；后续枚举 canonical 文件，仅重新解析新增或变化文件，并删除消失文件的派生事实。
6. 单文件损坏、未知版本或解析失败记录结构化状态，不阻断其他文件；索引版本不匹配支持整体重建。
7. 保留 `.maling/projects` 文件作为唯一权威事实源，SQLite 始终可删除重建。

### 阶段 F：日期范围报告接口

1. 将确定性统计入口拆分为 `sync_personal_analytics` 和 `query_personal_analytics_report(range)`。
2. 报告查询只读取 SQLite 聚合，不重新解析历史文件；state、事实聚合和 coverage 在同一读事务中取得，并携带该 snapshot 的 `indexRevision`、source coverage 和 range。
3. 使用本地时区包含起止日的自然日范围；Workflow run、AUTO outer run、AUTO child run 和 Direct session 保存在 `analytics_runs` 并按 `unitType` 区分，AUTO child run 由 canonical `childRunId` 转换出的完整 locator 识别且不进入 outer 可靠性、终局汇总或最近任务状态。Direct reply 状态与其他 counter 均按自身活动时间筛选，attempt 不跨日期拆分；历史起止对范围内 run 执行 min/max。任务 `lastActivityAt` 批量聚合范围内 conversation/run/attempt/counter；全局 task 活动和范围外最新导航 run 不得参与当前范围时间与排序。
4. Timeline counter 按稳定 `itemId` 的最高 revision 生成紧凑计数投影，同一逻辑事件的 processing/completed patch 只计一次；`conversationCount` 按范围内任务存在 `conversationSourcePath` 统计，不绑定任务模式。
5. 复用三个准确指标名称、累计执行耗时口径、Token 口径和 TOP10/TOP12 有界输出。
6. 索引同步失败保留旧报告和结构化错误，不用半更新索引生成新报告。

### 阶段 G：原生页面导航与筛选

1. 桌面宽度实现左侧章节导航和右侧报告内容；窄宽度切换为横向吸顶导航。
2. 使用 `IntersectionObserver` 标记当前章节，点击导航滚动到对应分区，不注册高频 scroll handler。
3. 增加全部、今天、近 7 天、近 30 天和自定义日期范围；自定义范围使用原生日期输入。
4. 切换范围后请求 SQLite 聚合报告，不在 React 端计算全历史统计。
5. 范围查询、同步完成刷新和洞察完成刷新共享报告请求 generation；旧请求返回后不得覆盖新请求报告。
6. 页面只展示与当前 `start + end` 完全相同的报告；新范围查询失败时显示结构化错误，不展示旧范围报告，同范围刷新失败可保留已有报告。
7. 任务摘要返回稳定 `projectId + taskId + latestRunId`；最近任务、耗时 TOP10 和 Token TOP10 复用该身份进入对应会话，Token TOP10 同时展示累计执行耗时。Run 在索引层按任务模式归一为 `workflow-run`、`auto-outer-run` 或 `direct-session`，因此 Direct 可进入两类排行但仍不进入最近任务；旧 `2.1.0` 缓存报告缺少导航身份时仅降级为不可点击。

### 阶段 H：Agent 洞察独立生命周期

1. “开始分析”改为只同步索引并生成确定性报告，删除开始分析即调用 Agent 的旧路径。
2. `start_personal_analytics_insights(agentType, modelId, thoughtLevelOptionId, thoughtLevelValue, range)` 复用现有 ACP runtime 和中英文固定提示词；`modelId=null` 使用 Agent 默认模型。思考强度两字段必须同时为空或同时有值，并通过最新 doctor capability 的 `category=thought_level` 校验，不能硬编码 Provider ID。显式值作为 analysis/repair 共用的 model override 与 `config_options`。
3. 洞察接受前冻结 `operationId + generation + range + schemaVersion + indexRevision + agentType + modelId + thoughtLevelOptionId + thoughtLevelValue` 到专用 `AgentInsightOperation`；durable `insight-state.json` 是唯一 lifecycle 权威，SQLite `analytics_insight_cache` 只保存通过校验的 completed JSON payload，并按同一冻结身份隔离不同模型与思考强度。
4. analysis/repair attempt 在调用通用 ACP runtime 前，必须先用共享初始化器写入当前版本 `acp.storage.json`，再通过共享 admission 在本 attempt 的 `acp.snapshot.json` claim `turnId + operationId + revision`；两类 prompt 使用 `PromptVisibility::Hidden + hiddenReason=personalAnalytics`，只保存在 analytics operation 目录，不进入会话列表或消息流，用户只看到 operation 状态和最终洞察。standalone 元数据不得替代或伪造 workflow `node.json`，缺失两类显式 owner 的 attempt 继续拒绝。该 provider-turn owner 只用于取消、终态结算和迟到写 CAS，不得成为 `AgentInsightOperation` 的平行权威。
5. Agent 输出的 `PersonalAnalyticsNarrative.schemaVersion` 必须与当前报告契约完全一致；Schema 或版本不合法仍只 repair 一次，repair 仅修结构，不重新分析或新增事实。
6. 洞察失败、取消或过期不影响确定性报告且不写 cache；旧洞察在 range 或 index revision 变化后不可复用。缓存命中仍推进当前 durable operation，但不创建 SQLite lifecycle 行。
7. 确定性报告与 Agent batch 复用同一读事务中的语义采样；eligible 使用完整范围计数，sampled/batch 同时受 120 项和 72,000 字符上限约束。
8. 洞察状态机限定 `Queued -> Analyzing -> ValidatingReport -> Completed`，repair 可回到 Analyzing；进入 `Cancelling` 后只能进入 `Cancelled`。取消与 completed cache commit 在同一互斥区串行化；cache 先提交后若 JSON 写入或进程中断，重启按冻结身份恢复 Completed，否则恢复 interrupted Failed。
9. 前端首次 snapshot、live event、start response 和 cancel response 统一进入 merge；不同 operation 先比较 generation，同一 operation 再比较 revision，终态不得被迟到响应回退。

### 阶段 I：测试与性能验收（代码与验收完成，性能遗留明确）

后端接口测试必须覆盖：

- [x] 首次全量索引和不同日期范围查询的任务、run、Token、耗时、coverage 一致。
- [x] 新增、修改、删除、损坏、未知版本文件只影响对应派生事实；SQLite source 状态、事实保持和视图聚合已由接口测试覆盖。
- [x] `index_revision` 单调；取消、关闭重开索引和重启恢复不产生半写入索引，无变化/变化 revision 由接口测试固化。
- [x] 日期边界按活动时间归属，attempt 不伪拆分。
- [x] 范围任务 `lastActivityAt` 不读取范围外 task/conversation 或导航 run 活动；范围内 attempt 活动可成为任务最新活动。
- [x] 同一任务跨日期 timeline counter 按各事件自身时间隔离，单日查询不串入其他日期计数。
- [x] 同一 timeline 逻辑事件的多个 revision 只按最高 revision 贡献一次 counter。
- [x] 历史 earliest/latest 对乱序 run 输入稳定执行 min/max。
- [x] 项目、usage、事件计数和洞察明细视图与物理表聚合一致，视图不保存第二份事实。
- [x] attempt 级 Token 汇总不丢失或重复计数，counter 的 `kind` 超出白名单时写入失败。
- [x] node/snapshot/usage 合并到同一 attempt 后，Token、耗时和身份来源可独立增量修改、删除；usage turn 去重，usage-only 不伪造节点或执行次数。
- [x] node outcome 经 SQLite 持久化与恢复后，失败 attempt 后出现成功 attempt 会计入“重试后恢复”。
- [x] AUTO workflow invocation 的 child run 以完整 locator 标记为 `auto-child-run`，不放大 outer 成功率且不覆盖最近任务的 outer 状态与导航身份。
- [x] AUTO dynamic leaf 的 node/snapshot/usage 合并为一条 canonical attempt；Node 身份、Agent、outcome、耗时和 Token 同行可追溯，并行 sibling leaf 不增加 retry，普通节点真实多 attempt 仍增加 retry。
- [x] 报告 state 与事实读取使用同一 SQLite snapshot，index revision 与任务事实不发生跨版本拼接。
- [x] 索引 schema 版本由单一常量写入；同步后关闭并重新打开数据库不会误判迁移并清空派生事实。
- [x] state/insight-state 原子写失败时不提交内存 revision/status，也不提前触发取消信号。
- [x] 生成确定性报告不调用 Agent。
- [x] 洞察成功、失败、取消、repair、缓存命中和过期失效互不污染统计报告；SQLite 不保存 processing/failed/cancelled，缓存查询不创建 lifecycle 行，完整 Agent repair 外部调用仍由既有生命周期测试覆盖。
- [x] Workflow/AUTO/Direct 的 canonical conversation 来源统一计入 `conversationCount`，不再只统计 Direct。
- [x] 当前 Direct 一个 run 内多个 prompt 按 `turn_id` 分别统计；completion-only 计入已开始且完成，started-only 计入 unknown，旧 `turn.json` 与旧 usage 不贡献指标；Direct run 不污染 Workflow 分母且可用于排行导航。
- [x] Direct started-only counter 按自身日期选中范围任务；跨日 Token attempt 在当期无 run 时使用最新 canonical run 完成排行导航，且不改变当期可靠性、run 数或最近任务。
- [x] 语义 coverage 返回完整 eligible 数；Agent batch 与 sampled 一致，并同时满足 120 项和 72,000 字符上限。
- [x] 语义文档有界读取按合法 UTF-8 字符边界截断；字节窗口末尾落在多字节字符中间不再误报 corrupt，上限内非法 UTF-8 仍隔离。
- [x] 主分析与洞察 runtime 均拒绝 `Cancelling -> Completed`，拒绝时 revision 不前进，随后仍可进入 `Cancelled`。
- [x] 洞察 generation 跨 operation 单调；cache commit 后重启恢复 Completed，commit 前中断恢复 Failed，已接受取消不写 completed cache。
- [x] 思考强度按 doctor capability 的 category 解析 Provider 原始 option ID，失效 ID/value 被结构化拒绝；analysis 与 repair 共享冻结 override，不同思考强度不复用 completed cache。

前端测试必须覆盖：

- [x] 章节导航渲染、点击滚动和当前章节状态；DOM 测试覆盖点击滚动与 `IntersectionObserver` 当前区。
- [x] 快捷日期与自定义日期的状态、禁用与错误提示。
- [x] 日期切换只查询报告，不触发 Agent。
- [x] Agent 洞察按钮的 loading、禁用、失败、取消和章节内展示。
- [x] 旧范围报告晚于新范围报告返回时不覆盖最新结果。
- [x] 洞察 completed/cancelled event 先到时，迟到的 queued/cancelling command response 不回退终态；旧 generation 事件不覆盖新 operation。
- [x] 新范围查询失败时旧范围报告不展示在当前选择下，并显示可恢复的结构化错误。
- [x] 数据覆盖字段名称的悬浮与键盘焦点说明，缺失耗时的说明保留历史版本原因。
- [x] 最近任务、耗时 TOP10 和 Token TOP10 使用稳定身份跳转；旧缓存任务缺少导航身份时保持展示且不触发跳转。
- [x] 1280px、720px、浅色/深色无重叠或页面横向溢出；720px 下章节导航允许自身内部横向滚动。
- [x] Agent、模型与思考强度按 Agent 记忆并拒绝失效 capability；组合选择在生成洞察和范围缓存查询时携带同一完整身份。

性能目标：

- [x] 首次全量索引不劣于 2026-08-17 的 `6.032s` 真实基线；2026-08-30 对 9,639 个 canonical 候选、约 298 MB 真实数据的 release 同轮实测为 `5.786s`。
- [x] 增量同步只读取变化文件；隔离测试固定无变化时 `reparsed_files=0` 且 revision 不变，2026-08-30 真实目录测试期间有 `1` 个文件变化，同轮为 `1.677s`，并固化变化时 revision 单调前进。
- [x] 日期切换聚合查询达到百毫秒级目标，2026-08-30 当前真实数据范围查询约 `121ms`。
- [x] 索引、语义样本、SQLite 洞察结果和页面 DTO 保持有界，不向 React 传递全历史明细；Agent 洞察 operation 磁盘目录不回收，按“永不修复的风险接受项”处理。

### 永不修复的风险接受项

- [x] **未知 schema 版本仍可能产出业务事实：永不修复。** 保持当前解析与 coverage warning 行为，接受未来字段语义变化污染统计的风险。
- [x] **Agent evidence locator 不校验 `authorizedLocators` 成员关系：永不修复。** 保持安全字符串形状校验，接受 Agent 洞察引用本次未提供证据的来源完整性风险；locator 不因此获得原文件读取权限。
- [x] **Agent 文件系统访问不建立技术隔离：永不修复。** 继续复用通用 ACP runtime 和 provider 既有能力，不新增 OS sandbox、低权限账户、虚拟文件系统、工具 denylist 或独立隔离进程。客户端仍只主动附加三个有界投影资源，prompt 仍禁止扫描 `.maling/projects`，客户端也不得按输出 locator 代为回读文件；但接受具备文件工具或工作区访问能力的 Agent 可能自行读取授权清单外本地文件，造成敏感数据暴露或洞察使用批次外事实。主动扩大附件、回读 locator、提升洞察 provider 权限或将批次外输出纳入确定性统计均超出接受边界，仍须修复。
- [x] **attempt 活动时间在来源删除后不回退：永不修复。** 保持历史最大 `activityEpoch`，接受 Token 与耗时可能继续归属原较晚日期的风险。
- [x] **conversation 删除或损坏后不刷新既有 run 类型：永不修复。** 保留最后一次有效 conversation 推导的 `unitType`，接受其与当前 unknown mode 不一致的风险。
- [x] **耗时补零 warning 翻译 key 不一致：永不修复。** 保持后端 `analytics.active-duration-zero-filled` 与前端 `analytics.duration-zero-filled` 的现状，接受 warning 触发时显示原始错误码的风险。
- [x] **timeline JSONL 单行损坏或追加写读取竞态：永不修复。** 保持整个 source 标记 corrupt 并删除旧 counters，接受对应工具、权限、Skill 和事件统计暂时或长期缺失的风险。
- [x] **确定性报告与 operation 终态不是跨文件原子提交：永不修复。** 保持 `latest-report.json` 与 `state.json` 分步写入，接受取消、持久化失败或进程退出发生在提交窗口时，失败 operation 的报告可能在重启后成为 latest report 的风险。
- [x] **Agent 洞察 operation 目录不随 SQLite retention 回收：永不修复。** 保持数据库最多 64 条 completed 洞察缓存的清理策略，不清理对应 operation 目录，接受磁盘占用和本地敏感分析内容持续累积的风险。
- [x] **全损坏 usage journal 在 SQLite 索引中仍标记为 parsed：永不修复。** 保持全损坏非空 journal 产生 parsed 空事实并替换旧计数的行为，接受 coverage 不显示 corrupt 且相关 Token、prompt 统计归零的风险。

### 2026-08-30 扫描性能收口记录

- 根因：扫描器递归进入 `runtime/worktrees/` checkout，并在 canonical 文件名判断前遍历普通源码/附件、逐文件 canonicalize；首次写入又对每个 source 重复执行全表 orphan 清理和同 task run-type 刷新。该问题属于正确 SQLite 派生索引设计下的扫描域与批次实现不完整，不新增第二事实源或目录 watermark。
- 实现：`WalkDir(follow_links=false)` 直接剪枝 worktree/诊断/构建目录，候选文件名过滤前置到 metadata/fingerprint 之前；相对 locator 由同一 canonical root 遍历生成并拒绝 symlink/reparse point。事务内按 source type 清理旧事实，批次末尾一次清理 orphan、按唯一 task 刷新 run type，重复 SQL 复用 rusqlite 有界 statement cache；旧索引中的 worktree/非候选 source 在下次同步按消失源删除。
- 聚焦接口回归：`sqlite_index_does_not_admit_managed_worktree_checkout_files` 先以 6 个入索引来源对预期 3 个失败，修复后固定 checkout shadow 与普通 `notes.txt` 不进入 `analytics_sources`；既有 `sqlite_index_syncs_reopens_incrementally_and_queries_date_ranges` 继续通过，覆盖新增、修改、删除、损坏、重开和无变化 revision。
- Release 真实基线：`C:\Users\unlik\.sasuke\projects` 全树约 188,985 文件/15.33 GB，受管 worktree 约 149,945 文件/7.96 GB；最终 9,639 个 canonical 候选约 298 MB，其中 timeline 约 274 MB。首次 `5.786s`，第二轮 `1.677s` 且只重解析运行期间变化的 1 个文件，范围查询 `121ms`；阶段为发现 `2.343s`、解析 `0.830s`、SQLite 写入 `2.611s`。230 runs、2033 attempts 与 Direct 1336/1103/233 计数保持可解释一致。
- 过度设计与性能评审：只使用既有 WalkDir/rusqlite、事务、blocking worker 和可重建索引；未新增表、字段、缓存平台、队列、线程池或并发写入。扫描复杂度从随 checkout/附件全树增长收敛为 O(canonical directory walk + candidate metadata + changed bytes)，SQLite orphan 清理由 O(sources × facts) 收敛为批次 O(sources + facts)，锁仍只覆盖单次原子事务。
- 最终合并验收：最新 main 上同一工作区的 CI 原命令 `cargo test --workspace --all-targets` 通过，其中 Rust lib `1069 passed / 1 ignored`，全部 integration target 与 desktop `557` 条通过；Web `250` 个文件、`1706` 条测试及生产构建通过，macOS installer、release checksum、README i18n 与 collaboration-skill 校验通过。Windows 本地用 `CARGO_PROFILE_TEST_DEBUG=0` 避免 MSVC PDB 上限，仅影响调试符号；扫描实现与上述 release 基线之间没有后续代码变更。
- 最新 main 合并后，personal analytics 的纯函数 output fixture 按新 `AcpPromptMessageOutput.source` 契约显式使用 `None`：该 fixture 没有 canonical timeline locator，只验证 JSON 选择，不伪造 branch/item identity，也不改变生产报告解析口径。

### 2026-09-01 思考强度与交互修复验证记录

- 首次进入 Bug 复现：新增最小 DOM 用例在修复前稳定得到 2 个失败；同步进行中仍调用 `queryPersonalAnalyticsReport` 1 次，且选择“个人数据分析”关闭 Help 菜单后 `document.activeElement` 仍为 Help 触发器，分别对应截图中的伪报告错误与绿色伪选中态。
- 首次进入修复回归：个人分析与 Help Tooltip 两个最小复现文件 25 项通过；扩大到个人分析 state/preferences、标题栏、ACP 选择器和 browser adapter 后共 7 个文件、69 项通过。覆盖同步期间零报告查询、历史失败洞察不展示、同步完成后保持原 Agent 自动启用洞察、目标页面接管焦点，以及 Help 导航不恢复触发器焦点。TypeScript 检查与 production build 通过，仅保留既有 dynamic import 和大 chunk warning。
- 首次进入浏览器验收：内置浏览器没有可用实例，按降级流程使用真实 Chromium 从 `/chat` 的 Help 菜单进入 `/chat/personal-analytics`。导航后 activeElement 为个人分析 `MAIN`，Help 为 `expanded=false` 且未聚焦，页面没有 role=alert、“无法开始分析”或“洞察生成失败”；普通 Esc 关闭菜单仍恢复 Help 焦点。720x900 下 `body.clientWidth = body.scrollWidth = 720`，4 个操作控件无相交。
- 前端回归：个人数据分析页面、偏好、标题栏帮助 Tooltip、browser mock 等聚焦测试共 37 项通过；覆盖 Agent/模型/思考强度恢复与提交、失效 capability 拒绝、帮助菜单导航后 Tooltip 关闭和浏览器预览 identity 保留。TypeScript 构建检查与 Vite production build 通过。
- Rust 核心回归：个人数据分析聚焦测试 21 项通过、2 项真实历史基线按需忽略；覆盖 Provider 原始思考强度 option ID 校验、analysis/repair 共用 ACP `config_options`，以及 high/low/未指定三类 completed cache identity 隔离。
- Desktop 回归：个人数据分析聚焦测试 16 项通过；覆盖思考强度结构化错误、冻结 operation identity、ACP model/config override 传播、Hidden analysis/repair attempt 和 standalone attempt admission。
- 浏览器验收：真实 Chromium deep link 进入 `/chat/personal-analytics`，组合菜单同时展示“模型”和“思考强度”，可选择 Default/Low/Medium/High/Max；选择 High 后本地 schema 2 偏好保存 Provider option ID/value，reload 后恢复为“模型 不指定 · High”。点击“生成 Agent 洞察”没有浏览器错误；720x900 下 `bodyClientWidth` 与 `bodyScrollWidth` 均为 720，选择器与刷新按钮保持对齐，生成按钮按容器宽度换行，无重叠或裁切。验证后关闭浏览器会话并停止开发服务。
- 格式与补丁检查：`cargo fmt --all -- --check` 和 `git diff --check` 通过；仅保留仓库既有 Rust dead-code warning 与工作区 LF/CRLF 提示。
- 过度设计评审：复用现有 `AcpModelThoughtSelects`、doctor capability、版本化本地偏好、ACP `config_options` 与洞察 cache，不新增通用配置框架、第二套选择器、状态机、队列或依赖。新增状态只表达本次执行必须冻结的思考强度 identity。
- 性能评审：每个 Agent 只增加常量大小的偏好字段，每次洞察只增加一个单项 `config_options` map 和两个可空 cache key 字段；没有新增网络请求、历史扫描、N+1、无界缓存、扩大 React 订阅或高频持久化。

### 2026-08-22 验证记录

- Rust 接口测试：`cargo test -p sasuke --test personal_analytics -- --nocapture` 通过，18 个常规测试通过、2 个真实历史基线按需忽略；新增用例固定日期范围内 task 活动只取范围内 task/run/attempt/counter 事实，并覆盖 completed 洞察 cache 不创建平行 lifecycle、identity 精确匹配和 64 条 retention。
- Rust 索引单元测试：`cargo test -p sasuke --lib personal_analytics::index -- --nocapture` 通过，6 个测试通过；覆盖单 SQLite snapshot 读取、乱序 min/max、schema 7 表结构和 insight cache 约束。
- Desktop 生命周期测试：`cargo test -p sasuke-desktop personal_analytics::tests -- --nocapture` 通过，13 个测试通过；覆盖状态转换、迟到终态拒绝、取消与 completed cache commit 竞态、持久化失败不推进内存状态，以及重启时按 durable commit marker 恢复 Completed/Failed。
- 编译与格式检查：`cargo check -p sasuke -p sasuke-desktop`、`cargo fmt --all -- --check` 通过；仅保留仓库既有 dead-code warning。
- 前端回归：个人数据分析页面、状态合并和 browser mock 共 3 个测试文件、37 项测试通过；`tsc -p web/tsconfig.build.json --noEmit`、主题构建和 Vite 生产构建通过，生产构建仅保留既有 dynamic import 与大 chunk warning。
- 浏览器验收：内置浏览器 deep link 进入 `/chat/personal-analytics`，验证 Agent 洞察完成后按钮恢复、取消入口消失；720x900 窄窗口无页面横向溢出和控件重叠，切换“今天”后无错误。验证后已关闭测试页、恢复 viewport 并停止临时服务。
- 补丁检查：`git diff --check` 通过，仅有工作区既有 LF/CRLF 转换提示。
- 性能评审：范围内 task 活动通过单个批量 CTE 汇总，复用既有 activity 和主键索引，不增加 N+1 或物理历史扫描；洞察 lifecycle 复用现有 JSON 原子替换、SQLite 事务、runtime mutex 和前端 merge，不增加依赖、队列或第二套数据库，completed cache 继续限制为 64 条。operation 目录增长仍按既有“永不修复的风险接受项”处理。
- 过度设计评审：专用 `AgentInsightOperation` 只承载同步 operation 无法表达的 Agent 状态、冻结 identity、generation/revision 和恢复不变量；删除 SQLite processing/failed/cancelled 平行生命周期，以 durable JSON 为唯一 lifecycle 权威、completed cache 为提交标记，没有复制 canonical 报告或引入额外 aggregate。

### 2026-08-21 验证记录

- Rust 接口测试：`cargo test --target-dir target/codex -p sasuke --test personal_analytics` 通过，17 个常规测试通过、2 个真实历史基线按需忽略；新增用例固定 AUTO dynamic leaf 的三来源合并、`id/provider/outcome` 解析、sibling 非重试、schema 5 到 6 重建，同时继续覆盖 AUTO outer/child 身份、真实普通节点重试恢复、Direct started-only 跨日、Token attempt 与 run 跨日排行导航，以及 UTF-8 多字节字符边界截断。
- Rust 索引单元测试：`cargo test --target-dir target/codex -p sasuke --lib personal_analytics::index` 通过，6 个测试通过。
- 桌面端检查：`cargo check --target-dir target/codex -p sasuke-desktop` 通过；仅保留仓库既有 dead-code warning。
- 前端顺序回归：个人数据分析页面测试 17 项通过，TypeScript 类型检查与 Vite 生产构建通过；DOM 契约固定章节导航和内容分区均按“使用概览 → 终局可靠性 → 最近任务”排列。
- 浏览器验收：内置浏览器 deep link 进入 `/chat/personal-analytics`，在桌面和窄窗口确认终局可靠性紧邻使用概览、导航与内容同序，且无重叠或页面横向溢出；验证后关闭测试页并停止临时服务。
- 格式与补丁检查：`cargo fmt --all -- --check`、`git diff --check` 通过。
- 真实历史 schema 6 回归：显式执行 ignored SQLite 基线通过；全量临时索引 `16.471s`、无变化增量 `6.916s` 且重解析为 `0`、范围查询 `139ms`。修复后 attempt 为 `264`，与修复前 `341` 减去已证实的 `77` 组 dynamic Node/Snapshot 拆分一致；Direct started/completed/unknown 仍为 `32/30/2`。
- 性能评审：schema 5 原有两个可空 attempt 列与 `(taskLocator, childRunLocator)` 部分索引保持不变；schema 6 不增加表、列、索引、缓存或查询，只在每个相关来源上按有限路径层级做 O(depth) identity 归一化，并以一次性可重建索引清除旧拆分行。范围查询仍使用一次 CTE + 窗口函数批量取已选中任务的最新 canonical run，语义项仍最多读取 4,800 字节；不引入日期切换物理历史扫描、N+1、无界缓存、队列或更大锁范围。

### 2026-08-19 验证记录

- Rust 接口测试：`CARGO_TARGET_DIR=target/codex cargo test -p sasuke --test personal_analytics` 通过，8 个常规测试通过、2 个真实基线按需忽略。
- Rust 单元测试：`CARGO_TARGET_DIR=target/codex cargo test -p sasuke --lib personal_analytics::index` 被无关既有测试编译错误阻塞（`RunState` 缺少 `worktree`）；索引行为已由上述集成接口测试执行。
- 桌面接口检查：`CARGO_TARGET_DIR=target/codex cargo check -p sasuke-desktop` 通过。桌面 bin 测试编译被无关既有 `RunState` / `ConversationCreateInputVm` / `apply_workflow_interview_preference` 错误阻塞。
- 前端测试：`node_modules/.bin/vitest.cmd run --config web/vitest.config.ts web/tests/personal-analytics-page.test.tsx web/tests/personal-analytics-state.test.ts` 通过，17 个测试通过；包含章节导航、快捷/非法日期、洞察失败/独立取消和同范围低 `indexRevision` 报告防迟到覆盖。
- 类型检查：`node_modules/.bin/tsc.cmd -p web/tsconfig.build.json --noEmit` 通过。
- 主题与生产构建：`node theme-sdk/build.mjs` 通过；当前运行时无 `npm`，按 `web:build` 脚本等价执行 `tsc -p web/tsconfig.build.json --noEmit` 与 `vite build --config web/vite.config.ts`，均通过。
- 真实基线：`MALING_PROJECTS_ROOT=C:\Users\user\.maling\projects cargo test -p sasuke --test personal_analytics real_history -- --ignored --nocapture`；同轮结果为投影 `17.600s`、SQLite 全量 `19.330s`、增量 `9.758s`、范围查询 `64ms`。
- 浏览器验收：`/chat/personal-analytics` 在 1280px/720px、浅色/深色下验证八个章节导航、点击滚动、当前区状态、原生自定义日期、非法范围禁用与提示；无页面横向溢出，720px 下章节导航仅内部横向滚动。

### 2026-08-20 验证记录

- Rust 接口测试：`cargo test --target-dir target/codex -p sasuke --test personal_analytics -- --nocapture` 通过，13 个常规测试通过、2 个真实基线按需忽略；新增覆盖同一 Direct run 多 prompt、completion-only / started-only、旧 `turn.json` 与旧 usage 不进入事实、Direct 不污染 Workflow 分母且保留真实 run 导航，并继续覆盖同一 timeline 事件多 revision 去重、Workflow conversation 计数、完整语义 eligible 数及 72,000 字符 batch 上限、跨日期 counter 隔离、同快照报告、Usage-only 不伪造节点、洞察缓存命中生命周期和跨连接重开。
- Rust 单元测试：`cargo test -p sasuke --lib personal_analytics::index -- --nocapture` 通过，6 个测试通过；新增覆盖乱序 run 的历史 earliest/latest min/max。
- 桌面 runtime 测试：`cargo test --target-dir target/codex -p sasuke-desktop personal_analytics::tests -- --nocapture` 通过，11 个测试通过；除主分析与洞察状态原子写失败不前进外，新增覆盖两套 runtime 均拒绝 `Cancelling -> Completed`、拒绝时 revision 不前进且仍可进入 `Cancelled`。
- 前端测试与类型检查：个人分析两个测试文件共 22 个测试通过，连同 browser adapter 契约测试的聚焦验证共 33 个测试通过；`tsc -p web/tsconfig.build.json --noEmit` 通过。新增覆盖新范围查询失败时隐藏旧范围报告并展示结构化错误、范围切换不增加索引同步调用、终局可靠性仅在专属章节展示一次，以及 Web 预览报告保留请求范围身份。
- 主题与生产构建：`node theme-sdk/build.mjs` 与 `node_modules/.bin/vite.cmd build --config web/vite.config.ts` 通过；仅保留既有动态导入提示与大 chunk warning。
- 浏览器回归：内置浏览器 deep link 进入 `/chat/personal-analytics`；1280px 报告与 8 个章节正常且无页面横向溢出，切到“今天”后旧“全部”报告不再展示，状态保持“分析完成”且不出现“正在同步”，回到“全部”后恢复；终局可靠性标题及三类指标均只在专属章节出现一次。720px 页面 `scrollWidth` 等于 `clientWidth`，章节导航仅自身内部横向滚动，标题、范围控件、Agent 控件和报告内容不重叠。验证后已关闭 tab、复位 viewport 并停止临时服务。
- 新版 Direct 真实历史回归：`MALING_PROJECTS_ROOT=C:\Users\user\.maling\projects cargo test --target-dir target/codex -p sasuke --test personal_analytics real_history_sqlite_index_and_range_query_baseline -- --ignored --nocapture` 通过；schema 4 全量临时索引约 13.3 秒、无变更增量约 5.2 秒且重解析数为 0、范围查询约 51 毫秒。Direct 指标只读取当前 attempt usage journal；按 `turn_id` 去重后 started（start/completion 并集）32、completed 30、unknown 2，旧 `turn.json` 和旧 usage 不贡献指标。

### 优化方案评审结论

- 根因：日期筛选和重复分析暴露出缺少持久派生投影；统计与洞察耦合是生命周期设计缺陷，不是 UI 缺件。
- 复用：现有 SQLite/rusqlite、WAL、事务、Agent runtime、shadcn/ui 和原生日期输入满足需求，不新增第三方依赖。
- 必要性：SQLite 索引是本优化必要新增；8 张物理表加逻辑视图保留身份边界与查询能力，章节导航和洞察独立按钮是用户可见能力拆分。
- 边界：不新建第二个数据库、不做通用缓存平台、不保存无限历史报表、不引入服务端同步。
- 风险：索引迁移、fingerprint 成本、跨日期归属和洞察缓存一致性必须在接口测试中固化；SQLite 永远可重建，不能反向成为权威事实源。
