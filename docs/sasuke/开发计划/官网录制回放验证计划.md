# 官网录制回放验证计划

对应设计：`../产品设计文档/interaction/官网录制回放验证.md`。

- [x] 确认复用 rrweb 2.1.1、rrweb-player 2.1.1 和现有 React 页面。
- [x] 增加独立 demo/capture HTML、录制边界、手动录制、布局演示、回放和导出。
- [x] 增加事件上限、字节上限、初始快照、停止幂等接口测试。
- [x] 检查真实会话与右栏场景，形成可直接观看的录制结果。
- [x] 完成类型检查、生产构建及真实浏览器回放对比。
- [x] 记录响应式、样式、资源、数据量和当前限制。

命令：`npm run demo:rrweb` 启动开发验证；`npm run demo:rrweb:build` 构建专用验证站点；产物位于 `.codex-temp/rrweb-demo-dist`。

自评审：数据与采集时间有界；复用客户端现有响应式机制，不新增旁路布局状态。正式官网方案保持待定，先根据验证效果选择录屏、DOM 回放或展示组件。

## 验证结果（2026-09-05）

- `npm run web:test -- web/tests/rrweb-demo-recording.test.ts`：4/4 通过。
- `npm run demo:rrweb:build`：类型检查和生产构建通过；现有应用重型 chunk 提示保留。
- 生产构建由 Vite preview 在 `http://127.0.0.1:1438/rrweb-demo/` 提供。点击“录制布局演示”，约 14 秒后自动进入官方播放器；也可手动操作预览页面后停止并回放。
- 自动审计脚本验证两套主题的 3→2→1→2→3 栏位、宽度、文件内容、背景色、录制源释放及手机外层布局。
- 另验证手动中文输入与打开代码 Diff 回放，均通过。
- 录制事件数据约 0.8 MB/14 秒，仅代表本次有限样例，不外推到长会话或 Canvas 场景。
- 结果与截图位置见对应设计文档；本轮只证明浏览器版 React 界面的 DOM 回放，不代表 EXE/WebView2、原生系统界面和真实 Agent 执行已验证。
- 清理验证浏览器及开发测试服务，保留本次交付的生产预览服务供用户查看。
