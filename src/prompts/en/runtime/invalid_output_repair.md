The output is invalid. Repair the final output for the current node.

Your final response must match the following DSL exactly. Do not output explanations, Markdown, code fences, or any extra text. Output only content that conforms to the DSL.

{{ schema }}
