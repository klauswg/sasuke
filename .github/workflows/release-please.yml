name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    name: Create release PR or draft release
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created || steps.release.outputs['.--release_created'] }}
      tag_name: ${{ steps.release.outputs.tag_name || steps.release.outputs['.--tag_name'] }}
      sha: ${{ steps.release.outputs.sha || steps.release.outputs['.--sha'] }}
    steps:
      - name: Release Please
        id: release
        uses: googleapis/release-please-action@v4
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          skip-github-pull-request: ${{ github.event_name == 'push' && contains(github.event.head_commit.message, 'release-please--branches--main') }}

      - name: Checkout release commit
        if: steps.release.outputs.release_created == 'true' || steps.release.outputs['.--release_created'] == 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.release.outputs.sha || steps.release.outputs['.--sha'] }}

      - name: Ensure release git tag exists
        if: steps.release.outputs.release_created == 'true' || steps.release.outputs['.--release_created'] == 'true'
        shell: bash
        env:
          RELEASE_TAG: ${{ steps.release.outputs.tag_name || steps.release.outputs['.--tag_name'] }}
          RELEASE_SHA: ${{ steps.release.outputs.sha || steps.release.outputs['.--sha'] }}
        run: |
          if [[ -z "${RELEASE_TAG}" || -z "${RELEASE_SHA}" ]]; then
            echo "Release tag and SHA are required."
            exit 1
          fi

          git fetch origin "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}" --force || true

          if git rev-parse --verify --quiet "refs/tags/${RELEASE_TAG}" >/dev/null; then
            existing_sha=$(git rev-list -n 1 "refs/tags/${RELEASE_TAG}")
            if [[ "${existing_sha}" != "${RELEASE_SHA}" ]]; then
              echo "Tag ${RELEASE_TAG} already points to ${existing_sha}, expected ${RELEASE_SHA}."
              exit 1
            fi
            echo "Release tag ${RELEASE_TAG} already exists."
            exit 0
          fi

          git tag "${RELEASE_TAG}" "${RELEASE_SHA}"
          git push origin "refs/tags/${RELEASE_TAG}"

  release-preflight:
    name: Verify release commit on Linux
    needs: release-please
    if: needs.release-please.outputs.release_created == 'true'
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-please.outputs.sha }}

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Check Rust workspace
        run: cargo check --workspace --all-targets

  build:
    name: Build ${{ matrix.platform.name }}
    needs:
      - release-please
      - release-preflight
    if: needs.release-please.outputs.release_created == 'true'
    runs-on: ${{ matrix.platform.runner }}

    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: windows-x64
            runner: windows-latest
          - name: linux-x64
            runner: ubuntu-22.04
          - name: macos-x64
            runner: macos-15-intel
          - name: macos-arm64
            runner: macos-15

    env:
      RELEASE_TAG: ${{ needs.release-please.outputs.tag_name }}
      RELEASE_SHA: ${{ needs.release-please.outputs.sha }}

    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ env.RELEASE_SHA }}

      - name: Install Linux dependencies
        if: matrix.platform.runner == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            wget \
            file \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            rpm

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Sync release version from tag
        shell: bash
        run: npm run version:sync -- "${RELEASE_TAG#v}"

      - name: Install frontend dependencies
        run: npm ci

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Generate default Tauri channel config
        shell: bash
        run: |
          node scripts/channel-config.mjs default src-tauri/target/channel/tauri.default.conf.json
          test -f src-tauri/target/channel/tauri.default.conf.json

      - name: Ensure Tauri action npm script
        run: npm pkg set scripts.tauri=tauri

      - name: Configure optional Apple credentials
        if: startsWith(matrix.platform.name, 'macos-')
        env:
          MACOS_APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          MACOS_APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          MACOS_APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          MACOS_APPLE_ID: ${{ secrets.APPLE_ID }}
          MACOS_APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          MACOS_APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: node scripts/configure-macos-signing.mjs

      - name: Build and publish Tauri release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SASUKE_RELEASE_CHANNEL: default
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.DEFAULT_TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.DEFAULT_TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          args: --config src-tauri/target/channel/tauri.default.conf.json
          tagName: ${{ env.RELEASE_TAG }}
          releaseName: sasuke ${{ env.RELEASE_TAG }}
          releaseDraft: true
          prerelease: false

      - name: Verify macOS application signatures
        if: startsWith(matrix.platform.name, 'macos-')
        shell: bash
        run: |
          find target -type d -path '*/bundle/macos/*.app' -print > macos-app-bundles.txt
          if [[ ! -s macos-app-bundles.txt ]]; then
            echo "No macOS app bundle was produced."
            exit 1
          fi
          while IFS= read -r app_bundle; do
            codesign --verify --deep --strict "$app_bundle"
          done < macos-app-bundles.txt

  updater-json:
    name: Generate updater manifest
    runs-on: ubuntu-22.04
    needs:
      - release-please
      - build
    if: needs.release-please.outputs.release_created == 'true'

    env:
      RELEASE_TAG: ${{ needs.release-please.outputs.tag_name }}
      RELEASE_SHA: ${{ needs.release-please.outputs.sha }}

    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_SHA }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Download release assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release download "${RELEASE_TAG}" --dir release-assets --clobber

      - name: Generate latest.json
        run: node scripts/generate-updater-json.mjs release-assets latest.json

      - name: Generate release checksums
        run: node scripts/generate-release-checksums.mjs release-assets

      - name: Upload updater manifest, macOS installer, and release checksums
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${RELEASE_TAG}" latest.json scripts/install-sasuke-macos.sh release-assets/*.sha256 --clobber
